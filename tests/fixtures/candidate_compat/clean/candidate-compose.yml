# CLEAN candidate compose — declares only env keys the downstream prod stage can
# supply (OMNISIGHT_ENV + OMNISIGHT_AUTH_MODE are both present in prod-compose.yml),
# so the env-contract delta is empty.
services:
  backend-a:
    image: sora.services:49160/omnisight/omnisight-productizer/backend@sha256:d3dd073187890a7d10e324912547d0408b396aeb9457a3d379622ed84ed3bd9d
    environment:
      - OMNISIGHT_ENV=production
      - OMNISIGHT_AUTH_MODE=strict
