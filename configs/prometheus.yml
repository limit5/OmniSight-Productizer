# Phase 52 — Prometheus scrape config for OmniSight backend.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: omnisight-backend
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ["backend:8000"]
