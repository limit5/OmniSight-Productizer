# OmniSight Git Credential Registry
# Copy to git_credentials.yaml and fill in your credentials.
# Tokens can reference environment variables with ${VAR_NAME} syntax.
#
# Priority: this file > config.py JSON maps > config.py scalar values

repositories:
  # GitHub repository
  # - id: main-github
  #   url: "https://github.com/company/omnisight-core"
  #   platform: github
  #   token: "${GITHUB_TOKEN_MAIN}"
  #   ssh_key: "~/.ssh/id_ed25519"

  # Self-hosted GitLab (vendor SDK)
  # - id: vendor-gitlab
  #   url: "https://gitlab.vendor.com/sdk/camera-sdk"
  #   platform: gitlab
  #   token: "${GITLAB_TOKEN_VENDOR}"
  #   ssh_key: "~/.ssh/id_vendor"

  # Internal GitLab
  # - id: internal-gitlab
  #   url: "https://gitlab.company.com/embedded/firmware"
  #   platform: gitlab
  #   token: "${GITLAB_TOKEN_INTERNAL}"

  # Gerrit Code Review
  # - id: gerrit-main
  #   url: "https://gerrit.company.com"
  #   platform: gerrit
  #   ssh_host: "gerrit.company.com"
  #   ssh_port: 29418
  #   project: "omnisight"
  #   webhook_secret: "${GERRIT_SECRET_MAIN}"

  # Second Gerrit (vendor)
  # - id: gerrit-vendor
  #   url: "https://gerrit.vendor.com"
  #   platform: gerrit
  #   ssh_host: "gerrit.vendor.com"
  #   ssh_port: 29418
  #   project: "camera-hal"
  #   webhook_secret: "${GERRIT_SECRET_VENDOR}"
