# {{ product_name | default("Product") }} — Security Assessment

**Document ID:** {{ doc_id | default("SEC-0001") }}
**Revision:** {{ revision | default("1.0") }}
**Date:** {{ date }}
**Product Class:** {{ project_class | default("embedded_product") }}
**Classification:** {{ classification | default("Internal") }}

---

## 1. Executive Summary

{% if executive_summary %}
{{ executive_summary }}
{% else %}
This document provides the security assessment for {{ product_name | default("the product") }}, covering threat modeling, secure boot chain, data protection, and vulnerability management.
{% endif %}

## 2. Threat Model

### 2.1 Attack Surface

{% if attack_surfaces %}
| Surface | Protocol | Exposure | Risk Level |
|---------|----------|----------|------------|
{% for s in attack_surfaces %}
| {{ s.name }} | {{ s.protocol | default("—") }} | {{ s.exposure | default("Internal") }} | {{ s.risk | default("Medium") }} |
{% endfor %}
{% else %}
| Surface | Protocol | Exposure | Risk Level |
|---------|----------|----------|------------|
| Network | TCP/IP | External | High |
| USB | USB 2.0/3.0 | Physical | Medium |
| Serial | UART | Physical | Low |
| Storage | eMMC/SD | Physical | Medium |
{% endif %}

### 2.2 Threat Actors

{% if threat_actors %}
{% for actor in threat_actors %}
- **{{ actor.name }}**: {{ actor.description }}
{% endfor %}
{% else %}
- **Remote attacker**: Network-based exploitation
- **Physical attacker**: Device tampering or data extraction
- **Malicious insider**: Authorized user misuse
{% endif %}

## 3. Secure Boot Chain

{% if secure_boot %}
| Stage | Verification | Key Storage |
|-------|-------------|-------------|
{% for stage in secure_boot %}
| {{ stage.name }} | {{ stage.verification | default("—") }} | {{ stage.key_storage | default("—") }} |
{% endfor %}
{% else %}
| Stage | Verification | Key Storage |
|-------|-------------|-------------|
| ROM Bootloader | Hash | OTP fuses |
| U-Boot SPL | RSA-2048 / Ed25519 | Signed header |
| Kernel | dm-verity | Ramdisk |
| Rootfs | dm-verity | Kernel cmdline |
{% endif %}

## 4. Data Protection

### 4.1 Data at Rest

{% if data_at_rest %}
{{ data_at_rest }}
{% else %}
- Filesystem encryption: dm-crypt / LUKS (if applicable)
- Credential storage: encrypted keystore
- Sensitive configuration: AES-256-GCM encrypted
{% endif %}

### 4.2 Data in Transit

{% if data_in_transit %}
{{ data_in_transit }}
{% else %}
- TLS 1.2+ for all network communication
- Certificate pinning for cloud endpoints
- mTLS for device-to-device communication (if applicable)
{% endif %}

### 4.3 Key Management

{% if key_management %}
{{ key_management }}
{% else %}
- Hardware key storage: TEE / SE / TPM (platform dependent)
- Key rotation policy: per-deployment
- No hardcoded secrets in firmware images
{% endif %}

## 5. Authentication & Authorization

{% if auth_model %}
{{ auth_model }}
{% else %}
- Device authentication: X.509 client certificate
- User authentication: Token-based (JWT)
- Authorization: Role-based access control (RBAC)
{% endif %}

## 6. Vulnerability Management

### 6.1 Known Vulnerabilities

{% if known_vulnerabilities %}
| CVE | Component | Severity | Status |
|-----|-----------|----------|--------|
{% for v in known_vulnerabilities %}
| {{ v.cve }} | {{ v.component }} | {{ v.severity }} | {{ v.status | default("Open") }} |
{% endfor %}
{% else %}
*No known vulnerabilities at time of assessment.*
{% endif %}

### 6.2 Dependency Audit

{% if dependency_audit %}
| Package | Version | License | Risk |
|---------|---------|---------|------|
{% for d in dependency_audit %}
| {{ d.package }} | {{ d.version }} | {{ d.license | default("—") }} | {{ d.risk | default("Low") }} |
{% endfor %}
{% else %}
*Run SBOM generator to produce dependency audit.*
{% endif %}

## 7. Compliance Mapping

{% if security_compliance %}
{% for sc in security_compliance %}
- **{{ sc.standard }}**: {{ sc.status | default("Pending assessment") }}
{% endfor %}
{% else %}
- **IEC 62443**: Industrial cybersecurity (if applicable)
- **OWASP IoT Top 10**: IoT security baseline
- **NIST SP 800-183**: Networks of Things
{% endif %}

## 8. Recommendations

{% if recommendations %}
{% for rec in recommendations %}
{{ loop.index }}. **{{ rec.title }}** ({{ rec.priority | default("Medium") }}): {{ rec.description }}
{% endfor %}
{% else %}
1. **Enable secure boot** (High): Ensure full chain-of-trust from ROM to application.
2. **Harden network services** (High): Disable unused ports and protocols.
3. **Implement OTA signing** (High): All firmware updates must be cryptographically signed.
4. **Regular vulnerability scanning** (Medium): Schedule periodic scans of all dependencies.
{% endif %}

---

*Generated by OmniSight Document Suite Generator*
