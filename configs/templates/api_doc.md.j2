# {{ product_name | default("Product") }} — API Reference

**Document ID:** {{ doc_id | default("API-0001") }}
**Revision:** {{ revision | default("1.0") }}
**Date:** {{ date }}
**Product Class:** {{ project_class | default("embedded_product") }}

---

## 1. Overview

{{ api_overview | default("This document describes the programmatic interface for the product.") }}

**Base URL:** `{{ base_url | default("http://localhost:8080/api/v1") }}`
**Authentication:** {{ auth_method | default("Bearer token") }}

## 2. Authentication

{% if auth_details %}
{{ auth_details }}
{% else %}
All API requests require a valid bearer token in the `Authorization` header:

```
Authorization: Bearer <token>
```
{% endif %}

## 3. Endpoints

{% if endpoints %}
{% for ep in endpoints %}
### {{ ep.method | upper }} `{{ ep.path }}`

{{ ep.description | default("") }}

{% if ep.parameters %}
**Parameters:**

| Name | Location | Type | Required | Description |
|------|----------|------|----------|-------------|
{% for p in ep.parameters %}
| `{{ p.name }}` | {{ p.location | default("query") }} | {{ p.type | default("string") }} | {{ "Yes" if p.required else "No" }} | {{ p.description | default("") }} |
{% endfor %}
{% endif %}

{% if ep.request_body %}
**Request Body:**

```json
{{ ep.request_body }}
```
{% endif %}

{% if ep.response_example %}
**Response ({{ ep.response_code | default("200") }}):**

```json
{{ ep.response_example }}
```
{% endif %}

{% if ep.error_codes %}
**Error Codes:**

| Code | Description |
|------|-------------|
{% for ec in ep.error_codes %}
| {{ ec.code }} | {{ ec.description }} |
{% endfor %}
{% endif %}

---

{% endfor %}
{% else %}
*API endpoints to be documented per product implementation.*
{% endif %}

## 4. Data Models

{% if data_models %}
{% for model in data_models %}
### {{ model.name }}

{{ model.description | default("") }}

| Field | Type | Description |
|-------|------|-------------|
{% for f in model.fields %}
| `{{ f.name }}` | `{{ f.type }}` | {{ f.description | default("") }} |
{% endfor %}

{% endfor %}
{% else %}
*Data model documentation to be generated from schema definitions.*
{% endif %}

## 5. Error Handling

{% if error_handling %}
{{ error_handling }}
{% else %}
All errors follow a standard format:

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable error description",
    "details": {}
  }
}
```

| HTTP Status | Meaning |
|-------------|---------|
| 400 | Bad Request — invalid input |
| 401 | Unauthorized — missing or invalid token |
| 403 | Forbidden — insufficient permissions |
| 404 | Not Found — resource does not exist |
| 500 | Internal Server Error |
{% endif %}

## 6. Rate Limiting

{% if rate_limiting %}
{{ rate_limiting }}
{% else %}
Default rate limits apply. Check `X-RateLimit-*` response headers for current limits.
{% endif %}

## 7. SDK & Examples

{% if sdk_examples %}
{% for ex in sdk_examples %}
### {{ ex.language }}

```{{ ex.language | lower }}
{{ ex.code }}
```

{% endfor %}
{% else %}
*SDK libraries and code examples to be provided.*
{% endif %}

---

*Generated by OmniSight Document Suite Generator*
