# hil/ - OP-1815 skeleton

Hardware-in-the-loop recipes for attached UVC cameras land with the
sequenced follow-on capture ticket in this same pack. This directory
exists so the `hil` artifact declared in `skill.yaml` validates.
