# docs/ - OP-1815 skeleton

Operator docs for UVC host enumeration, capture, and preview behavior
land with sequenced follow-on tickets in this same pack. This directory
exists so the `docs` artifact declared in `skill.yaml` validates.
