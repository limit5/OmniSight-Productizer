# tests/ - OP-1815 skeleton

Dispatch/render coverage for this pack lives in the backend test suite at
`backend/tests/test_skill_windows_uvc_host.py`.

Per-pack UVC host capture tests land with the sequenced follow-on tickets
in this same pack. This directory exists so the `tests` artifact declared
in `skill.yaml` validates.
