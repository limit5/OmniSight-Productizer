{
  "name": "{{ project_name }}",
  "version": "0.1.0",
  "private": true,
  "description": "Generated by SKILL-NEXTJS (W6 #280). See configs/skills/skill_nextjs/SKILL.md.",
  "scripts": {
    "dev": "next dev",
    "dev:turbo": "next dev --turbopack",
    "build": "next build"{% if prisma %},
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev"{% endif %},
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "build:vercel": "vercel build",
    "build:cloudflare": "next-on-pages"
  },
  "dependencies": {
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"{% if prisma %},
    "@prisma/client": "^6.0.0"{% endif %}{% if auth == "nextauth" %},
    "next-auth": "^5.0.0"{% endif %}{% if auth == "clerk" %},
    "@clerk/nextjs": "^6.0.0"{% endif %}{% if trpc %},
    "@trpc/server": "^11.0.0",
    "@trpc/client": "^11.0.0",
    "@trpc/react-query": "^11.0.0",
    "@trpc/next": "^11.0.0",
    "@tanstack/react-query": "^5.0.0",
    "zod": "^3.23.0"{% endif %}{% if resend %},
    "resend": "^4.0.0"{% endif %}
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^16.0.0",
    "vitest": "^2.1.0",
    "@vitest/ui": "^2.1.0",
    "@testing-library/react": "^16.0.0",
    "@testing-library/jest-dom": "^6.5.0",
    "jsdom": "^25.0.0",
    "@playwright/test": "^1.48.0",
    "@omnisight/vite-plugin": "^0.1.0"{% if prisma %},
    "prisma": "^6.0.0"{% endif %}{% if target in ["cloudflare", "both"] %},
    "@cloudflare/next-on-pages": "^1.13.0",
    "wrangler": "^3.80.0"{% endif %}{% if target in ["vercel", "both"] %},
    "vercel": "^37.0.0"{% endif %}
  }
}
