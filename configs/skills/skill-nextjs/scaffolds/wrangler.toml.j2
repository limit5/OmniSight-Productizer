# Generated by SKILL-NEXTJS (W6 #280).
# Cloudflare Pages via @cloudflare/next-on-pages.
# Bundle ceiling: 1 MiB compressed per Edge isolate — keep an eye on
# the W2 bundle gate (runs against configs/platforms/web-edge-cloudflare.yaml).

name = "{{ project_name }}"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

[vars]
BACKEND_URL = "{{ backend_url }}"

# [[kv_namespaces]]
# binding = "SESSIONS"
# id = "replace-with-namespace-id"
