// Generated by SKILL-NEXTJS (W6 #280).
// Root layout — Server Component by default. Client Components live
// inside child routes only where interactivity is required.

import type { Metadata } from "next"
{% if auth == "clerk" %}import { ClerkProvider } from "@clerk/nextjs"
{% endif %}import "./globals.css"

export const metadata: Metadata = {
  title: "{{ project_name }}",
  description: "Generated by SKILL-NEXTJS — W6 pilot (OmniSight).",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    {% if auth == "clerk" %}<ClerkProvider>
      <html lang="en">
        <body>{children}</body>
      </html>
    </ClerkProvider>{% else %}<html lang="en">
      <body>{children}</body>
    </html>{% endif %}
  )
}
