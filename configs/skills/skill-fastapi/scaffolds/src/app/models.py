"""ORM models.

Generated by SKILL-FASTAPI (X5 #301). Edit freely — this file lives
outside the scaffold's idempotent surface after the first render is
not typical, but re-running the scaffold will overwrite it, so guard
destructive edits behind a branch.
"""

from __future__ import annotations

from datetime import datetime

from sqlalchemy import DateTime, String, func
from sqlalchemy.orm import Mapped, mapped_column

from .db import Base


class Item(Base):
    __tablename__ = "items"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(length=200), index=True)
    description: Mapped[str] = mapped_column(String(length=2000), default="")
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now()
    )
