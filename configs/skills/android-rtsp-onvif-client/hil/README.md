# hil/ — OP-1799 skeleton

Hardware-in-the-loop recipes for this pack (e.g. playing a real ONVIF
camera's RTSP stream on a device) land with the sequenced **follow-on**
RTSP-playback ticket in this same pack. This directory exists so the
`hil` artifact declared in `skill.yaml` validates (`skill_manifest`).
