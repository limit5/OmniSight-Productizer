# docs/ — OP-1799 skeleton

Datasheet / integration docs for the ONVIF-discovery and RTSP-playback
client layers land with their sequenced **follow-on** tickets in this
same pack. This directory exists so the `docs` artifact declared in
`skill.yaml` validates (`skill_manifest`).

See `../SKILL.md` for the pack overview and the device-side vs.
client-side naming-trap note.
