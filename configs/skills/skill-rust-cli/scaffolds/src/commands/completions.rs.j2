// Generated by SKILL-RUST-CLI (X7 #303).
//
// `{{ bin_name }} completions <shell>` — emit shell completion
// scripts for clap_complete::Shell. Keeps the release archive
// slim (no pre-generated files) while satisfying the cli-tooling
// role "bash + zsh minimum" mandate.

use anyhow::Result;
use clap::{CommandFactory, Parser};
use clap_complete::{generate, Shell};

use crate::cli::Cli;

#[derive(Debug, Parser)]
pub struct Args {
    /// Target shell. One of: bash, zsh, fish, powershell, elvish.
    #[arg(value_enum)]
    pub shell: Shell,
}

pub fn run(args: Args) -> Result<()> {
    let mut cmd = Cli::command();
    let bin_name = cmd.get_name().to_string();
    generate(args.shell, &mut cmd, bin_name, &mut std::io::stdout());
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn command_factory_resolves() {
        // If this ever fails, clap_complete cannot introspect the tree.
        let cmd = Cli::command();
        assert_eq!(cmd.get_name(), "{{ bin_name }}");
    }
}
