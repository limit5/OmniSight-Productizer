// Generated by SKILL-RUST-CLI (X7 #303).
//
// Subcommand module roots. Each subcommand owns:
//   * an `Args` struct (clap derive)
//   * a `run()` entry point (async when the top-level runtime
//     is tokio) + a `run_sync()` fallback when runtime=sync.
//   * a #[cfg(test)] mod tests inside the file so unit tests
//     travel with the command they exercise.
//
// The `completions` module is mandatory — the cli-tooling role
// rule "at least bash + zsh" is satisfied by emitting all four
// shells (bash/zsh/fish/pwsh) via clap_complete.

pub mod completions;
pub mod init;
pub mod run;
pub mod status;
pub mod version;
