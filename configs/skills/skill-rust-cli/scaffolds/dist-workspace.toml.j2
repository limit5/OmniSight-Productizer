# Generated by SKILL-RUST-CLI (X7 #303).
#
# cargo-dist workspace config (X3 #299). The bulk of the release
# matrix lives in `[workspace.metadata.dist]` inside Cargo.toml —
# this file is the top-level anchor cargo-dist expects and pins the
# minimum cargo-dist version so `cargo dist plan` fails fast when
# the installed toolchain is older than the scaffold's invariants.

[workspace]
members = ["."]

[dist]
cargo-dist-version = "0.20.0"
default-members = ["{{ crate_name }}"]
