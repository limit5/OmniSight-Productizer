// Generated by SKILL-DESKTOP-TAURI (X8 #304).
// Vue 3 entry point. The CSP in tauri.conf.json forbids inline
// script — Vue is loaded as a module via Vite, not eval'd.

import { createApp } from "vue";
import App from "./App.vue";
import "./App.css";

const app = createApp(App);
app.mount("#root");
