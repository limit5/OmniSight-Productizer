// Generated by SKILL-DESKTOP-TAURI (X8 #304).
// React 18 entry point — StrictMode catches concurrent-rendering
// regressions early. The CSP in tauri.conf.json forbids inline
// script, so the SPA is loaded by Vite as a module, not eval'd.

import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./App.css";

const rootEl = document.getElementById("root");
if (!rootEl) {
  throw new Error("missing #root element in index.html");
}

ReactDOM.createRoot(rootEl).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
