<!doctype html>
<!--
  Generated by SKILL-DESKTOP-TAURI (X8 #304).

  Vite SPA entry. The CSP header that actually ships at runtime lives
  in src-tauri/tauri.conf.json (app.security.csp); the `<meta>` tag
  here is only for the Vite dev server because dev-mode requests come
  through HTTP loopback rather than the `tauri://` custom scheme.
-->
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' ipc: http://ipc.localhost" />
    <title>{{ app_name }}</title>
  </head>
  <body>
    <div id="root"></div>
{%- if frontend == "react" %}
    <script type="module" src="/src/main.tsx"></script>
{%- else %}
    <script type="module" src="/src/main.ts"></script>
{%- endif %}
  </body>
</html>
