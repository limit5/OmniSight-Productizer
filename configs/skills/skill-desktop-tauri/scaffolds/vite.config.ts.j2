// Generated by SKILL-DESKTOP-TAURI (X8 #304).
//
// Tauri-aware Vite config. The dev server binds to 1420 (Tauri's
// default expectation; tauri.conf.json `build.devUrl` mirrors it),
// HMR runs over WebSocket, and the build target matches the system
// webview baseline (Edge for Windows / Safari for macOS / WebKitGTK
// for Linux — `safari13` is the conservative common floor).

import { defineConfig } from "vite";
{%- if frontend == "react" %}
import react from "@vitejs/plugin-react";
{%- else %}
import vue from "@vitejs/plugin-vue";
{%- endif %}

const host = process.env.TAURI_DEV_HOST;

export default defineConfig(async () => ({
  plugins: [
{%- if frontend == "react" %}
    react(),
{%- else %}
    vue(),
{%- endif %}
  ],

  // Tauri expects a fixed port. Failing here is better than the
  // dev server picking a random port that tauri.conf.json doesn't
  // know about.
  clearScreen: false,
  server: {
    port: 1420,
    strictPort: true,
    host: host || false,
    hmr: host ? { protocol: "ws", host, port: 1421 } : undefined,
    watch: {
      // Don't crash the dev server on Rust file edits; Cargo handles those.
      ignored: ["**/src-tauri/**"],
    },
  },

  // Tauri 2 supports the modern targets natively.
  build: {
    target: process.env.TAURI_ENV_PLATFORM == "windows" ? "chrome105" : "safari13",
    minify: !process.env.TAURI_ENV_DEBUG ? "esbuild" : false,
    sourcemap: !!process.env.TAURI_ENV_DEBUG,
  },

  test: {
    environment: "jsdom",
    globals: true,
    coverage: {
      provider: "v8",
      reporter: ["text", "json", "html"],
      // Frontend track of the X1 software simulate gate (Node 80% floor).
      thresholds: {
        lines: 80,
        functions: 80,
        branches: 80,
        statements: 80,
      },
      include: ["src/**/*.{ts,tsx,vue}"],
      exclude: ["src/**/*.test.{ts,tsx}", "src/main.{ts,tsx}"],
    },
  },
}));
