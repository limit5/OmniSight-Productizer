// Generated by SKILL-DESKTOP-TAURI (X8 #304).
//
// Entry point — the Tauri 2.x convention is to keep `main` minimal
// and route everything through `lib.rs`'s `run()`. The `cfg_attr`
// on Windows release suppresses the console window so the app
// launches without a flashing cmd.exe shell.

#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    {{ crate_name }}_lib::run()
}
