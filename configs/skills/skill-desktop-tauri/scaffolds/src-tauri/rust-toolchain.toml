# Generated by SKILL-DESKTOP-TAURI (X8 #304).
#
# Pinned toolchain — rustup auto-installs when the user runs any
# cargo command in this tree. Matches the backend-rust role "MSRV
# 1.76" baseline. Update in lockstep with Cargo.toml `rust-version`.

[toolchain]
channel = "1.76.0"
components = ["rustfmt", "clippy", "rust-src"]
profile = "minimal"
