# hil/ — OP-1816 skeleton

Hardware-in-the-loop recipes for this pack (e.g. running the ARKit world
tracking + MapKit location feed on a real device) land with the sequenced
**follow-on** ARKit/MapKit tickets in this same pack. This directory
exists so the `hil` artifact declared in `skill.yaml` validates
(`skill_manifest`).
