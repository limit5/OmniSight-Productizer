<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose supplies the runtime theme; the XML stub keeps AGP's
         Manifest Merger happy when it rewrites references. -->
    <style name="Theme.{{ project_name }}" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
