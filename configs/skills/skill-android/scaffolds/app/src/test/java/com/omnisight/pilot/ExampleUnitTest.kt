// SKILL-ANDROID (P8 #293) — unit test skeleton.
//
// JUnit4 is the P2 simulate-track's unit framework for Android. The
// Espresso `androidTest` surface is covered separately in
// `app/src/androidTest/`.

package com.omnisight.pilot

import org.junit.Assert.assertEquals
import org.junit.Test

class ExampleUnitTest {

    @Test
    fun arithmeticBaseline() {
        assertEquals(4, 2 + 2)
    }
}
