# Generated by SKILL-NUXT (W7 #281).
# Cloudflare Pages via Nitro `cloudflare-pages` preset.
# Bundle ceiling: 1 MiB compressed per Edge isolate — the W2 bundle
# gate (runs against configs/platforms/web-edge-cloudflare.yaml) will
# fire before `wrangler deploy` does if we blow this.

name = "{{ project_name }}"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "dist"

[vars]
NUXT_BACKEND_URL = "{{ backend_url }}"

# [[kv_namespaces]]
# binding = "SESSIONS"
# id = "replace-with-namespace-id"
