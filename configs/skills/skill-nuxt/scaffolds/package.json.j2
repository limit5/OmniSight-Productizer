{
  "name": "{{ project_name }}",
  "version": "0.1.0",
  "private": true,
  "description": "Generated by SKILL-NUXT (W7 #281). See configs/skills/skill-nuxt/SKILL.md.",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "build:node": "NITRO_PRESET=node-server nuxt build",
    "build:vercel": "NITRO_PRESET=vercel nuxt build",
    "build:cloudflare": "NITRO_PRESET=cloudflare-pages nuxt build",
    "build:bun": "NITRO_PRESET=bun nuxt build"
  },
  "dependencies": {
    "nuxt": "^4.0.0",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"{% if pinia %},
    "pinia": "^2.3.0",
    "@pinia/nuxt": "^0.9.0"{% endif %}{% if auth == "sidebase" %},
    "@sidebase/nuxt-auth": "^0.9.0"{% endif %}{% if auth == "clerk" %},
    "@clerk/nuxt": "^1.0.0"{% endif %}
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "@nuxt/eslint": "^0.7.0",
    "vitest": "^2.1.0",
    "@vue/test-utils": "^2.4.6",
    "@vitest/ui": "^2.1.0",
    "happy-dom": "^15.0.0",
    "@playwright/test": "^1.48.0"{% if target in ["cloudflare", "all"] %},
    "wrangler": "^3.80.0"{% endif %}{% if target in ["vercel", "all"] %},
    "vercel": "^37.0.0"{% endif %}
  }
}
