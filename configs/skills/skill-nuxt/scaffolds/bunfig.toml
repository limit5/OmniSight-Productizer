# Generated by SKILL-NUXT (W7 #281).
# Bun config — applied when the project is built/run under Bun
# (NITRO_PRESET=bun). The preset produces a Bun-compatible bundle
# at .output/server/index.mjs that runs via `bun run`.

[install]
registry = "https://registry.npmjs.org/"

[install.lockfile]
save = true

[run]
# Run the built server under Bun. Matches the HIL `bun-runtime-smoke`
# recipe — if this command regresses, the HIL probe catches it.
bun = true
