// Generated by SKILL-ASTRO (W8 #282).
// Content collection config — Astro's idiomatic way to schema
// .mdx / .md frontmatter. The Zod schema below is what the content
// pipeline enforces at build time; mismatches fail the build. This
// is the content-vertical's answer to "typed data": the CMS sits
// behind this schema regardless of whether the source is local MDX,
// Sanity, or Contentful.

import { defineCollection, z } from "astro:content"

const blog = defineCollection({
  type: "content",
  schema: z.object({
    title: z.string(),
    description: z.string(),
    pubDate: z.coerce.date(),
    updatedDate: z.coerce.date().optional(),
    author: z.string().default("{{ project_name }} team"),
    tags: z.array(z.string()).default([]),
    heroImage: z.string().optional(),
    draft: z.boolean().default(false),
  }),
})

export const collections = { blog }
