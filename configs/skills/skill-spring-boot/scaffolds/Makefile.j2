# Generated by SKILL-SPRING-BOOT (X9 #305).
# `make help` lists every target.

.PHONY: help run test verify package docker helm clean

help:
	@grep -E '^[a-zA-Z_-]+:.*?## .*$$' $(MAKEFILE_LIST) | awk 'BEGIN {FS = ":.*?## "}; {printf "\033[36m%-20s\033[0m %s\n", $$1, $$2}'

{%- if build_tool == "maven" %}

run:  ## start dev server (mvn spring-boot:run)
	mvn spring-boot:run

test:  ## mvn test
	mvn test

verify:  ## mvn verify (runs tests + JaCoCo 70% floor)
	mvn verify

package:  ## mvn package -DskipTests (fat jar)
	mvn package -DskipTests

{%- else %}

run:  ## start dev server (./gradlew bootRun)
	./gradlew bootRun

test:  ## ./gradlew test
	./gradlew test

verify:  ## ./gradlew check (runs tests + JaCoCo 70% floor)
	./gradlew check

package:  ## ./gradlew bootJar (fat jar)
	./gradlew bootJar

{%- endif %}

docker:  ## docker build
	docker build -t {{ artifact_id }}:0.1.0 .

helm:  ## helm lint + package
	helm lint deploy/helm
	mkdir -p dist
	helm package deploy/helm -d dist/

clean:
{%- if build_tool == "maven" %}
	mvn clean
{%- else %}
	./gradlew clean
{%- endif %}
	rm -rf dist/
