// Generated by SKILL-SPRING-BOOT (X9 #305).
//
// backend-java role: no scattered raw env reads — every config knob
// binds through `@ConfigurationProperties`. Any new knob gets a
// typed field here; `application.yaml` provides the default.
package {{ base_package }}.config;

import jakarta.validation.constraints.NotBlank;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.validation.annotation.Validated;

@Validated
@ConfigurationProperties(prefix = "app")
public record AppProperties(
        @NotBlank String name,
        @NotBlank String environment
) {
}
