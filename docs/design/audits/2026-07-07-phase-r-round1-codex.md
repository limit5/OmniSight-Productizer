_jira_event` routes `jira:issue_updated` at `backend/routers/webhooks.py:1941-1963`. But `handle_issue_updated()` returns ignored unless changelog has a status transition and target status is whitelisted at `backend/jira_event_router.py:291-302`. Link/parent/label edits can miss invalidation if the hook is added inside that handler after the status filter. Suggested fix: invalidate at the top of `handle_issue_updated()` or in `_on_jira_event()` before status-specific routing, and cover non-status changelog events.

5. **HIGH — R2 budget math is asserted, not designed.** Evidence: structural budget is pinned at 800 ms in `backend/agents/project_state_aggregator.py:51-56`, while the proposed cold path uses `build_default_jira_adapter().fetch_story()` from `docs/design/...:67-76`. `fetch_story()` performs a live REST GET and audit write at `backend/jira_adapter.py:204-211`; no timeout parameter is visible in that adapter call path. Suggested fix: add an explicit transport timeout/budget wrapper for Jira fetch, measure p95/p99, and make cold timeout serve null/stale without blocking the 800 ms slice.

6. **MED — R2’s field-source assumption is mostly plausible but not guaranteed by code.** Evidence: `fetch_story()` returns `raw=body` at `backend/jira_adapter.py:225-233`, so if Jira returns `fields.issuelinks/parent/status/labels`, mapping can read them. But the GET path has no `fields=` or `expand=` selection at `backend/jira_adapter.py:204-206`, so the design has not pinned the exact Jira response shape. Suggested fix: request the required fields explicitly and add adapter fixtures containing `issuelinks`, `parent`, `labels`, and `status`.

7. **MED — R4 overlay source exists, but lifecycle fallback is under-specified.** Evidence: `get_deploy_overlay()` returns cached overlay or `None` at `backend/api_versioning.py:443-464`; `build_git_sha` is only populated from a complete lock field map at `backend/api_versioning.py:323-330`, and incomplete/absent locks return `None` at `backend/api_versioning.py:390-423`. Suggested fix: implement `_resolve_develop_sha()` as overlay dict lookup plus env fallback plus dev git fallback, and test absent/incomplete overlay explicitly.

8. **MED — R5’s proposed temporal shape breaks current tests/contracts unless updated deliberately.** Evidence: `fetch_temporal_axis()` currently normalizes timeline into `prior_similar_tickets`, `avg_completion_seconds`, and `recent_events` at `backend/agents/project_state_aggregator.py:259-268`. Tests pin that shape at `backend/tests/test_project_state.py:77-83` and `backend/tests/test_project_state.py:127-136`; timeout behavior pins `temporal is None` at `backend/tests/test_project_state.py:161-175`. Suggested fix: update contract tests/runbook/runner expectations for the new `{"status":"unavailable"}` shape, or keep legacy keys with a status marker.

9. **MED — R5 root cause is real, but the design omits removing the dead client seam from tests/docs.** Evidence: `_temporal_graphiti()` probes `graphiti_mcp_client._DEFAULT_DISPATCHER` and returns `{}` when absent at `backend/agents/project_state_aggregator.py:418-430`; the dispatcher call only happens if that private global exists at `backend/agents/project_state_aggregator.py:431-447`. Suggested fix: delete the private dispatcher probe, replace with explicit unavailable payload, and update docs mentioning Graphiti MCP temporal context.

10. **LOW — R2/R4 current-root-cause claims are verified.** Evidence: structural Jira is a literal dead stub: `make_client(...) if False else None` then `return {}` at `backend/agents/project_state_aggregator.py:297-305`. `_resolve_develop_sha()` shells out to `git rev-parse origin/develop` with `cwd=Path.cwd()` and returns `"unknown"` on failure at `backend/api/project_state.py:66-87`. Suggested fix: proceed with these repairs, but include removal of the now-unused `subprocess` import at `backend/api/project_state.py:24-28`.

11. **LOW — Phase R omits current observability surfaces that are directly relevant to proving repair.** Evidence: the API already exposes `/project-state/metrics` with cache stats and traces at `backend/api/project_state.py:161-192`; traces are in-memory FIFO at `backend/agents/project_state_aggregator.py:172-223`. The design defers observability to Phase S at `docs/design/...:7-8`, but R0 relies on non-empty rates and p95 checks at `docs/design/...:21-31`. Suggested fix: Phase R should add trace fields for per-axis non-empty/stale/unavailable status and define how staging/prod R0 results are captured.

**Verdict: NO-GO** for implementing this design as written. R1/R2/R4/R5 contain repairable ideas, but R3 is materially wrong about persistence, R2 stale/cache/webhook semantics need concrete API changes, and the test/contract fallout for R5 is not accounted for.
tokens used
159,036
1. **BLOCKER — R3 is diagnosing the wrong write path.** Evidence: `record_runner_incident()` calls `_persist_runner_incident(record)` at `backend/agents/incident_recorder.py:222`, and `_persist_runner_incident()` only appends to `_runner_buffer` at `backend/agents/incident_recorder.py:226-230`. No DB connection is opened there. The warning in `jira_dispatch` only catches exceptions from that in-memory append path at `backend/agents/jira_dispatch.py:2715-2729`, so “zero DB rows + zero `record_runner_incident_failed` warnings” is expected. Suggested fix: R3.0 must first decide whether runner incidents are supposed to write via `incident_recorder`, `memory_writeback`, or another DB writer, then add an actual durable insert path or route all terminal writes through the existing durable writer.

2. **BLOCKER — R3.1 claims “no schema migration needed” while proposing idempotency keys the table cannot enforce.** Evidence: design says no schema migration at `docs/design/2026-07-07-phase-r-3d-memory-repair-design.md:156-157` and proposes idempotency key `(ticket, terminal, attempt)` at `docs/design/2026-07-07-phase-r-3d-memory-repair-design.md:110-114`. Existing `runner_incidents` has only `incident_id` as PK plus indexes on ticket/class/mutex, no terminal/attempt/idempotency column: `backend/alembic/versions/0206_runner_incidents.py:83-116`. Suggested fix: add an alembic migration for terminal/attempt/idempotency_key with a unique constraint, or drop the idempotency-key claim.

3. **HIGH — R2 stale-while-revalidate is not implementable with the current cache API.** Evidence: expired entries are deleted and returned as miss in `ProjectStateCache.get()` at `backend/agents/project_state_cache.py:104-113`; the public protocol only exposes `get/set/invalidate/invalidate_ticket/clear/stats` at `backend/agents/project_state_cache.py:69-75`. Suggested fix: add an explicit `get_stale()` / `get_with_state()` API that can return expired-but-valid payloads, plus tests for stale marking and refresh races.

4. **HIGH — R2 webhook invalidation is only partly wired; the current issue-updated handler ignores non-status edits.** Evidence: `/webhooks/jira` verifies bearer secret and calls `_on_jira_event(event)` for all events at `backend/routers/webhooks.py:1825-1881`; `_on_jira_event` routes `jira:issue_updated` at `backend/routers/webhooks.py:1941-1963`. But `handle_issue_updated()` returns ignored unless changelog has a status transition and target status is whitelisted at `backend/jira_event_router.py:291-302`. Link/parent/label edits can miss invalidation if the hook is added inside that handler after the status filter. Suggested fix: invalidate at the top of `handle_issue_updated()` or in `_on_jira_event()` before status-specific routing, and cover non-status changelog events.

5. **HIGH — R2 budget math is asserted, not designed.** Evidence: structural budget is pinned at 800 ms in `backend/agents/project_state_aggregator.py:51-56`, while the proposed cold path uses `build_default_jira_adapter().fetch_story()` from `docs/design/...:67-76`. `fetch_story()` performs a live REST GET and audit write at `backend/jira_adapter.py:204-211`; no timeout parameter is visible in that adapter call path. Suggested fix: add an explicit transport timeout/budget wrapper for Jira fetch, measure p95/p99, and make cold timeout serve null/stale without blocking the 800 ms slice.

6. **MED — R2’s field-source assumption is mostly plausible but not guaranteed by code.** Evidence: `fetch_story()` returns `raw=body` at `backend/jira_adapter.py:225-233`, so if Jira returns `fields.issuelinks/parent/status/labels`, mapping can read them. But the GET path has no `fields=` or `expand=` selection at `backend/jira_adapter.py:204-206`, so the design has not pinned the exact Jira response shape. Suggested fix: request the required fields explicitly and add adapter fixtures containing `issuelinks`, `parent`, `labels`, and `status`.

7. **MED — R4 overlay source exists, but lifecycle fallback is under-specified.** Evidence: `get_deploy_overlay()` returns cached overlay or `None` at `backend/api_versioning.py:443-464`; `build_git_sha` is only populated from a complete lock field map at `backend/api_versioning.py:323-330`, and incomplete/absent locks return `None` at `backend/api_versioning.py:390-423`. Suggested fix: implement `_resolve_develop_sha()` as overlay dict lookup plus env fallback plus dev git fallback, and test absent/incomplete overlay explicitly.

8. **MED — R5’s proposed temporal shape breaks current tests/contracts unless updated deliberately.** Evidence: `fetch_temporal_axis()` currently normalizes timeline into `prior_similar_tickets`, `avg_completion_seconds`, and `recent_events` at `backend/agents/project_state_aggregator.py:259-268`. Tests pin that shape at `backend/tests/test_project_state.py:77-83` and `backend/tests/test_project_state.py:127-136`; timeout behavior pins `temporal is None` at `backend/tests/test_project_state.py:161-175`. Suggested fix: update contract tests/runbook/runner expectations for the new `{"status":"unavailable"}` shape, or keep legacy keys with a status marker.

9. **MED — R5 root cause is real, but the design omits removing the dead client seam from tests/docs.** Evidence: `_temporal_graphiti()` probes `graphiti_mcp_client._DEFAULT_DISPATCHER` and returns `{}` when absent at `backend/agents/project_state_aggregator.py:418-430`; the dispatcher call only happens if that private global exists at `backend/agents/project_state_aggregator.py:431-447`. Suggested fix: delete the private dispatcher probe, replace with explicit unavailable payload, and update docs mentioning Graphiti MCP temporal context.

10. **LOW — R2/R4 current-root-cause claims are verified.** Evidence: structural Jira is a literal dead stub: `make_client(...) if False else None` then `return {}` at `backend/agents/project_state_aggregator.py:297-305`. `_resolve_develop_sha()` shells out to `git rev-parse origin/develop` with `cwd=Path.cwd()` and returns `"unknown"` on failure at `backend/api/project_state.py:66-87`. Suggested fix: proceed with these repairs, but include removal of the now-unused `subprocess` import at `backend/api/project_state.py:24-28`.

11. **LOW — Phase R omits current observability surfaces that are directly relevant to proving repair.** Evidence: the API already exposes `/project-state/metrics` with cache stats and traces at `backend/api/project_state.py:161-192`; traces are in-memory FIFO at `backend/agents/project_state_aggregator.py:172-223`. The design defers observability to Phase S at `docs/design/...:7-8`, but R0 relies on non-empty rates and p95 checks at `docs/design/...:21-31`. Suggested fix: Phase R should add trace fields for per-axis non-empty/stale/unavailable status and define how staging/prod R0 results are captured.

**Verdict: NO-GO** for implementing this design as written. R1/R2/R4/R5 contain repairable ideas, but R3 is materially wrong about persistence, R2 stale/cache/webhook semantics need concrete API changes, and the test/contract fallout for R5 is not accounted for.
