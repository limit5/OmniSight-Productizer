Reading additional input from stdin...
OpenAI Codex v0.128.0 (research preview)
--------
workdir: /home/user/work/sora/OmniSight-Productizer
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, /home/user/.codex/memories]
reasoning effort: none
reasoning summaries: none
session id: 019e503c-0527-7f33-8548-fe10e22a8013
--------
user
Adversarially audit a DESIGN for the OmniSight pipeline-coordinator §6 live action executor + acting-flip. READ: docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md plus the referenced code — backend/agents/pipeline_coordinator.py (ShadowActionExecutor :266, ActionExecutor seam :295, ColdStartGateway protocol :576, JiraDispatchColdStartGateway :606 incl mark_resumable/transition_under_review/reset_to_todo/clear_assignee/mention_operator, build_default_coordinator :1862, CLI main :1909), backend/agents/pipeline_coordinator_rules.py (Action :81, ALLOWED_ACTION_KINDS), backend/agents/jira_dispatch.py (clear_assignee :2023), backend/agents/rate_limiter.py (RetryableExecutor :431). CONTEXT: this is the UNBUILT autonomous layer that, when acting=True, lets the coordinator MUTATE live JIRA + steer the runner fleet (reset_to_todo/clear_assignee/transition_under_review/mark_resumable/mention_operator/escalate). The design reuses JiraDispatchColdStartGateway as the live op backend. ANSWER each open question Q1-Q6 in the design with file:line evidence: Q1 escalate semantics (is escalate even emitted by any rule? what should it do?); Q2 do the gateway ops dedupe/take idem keys or can a restart double-apply?; Q3 is there an existing per-tick/rate blast-radius cap or must one be added, and what ceiling is safe?; Q4 is JiraDispatchColdStartGateway safe to call from the STEADY-STATE path or does it assume cold-start/boot context?; Q5 combined cold-start+steady-state first-boot safety when acting=True; Q6 what the design MISSED. Also audit: the dispatch mapping correctness, fail-open vs fail-closed choice (D1), the env-gate default-safety (D4), and whether reusing the cold-start gateway is actually correct. Output PRIORITIZED findings (BLOCKER/SHOULD-FIX/NICE) with file:line. Audit only, modify nothing.
warning: Codex could not find bubblewrap on PATH. Install bubblewrap with your OS package manager. See the sandbox prerequisites: https://developers.openai.com/codex/concepts/sandboxing#prerequisites. Codex will use the vendored bubblewrap in the meantime.
codex
I’ll audit only and avoid edits. I’m going to read the design and the named implementation points first, then cross-check call paths and current tests/config so the findings are tied to concrete lines.
exec
/bin/bash -lc "sed -n '250,720p' backend/agents/pipeline_coordinator.py" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "sed -n '1,260p' docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:

    Used so a record's own timestamp picks the day-file (rather than the
    write moment), keeping the log partition consistent under clock skew.
    """
    raw = record.get("ts")
    if not isinstance(raw, str):
        return None
    try:
        return datetime.fromisoformat(raw)
    except ValueError:
        return None


# ── Action layer (ADR-0021 §6) — shadow stub for 29f-3 ────────────────


class ShadowActionExecutor:
    """Records actions instead of executing them (ADR §6 / shadow mode).

    The real §6 action layer (JIRA mutate / @-operator) lands in a later
    phase; until then — and during the 29f-14 7-day shadow canary — the
    coordinator must *observe* what a rule would do without touching JIRA.
    This executor returns a per-action result record (``executed=False``)
    that the daemon folds into the decision-log line, so the integration AC
    ("rule returns Action → action layer executes it (mock if not yet built)
    → action recorded in decision log") is satisfied without any mutation.

    A real executor is a drop-in: same ``execute`` signature, returning
    ``executed=True`` plus the mutation outcome.
    """

    #: Stamped into each result so a decision-log reader can tell shadow
    #: (observed-only) executions from real ones.
    executed: bool = False

    def execute(self, action: Action, ctx: DecisionContext) -> dict[str, Any]:
        return {
            "kind": action.kind,
            "target": action.target,
            "executed": self.executed,
            "shadow": True,
        }


# Action executor seam: anything with ``execute(action, ctx) -> dict``.
ActionExecutor = Callable[[Action, DecisionContext], dict]


# ── Event sources (ADR-0021 §4) ──────────────────────────────────────


class EventDeduper:
    """Five-minute coalescing window for identical source events."""

    def __init__(
        self,
        *,
        window_seconds: float = DEFAULT_EVENT_DEDUPE_SECONDS,
        clock: Clock = _utc_now,
    ) -> None:
        self._window = timedelta(seconds=window_seconds)
        self._clock = clock
        self._seen: dict[str, datetime] = {}

    def fresh(self, event: dict[str, Any]) -> bool:
        key = self._key(event)
        now = self._clock()
        last = self._seen.get(key)
        if last is not None and now - last < self._window:
            return False
        self._seen[key] = now
        return True

    def _key(self, event: dict[str, Any]) -> str:
        payload = event.get("payload")
        if isinstance(payload, dict):
            change = payload.get("change")
            change = change if isinstance(change, dict) else {}
            parts = [
                str(event.get("source") or ""),
                str(payload.get("type") or payload.get("webhookEvent") or ""),
                str(payload.get("id") or ""),
                str(change.get("id") or ""),
                str(change.get("number") or ""),
                str(payload.get("key") or ""),
            ]
            if any(parts[1:]):
                return "\x1f".join(parts)
        return json.dumps(event, sort_keys=True, default=str)


class BridgeEventTailer:
    """Tail the bridge JSONL event tap without blocking the coordinator."""

    def __init__(self, path: Path) -> None:
        self._path = Path(path)
        self._offset = 0

    @property
    def path(self) -> Path:
        return self._path

    def poll(self) -> list[dict[str, Any]]:
        if not self._path.exists():
            return []
        size = self._path.stat().st_size
        if size < self._offset:
            self._offset = 0
        events: list[dict[str, Any]] = []
        with self._path.open("r", encoding="utf-8") as fh:
            fh.seek(self._offset)
            for line in fh:
                if not line.strip():
                    continue
                try:
                    record = json.loads(line)
                except json.JSONDecodeError:
                    continue
                payload = record.get("event") if isinstance(record, dict) else record
                if isinstance(payload, dict):
                    events.append(
                        {
                            "source": "bridge",
                            "trigger": f"bridge-event:{payload.get('type', 'unknown')}",
                            "payload": payload,
                            "bridge_record_ts": record.get("ts")
                            if isinstance(record, dict)
                            else None,
                        }
                    )
            self._offset = fh.tell()
        return events


class JiraEventPoller:
    """60s JIRA poll for coordinator-triggering ticket states."""

    def __init__(
        self,
        *,
        agent_class: str,
        clock: Clock = _utc_now,
        interval_seconds: float = DEFAULT_JIRA_POLL_INTERVAL_SECONDS,
        client_factory: Callable[[str], Any] | None = None,
        search: Callable[[Any, str], list[dict[str, Any]]] | None = None,
    ) -> None:
        self._agent_class = agent_class
        self._clock = clock
        self._interval = timedelta(seconds=interval_seconds)
        self._client_factory = client_factory
        self._search = search
        self._client: Any | None = None
        self._last_poll: datetime | None = None

    def due(self) -> bool:
        return (
            self._last_poll is None
            or self._clock() - self._last_poll >= self._interval
        )

    def poll(self) -> list[dict[str, Any]]:
        if not self.due():
            return []
        self._last_poll = self._clock()
        try:
            client = self._client or self._make_client()
            self._client = client
            issues = self._search_issues(client, coordinator_jql(client.project_key))
        except Exception as exc:  # noqa: BLE001 — source failure is non-fatal
            logger.warning("[pipeline_coordinator] jira poll failed: %s", exc)
            return []
        events: list[dict[str, Any]] = []
        for issue in issues:
            key = str(issue.get("key") or "")
            if not key:
                continue
            events.append(
                {
                    "source": "jira",
                    "trigger": "jira-poll",
                    "ticket_key": key,
                    "payload": issue,
                }
            )
        return events

    def _make_client(self) -> Any:
        if self._client_factory is not None:
            return self._client_factory(self._agent_class)
        from backend.agents import jira_dispatch

        return jira_dispatch.make_client(self._agent_class)

    def _search_issues(self, client: Any, jql: str) -> list[dict[str, Any]]:
        if self._search is not None:
            return self._search(client, jql)
        from backend.agents import jira_dispatch

        resp = jira_dispatch._request(
            client,
            "POST",
            "/search/jql",
            {
                "jql": jql,
                "fields": ["summary", "labels", "status", "assignee", "updated"],
                "maxResults": 100,
            },
        )
        return list(resp.get("issues", []))


def coordinator_jql(project_key: str) -> str:
    """ADR-0021 §4 JQL for the 60s coordinator poll."""

    return (
        f'project = "{project_key}" '
        'AND (labels is EMPTY OR labels not in ("coord-skip")) '
        "AND ("
        'labels = "needs-coordinator" '
        'OR (status in ("Done", "Closed", "Won\'t Do", "却下") '
        "AND statusCategoryChangedDate >= -1d) "
        'OR (status = "進行中" AND assignee is EMPTY)'
        ") "
        "ORDER BY updated ASC"
    )



# ══════════════════════════════════════════════════════════════════════
# Cold-start 4-phase recovery (29f-7 / OP-1005) — ADR-0021 §3.3 + §9 L6
# ══════════════════════════════════════════════════════════════════════
#
# "The coordinator is *the* canonical startup orchestrator — if it is healthy,
# everything else is" (ADR §2.4). On boot it runs four phases before entering
# the steady-state loop:
#
#   Startup-1  infra verify   — deployment-audit.sh + systemctl --user start
#   (L6)       crash recovery — replay last 24h decision log (ADR §9 L6)
#   Startup-2  JIRA reconcile — interrupted-ticket decision tree (§3.3)
#   Startup-3  stale sweep    — orphaned claim:* / dead waiting-* markers
#   Startup-4  enter loop     — log steady-state entry, hand off to run_forever
#
# Everything that touches infra / JIRA / git is funnelled through a single
# injected :class:`ColdStartGateway` seam so the orchestration stays a pure,
# deterministically-testable decision tree; the production default
# (:class:`JiraDispatchColdStartGateway`) wires the real adapters and is
# fail-safe (a degraded collaborator never raises out of startup — it logs +
# yields an empty result so the daemon still reaches its loop).

# Decision-log event names for the cold-start phases (grep-able per §9 L4).
COLD_START_BEGAN_EVENT = "cold_start_began"
COLD_START_COMPLETE_EVENT = "cold_start_complete"
STARTUP_PHASE_EVENT = "startup_phase"
CRASH_RECOVERY_EVENT = "crash_recovery_applied"
STEADY_STATE_ENTERED_EVENT = "coordinator_entered_steady_state"

# Label the runner reads to continue from an existing feature branch (§3.3 (a)).
RESUME_FROM_FEATURE_LABEL = "runner:resume-from-feature-branch"

# L6 replays at most this far back (ADR §9 L6: "last 24h of decision log").
L6_REPLAY_WINDOW_HOURS = 24.0


@dataclass(frozen=True)
class InfraUnit:
    """One expected-live unit as reported by the deployment audit (Startup-1)."""

    name: str
    live: bool


@dataclass(frozen=True)
class InfraAuditResult:
    """Outcome of a Startup-1 deployment audit pass."""

    units: tuple[InfraUnit, ...] = ()

    @property
    def down(self) -> tuple[str, ...]:
        return tuple(u.name for u in self.units if not u.live)


@dataclass(frozen=True)
class InterruptedTicket:
    """A ticket left ``進行中`` by an interrupted/crashed runner (Startup-2).

    ``stale`` is the gateway's verdict on whether the ticket has had no
    activity for > 2× the CLI timeout (ADR §3.3 (c)) — it separates a truly
    abandoned ticket (revert) from one a runner may have only just started
    on (ambiguous → operator, case (d)).
    """

    key: str
    status: str = ""
    stale: bool = False


@dataclass(frozen=True)
class StaleSweepPlan:
    """The Startup-3 sweep work the gateway found, ready to apply.

    ``stale_claims`` maps a ticket to the orphaned ``claim:*`` labels to drop;
    ``orphan_assignees`` are To-Do tickets still bot-assigned with no claim;
    ``resolved_waiting`` maps a ticket to ``runner-blocked:waiting-X`` markers
    whose blocker X is already 公開済み (a dead wait — drop the marker).
    """

    stale_claims: Mapping[str, tuple[str, ...]] = field(default_factory=dict)
    orphan_assignees: tuple[str, ...] = ()
    resolved_waiting: Mapping[str, tuple[str, ...]] = field(default_factory=dict)


@dataclass
class ColdStartReport:
    """Mutable accumulator for what the 4-phase recovery did (one boot)."""

    started_units: list[str] = field(default_factory=list)
    still_down_units: list[str] = field(default_factory=list)
    phase1_halted: bool = False
    crash_recoveries: list[dict[str, Any]] = field(default_factory=list)
    reconciled: list[dict[str, Any]] = field(default_factory=list)
    swept: list[dict[str, Any]] = field(default_factory=list)
    entered_loop: bool = False


@runtime_checkable
class ColdStartGateway(Protocol):
    """The infra / JIRA / git seam the 4-phase recovery drives (ADR §3.3).

    Every method is best-effort in production: an adapter that cannot reach
    its backend returns an empty / conservative answer rather than raising,
    so a degraded environment can never wedge the daemon before its loop.
    """

    # ── Startup-1 (infra) ──
    def audit_infra(self) -> InfraAuditResult: ...
    def start_unit(self, unit: str) -> bool: ...

    # ── Startup-2 (JIRA reconcile) ──
    def interrupted_tickets(self) -> list[InterruptedTicket]: ...
    def has_live_runner(self, key: str) -> bool: ...
    def gerrit_change_mergeable(self, key: str) -> bool: ...
    def branch_has_commits(self, key: str) -> bool: ...
    def mark_resumable(self, key: str) -> None: ...
    def transition_under_review(self, key: str) -> None: ...
    def reset_to_todo(self, key: str) -> None: ...

    # ── Startup-3 (stale sweep) ──
    def stale_sweep_plan(self) -> StaleSweepPlan: ...
    def remove_label(self, key: str, label: str) -> None: ...
    def clear_assignee(self, key: str) -> None: ...

    # ── shared (Startup-1 halt / ambiguous Startup-2 (d)) ──
    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None: ...


class JiraDispatchColdStartGateway:
    """Production :class:`ColdStartGateway` over ``jira_dispatch`` + git + shell.

    Heavy collaborators (``jira_dispatch.make_client``, the Gerrit SSH path)
    are imported + constructed lazily and wrapped so a failure degrades to a
    safe default — the daemon must reach its loop even on a half-up host.
    The agent class used for the JIRA/Gerrit identity defaults to the
    coordinator's own bot account (``claude``); it only ever *reads* + applies
    the §6.1-bounded actions (relabel / transition / comment), never +2.
    """

    def __init__(
        self,
        *,
        repo_root: Path | None = None,
        agent_class: str = "claude",
        cli_timeout_seconds: float = 60.0,
    ) -> None:
        self._repo_root = Path(repo_root) if repo_root is not None else _repo_root()
        self._agent_class = agent_class
        self._cli_timeout = cli_timeout_seconds
        self._client: Any = None
        self._client_tried = False

    # ── lazy JIRA client (fail-open) ──
    def _jira(self) -> Any:
        if self._client is None and not self._client_tried:
            self._client_tried = True
            try:
                from backend.agents import jira_dispatch

                self._client = jira_dispatch.make_client(self._agent_class)
            except Exception as exc:  # noqa: BLE001 — degrade, never wedge boot
                logger.warning(
                    "[pipeline_coordinator] cold-start JIRA client unavailable "
                    "(reconcile/sweep degraded): %s",
                    exc,
                )
                self._client = None
        return self._client

    # ── Startup-1 ──
    def audit_infra(self) -> InfraAuditResult:
        return run_deployment_audit(
            repo_root=self._repo_root, timeout_seconds=self._cli_timeout
        )

    def start_unit(self, unit: str) -> bool:
        try:
            proc = subprocess.run(
                ["systemctl", "--user", "start", unit],
                capture_output=True,
                text=True,
                timeout=self._cli_timeout,
            )
        except (OSError, subprocess.SubprocessError) as exc:
            logger.warning("[pipeline_coordinator] systemctl start %s failed: %s", unit, exc)
            return False
        if proc.returncode != 0:
            logger.warning(
                "[pipeline_coordinator] systemctl start %s rc=%d: %s",
                unit, proc.returncode, proc.stderr.strip(),
            )
        return proc.returncode == 0

    # ── Startup-2 ──
    def interrupted_tickets(self) -> list[InterruptedTicket]:
        client = self._jira()
        if client is None:
            return []
        try:
            from backend.agents import jira_dispatch

            jql = (
                f'project = "{client.project_key}" '
                'AND status in ("In Progress", "進行中") '
                "ORDER BY updated ASC"
            )
            resp = jira_dispatch._request(
                client, "POST", "/search/jql",
                {"jql": jql, "fields": ["status", "updated"], "maxResults": 100},
            )
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] interrupted-ticket query failed: %s", exc)
            return []
        from backend.agents.pipeline_coordinator_rules import (
            DEFAULT_CLI_TIMEOUT_SECONDS,
            STALE_MULTIPLIER,
        )

        stale_after = DEFAULT_CLI_TIMEOUT_SECONDS * STALE_MULTIPLIER
        now = datetime.now(timezone.utc)
        out: list[InterruptedTicket] = []
        for issue in resp.get("issues", []):
            fields = issue.get("fields") or {}
            status = str((fields.get("status") or {}).get("name", ""))
            # ``updated`` is a coarse last-activity proxy for staleness (the
            # exact 進行中-entry time needs the changelog; updated is a safe,
            # cheaper lower bound — if it's old, the ticket is certainly stale).
            stale = False
            updated_raw = fields.get("updated")
            if isinstance(updated_raw, str):
                try:
                    updated = datetime.fromisoformat(updated_raw.replace("Z", "+00:00"))
                    stale = (now - updated).total_seconds() > stale_after
                except ValueError:
                    pass
            out.append(
                InterruptedTicket(key=issue.get("key", "?"), status=status, stale=stale)
            )
        return out

    def has_live_runner(self, key: str) -> bool:
        return _ticket_has_live_runner(key, repo_root=self._repo_root)


 succeeded in 0ms:
# Coordinator §6 live action executor + acting-flip — DESIGN (for codex audit)

**Status**: DRAFT for codex audit (2026-05-22). This is the unbuilt §6 action layer that `acting=True` requires — NOT a CLI switch. Implements the autonomous layer that mutates live JIRA + steers the runner fleet.

## Problem / scope
`build_default_coordinator(config, acting=True, action_executor=...)` raises `ValueError` without an `action_executor` (pipeline_coordinator.py:1884). The CLI (`:1936`) only ever builds `acting=False` (shadow). The real executor "lands in a later phase" (ShadowActionExecutor docstring, :266) — it is unbuilt. This design builds it.

## Key reuse insight (keeps risk low)
`JiraDispatchColdStartGateway` (pipeline_coordinator.py:606) is the LIVE gateway already used by the **cold-start** live path (`_default_cold_start_gateway(acting=True)`), and it already implements EVERY mutation the steady-state actions need:
- `mark_resumable` (:735), `transition_under_review` (:738), `reset_to_todo` (:750), `clear_assignee` (:787), `mention_operator` (:799).

So the steady-state §6 executor is a **thin dispatcher over the SAME proven live gateway** — not new mutation code. (`Action`: `pipeline_coordinator_rules.py:81`, frozen dataclass `kind`/`target`/`params`; `kind` ∈ `ALLOWED_ACTION_KINDS`.)

## Design — `LiveActionExecutor`
A callable `execute(action, ctx) -> dict` (same seam as `ShadowActionExecutor`, `ActionExecutor = Callable[[Action, DecisionContext], dict]`, :295), wrapping a `JiraDispatchColdStartGateway`:

| Action.kind | live op | notes |
|---|---|---|
| `clear_assignee` | `gw.clear_assignee(target)` | also jira_dispatch.clear_assignee(:2023) |
| `reset_to_todo` | `gw.reset_to_todo(target)` | |
| `transition_under_review` | `gw.transition_under_review(target)` | |
| `mark_resumable` | `gw.mark_resumable(target)` | |
| `mention_operator` | `gw.mention_operator(target, params["message"], urgency=params.get("urgency","high"))` | |
| `escalate` | **OPEN Q1** — no gateway op; proposed = `mention_operator` with `urgency="critical"` + an `escalated` result flag (do NOT silently drop) | |
| `noop` | no mutation; `executed=False` | |

Returns `{"kind","target","executed":True,"outcome":...}` on success, or `{"executed":False,"error":...}` on failure (folded into the decision-log line, same shape as the shadow result so readers can diff shadow-vs-live).

## Design decisions (for audit)
- **D1 — error posture: fail-OPEN per-action.** A failed mutation logs + records `executed:False,error:...` and the tick CONTINUES (never crash the daemon / wedge the coordinator on one bad JIRA call). Rationale: the coordinator is the fleet's brain — one failed action must not take it down. (Mirrors the gateway's lazy/fail-open construction note, :608.)
- **D2 — idempotency.** Derive a per-action idem key (`coord:{decision_id}:{kind}:{target}`) so a retried tick / restart does not double-apply. clear_assignee already takes `idem_key`; the other gateway ops need a check (OPEN Q2 — do they dedupe?).
- **D3 — blast-radius cap.** A per-tick mutation ceiling (e.g., ≤ N executed mutations/tick, default small) + a kill-switch env so a misfiring rule can't mass-mutate the fleet. (OPEN Q3 — right ceiling? reuse rate_limiter.RetryableExecutor:431?)
- **D4 — env-gate, default-safe.** CLI `main()` reads `OMNISIGHT_COORDINATOR_ACTING` (truthy → `acting=True` + `LiveActionExecutor`; default/unset → `acting=False` shadow). Code default stays acting=False. The flip is then a reversible env change + a coordinator restart — NOT a redeploy.
- **D5 — budget coupling.** Removing `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` happens AT the flip (restoring it while shadow pays for un-executed LLM calls). The env-gate doc must state both env changes go together.
- **D6 — decision-log audit.** Every executed action writes `executed:True` + outcome to the decision-log (the audit trail for "what did the autonomous coordinator actually do"). Already the daemon folds the executor result into the line.

## Open questions for codex
- Q1: `escalate` semantics — mention_operator(critical) vs a distinct path? Is `escalate` ever emitted today (grep the rules)?
- Q2: do `reset_to_todo`/`transition_under_review`/`mark_resumable`/`mention_operator` on `JiraDispatchColdStartGateway` already dedupe / take idem keys, or can a restart double-apply?
- Q3: blast-radius — is there an existing per-tick/rate cap, or must D3 add one? What ceiling is safe for the live fleet?
- Q4: is `JiraDispatchColdStartGateway` safe to call from the STEADY-STATE path, or does it assume cold-start context (e.g., only-once semantics, boot-time assumptions)?
- Q5: cold-start vs steady-state — when acting=True, BOTH the cold-start gateway AND this executor go live in the same restart. Is the combined first-boot behavior safe (the cold_start_complete=[] today says nothing to reset, but confirm)?
- Q6: anything the design MISSED for an autonomous layer that mutates live JIRA + steers the fleet.

## Tests (planned)
- Dispatch: each kind → the right gateway method called with the right args (mock gateway).
- Fail-open: a gateway op raising → result `executed:False,error` + tick continues.
- Idempotency: same action twice → one effective mutation.
- Blast-radius: > ceiling actions in a tick → capped + logged.
- env-gate: `OMNISIGHT_COORDINATOR_ACTING` unset → shadow (acting=False, executor=Shadow); set → acting=True + LiveActionExecutor; `acting=True` without executor still raises.

## Activation sequence (after merge + your +2)
1. Set `OMNISIGHT_COORDINATOR_ACTING=1` + remove `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` in the coordinator `env.conf` drop-in.
2. `systemctl --user restart pipeline-coordinator`.
3. Watch the decision-log for `executed:True` actions + verify they match intent; rollback = unset the env + restart (reversible).

exec
/bin/bash -lc "sed -n '1840,1975p' backend/agents/pipeline_coordinator.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:

    def _install_signal_handlers(self) -> None:
        if self._signals_installed:
            return

        def _handler(signum: int, _frame: Any) -> None:
            logger.warning("[pipeline_coordinator] caught signal %d — draining", signum)
            self.request_stop()

        for sig in (signal.SIGTERM, signal.SIGINT):
            try:
                signal.signal(sig, _handler)
            except (ValueError, OSError) as exc:
                # Non-main thread / unsupported platform — caller drives
                # stop via request_stop() instead.
                logger.debug(
                    "[pipeline_coordinator] could not install handler for %s: %s",
                    sig, exc,
                )
        self._signals_installed = True


def build_default_coordinator(
    config: CoordinatorConfig | None = None,
    *,
    acting: bool = False,
    action_executor: ActionExecutor | None = None,
) -> PipelineCoordinator:
    """Production wiring: env config, hybrid Tier-1+Tier-2 engine, shadow layer.

    29f-3 shipped the Tier-1 rule engine; 29f-6 fronts it with the Tier-2 LLM
    consultant (:class:`HybridDecisionEngine`) so a tick that no Tier-1 rule
    matches (or that the personality mode escalates) consults the claude CLI
    under a daily budget cap. Constructing the engine logs the loaded rule
    registry (Deploy AC). The budget guard rebuilds the day's spend from the
    decision log (ADR §3.2 stateless-across-restarts). The action layer
    defaults to shadow (observe-only) so this stays safe for the 29f-14 7-day
    shadow canary. ``acting=True`` is the single switch that permits the main
    action executor, the hourly sprint re-plan handler, AND the 4-phase
    cold-start recovery to mutate; ``acting=False`` wires every one of them
    observe-only (cold-start via :class:`ShadowColdStartGateway`, OP-1555).
    """
    config = config or CoordinatorConfig.from_env()
    if acting and action_executor is None:
        raise ValueError("acting=True requires an explicit action_executor")
    if not acting and action_executor is not None:
        raise ValueError("action_executor requires acting=True")
    decision_log = DecisionLog(config.decision_log_dir)
    # 29f-11: the learning loop reads + appends to the same decision log; build
    # it from the log so its daily/weekly schedule survives a restart (§3.2).
    from backend.agents.learning_loop import build_default_learning_loop

    return PipelineCoordinator(
        config,
        engine=build_hybrid_engine(decision_log_dir=config.decision_log_dir),
        action_executor=action_executor,
        cold_start_gateway=_default_cold_start_gateway(config, acting=acting),
        sprint_replan_handler=build_default_sprint_replan_handler(
            config_dir=config.config_dir,
            decision_log_dir=config.decision_log_dir,
            capacity_path=config.capacity_path,
            agent_class=config.jira_agent_class,
            shadow=not acting,
        ),
        decision_log=decision_log,
        learning_loop=build_default_learning_loop(decision_log),
    )


def main(argv: list[str] | None = None) -> int:
    """CLI entrypoint. ``--once`` runs a single tick; default runs forever."""
    import argparse

    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument(
        "--once",
        action="store_true",
        help="run a single tick (heartbeat + one no-op decision) and exit "
             "(skips cold-start recovery — a diagnostic tick, not a boot)",
    )
    parser.add_argument(
        "--no-cold-start",
        action="store_true",
        help="enter the steady-state loop directly, skipping the 4-phase "
             "cold-start recovery (ADR §3.3) — for local debugging only",
    )
    parser.add_argument(
        "--config-dir",
        type=Path,
        default=None,
        help="override the coordinator config dir (default ~/.config/omnisight/coordinator)",
    )
    args = parser.parse_args(argv)

    logging.basicConfig(level=logging.INFO)
    config = CoordinatorConfig.from_env(config_dir=args.config_dir)
    coordinator = build_default_coordinator(config)
    if args.once:
        coordinator.run_once()
        return 0
    if args.no_cold_start:
        coordinator.run_forever()
        return 0
    # Production boot: 4-phase cold-start recovery, then steady-state loop.
    coordinator.run()
    return 0


if __name__ == "__main__":
    import sys

    raise SystemExit(main(sys.argv[1:]))

exec
/bin/bash -lc "sed -n '1,150p' backend/agents/pipeline_coordinator_rules.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
"""Pipeline-coordinator decision-engine seam (29f-coord skeleton).

ADR-0021 §2.1 specifies a *hybrid* decision engine: deterministic Python
Tier-1 rules handle ≥90 % of decisions, with LLM consultation reserved for
novel / multi-plausible cases. None of that logic exists yet — this is the
foundation ticket (29f-coord). What ships here is the **contract** the real
rule set (29f-3), cold-start orchestrator (29f-7) and event subscribers
(29f-8) all plug into without changing the daemon:

    DecisionContext  — everything the engine is allowed to read for a tick
    DecisionEngine.evaluate(ctx) -> DecisionResult
    Action / NoopAction          — the action vocabulary
    DecisionResult               — actions + reason + decision_id

The skeleton engine is intentionally empty: it returns a single
``NoopAction`` with reason ``skeleton_noop`` and ``dry_run=True`` so the
daemon can run end-to-end (heartbeat + decision-log) before any real
decision logic exists.

Module-global state audit (per project SOP)
-------------------------------------------
Frozen dataclasses, a Protocol, and a stateless engine class. No module
globals, no import-time I/O. ``engine_version`` is a class constant so the
decision-log records which engine produced each tick.
"""

from __future__ import annotations

import inspect
import logging
import os
import uuid
from dataclasses import dataclass, field
from datetime import datetime
from typing import Any, Callable, Mapping, Protocol, runtime_checkable

from backend.agents.pipeline_coordinator_capacity import CapacitySnapshot
from backend.agents.pipeline_coordinator_modes import (
    ModeSelector,
    PersonalityMode,
    SituationProfile,
    mode_behavior,
)

logger = logging.getLogger(__name__)

# Bumped when the engine's decision semantics change. 0.x = skeleton era.
SKELETON_ENGINE_VERSION = "0.1.0-skeleton"

# Stable reason string the skeleton emits each tick (asserted by tests and
# grep-able in the decision log per ADR-0021 §9 L4).
SKELETON_NOOP_REASON = "skeleton_noop"

# ── Action vocabulary (ADR-0021 §6.1) ─────────────────────────────────
# The *closed* set of action kinds a rule (or the Tier-2 LLM) may emit.
# §6.1 deliberately keeps this list closed for safety; new kinds are added
# by reviewable operator patch (§12 open-question 6), never by an LLM
# proposal. Anything outside this set is dropped by the action layer.
ACTION_NOOP = "noop"
ACTION_FILE_TICKET = "file_ticket"
ACTION_RELABEL = "relabel"
ACTION_TRANSITION = "transition"
ACTION_MENTION_OPERATOR = "mention_operator"
ACTION_MARK_FOR_FOLLOWUP = "mark_for_followup"
ACTION_ESCALATE = "escalate"

ALLOWED_ACTION_KINDS: frozenset[str] = frozenset(
    {
        ACTION_NOOP,
        ACTION_FILE_TICKET,
        ACTION_RELABEL,
        ACTION_TRANSITION,
        ACTION_MENTION_OPERATOR,
        ACTION_MARK_FOR_FOLLOWUP,
        ACTION_ESCALATE,
    }
)


@dataclass(frozen=True)
class Action:
    """A single thing the coordinator decides to do.

    The skeleton only ever emits :class:`NoopAction`; the Tier-1 rule set
    (29f-3) emits the §6.1 action kinds via the factory classmethods below.
    The envelope is unchanged: ``kind`` is one of :data:`ALLOWED_ACTION_KINDS`,
    ``target`` is the JIRA key the action applies to, ``params`` carries the
    kind-specific payload, and ``dry_run`` defaults to True so a half-wired
    (or shadow-mode, 29f-14) daemon can never mutate JIRA/Gerrit.

    "Union type" (Code AC): :class:`Action` + :class:`NoopAction` over the
    constrained :data:`ALLOWED_ACTION_KINDS` *is* the action union — a closed
    set discriminated by ``kind`` rather than a ``typing.Union`` of N classes,
    so the decision-log serialisation (:meth:`to_record`) stays uniform.
    """

    kind: str
    target: str = ""
    params: Mapping[str, Any] = field(default_factory=dict)
    dry_run: bool = True

    def __post_init__(self) -> None:
        if not isinstance(self.kind, str) or not self.kind.strip():
            raise ValueError("Action.kind must be a non-empty string")
        if self.kind not in ALLOWED_ACTION_KINDS:
            raise ValueError(
                f"Action.kind {self.kind!r} is not in the §6.1 allowed set "
                f"{sorted(ALLOWED_ACTION_KINDS)}"
            )
        object.__setattr__(self, "params", dict(self.params))

    def to_record(self) -> dict[str, Any]:
        """Serialisable form embedded in a decision-log ``actions`` array."""
        return {
            "kind": self.kind,
            "target": self.target,
            "params": dict(self.params),
            "dry_run": self.dry_run,
        }

    # ── §6.1 factory constructors ─────────────────────────────────────
    # Each rule emits one of these so payloads stay well-shaped (the action
    # layer reads fixed param keys). ``dry_run`` defaults True everywhere;
    # the action layer / shadow-mode toggle flips it, never a rule.

    @classmethod
    def file_ticket(
        cls,
        *,
        blocking: str,
        target_area: str,
        description: str,
        dry_run: bool = True,
    ) -> "Action":
        """§6.1 file_ticket — file a dependency/work ticket in another area."""
        return cls(
            kind=ACTION_FILE_TICKET,
            target=blocking,
            params={
                "blocking": blocking,
                "target_area": target_area,
                "description": description,
            },
            dry_run=dry_run,
        )

    @classmethod
    def relabel(
        cls,
        ticket: str,

exec
/bin/bash -lc "sed -n '720,860p' backend/agents/pipeline_coordinator.py" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "sed -n '150,360p' backend/agents/pipeline_coordinator_rules.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
        ticket: str,
        *,
        add: tuple[str, ...] = (),
        remove: tuple[str, ...] = (),
        dry_run: bool = True,
    ) -> "Action":
        """§6.1 relabel — add/remove labels (never operator-only labels)."""
        return cls(
            kind=ACTION_RELABEL,
            target=ticket,
            params={"add": list(add), "remove": list(remove)},
            dry_run=dry_run,
        )

    @classmethod
    def transition(
        cls, ticket: str, *, to_status: str, dry_run: bool = True
    ) -> "Action":
        """§6.1 transition — move a ticket through an allowed status edge."""
        return cls(
            kind=ACTION_TRANSITION,
            target=ticket,
            params={"to_status": to_status},
            dry_run=dry_run,
        )

    @classmethod
    def mention_operator(
        cls,
        ticket: str,
        *,
        message: str,
        urgency: str = "medium",
        dry_run: bool = True,
    ) -> "Action":
        """§6.1 mention_operator — @-mention operator; high urgency tags too."""
        return cls(
            kind=ACTION_MENTION_OPERATOR,
            target=ticket,
            params={"message": message, "urgency": urgency},
            dry_run=dry_run,
        )

    @classmethod
    def mark_for_followup(
        cls, ticket: str, *, when: str, why: str, dry_run: bool = True
    ) -> "Action":
        """§6.1 mark_for_followup — defer attention via coord-resume-after."""
        return cls(
            kind=ACTION_MARK_FOR_FOLLOWUP,
            target=ticket,
            params={"when": when, "why": why},
            dry_run=dry_run,
        )

    @classmethod
    def escalate(cls, ticket: str, *, reason: str, dry_run: bool = True) -> "Action":
        """§6.1 escalate — needs-operator-action label + reason comment."""
        return cls(
            kind=ACTION_ESCALATE,
            target=ticket,
            params={"reason": reason},
            dry_run=dry_run,
        )


@dataclass(frozen=True)
class NoopAction(Action):
    """The do-nothing action the skeleton engine returns every tick."""

    def __init__(self) -> None:
        super().__init__(kind="noop", target="", params={}, dry_run=True)


@dataclass(frozen=True)
class DecisionContext:
    """Everything the engine may read to make one decision (a "tick").

    Stateless-across-restarts (ADR-0021 §3.2): the daemon re-builds this
    each tick from the injected clock + work-graph cache + capacity
    snapshot. The skeleton populates only ``now`` and ``capacity``; later
    phases add the JIRA work graph, bridge-log tail and event payload.
    """

    now: datetime
    capacity: CapacitySnapshot
    mode: str = "skeleton"
    # Per-situation personality inputs (ADR-0021 §7). The daemon attaches a
    # ``situation`` profile + any operator ``coord-mode:*`` override when a
    # tick is actually deciding about a ticket/event; an idle tick leaves
    # both ``None`` so the engine keeps the skeleton ``mode``.
    situation: SituationProfile | None = None
    mode_override: str | None = None
    # Open-ended bag for later phases (event payload, work-graph cache,
    # bridge-log tail). Skeleton leaves it empty.
    work_graph: Mapping[str, Any] = field(default_factory=dict)
    # 29f-3: the ticket this tick is reasoning about (the "focal" ticket).
    # ``None`` when the daemon ticks with an empty work-graph (skeleton /
    # idle); the Tier-1 rules each early-return ``None`` in that case.
    ticket: "Ticket | None" = None
    # The wider work-graph slice the cross-ticket rules traverse
    # (runner-blocked-marker-stale resolves the blocker's status;
    # chain-deadlock walks the blockedBy DAG). Keyed by JIRA key.
    tickets: Mapping[str, "Ticket"] = field(default_factory=dict)

    def __post_init__(self) -> None:
        if self.now.tzinfo is None:
            raise ValueError("DecisionContext.now must be timezone-aware")
        object.__setattr__(self, "work_graph", dict(self.work_graph))
        object.__setattr__(self, "tickets", dict(self.tickets))


@dataclass(frozen=True)
class DecisionResult:
    """Outcome of one ``evaluate`` call.

    ``decision_id`` is a fresh UUID per tick so a decision-log line can be
    correlated with downstream action records (and L6 crash-recovery can
    pair ``shutdown_began`` / ``shutdown_complete`` with the right tick).
    """

    decision_id: str
    actions: tuple[Action, ...]
    reason: str
    mode: str
    engine_version: str
    # The selected mode's behavior overlay (ADR-0021 §7.2), or ``None`` for
    # an idle/skeleton tick. Tier-1 rules + Tier-2 LLM read this to know how
    # hard to lean on rules vs. LLM, how wide to make the LLM context, and
    # whether to file a follow-up — the Integration-AC "see + respect the
    # selected mode" seam.
    mode_behavior: PersonalityMode | None = None
    # 29f-3: which Tier-1 rule produced this result (ADR Appendix C
    # ``rule_name``) and which tier decided. Default ``None``/``None`` keeps
    # the skeleton engine's construction (which names neither) valid.
    rule_name: str | None = None
    tier: int | None = None
    # 29f-6: the Tier-2 LLM-consultation metadata block (ADR Appendix C
    # ``llm_consultation`` — context/response token counts, cost, confidence,
    # decision rationale, cited lessons, or a budget-degrade marker). ``None``
    # for a Tier-1 / skeleton tick that never consulted the LLM.
    llm_consultation: Mapping[str, Any] | None = None

    @property
    def is_noop(self) -> bool:
        return all(isinstance(a, NoopAction) for a in self.actions) or not self.actions


@runtime_checkable
class DecisionEngineProtocol(Protocol):
    """The seam 29f-3 implements; the daemon depends on this, not the class."""

    engine_version: str

    def evaluate(self, ctx: DecisionContext) -> DecisionResult: ...


class DecisionEngine:
    """Skeleton decision engine — selects a mode, then logs one no-op per tick.

    Real Tier-1 rules + Tier-2 LLM consultation land in 29f-3 / 29f-6. What
    AUDIT-29f-5 wires here is the **mode-selection-before-rule-evaluation**
    contract (ADR-0021 §7, Integration AC): when a tick carries a situation
    profile, the engine asks its :class:`ModeSelector` for the personality
    mode *before* any rule body would run, and threads the selected mode +
    its behavior overlay through the result so the (future) Tier-1 rules and
    Tier-2 LLM consultation can read + respect it. An idle tick (no
    ``situation``) keeps the skeleton ``mode`` so a no-op still logs "skeleton".

    Keeping the skeleton a concrete class (rather than only a Protocol) lets
    the daemon default-construct an engine and run end-to-end today.
    """

    engine_version: str = SKELETON_ENGINE_VERSION

    def __init__(self, *, mode_selector: ModeSelector | None = None) -> None:
        self._mode_selector = mode_selector or ModeSelector()

    def evaluate(self, ctx: DecisionContext) -> DecisionResult:
        """Select the mode, then return a single :class:`NoopAction`.

        Mode selection runs first (before any rule body) and is the only
        thing this skeleton engine decides; the action set stays a no-op
        until 29f-3 lands the Tier-1 rules. No I/O.
        """
        mode = ctx.mode
        if ctx.situation is not None:
            mode = self._mode_selector.select(ctx.situation, override=ctx.mode_override)
        behavior = mode_behavior(mode)
        # (29f-3 Tier-1 rules + 29f-6 Tier-2 LLM consultation read `behavior`
        #  here to steer rule-vs-LLM weighting, LLM context width, follow-up
        #  filing — then populate `actions`. The skeleton stays no-op.)
        return DecisionResult(
            decision_id=uuid.uuid4().hex,
            actions=(NoopAction(),),
            reason=SKELETON_NOOP_REASON,
            mode=mode,
            engine_version=self.engine_version,
            mode_behavior=behavior,
        )


# ══════════════════════════════════════════════════════════════════════
# Tier-1 deterministic rules (29f-3 / OP-1001) — ADR-0021 §5.1
# ══════════════════════════════════════════════════════════════════════
#
# The 10 rules below handle the ≥90% of decisions that don't need an LLM.
# Each is a *pure* function ``(DecisionContext) -> Action | None``:
#   - return an :class:`Action`  → this rule fires (first match wins)
#   - return ``None``            → rule abstains; the next rule (or Tier-2)
#                                  takes the situation

 succeeded in 0ms:

    def gerrit_change_mergeable(self, key: str) -> bool:
        # Conservative: only a *positively confirmed* mergeable open change
        # routes a ticket to Under Review (§3.3 (b)). When the Gerrit query
        # is unavailable we return False so the ticket falls through to the
        # resume / revert / operator branches rather than a wrong transition.
        try:
            return _gerrit_open_change_mergeable(key, self._agent_class)
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] gerrit mergeable check failed for %s: %s", key, exc)
            return False

    def branch_has_commits(self, key: str) -> bool:
        return _feature_branch_has_commits(key, repo_root=self._repo_root)

    def mark_resumable(self, key: str) -> None:
        self._add_label(key, RESUME_FROM_FEATURE_LABEL)

    def transition_under_review(self, key: str) -> None:
        client = self._jira()
        if client is None:
            return
        try:
            from backend.agents import jira_dispatch

            # Idempotent per OP-691: skips when already Under Review.
            jira_dispatch.transition_to_under_review_if_needed(client, key)
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] transition_under_review(%s) failed: %s", key, exc)

    def reset_to_todo(self, key: str) -> None:
        client = self._jira()
        if client is None:
            return
        try:
            from backend.agents import jira_dispatch

            jira_dispatch.transition_back_to_todo(
                client, key,
                reason="[cold-start Startup-2(c)] interrupted with no commits/activity; "
                       "reverting to To Do for clean re-pickup.",
            )
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] reset_to_todo(%s) failed: %s", key, exc)

    # ── Startup-3 ──
    def stale_sweep_plan(self) -> StaleSweepPlan:
        client = self._jira()
        if client is None:
            return StaleSweepPlan()
        try:
            return _build_stale_sweep_plan(client, repo_root=self._repo_root)
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] stale-sweep plan failed: %s", exc)
            return StaleSweepPlan()

    def remove_label(self, key: str, label: str) -> None:
        client = self._jira()
        if client is None:
            return
        try:
            from backend.agents import jira_dispatch

            jira_dispatch.remove_label(client, key, label)
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] remove_label(%s, %s) failed: %s", key, label, exc)

    def clear_assignee(self, key: str) -> None:
        client = self._jira()
        if client is None:
            return
        try:
            from backend.agents import jira_dispatch

            jira_dispatch.clear_assignee(client, key)
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] clear_assignee(%s) failed: %s", key, exc)

    # ── shared ──
    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
        client = self._jira()
        if client is None:
            logger.error("[pipeline_coordinator] OPERATOR-ALERT (JIRA down) %s: %s", key, message)
            return
        try:
            from backend.agents import jira_dispatch

            jira_dispatch.add_comment(client, key, f"@nanakusa-sora {message}")
            if urgency == "high":
                jira_dispatch.add_label(client, key, "needs-operator-action")
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] mention_operator(%s) failed: %s", key, exc)

    def _add_label(self, key: str, label: str) -> None:
        client = self._jira()
        if client is None:
            return
        try:
            from backend.agents import jira_dispatch

            jira_dispatch.add_label(client, key, label)
        except Exception as exc:  # noqa: BLE001
            logger.warning("[pipeline_coordinator] add_label(%s, %s) failed: %s", key, label, exc)


class ShadowColdStartGateway:
    """Observe-only :class:`ColdStartGateway` for the 7-day shadow canary (OP-1555).

    Wraps a real gateway and forwards every *read* (``audit_infra`` /
    ``interrupted_tickets`` / ``stale_sweep_plan`` + the per-ticket probes) so
    the 4-phase recovery still sees the true world — observation is the whole
    point of shadow mode — but turns every *mutator* into a record-only no-op:
    the would-be ``reset_to_todo`` / ``mark_resumable`` /
    ``transition_under_review`` / ``remove_label`` / ``clear_assignee`` /
    ``start_unit`` / ``mention_operator`` is logged (and lands in the decision
    log via the orchestrator's per-phase ``startup_phase`` records) without
    ever touching live JIRA / git / systemctl.

    This is the cold-start sibling of :class:`ShadowActionExecutor` and of the
    ``sprint_replan(shadow=not acting)`` wiring: the single ``acting`` switch in
    :func:`build_default_coordinator` flips engine + sprint-replan + cold-start
    together. Before OP-1555, ``build_default_coordinator`` wired the live
    gateway unconditionally, so a coordinator deployed "in shadow mode"
    (``acting=False``) still mutated live JIRA on every boot — the gap this
    closes (sibling of OP-1552).
    """

    #: Marks this gateway observe-only (parallels ``ShadowActionExecutor.executed``).
    acting: bool = False

    def __init__(self, delegate: ColdStartGateway) -> None:
        self._delegate = delegate

    # ── reads: forwarded unchanged (observation is the point) ──
    def audit_infra(self) -> InfraAuditResult:
        return self._delegate.audit_infra()

    def interrupted_tickets(self) -> list[InterruptedTicket]:
        return self._delegate.interrupted_tickets()

    def has_live_runner(self, key: str) -> bool:

exec
/bin/bash -lc "sed -n '1980,2055p' backend/agents/jira_dispatch.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
            )
        except Exception as exc:  # noqa: BLE001 — best-effort wiring
            log.warning(
                "jira_dispatch.record_runner_incident_failed key=%s err=%s",
                key,
                exc,
            )
    add_comment(
        client, key, f"Reverting to TODO. Reason:\n{reason}",
        idem_key=f"{base_key}-comment",
    )
    clear_assignee(client, key, idem_key=f"{base_key}-clear-assignee")
    _request_idempotent(
        client, "POST", f"/issue/{key}/transitions",
        {"transition": {"id": TRANSITION_IDS["back_to_todo"]}},
        f"{base_key}-transition",
    )

    # OP-1140: stoploss circuit-breaker recording — best-effort.
    # A recorder fault (network blip, label-fetch 404 on a freshly-created
    # ticket, ...) must NEVER prevent the §11-revert from completing; the
    # operator still needs the ticket re-armed for triage even if the
    # counter slips one tick.
    try:
        from backend.agents import runner_stoploss

        labels_now = fetch_labels(client, key)
        runner_stoploss.register_revert(client, key, labels_now)
    except Exception as exc:  # noqa: BLE001 — best-effort wiring
        log.warning(
            "jira_dispatch.register_revert_failed key=%s err=%s", key, exc,
        )


def add_comment(client: DispatchClient, key: str, text: str, idem_key: str | None = None) -> None:
    idem_key = idem_key or f"comment-{key}-{uuid.uuid4().hex[:12]}"
    _request_idempotent(
        client, "POST", f"/issue/{key}/comment",
        {"body": _adf_paragraph(text)},
        idem_key,
    )


def clear_assignee(client: DispatchClient, key: str, idem_key: str | None = None) -> None:
    idem_key = idem_key or f"clear-assignee-{key}-{uuid.uuid4().hex[:12]}"
    _request_idempotent(
        client, "PUT", f"/issue/{key}",
        {"fields": {"assignee": None}},
        idem_key,
    )


def add_label(client: DispatchClient, key: str, label: str, idem_key: str | None = None) -> None:
    """Add one JIRA label without replacing the existing label set."""
    idem_key = idem_key or f"label-add-{key}-{label}-{uuid.uuid4().hex[:12]}"
    _request_idempotent(
        client, "PUT", f"/issue/{key}",
        {"update": {"labels": [{"add": label}]}},
        idem_key,
    )


def remove_label(client: DispatchClient, key: str, label: str, idem_key: str | None = None) -> None:
    """Remove one JIRA label if present; JIRA treats absent labels as a no-op."""
    idem_key = idem_key or f"label-remove-{key}-{label}-{uuid.uuid4().hex[:12]}"
    _request_idempotent(
        client, "PUT", f"/issue/{key}",
        {"update": {"labels": [{"remove": label}]}},
        idem_key,
    )


def dependency_waiting_label(blocker_key: str) -> str:
    """Return the runner label used while ``blocker_key`` gates pickup."""
    return f"{DEPENDENCY_WAITING_LABEL_PREFIX}{blocker_key}"


exec
/bin/bash -lc "sed -n '410,500p' backend/agents/rate_limiter.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
        items = list(self._entries.values())
        if since:
            items = [e for e in items if e.created_at >= since]
        return sorted(items, key=lambda e: e.created_at, reverse=True)

    async def remove(self, entry_id: str) -> bool:
        return self._entries.pop(entry_id, None) is not None

    def __len__(self) -> int:
        return len(self._entries)


# ─── Retryable executor ──────────────────────────────────────────


CallableAsync = Callable[[], Awaitable[Any]]
"""The thing we wrap — a zero-arg async closure that performs the
actual API call. The caller closes over their request data, so the
executor doesn't need to know the shape."""


class RetryableExecutor:
    """Execute an async API call with retries + DLQ.

    Caller provides:
      - ``call_factory``: zero-arg coroutine factory; called once per attempt
      - ``status_extractor``: optional fn that extracts HTTP status from
        an exception (Anthropic SDK puts it on .status_code attribute)
      - ``retry_after_extractor``: optional fn that extracts the
        Retry-After header value from an exception (None → use backoff)

    Behaviour:
      - Attempt 1..max_retries+1 with exponential backoff + jitter
      - Honours `retry-after` from rate-limit responses
      - Records to RateLimitTracker on success
      - Deposits to DLQ on max-retry exhaustion or non_retryable error
    """

    def __init__(
        self,
        *,
        policy: RetryPolicy | None = None,
        tracker: RateLimitTracker | None = None,
        dlq: DeadLetterQueue | None = None,
        sleep: Callable[[float], Awaitable[None]] = asyncio.sleep,
    ) -> None:
        # Use ``is None`` checks rather than ``or``: an empty
        # InMemoryDeadLetterQueue is falsy via ``__len__`` and would
        # silently get replaced with a fresh one, dropping the caller's
        # reference (and its inspection ability).
        self.policy = policy if policy is not None else RetryPolicy()
        self.tracker = tracker if tracker is not None else RateLimitTracker()
        self.dlq: DeadLetterQueue = (
            dlq if dlq is not None else InMemoryDeadLetterQueue()
        )
        self._sleep = sleep

    async def execute(
        self,
        call_factory: CallableAsync,
        *,
        workspace: str,
        model: str,
        input_tokens_estimated: int = 0,
        output_tokens_estimated: int = 0,
        request_metadata: dict[str, Any] | None = None,
        status_extractor: Callable[[BaseException], int | None] = lambda _: None,
        retry_after_extractor: Callable[[BaseException], str | None] = lambda _: None,
    ) -> Any:
        """Run with retries. Returns the call result on success.

        On final failure: deposits DLQ entry + re-raises the last
        exception so the caller can choose to handle / propagate.
        """
        last_exception: BaseException | None = None
        last_classification: RetryClassification = "retryable"
        last_status_code: int | None = None
        last_reason: str = ""

        # Pre-flight: tracker check.
        breached, reason = self.tracker.would_exceed(
            workspace=workspace, model=model,
            input_tokens_estimated=input_tokens_estimated,
            output_tokens_estimated=output_tokens_estimated,
        )
        if breached:
            # Treat predicted breach as a synthetic 429 — backoff + retry.
            logger.info(
                "RateLimitTracker preflight: workspace=%s model=%s would breach (%s); "
                "delaying before submit",
                workspace, model, reason,

codex
The first pass already shows a vocabulary mismatch: the design dispatch table names live ops like `reset_to_todo`, while the current closed action vocabulary is broader ADR names like `transition`, `relabel`, and `mark_for_followup`. I’m checking rule emitters and the daemon execution path now, because that determines whether this is a design bug or just a planned vocabulary change.
exec
/bin/bash -lc "rg -n \"action_executor|execute\\(|decision_log|actions\" backend/agents/pipeline_coordinator.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
125:    decision_log_dir: Path
162:        decision_log_dir = Path(
168:            decision_log_dir=decision_log_dir,
267:    """Records actions instead of executing them (ADR §6 / shadow mode).
285:    def execute(self, action: Action, ctx: DecisionContext) -> dict[str, Any]:
294:# Action executor seam: anything with ``execute(action, ctx) -> dict``.
614:    the §6.1-bounded actions (relabel / transition / comment), never +2.
1173:        decision_log: DecisionLog | None = None,
1176:        action_executor: ActionExecutor | None = None,
1190:        self._decision_log = decision_log or DecisionLog(config.decision_log_dir, clock=clock)
1198:        executor = action_executor or ShadowActionExecutor().execute
1199:        self._action_executor: ActionExecutor = executor
1316:                        "actions": [],
1356:        # A no-op tick records ``actions:[]``; only real (non-noop) actions
1358:        actions = [a.to_record() for a in result.actions if not isinstance(a, NoopAction)]
1359:        dry_run = all(a.dry_run for a in result.actions) if result.actions else True
1366:            "actions": actions,
1408:        self._decision_log.append(base)
1487:        records = self._read_recent_decision_log()
1493:            if event == "decision_tick" and not rec.get("dry_run", True) and rec.get("actions"):
1505:            resolutions = self._resolve_interrupted_actions(tick)
1514:    def _resolve_interrupted_actions(self, tick: dict[str, Any] | None) -> list[dict[str, Any]]:
1520:        for action in tick.get("actions", []):
1544:    def _read_recent_decision_log(self) -> list[dict[str, Any]]:
1557:            path = self._decision_log.directory / f"{day.isoformat()}.jsonl"
1704:            self._action_executor(a, ctx)
1705:            for a in result.actions
1709:        self._decision_log.append(record)
1737:            self._decision_log.append(
1820:        self._decision_log.append(
1830:        self._decision_log.append(
1866:    action_executor: ActionExecutor | None = None,
1883:    if acting and action_executor is None:
1884:        raise ValueError("acting=True requires an explicit action_executor")
1885:    if not acting and action_executor is not None:
1886:        raise ValueError("action_executor requires acting=True")
1887:    decision_log = DecisionLog(config.decision_log_dir)
1894:        engine=build_hybrid_engine(decision_log_dir=config.decision_log_dir),
1895:        action_executor=action_executor,
1899:            decision_log_dir=config.decision_log_dir,
1904:        decision_log=decision_log,
1905:        learning_loop=build_default_learning_loop(decision_log),

exec
/bin/bash -lc "rg -n \"escalate|ACTION_ESCALATE|Action\\.escalate|mention_operator|reset_to_todo|transition_under_review|mark_resumable|clear_assignee|mark_for_followup|ACTION_MARK_FOR_FOLLOWUP|ACTION_TRANSITION|ACTION_RELABEL|Action\\.transition|Action\\.relabel|Action\\.mention_operator\" backend/agents backend/tests docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:10:- `mark_resumable` (:735), `transition_under_review` (:738), `reset_to_todo` (:750), `clear_assignee` (:787), `mention_operator` (:799).
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:19:| `clear_assignee` | `gw.clear_assignee(target)` | also jira_dispatch.clear_assignee(:2023) |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:20:| `reset_to_todo` | `gw.reset_to_todo(target)` | |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:21:| `transition_under_review` | `gw.transition_under_review(target)` | |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:22:| `mark_resumable` | `gw.mark_resumable(target)` | |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:23:| `mention_operator` | `gw.mention_operator(target, params["message"], urgency=params.get("urgency","high"))` | |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:24:| `escalate` | **OPEN Q1** — no gateway op; proposed = `mention_operator` with `urgency="critical"` + an `escalated` result flag (do NOT silently drop) | |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:31:- **D2 — idempotency.** Derive a per-action idem key (`coord:{decision_id}:{kind}:{target}`) so a retried tick / restart does not double-apply. clear_assignee already takes `idem_key`; the other gateway ops need a check (OPEN Q2 — do they dedupe?).
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:38:- Q1: `escalate` semantics — mention_operator(critical) vs a distinct path? Is `escalate` ever emitted today (grep the rules)?
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:39:- Q2: do `reset_to_todo`/`transition_under_review`/`mark_resumable`/`mention_operator` on `JiraDispatchColdStartGateway` already dedupe / take idem keys, or can a restart double-apply?
backend/agents/pipeline_coordinator_watchdog.py:8:within the re-die window after a restart, escalates to the operator.
backend/agents/pipeline_coordinator_watchdog.py:70:    def escalate(self, reason: str, context: dict[str, Any]) -> None:
backend/agents/pipeline_coordinator_watchdog.py:94:    def escalate(self, reason: str, context: dict[str, Any]) -> None:
backend/agents/pipeline_coordinator_watchdog.py:149:    escalated: bool = False
backend/agents/pipeline_coordinator_watchdog.py:229:            self._escalator.escalate("pipeline-coordinator re-died", context)
backend/agents/pipeline_coordinator_watchdog.py:230:            return WatchdogTick(heartbeat_age_seconds=age, action="escalate",
backend/agents/pipeline_coordinator_watchdog.py:231:                                escalated=True)
backend/agents/pipeline_coordinator_watchdog.py:241:            self._escalator.escalate("pipeline-coordinator restart failed", fail_context)
backend/agents/pipeline_coordinator_watchdog.py:242:            return WatchdogTick(heartbeat_age_seconds=age, action="escalate",
backend/agents/pipeline_coordinator_watchdog.py:243:                                escalated=True)
backend/agents/pipeline_coordinator.py:61:    ACTION_RELABEL,
backend/agents/pipeline_coordinator.py:62:    ACTION_TRANSITION,
backend/agents/pipeline_coordinator.py:593:    def mark_resumable(self, key: str) -> None: ...
backend/agents/pipeline_coordinator.py:594:    def transition_under_review(self, key: str) -> None: ...
backend/agents/pipeline_coordinator.py:595:    def reset_to_todo(self, key: str) -> None: ...
backend/agents/pipeline_coordinator.py:600:    def clear_assignee(self, key: str) -> None: ...
backend/agents/pipeline_coordinator.py:603:    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None: ...
backend/agents/pipeline_coordinator.py:735:    def mark_resumable(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:738:    def transition_under_review(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:748:            logger.warning("[pipeline_coordinator] transition_under_review(%s) failed: %s", key, exc)
backend/agents/pipeline_coordinator.py:750:    def reset_to_todo(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:763:            logger.warning("[pipeline_coordinator] reset_to_todo(%s) failed: %s", key, exc)
backend/agents/pipeline_coordinator.py:787:    def clear_assignee(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:794:            jira_dispatch.clear_assignee(client, key)
backend/agents/pipeline_coordinator.py:796:            logger.warning("[pipeline_coordinator] clear_assignee(%s) failed: %s", key, exc)
backend/agents/pipeline_coordinator.py:799:    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
backend/agents/pipeline_coordinator.py:811:            logger.warning("[pipeline_coordinator] mention_operator(%s) failed: %s", key, exc)
backend/agents/pipeline_coordinator.py:832:    the would-be ``reset_to_todo`` / ``mark_resumable`` /
backend/agents/pipeline_coordinator.py:833:    ``transition_under_review`` / ``remove_label`` / ``clear_assignee`` /
backend/agents/pipeline_coordinator.py:834:    ``start_unit`` / ``mention_operator`` is logged (and lands in the decision
backend/agents/pipeline_coordinator.py:881:    def mark_resumable(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:882:        self._record_shadow("mark_resumable", key)
backend/agents/pipeline_coordinator.py:884:    def transition_under_review(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:885:        self._record_shadow("transition_under_review", key)
backend/agents/pipeline_coordinator.py:887:    def reset_to_todo(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:888:        self._record_shadow("reset_to_todo", key)
backend/agents/pipeline_coordinator.py:893:    def clear_assignee(self, key: str) -> None:
backend/agents/pipeline_coordinator.py:894:        self._record_shadow("clear_assignee", key)
backend/agents/pipeline_coordinator.py:896:    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
backend/agents/pipeline_coordinator.py:900:        self._record_shadow("mention_operator", f"{key} ({urgency})")
backend/agents/pipeline_coordinator.py:1463:            gw.mention_operator(
backend/agents/pipeline_coordinator.py:1484:        *escalate*, everything else is a logged no-op. Every resolution writes
backend/agents/pipeline_coordinator.py:1523:            if kind in (ACTION_RELABEL, ACTION_TRANSITION):
backend/agents/pipeline_coordinator.py:1525:                if kind == ACTION_TRANSITION and action.get("params", {}).get(
backend/agents/pipeline_coordinator.py:1528:                    gw.transition_under_review(target)
backend/agents/pipeline_coordinator.py:1532:                gw.mention_operator(
backend/agents/pipeline_coordinator.py:1538:                resolution = "escalated"
backend/agents/pipeline_coordinator.py:1599:                gw.transition_under_review(key)
backend/agents/pipeline_coordinator.py:1602:                gw.mark_resumable(key)
backend/agents/pipeline_coordinator.py:1606:                gw.reset_to_todo(key)
backend/agents/pipeline_coordinator.py:1610:                gw.mention_operator(
backend/agents/pipeline_coordinator.py:1633:            gw.clear_assignee(key)
backend/agents/pipeline_coordinator.py:1636:            gw.clear_assignee(key)
backend/agents/pipeline_coordinator.py:1872:    matches (or that the personality mode escalates) consults the claude CLI
backend/agents/live_state_check.py:446:        # ``clear_assignee``) while OUR per-instance fencing-token claim is
backend/agents/runner_orphan_reaper.py:47:    clear_assignee,
backend/agents/runner_orphan_reaper.py:291:        clear_assignee(client, cand.ticket_key)
backend/agents/runner_orphan_reaper.py:293:        log.warning("reaper.clear_assignee failed key=%s: %s", cand.ticket_key, exc)
backend/agents/critic_agent.py:21:  dissent → ``CriticReviewOutcome.escalated=True`` so the launcher can
backend/agents/critic_agent.py:90:    escalated: bool
backend/agents/critic_agent.py:94:        return self.final_verdict.verdict == "pass" and not self.escalated
backend/agents/critic_agent.py:276:    * 1st pass → return immediately, ``escalated=False``.
backend/agents/critic_agent.py:279:    * 2nd dissent → return ``escalated=True`` (no further retry).
backend/agents/critic_agent.py:299:                escalated=False,
backend/agents/critic_agent.py:305:                escalated=True,
backend/agents/critic_agent.py:309:        except Exception as exc:  # noqa: BLE001 — coder failure → escalate
backend/agents/critic_agent.py:317:                escalated=True,
backend/agents/state.py:143:    # the runtime has already escalated to the operator this run, so a
backend/agents/state.py:150:    vite_escalated_signatures: list[str] = Field(default_factory=list)
backend/agents/feature_list_parser.py:14:  escalate ``legacy_up_convert_fail`` per B6 AC #6.
backend/agents/pipeline_coordinator_llm_consultation.py:5:the remaining novel / multi-plausible / cross-cutting situations escalate to
backend/agents/pipeline_coordinator_llm_consultation.py:14:      parse_decision_json   invalid → escalate-operator (Code AC)
backend/agents/pipeline_coordinator_llm_consultation.py:56:    ACTION_ESCALATE,
backend/agents/pipeline_coordinator_llm_consultation.py:58:    ACTION_MARK_FOR_FOLLOWUP,
backend/agents/pipeline_coordinator_llm_consultation.py:61:    ACTION_RELABEL,
backend/agents/pipeline_coordinator_llm_consultation.py:62:    ACTION_TRANSITION,
backend/agents/pipeline_coordinator_llm_consultation.py:84:# The LLM response could not be strict-parsed → escalate to operator.
backend/agents/pipeline_coordinator_llm_consultation.py:86:# The CLI invocation itself failed (non-zero exit / timeout) → escalate.
backend/agents/pipeline_coordinator_llm_consultation.py:120:    "relabel": ACTION_RELABEL,
backend/agents/pipeline_coordinator_llm_consultation.py:121:    "transition": ACTION_TRANSITION,
backend/agents/pipeline_coordinator_llm_consultation.py:122:    "mention_operator": ACTION_MENTION_OPERATOR,
backend/agents/pipeline_coordinator_llm_consultation.py:123:    "mark_for_followup": ACTION_MARK_FOR_FOLLOWUP,
backend/agents/pipeline_coordinator_llm_consultation.py:124:    "escalate": ACTION_ESCALATE,
backend/agents/pipeline_coordinator_llm_consultation.py:483:    "- mention_operator(ticket, message, urgency)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:484:    "- mark_for_followup(ticket, when, why)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:485:    "- escalate(ticket, reason)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:495:    '  "escalate_if_wrong": true,\n'
backend/agents/pipeline_coordinator_llm_consultation.py:569:# ── Response parsing (Code AC: strict-parse; invalid → escalate) ───────
backend/agents/pipeline_coordinator_llm_consultation.py:627:    escalate_if_wrong: bool
backend/agents/pipeline_coordinator_llm_consultation.py:637:    "JSON response strict-parsed; invalid → escalate operator").
backend/agents/pipeline_coordinator_llm_consultation.py:658:        escalate_if_wrong=bool(payload.get("escalate_if_wrong", False)),
backend/agents/pipeline_coordinator_llm_consultation.py:803:            return self._escalate_invocation_failure(ctx, invocation, lesson_ids)
backend/agents/pipeline_coordinator_llm_consultation.py:809:            return self._escalate_parse_failure(ctx, invocation, lesson_ids, exc)
backend/agents/pipeline_coordinator_llm_consultation.py:830:        # A consult that returned zero executable actions but asked to escalate
backend/agents/pipeline_coordinator_llm_consultation.py:833:        if not actions and parsed.escalate_if_wrong:
backend/agents/pipeline_coordinator_llm_consultation.py:835:                Action.escalate(
backend/agents/pipeline_coordinator_llm_consultation.py:874:            actions=(Action.mention_operator(_focal_key(ctx), message=msg, urgency="high"),),
backend/agents/pipeline_coordinator_llm_consultation.py:885:    def _escalate_invocation_failure(
backend/agents/pipeline_coordinator_llm_consultation.py:895:            actions=(Action.escalate(_focal_key(ctx), reason=msg),),
backend/agents/pipeline_coordinator_llm_consultation.py:904:    def _escalate_parse_failure(
backend/agents/pipeline_coordinator_llm_consultation.py:914:            actions=(Action.escalate(_focal_key(ctx), reason=msg),),
backend/agents/nodes.py:68:    should_escalate_vite_pattern,
backend/agents/nodes.py:533:      * ``{"vite_escalated_signatures": [...]}`` — the freshly-escalated
backend/agents/nodes.py:538:    :func:`backend.web.vite_retry_budget.should_escalate_vite_pattern`
backend/agents/nodes.py:544:    decision = should_escalate_vite_pattern(
backend/agents/nodes.py:546:        already_escalated=tuple(state.vite_escalated_signatures),
backend/agents/nodes.py:556:        "vite_escalated_signatures": (
backend/agents/nodes.py:557:            list(state.vite_escalated_signatures) + [decision.pattern]
backend/agents/nodes.py:611:        # escalate to the operator once per (graph run, signature)
backend/agents/nodes.py:613:        # ``vite_escalated_signatures`` only when a fresh emission
backend/agents/nodes.py:1231:      (file mode reset by another process) escalates instead of
backend/agents/nodes.py:1378:                        "env_fix_escalated",
backend/agents/pipeline_coordinator_rules.py:61:ACTION_RELABEL = "relabel"
backend/agents/pipeline_coordinator_rules.py:62:ACTION_TRANSITION = "transition"
backend/agents/pipeline_coordinator_rules.py:63:ACTION_MENTION_OPERATOR = "mention_operator"
backend/agents/pipeline_coordinator_rules.py:64:ACTION_MARK_FOR_FOLLOWUP = "mark_for_followup"
backend/agents/pipeline_coordinator_rules.py:65:ACTION_ESCALATE = "escalate"
backend/agents/pipeline_coordinator_rules.py:71:        ACTION_RELABEL,
backend/agents/pipeline_coordinator_rules.py:72:        ACTION_TRANSITION,
backend/agents/pipeline_coordinator_rules.py:74:        ACTION_MARK_FOR_FOLLOWUP,
backend/agents/pipeline_coordinator_rules.py:75:        ACTION_ESCALATE,
backend/agents/pipeline_coordinator_rules.py:158:            kind=ACTION_RELABEL,
backend/agents/pipeline_coordinator_rules.py:170:            kind=ACTION_TRANSITION,
backend/agents/pipeline_coordinator_rules.py:177:    def mention_operator(
backend/agents/pipeline_coordinator_rules.py:185:        """§6.1 mention_operator — @-mention operator; high urgency tags too."""
backend/agents/pipeline_coordinator_rules.py:194:    def mark_for_followup(
backend/agents/pipeline_coordinator_rules.py:197:        """§6.1 mark_for_followup — defer attention via coord-resume-after."""
backend/agents/pipeline_coordinator_rules.py:199:            kind=ACTION_MARK_FOR_FOLLOWUP,
backend/agents/pipeline_coordinator_rules.py:206:    def escalate(cls, ticket: str, *, reason: str, dry_run: bool = True) -> "Action":
backend/agents/pipeline_coordinator_rules.py:207:        """§6.1 escalate — needs-operator-action label + reason comment."""
backend/agents/pipeline_coordinator_rules.py:209:            kind=ACTION_ESCALATE,
backend/agents/pipeline_coordinator_rules.py:417:# merger-repeat-fail: escalate after > N attempts on the same ticket (§5.1 #4).
backend/agents/pipeline_coordinator_rules.py:588:#   20  merger-repeat-fail         — escalate stuck merger
backend/agents/pipeline_coordinator_rules.py:618:    """§5.1 #8 — A blocks B blocks A (or longer cycle): diagnose + escalate.
backend/agents/pipeline_coordinator_rules.py:635:            return Action.mention_operator(
backend/agents/pipeline_coordinator_rules.py:662:    return Action.relabel(
backend/agents/pipeline_coordinator_rules.py:670:    """§5.1 #4 — merger failed > 3× on one ticket: escalate with context."""
backend/agents/pipeline_coordinator_rules.py:677:        return None  # already escalated
backend/agents/pipeline_coordinator_rules.py:678:    return Action.escalate(
backend/agents/pipeline_coordinator_rules.py:704:    return Action.transition(t.key, to_status="To Do")
backend/agents/pipeline_coordinator_rules.py:724:    return Action.relabel(t.key, remove=claim_labels)
backend/agents/pipeline_coordinator_rules.py:748:    return Action.relabel(t.key, remove=tuple(stale))
backend/agents/pipeline_coordinator_rules.py:824:    return Action.relabel(t.key, add=(CAPABILITY_ENABLE_LABEL,))
backend/agents/pipeline_coordinator_rules.py:846:    return Action.mention_operator(
backend/agents/incident_recorder.py:82:    escalate: bool
backend/agents/incident_recorder.py:134:        escalate=decision.escalate,
backend/agents/incident_recorder.py:283:        operator (escalate=True for tier:X).
backend/agents/coder_agent.py:102:    """End-to-end coder result. Runner consumes this to decide push vs. escalate."""
backend/agents/coder_agent.py:280:         escalate to ``run_monolithic_coder``.
backend/agents/sprint_replan.py:44:    ACTION_RELABEL,
backend/agents/sprint_replan.py:131:    def mention_operator(self, key: str, message: str, *, urgency: str) -> None: ...
backend/agents/sprint_replan.py:142:        if action.kind == ACTION_RELABEL:
backend/agents/sprint_replan.py:218:        self._gateway.mention_operator(
backend/agents/sprint_replan.py:368:    def mention_operator(self, key: str, message: str, *, urgency: str) -> None:
backend/agents/sprint_replan.py:459:        if action.kind not in {ACTION_RELABEL, ACTION_FILE_TICKET, ACTION_MENTION_OPERATOR}:
backend/agents/sprint_replan.py:463:            normalised = Action.mention_operator(
backend/agents/sprint_replan.py:487:            Action.mention_operator(
backend/agents/memory_tool_handler.py:234:    C6 extension: carries an ``escalate`` flag so callers can route
backend/agents/memory_tool_handler.py:236:    are merely logged. The runner promotes ``escalate=True`` refusals
backend/agents/memory_tool_handler.py:250:        escalate: bool = False,
backend/agents/memory_tool_handler.py:259:        self.escalate = escalate
backend/agents/memory_tool_handler.py:1034:            escalate=False,
backend/agents/memory_tool_handler.py:1115:    * ``escalate`` — true when the operator must be paged (tier:X
backend/agents/memory_tool_handler.py:1116:      refusal). Refusals at tier:M / tier:L do not escalate because
backend/agents/memory_tool_handler.py:1127:    escalate: bool
backend/agents/memory_tool_handler.py:1172:        return _allow(request, escalate=False, note="tier:S unrestricted")
backend/agents/memory_tool_handler.py:1176:            return _allow(request, escalate=False, note="tier:M same-fleet")
backend/agents/memory_tool_handler.py:1181:                escalate=False,
backend/agents/memory_tool_handler.py:1186:            escalate=False,
backend/agents/memory_tool_handler.py:1198:                escalate=False,
backend/agents/memory_tool_handler.py:1206:                    escalate=False,
backend/agents/memory_tool_handler.py:1212:        # tier:L permitted recalls always escalate to operator-visible
backend/agents/memory_tool_handler.py:1218:            escalate=True,
backend/agents/memory_tool_handler.py:1222:    # tier:X — always refused, always escalate (operator pager).
backend/agents/memory_tool_handler.py:1226:            escalate=True,
backend/agents/memory_tool_handler.py:1279:            escalate=decision.escalate,
backend/agents/memory_tool_handler.py:1307:def _allow(request: RecallRequest, *, escalate: bool, note: str) -> PolicyDecision:
backend/agents/memory_tool_handler.py:1312:        escalate=escalate,
backend/agents/memory_tool_handler.py:1318:def _refuse(request: RecallRequest, *, escalate: bool, reason: str) -> PolicyDecision:
backend/agents/memory_tool_handler.py:1323:        escalate=escalate,
backend/agents/ai_reviewer.py:33:default to haiku, escalate to sonnet for general backend, opus only on
backend/agents/ai_reviewer.py:843:    Per CLAUDE.md L1 the AI's max signal is +1; we never escalate to
backend/agents/locator_agent.py:30:* ``status="needs_refinement"``: >50 candidates — escalate ticket.
backend/agents/locator_agent.py:32:  the candidate files — escalate.
backend/agents/locator_agent.py:209:        "best 50 — the caller will escalate `needs:refinement`. If you "
backend/agents/op_queue.py:202:def _default_escalate(op: Op) -> None:
backend/agents/op_queue.py:231:        logger.exception("op_queue: failed to escalate op id=%s", op.id)
backend/agents/op_queue.py:251:        escalate_fn: Callable[[Op], None] | None = None,
backend/agents/op_queue.py:257:        self._escalate = escalate_fn or _default_escalate
backend/agents/op_queue.py:410:        * Else → status=failed, escalate to operator-notifier.
backend/agents/op_queue.py:434:        self._escalate(terminal)
backend/agents/op_queue.py:606:def _handle_jira_clear_assignee(args: dict[str, Any], idem_key: str) -> None:
backend/agents/op_queue.py:607:    from backend.agents.jira_dispatch import clear_assignee
backend/agents/op_queue.py:609:    clear_assignee(client, args["key"], idem_key=idem_key)
backend/agents/op_queue.py:646:    register_handler("jira.clear_assignee", _handle_jira_clear_assignee)
backend/agents/submit_checklist.py:89:    Terminal for the B6 cycle — the runner escalates to operator review
backend/agents/progress_log.py:26:to satisfy AC #8 — "owner mismatch (different bot identity) → escalate
backend/agents/failure_graph.py:821:        "escalate to human')."
backend/agents/runner_failure_classifier.py:44:    return "unknown", "escalate"
backend/agents/blast_radius_gate.py:141:                "options": ["auto_split", "needs_split_revert", "escalate"],
backend/agents/ci_recovery.py:25:    (r"(?i)docker\.errors|build.*failed.*image", "infra", "auto-retry-or-escalate"),
backend/agents/ci_recovery.py:26:    (r"CI worker timeout|killed by signal", "infra", "auto-retry-or-escalate"),
backend/agents/ci_recovery.py:130:    def escalate(self, change: Change, reason: str) -> None: ...
backend/agents/ci_recovery.py:246:    if strategy == "auto-retry-or-escalate":
backend/agents/ci_recovery.py:251:        hooks.escalate(change, "CI infra suspected after retry")
backend/agents/ci_recovery.py:252:        return RecoveryDecision(category, strategy, attempt_count, "escalate")
backend/agents/ci_recovery.py:258:        hooks.escalate(change, "unknown failure pattern")
backend/agents/ci_recovery.py:259:        return RecoveryDecision(category, strategy, attempt_count, "escalate")
backend/agents/ci_recovery.py:261:    hooks.escalate(change, f"unsupported recovery strategy: {strategy}")
backend/agents/ci_recovery.py:262:    return RecoveryDecision(category, strategy, attempt_count, "escalate")
backend/agents/ci_recovery.py:300:        return RecoveryDecision("infra", "auto-retry-or-escalate", timeout_count, "retry")
backend/agents/ci_recovery.py:303:    hooks.escalate(change, "CI timeout repeated")
backend/agents/ci_recovery.py:304:    return RecoveryDecision("infra", "auto-retry-or-escalate", timeout_count, "escalate")
backend/agents/ci_recovery.py:463:    def escalate(self, change: Change, reason: str) -> None:
backend/agents/ci_recovery.py:464:        self.calls.append({"action": "escalate", "reason": reason, "change": change.number})
backend/agents/ci_recovery.py:465:        self.audit("ci_recovery_escalate", change, reason=reason)
backend/agents/ci_recovery.py:503:            self.escalate(entry.change, reason or "operator abandoned PS")
backend/tests/test_op_queue.py:54:    return OpQueue(path=tmp_path / "op-queue.db", clock=clock.now, escalate_fn=lambda op: None)
backend/tests/test_op_queue.py:72:    OpQueue(path=db_path, escalate_fn=lambda op: None)
backend/tests/test_op_queue.py:156:    queue1 = OpQueue(path=db_path, escalate_fn=lambda op: None)
backend/tests/test_op_queue.py:160:    queue2 = OpQueue(path=db_path, escalate_fn=lambda op: None)
backend/tests/test_op_queue.py:221:    """AC: 'exponential backoff up to 5 attempts; then escalate'."""
backend/tests/test_op_queue.py:227:        escalate_fn=escalations.append,
backend/tests/test_op_queue.py:282:        escalate_fn=lambda op: None,
backend/tests/test_op_queue.py:316:    queue = OpQueue(path=tmp_path / "q.db", clock=clock.now, escalate_fn=lambda op: None)
backend/tests/test_op_queue.py:431:    queue = OpQueue(path=tmp_path / "q.db", clock=clock.now, escalate_fn=lambda op: None)
backend/tests/test_op_queue.py:496:    queue1 = OpQueue(path=db_path, escalate_fn=lambda op: None)
backend/tests/test_op_queue.py:504:    queue2 = OpQueue(path=db_path, escalate_fn=lambda op: None)
backend/agents/learning_loop.py:555:    ``relabel`` added/removed its labels; an ``escalate`` left the
backend/agents/learning_loop.py:633:    if kind == "escalate":
backend/agents/learning_loop.py:635:    # file_ticket / mention_operator / mark_for_followup have no directly
backend/agents/learning_loop.py:826:# owns real @-mentions). Signature mirrors Action.mention_operator inputs.
backend/agents/jira_dispatch.py:1991:    clear_assignee(client, key, idem_key=f"{base_key}-clear-assignee")
backend/agents/jira_dispatch.py:2023:def clear_assignee(client: DispatchClient, key: str, idem_key: str | None = None) -> None:
backend/agents/jira_dispatch.py:2940:    "JIRA itself is unreachable" (escalate / pause). The runner converts
backend/agents/jira_dispatch.py:3092:    sibling instance clears the assignee (its own revert → ``clear_assignee``
backend/tests/test_nodes.py:217:    async def test_retries_exhausted_escalates(self):
backend/tests/test_nodes.py:218:        """When retries exhausted with errors, escalate to human."""
backend/tests/test_ai_reviewer.py:93:    """Spec-listed HIGH-RISK paths *always* escalate to opus."""
backend/tests/test_ci_recovery.py:59:        ("docker.errors.BuildError: build failed image", ("infra", "auto-retry-or-escalate")),
backend/tests/test_ci_recovery.py:91:def test_infra_second_attempt_escalates() -> None:
backend/tests/test_ci_recovery.py:101:    assert decision.action == "escalate"
backend/tests/test_ci_recovery.py:102:    assert _actions(hooks, "escalate")
backend/tests/test_ci_recovery.py:106:def test_unknown_second_attempt_escalates() -> None:
backend/tests/test_ci_recovery.py:112:    assert decision.action == "escalate"
backend/tests/test_ci_recovery.py:113:    assert _actions(hooks, "escalate")[0]["reason"] == "unknown failure pattern"
backend/tests/test_ci_recovery.py:196:def test_ci_timeout_requeues_once_then_escalates() -> None:
backend/tests/test_ci_recovery.py:212:    assert second.action == "escalate"
backend/tests/test_ci_recovery.py:213:    assert _actions(hooks, "escalate")[-1]["reason"] == "CI timeout repeated"
backend/tests/test_web_sandbox_pep.py:643:        # risky — operator UI doesn't escalate to red for a routine
backend/tests/test_intelligence_mitigation.py:180:async def test_critical_after_open_route_escalates_to_contain(db_for_audit):
backend/tests/test_intelligence_mitigation.py:229:    await mit.propose_for_agent("a-contain")  # escalates to L3 contain
backend/tests/test_workspace_discard_recreate.py:235:    worktree. Caller (orchestrator) escalates from here."""
backend/tests/test_pipeline_coordinator_rules.py:32:    ACTION_ESCALATE,
backend/tests/test_pipeline_coordinator_rules.py:35:    ACTION_RELABEL,
backend/tests/test_pipeline_coordinator_rules.py:36:    ACTION_TRANSITION,
backend/tests/test_pipeline_coordinator_rules.py:72:    assert Action.relabel("OP-1", add=("x",)).kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:74:    assert Action.transition("OP-1", to_status="To Do").kind == ACTION_TRANSITION
backend/tests/test_pipeline_coordinator_rules.py:75:    assert Action.mention_operator("OP-1", message="m").kind == ACTION_MENTION_OPERATOR
backend/tests/test_pipeline_coordinator_rules.py:76:    assert Action.escalate("OP-1", reason="r").kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_rules.py:82:    assert Action.relabel("OP-1", add=("x",)).dry_run is True
backend/tests/test_pipeline_coordinator_rules.py:83:    assert Action.escalate("OP-1", reason="r").dry_run is True
backend/tests/test_pipeline_coordinator_rules.py:129:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:147:    assert action is not None and action.kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_rules.py:160:    assert action is not None and action.kind == ACTION_TRANSITION
backend/tests/test_pipeline_coordinator_rules.py:184:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:209:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:280:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:425:    assert record["actions"][0]["kind"] == ACTION_RELABEL
backend/tests/test_lifecycle.py:309:    pod and escalate to SIGKILL before the drain window completes."""
backend/tests/test_pipeline_coordinator_coldstart.py:93:    ACTION_RELABEL,
backend/tests/test_pipeline_coordinator_coldstart.py:94:    ACTION_TRANSITION,
backend/tests/test_pipeline_coordinator_coldstart.py:173:    def mark_resumable(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:176:    def transition_under_review(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:179:    def reset_to_todo(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:189:    def clear_assignee(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:193:    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:220:    def mark_resumable(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:223:    def transition_under_review(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:226:    def reset_to_todo(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:235:    def clear_assignee(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:238:    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:293:        "gerrit_change_mergeable", "branch_has_commits", "mark_resumable",
backend/tests/test_pipeline_coordinator_coldstart.py:294:        "transition_under_review", "reset_to_todo", "stale_sweep_plan",
backend/tests/test_pipeline_coordinator_coldstart.py:295:        "remove_label", "clear_assignee", "mention_operator",
backend/tests/test_pipeline_coordinator_coldstart.py:476:    re-issued); file_ticket → escalated via @-operator; anything else → noop."""
backend/tests/test_pipeline_coordinator_coldstart.py:490:                    {"kind": ACTION_RELABEL, "target": "OP-10"},
backend/tests/test_pipeline_coordinator_coldstart.py:492:                        "kind": ACTION_TRANSITION,
backend/tests/test_pipeline_coordinator_coldstart.py:509:    assert resolutions[(ACTION_RELABEL, "OP-10")] == "completed"
backend/tests/test_pipeline_coordinator_coldstart.py:510:    assert resolutions[(ACTION_TRANSITION, "OP-11")] == "completed"
backend/tests/test_pipeline_coordinator_coldstart.py:511:    assert resolutions[(ACTION_FILE_TICKET, "OP-12")] == "escalated"
backend/tests/test_pipeline_coordinator_coldstart.py:534:                "actions": [{"kind": ACTION_RELABEL, "target": "OP-99"}],
backend/tests/test_pipeline_coordinator_coldstart.py:680:    gw.transition_under_review("OP-1")
backend/tests/test_pipeline_coordinator_coldstart.py:681:    gw.reset_to_todo("OP-1")
backend/tests/test_pipeline_coordinator_coldstart.py:682:    gw.mark_resumable("OP-1")
backend/tests/test_pipeline_coordinator_coldstart.py:684:    gw.clear_assignee("OP-1")
backend/tests/test_pipeline_coordinator_coldstart.py:685:    gw.mention_operator("OP-1", "down", urgency="high")
backend/tests/test_pipeline_coordinator_coldstart.py:828:    (transition / mark_resumable / reset_to_todo / @-operator), and all three
backend/tests/test_pipeline_coordinator_coldstart.py:829:    Startup-3 sweep classes (remove_label / clear_assignee)."""
backend/tests/test_pipeline_coordinator_coldstart.py:871:    gw.mark_resumable("OP-BRANCH")
backend/tests/test_pipeline_coordinator_coldstart.py:872:    gw.transition_under_review("OP-MERGE")
backend/tests/test_pipeline_coordinator_coldstart.py:873:    gw.reset_to_todo("OP-STALE")
backend/tests/test_pipeline_coordinator_coldstart.py:875:    gw.clear_assignee("OP-B")
backend/tests/test_pipeline_coordinator_coldstart.py:876:    gw.mention_operator("OP-AMBIG", "ambiguous", urgency="medium")
backend/tests/test_push_failure_classifier.py:108:        ("remote: unrelated Gerrit rejection", ("unknown", "escalate")),
backend/tests/test_push_failure_classifier.py:244:def test_unknown_failure_sets_loop_paused_label_and_escalates(
backend/tests/test_push_failure_classifier.py:256:    assert decision == ("unknown", "escalate")
backend/tests/test_decision_profiles.py:122:            {"id": "escalate", "label": "escalate"},
backend/tests/test_decision_profiles.py:125:        default_option_id="escalate",
backend/tests/test_failure_class.py:289:def test_recall_tier_x_is_refused_and_escalates():
backend/tests/test_failure_class.py:301:    assert excinfo.value.escalate is True
backend/tests/test_memory_tier_policy.py:66:    assert decision.escalate is False
backend/tests/test_memory_tier_policy.py:84:    assert decision.escalate is False
backend/tests/test_memory_tier_policy.py:118:    assert excinfo.value.escalate is False
backend/tests/test_memory_tier_policy.py:127:# ── Case 5: tier:L + OPTIN — allow + audit + escalate ─────────────
backend/tests/test_memory_tier_policy.py:130:def test_tier_l_with_optin_allows_and_escalates():
backend/tests/test_memory_tier_policy.py:136:    assert decision.escalate is True, "tier:L permitted recalls must surface for review"
backend/tests/test_memory_tier_policy.py:138:    assert row.escalate is True
backend/tests/test_memory_tier_policy.py:152:    assert excinfo.value.escalate is False
backend/tests/test_memory_tier_policy.py:157:# ── Case 7: tier:X — refuse + escalate ────────────────────────────
backend/tests/test_memory_tier_policy.py:160:def test_tier_x_refused_and_escalated():
backend/tests/test_memory_tier_policy.py:167:    assert excinfo.value.escalate is True, "tier:X refusal must page operator"
backend/tests/test_memory_tier_policy.py:170:    assert row.escalate is True
backend/tests/test_w15_4_vite_retry_budget.py:15:escalates to the operator instead of letting the loop spin.
backend/tests/test_w15_4_vite_retry_budget.py:25:§C — :func:`should_escalate_vite_pattern` (under threshold, exact
backend/tests/test_w15_4_vite_retry_budget.py:26:     threshold, above threshold, idempotency via already_escalated,
backend/tests/test_w15_4_vite_retry_budget.py:37:     and includes ``vite_escalated_signatures`` in its return dict
backend/tests/test_w15_4_vite_retry_budget.py:66:    should_escalate_vite_pattern,
backend/tests/test_w15_4_vite_retry_budget.py:171:    ``vite_escalated_signatures`` lookups without aliasing concerns."""
backend/tests/test_w15_4_vite_retry_budget.py:284:# §C — should_escalate_vite_pattern
backend/tests/test_w15_4_vite_retry_budget.py:288:def test_should_escalate_returns_none_below_threshold() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:290:    assert should_escalate_vite_pattern(history) is None
backend/tests/test_w15_4_vite_retry_budget.py:293:def test_should_escalate_returns_decision_at_exact_threshold() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:295:    decision = should_escalate_vite_pattern(history)
backend/tests/test_w15_4_vite_retry_budget.py:302:def test_should_escalate_returns_decision_above_threshold() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:303:    """Above-threshold trail still escalates (count carries the actual
backend/tests/test_w15_4_vite_retry_budget.py:307:    decision = should_escalate_vite_pattern(history)
backend/tests/test_w15_4_vite_retry_budget.py:312:def test_should_escalate_returns_none_when_already_escalated() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:313:    """Idempotency gate: signature in ``already_escalated`` short-
backend/tests/test_w15_4_vite_retry_budget.py:318:    decision = should_escalate_vite_pattern(history)
backend/tests/test_w15_4_vite_retry_budget.py:320:    decision2 = should_escalate_vite_pattern(
backend/tests/test_w15_4_vite_retry_budget.py:321:        history, already_escalated=[decision.pattern],
backend/tests/test_w15_4_vite_retry_budget.py:326:def test_should_escalate_returns_decision_for_new_pattern_after_old_escalated() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:328:    was escalated, the new pattern escalates fresh."""
backend/tests/test_w15_4_vite_retry_budget.py:331:    decision_a = should_escalate_vite_pattern(old_history)
backend/tests/test_w15_4_vite_retry_budget.py:337:    decision_b = should_escalate_vite_pattern(
backend/tests/test_w15_4_vite_retry_budget.py:338:        new_history, already_escalated=[decision_a.pattern],
backend/tests/test_w15_4_vite_retry_budget.py:344:def test_should_escalate_respects_custom_threshold_override() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:346:    consecutive same-sig entries escalate."""
backend/tests/test_w15_4_vite_retry_budget.py:349:    decision = should_escalate_vite_pattern(history, threshold=2)
backend/tests/test_w15_4_vite_retry_budget.py:354:def test_should_escalate_rejects_non_int_threshold() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:356:        should_escalate_vite_pattern([_entry()] * 3, threshold="3")  # type: ignore[arg-type]
backend/tests/test_w15_4_vite_retry_budget.py:359:def test_should_escalate_rejects_zero_threshold() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:361:        should_escalate_vite_pattern([_entry()] * 3, threshold=0)
backend/tests/test_w15_4_vite_retry_budget.py:364:def test_should_escalate_rejects_negative_threshold() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:366:        should_escalate_vite_pattern([_entry()] * 3, threshold=-1)
backend/tests/test_w15_4_vite_retry_budget.py:369:def test_should_escalate_returns_none_for_empty_history() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:370:    assert should_escalate_vite_pattern([]) is None
backend/tests/test_w15_4_vite_retry_budget.py:373:def test_should_escalate_returns_none_for_no_vite_entries() -> None:
backend/tests/test_w15_4_vite_retry_budget.py:374:    assert should_escalate_vite_pattern(["tool: x", "tool: y"]) is None
backend/tests/test_w15_4_vite_retry_budget.py:549:        assert "vite_escalated_signatures" in result
backend/tests/test_w15_4_vite_retry_budget.py:550:        assert len(result["vite_escalated_signatures"]) == 1
backend/tests/test_w15_4_vite_retry_budget.py:551:        assert result["vite_escalated_signatures"][0].startswith(
backend/tests/test_w15_4_vite_retry_budget.py:559:        trailing-3 trail must escalate exactly once.  The first call
backend/tests/test_w15_4_vite_retry_budget.py:560:        emits and seeds ``vite_escalated_signatures``; the second
backend/tests/test_w15_4_vite_retry_budget.py:587:        # Simulate the next LangGraph turn: state.vite_escalated_signatures
backend/tests/test_w15_4_vite_retry_budget.py:592:            vite_escalated_signatures=result1["vite_escalated_signatures"],
backend/tests/test_w15_4_vite_retry_budget.py:598:    def test_appends_not_replaces_when_new_pattern_escalates(
backend/tests/test_w15_4_vite_retry_budget.py:602:        existing ``vite_escalated_signatures`` rather than replacing
backend/tests/test_w15_4_vite_retry_budget.py:618:            vite_escalated_signatures=[prior_sig],
backend/tests/test_w15_4_vite_retry_budget.py:621:        assert prior_sig in result["vite_escalated_signatures"]
backend/tests/test_w15_4_vite_retry_budget.py:622:        assert len(result["vite_escalated_signatures"]) == 2
backend/tests/test_w15_4_vite_retry_budget.py:632:    def test_specialist_node_returns_vite_escalated_signatures_at_threshold(
backend/tests/test_w15_4_vite_retry_budget.py:635:        """The specialist node must spread ``vite_escalated_signatures``
backend/tests/test_w15_4_vite_retry_budget.py:667:        assert "vite_escalated_signatures" in ret
backend/tests/test_w15_4_vite_retry_budget.py:668:        assert len(ret["vite_escalated_signatures"]) == 1
backend/tests/test_w15_4_vite_retry_budget.py:674:        ``vite_escalated_signatures`` key — the LangGraph reducer is
backend/tests/test_w15_4_vite_retry_budget.py:701:        assert "vite_escalated_signatures" not in ret
backend/tests/test_w15_4_vite_retry_budget.py:741:        assert "vite_escalated_signatures" in ret
backend/tests/test_w15_4_vite_retry_budget.py:759:    "should_escalate_vite_pattern",
backend/tests/test_w15_4_vite_retry_budget.py:776:    """The ``vite_escalated_signatures`` field must appear on the
backend/tests/test_w15_4_vite_retry_budget.py:782:    assert hasattr(state, "vite_escalated_signatures")
backend/tests/test_w15_4_vite_retry_budget.py:783:    assert state.vite_escalated_signatures == []
backend/tests/test_w15_4_vite_retry_budget.py:786:    assert isinstance(state.vite_escalated_signatures, list)
backend/tests/test_incident_recorder.py:29:    escalate: bool = False,
backend/tests/test_incident_recorder.py:35:        escalate=escalate,
backend/tests/test_incident_recorder.py:47:        _audit_decision(summary="denied recall", permitted=False, escalate=True, tier="X"),
backend/tests/test_incident_recorder.py:56:    assert rows[1].escalate is True
backend/tests/test_red_card.py:152:    async def escalate_human(
backend/tests/test_red_card.py:520:    assert decision.should_escalate_human is False
backend/tests/test_red_card.py:523:def test_six_subcalls_red_card_cuts_agent_and_escalates_human() -> None:
backend/tests/test_red_card.py:530:    assert decision.should_escalate_human is True
backend/tests/test_red_card.py:628:def test_execute_recursive_subcall_budget_red_card_cuts_and_escalates() -> None:
backend/tests/test_r8_retry_integration.py:582:# 5) Legacy CATC without anchor — orchestrator escalates, NOT silently no-ops
backend/tests/test_r8_retry_integration.py:595:    asserts the orchestrator escalates cleanly (RuntimeError with a
backend/tests/test_pipeline_coordinator_integration.py:9:    - Scenario B: repeated merger failure escalates with full context
backend/tests/test_pipeline_coordinator_integration.py:49:    ACTION_ESCALATE,
backend/tests/test_pipeline_coordinator_integration.py:50:    ACTION_RELABEL,
backend/tests/test_pipeline_coordinator_integration.py:172:        if action.kind == ACTION_RELABEL:
backend/tests/test_pipeline_coordinator_integration.py:195:        elif action.kind == ACTION_ESCALATE:
backend/tests/test_pipeline_coordinator_integration.py:227:    def mark_resumable(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_integration.py:230:    def transition_under_review(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_integration.py:233:    def reset_to_todo(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_integration.py:265:    def clear_assignee(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_integration.py:284:    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
backend/tests/test_pipeline_coordinator_integration.py:307:            actions=(Action.escalate(ctx.ticket.key, reason=reason),),
backend/tests/test_pipeline_coordinator_integration.py:426:    assert scenario_a.actions[0].kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_integration.py:432:    assert scenario_b.actions[0].kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_integration.py:465:        {"kind": ACTION_RELABEL, "target": "OP-A", "executed": True, "shadow": False}
backend/tests/test_pipeline_coordinator_integration.py:472:        {"kind": ACTION_ESCALATE, "target": "OP-B", "executed": True, "shadow": False}
backend/tests/test_w15_5_vite_config_injection.py:7:the agent on every LLM turn.  W15.4 escalates to the operator when
backend/tests/test_tools_patch_wrappers.py:118:    so Phase 63-B can escalate to L1 calibrate."""
backend/tests/test_cognee_drift_detect.py:394:    inherit an ancient first_seen and prematurely escalate."""
backend/tests/test_sigterm_integration.py:196:    # don't escalate to SIGKILL before the drain window elapses.
backend/tests/test_tier_cooldown.py:282:    def test_repeat_in_90d_escalates_to_90d(self, db_path: Path) -> None:
backend/tests/test_tier_cooldown.py:311:            f"second cooldown in 90d should escalate to 90d, got {outcome.new_level}"
backend/tests/test_tier_cooldown.py:391:        # History row stays so the next trigger can still escalate.
backend/tests/test_locator_coder_pipeline.py:8: 3. ``test_too_many_candidates_escalates_refinement`` — AC #6.
backend/tests/test_locator_coder_pipeline.py:204:async def test_too_many_candidates_escalates_refinement(ticket_ctx):
backend/tests/test_locator_coder_pipeline.py:439:    in-flight PS, not a peer conflict — must NOT escalate (AC #8)."""
backend/tests/test_critic_agent.py:7:3. ``test_two_dissents_escalate`` — 2nd dissent triggers escalation.
backend/tests/test_critic_agent.py:126:    assert outcome.escalated is False
backend/tests/test_critic_agent.py:159:    assert outcome.escalated is False
backend/tests/test_critic_agent.py:173:# ── 3. Dissent + 2nd-retry escalate ──────────────────────────────────
backend/tests/test_critic_agent.py:177:async def test_two_dissents_escalate(diff: str, ac_text: str) -> None:
backend/tests/test_critic_agent.py:195:    assert outcome.escalated is True
backend/tests/test_stuck_detector.py:51:    def test_blocked_forever_escalates(self):
backend/tests/test_stuck_detector.py:52:        assert sd.pick_strategy(sd.StuckReason.blocked_forever) == sd.Strategy.escalate
backend/tests/test_stuck_detector.py:81:        assert sig.suggested_strategy == sd.Strategy.escalate
backend/tests/test_stuck_detector.py:94:        # Options include the recommended + retry + escalate
backend/tests/test_stuck_detector.py:98:        assert "escalate" in opt_ids
backend/tests/test_stuck_detector.py:110:    def test_escalate_requires_turbo_for_auto(self):
backend/tests/test_pipeline_coordinator_llm_consultation.py:9:                                      test_consult_parse_failure_escalates
backend/tests/test_pipeline_coordinator_llm_consultation.py:54:    ACTION_ESCALATE,
backend/tests/test_pipeline_coordinator_llm_consultation.py:56:    ACTION_RELABEL,
backend/tests/test_pipeline_coordinator_llm_consultation.py:216:# Code AC — strict JSON parse (invalid → escalate)
backend/tests/test_pipeline_coordinator_llm_consultation.py:239:                "escalate_if_wrong": False,
backend/tests/test_pipeline_coordinator_llm_consultation.py:245:    assert parsed.actions[0].kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_llm_consultation.py:284:            {"action": "escalate", "ticket": "OP-2", "params": {"reason": "r"}},
backend/tests/test_pipeline_coordinator_llm_consultation.py:287:    assert {a.kind for a in actions} == {ACTION_RELABEL, ACTION_ESCALATE}
backend/tests/test_pipeline_coordinator_llm_consultation.py:397:    assert outcome.actions[0].kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_llm_consultation.py:406:    runner = _ok_runner({"actions": [], "confidence": "low", "escalate_if_wrong": False})
backend/tests/test_pipeline_coordinator_llm_consultation.py:421:def test_consult_parse_failure_escalates() -> None:
backend/tests/test_pipeline_coordinator_llm_consultation.py:425:    assert outcome.actions[0].kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_llm_consultation.py:429:def test_consult_invocation_failure_escalates() -> None:
backend/tests/test_pipeline_coordinator_llm_consultation.py:433:    assert outcome.actions[0].kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_llm_consultation.py:460:    assert [a.kind for a in outcome.actions] == [ACTION_RELABEL]
backend/tests/test_pipeline_coordinator_llm_consultation.py:492:        {"actions": [{"action": "mention_operator", "ticket": "OP-1", "params": {"message": "m"}}],
backend/tests/test_pipeline_coordinator_llm_consultation.py:537:    runner = _ok_runner({"actions": [], "confidence": "low", "escalate_if_wrong": False})
backend/tests/test_pipeline_coordinator_llm_consultation.py:582:    # (relabel) AND merger-repeat-fail (escalate) — mutually distinct actions.
backend/tests/test_pipeline_coordinator_llm_consultation.py:626:    assert ACTION_RELABEL in kinds            # the Tier-1 deterministic action
backend/tests/test_pipeline_coordinator_llm_consultation.py:668:            "actions": [{"action": "mention_operator", "ticket": "OP-3",
backend/tests/test_hd_pcb_si.py:187:def test_diff_pair_length_mismatch_escalates_to_error():
backend/tests/test_pipeline_coordinator_learning.py:648:                    "actions": [{"action": "mention_operator", "ticket": "OP-77",
backend/tests/test_burn_rate_matrix.py:532:        (axis 3 silent), then escalates past the budget threshold
backend/tests/test_burn_rate_matrix.py:571:        # Rule fires on the escalated rate.
backend/tests/test_sprint_replan.py:42:    ACTION_RELABEL,
backend/tests/test_sprint_replan.py:43:    ACTION_TRANSITION,
backend/tests/test_sprint_replan.py:90:    def mention_operator(self, key: str, message: str, *, urgency: str) -> None:
backend/tests/test_sprint_replan.py:92:            ("mention_operator", (key, message), {"urgency": urgency})
backend/tests/test_sprint_replan.py:144:            Action.relabel("OP-1", add=("coord-picked",), remove=("todo",)),
backend/tests/test_sprint_replan.py:150:            Action.mention_operator(
backend/tests/test_sprint_replan.py:167:        ACTION_RELABEL,
backend/tests/test_sprint_replan.py:179:            Action.relabel("OP-1", add=("coord-picked",), remove=("todo",)),
backend/tests/test_sprint_replan.py:185:            Action.mention_operator(
backend/tests/test_sprint_replan.py:200:        "mention_operator",
backend/tests/test_sprint_replan.py:210:        actions=(Action.transition("OP-1", to_status="In Progress"),),
backend/tests/test_idempotency.py:55:    jd.clear_assignee(_Client(), "OP-749", idem_key="clear-1")
backend/tests/test_pep_gateway_install_coaching.py:466:        # rules. Operator doesn't see install holds escalate to red.
backend/tests/test_pipeline_coordinator_watchdog.py:50:    def escalate(self, reason: str, context: dict) -> None:
backend/tests/test_pipeline_coordinator_watchdog.py:130:def test_restart_failure_escalates_operator(tmp_path: Path) -> None:
backend/tests/test_pipeline_coordinator_watchdog.py:131:    """Integration AC: failed restart escalates without restart event."""
backend/tests/test_pipeline_coordinator_watchdog.py:144:    assert out.action == "escalate"
backend/tests/test_pipeline_coordinator_watchdog.py:145:    assert out.escalated is True
backend/tests/test_pipeline_coordinator_watchdog.py:153:def test_re_dies_within_five_minutes_escalates_without_second_restart(
backend/tests/test_pipeline_coordinator_watchdog.py:156:    """Integration AC: re-die window escalates instead of restart-looping."""
backend/tests/test_pipeline_coordinator_watchdog.py:170:    assert out.action == "escalate"
backend/tests/test_merge_arbiter.py:507:def test_high_risk_control_flow_escalates_without_merger():
backend/tests/test_merge_arbiter.py:531:    assert outcome.reason is arb.ArbiterReason.merger_refused_escalated
backend/tests/test_merge_arbiter.py:534:    assert jira.calls[0]["merger_reason"] == "refused_escalated"
backend/tests/test_merge_arbiter.py:605:def test_escalated_reason_maps_to_escalated_outcome():
backend/tests/test_merge_arbiter.py:609:            task, reason=ma.MergerReason.refused_escalated,
backend/tests/test_merge_arbiter.py:615:    assert outcome.reason is arb.ArbiterReason.merger_refused_escalated
backend/tests/test_w15_6_vite_self_fix.py:18:    mixed trail of three different classes does NOT escalate even
backend/tests/test_w15_6_vite_self_fix.py:38:§I — Round-trip with W15.4 (:func:`should_escalate_vite_pattern`).
backend/tests/test_w15_6_vite_self_fix.py:63:    should_escalate_vite_pattern,
backend/tests/test_w15_6_vite_self_fix.py:565:# §I — Round-trip with W15.4 (should_escalate_vite_pattern)
backend/tests/test_w15_6_vite_self_fix.py:572:    (file/line/phase/kind) MUST escalate; a mixed-class trail that
backend/tests/test_w15_6_vite_self_fix.py:578:    def test_same_syntax_error_three_times_escalates(self, message: str) -> None:
backend/tests/test_w15_6_vite_self_fix.py:580:        decision = should_escalate_vite_pattern(history)
backend/tests/test_w15_6_vite_self_fix.py:585:    def test_same_undefined_symbol_three_times_escalates(self) -> None:
backend/tests/test_w15_6_vite_self_fix.py:590:        decision = should_escalate_vite_pattern(history)
backend/tests/test_w15_6_vite_self_fix.py:593:    def test_same_import_typo_three_times_escalates(self) -> None:
backend/tests/test_w15_6_vite_self_fix.py:598:        decision = should_escalate_vite_pattern(history)
backend/tests/test_w15_6_vite_self_fix.py:611:        assert should_escalate_vite_pattern(history) is None
backend/tests/test_w15_6_vite_self_fix.py:613:    def test_mixed_three_classes_does_not_escalate(self) -> None:
backend/tests/test_w15_6_vite_self_fix.py:615:        different files so the head differs) MUST NOT escalate even
backend/tests/test_w15_6_vite_self_fix.py:627:        assert should_escalate_vite_pattern(history) is None
backend/tests/test_w15_6_vite_self_fix.py:632:        different wording still bucket together and escalate.  This
backend/tests/test_w15_6_vite_self_fix.py:643:        decision = should_escalate_vite_pattern(history)
backend/tests/test_w15_6_vite_self_fix.py:653:        decision = should_escalate_vite_pattern(history)
backend/tests/test_merger_agent.py:1519:def test_push_fail_no_vote_and_escalates():
backend/tests/test_merger_agent.py:1575:    assert final.reason is ma.MergerReason.refused_escalated
backend/tests/test_sdk_auto_escalate.py:1:"""OP-816 — SDK launcher auto-escalates one structural Sonnet failure to Opus."""
backend/tests/test_sdk_auto_escalate.py:94:async def test_structural_stop_escalates_once_to_opus_with_doubled_iterations(
backend/tests/test_threat_model_compose_lint.py:206:        "(none today, but defense-in-depth) cannot escalate."
backend/tests/test_submit_checklist.py:152:# ── 4. empty legacy conversion escalates ─────────────────────────────
backend/tests/test_submit_checklist.py:225:# ── 7. 3-cycle escalate ──────────────────────────────────────────────
backend/tests/test_submit_checklist.py:228:def test_three_cycle_escalate_raises():
backend/tests/test_pipeline_coordinator_chaos.py:267:    def mark_resumable(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_chaos.py:270:    def transition_under_review(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_chaos.py:273:    def reset_to_todo(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_chaos.py:294:    def clear_assignee(self, key: str) -> None:
backend/tests/test_pipeline_coordinator_chaos.py:298:    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:68:    should_escalate_vite_pattern,
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:526:      * ``{"vite_escalated_signatures": [...]}`` — the freshly-escalated
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:531:    :func:`backend.web.vite_retry_budget.should_escalate_vite_pattern`
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:537:    decision = should_escalate_vite_pattern(
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:539:        already_escalated=tuple(state.vite_escalated_signatures),
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:549:        "vite_escalated_signatures": (
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:550:            list(state.vite_escalated_signatures) + [decision.pattern]
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:604:        # escalate to the operator once per (graph run, signature)
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:606:        # ``vite_escalated_signatures`` only when a fresh emission
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:1224:      (file mode reset by another process) escalates instead of
backend/tests/fixtures/merger_replay/change_883/ancestor/backend/agents/nodes.py:1371:                        "env_fix_escalated",
backend/tests/fixtures/merger_replay/change_698/before/docs/operations/agent-rpg-system.md:510:sweep escalates. Operators tuning the noise can pass a different
backend/tests/fixtures/merger_replay/change_698/ancestor/docs/operations/agent-rpg-system.md:505:sweep escalates. Operators tuning the noise can pass a different
backend/tests/fixtures/merger_replay/change_899/ancestor/backend/merger_agent.py:150:    refused_escalated = "refused_escalated"
backend/tests/fixtures/merger_replay/change_899/ancestor/backend/merger_agent.py:914:            request, MergerReason.refused_escalated,
backend/tests/fixtures/merger_replay/change_899/before/backend/merger_agent.py:162:    refused_escalated = "refused_escalated"
backend/tests/fixtures/merger_replay/change_899/before/backend/merger_agent.py:1209:            request, MergerReason.refused_escalated,
backend/tests/fixtures/merger_replay/change_930/ancestor/backend/merge_arbiter.py:19:     * ``refused_*`` (security / test / escalated) → same JIRA route +
backend/tests/fixtures/merger_replay/change_930/ancestor/backend/merge_arbiter.py:172:    merger_refused_escalated = "merger_refused_escalated"
backend/tests/fixtures/merger_replay/change_930/ancestor/backend/merge_arbiter.py:1089:        reason=ma.MergerReason.refused_escalated,
backend/tests/fixtures/merger_replay/change_930/ancestor/backend/merge_arbiter.py:1298:        ma.MergerReason.refused_escalated:
backend/tests/fixtures/merger_replay/change_930/ancestor/backend/merge_arbiter.py:1299:            ArbiterReason.merger_refused_escalated,
backend/tests/fixtures/merger_replay/change_930/after/backend/merge_arbiter.py:19:     * ``refused_*`` (security / test / escalated) → same JIRA route +
backend/tests/fixtures/merger_replay/change_930/after/backend/merge_arbiter.py:238:    merger_refused_escalated = "merger_refused_escalated"
backend/tests/fixtures/merger_replay/change_930/after/backend/merge_arbiter.py:1170:        reason=ma.MergerReason.refused_escalated,
backend/tests/fixtures/merger_replay/change_930/after/backend/merge_arbiter.py:1379:        ma.MergerReason.refused_escalated:
backend/tests/fixtures/merger_replay/change_930/after/backend/merge_arbiter.py:1380:            ArbiterReason.merger_refused_escalated,
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:68:    should_escalate_vite_pattern,
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:533:      * ``{"vite_escalated_signatures": [...]}`` — the freshly-escalated
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:538:    :func:`backend.web.vite_retry_budget.should_escalate_vite_pattern`
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:544:    decision = should_escalate_vite_pattern(
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:546:        already_escalated=tuple(state.vite_escalated_signatures),
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:556:        "vite_escalated_signatures": (
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:557:            list(state.vite_escalated_signatures) + [decision.pattern]
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:611:        # escalate to the operator once per (graph run, signature)
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:613:        # ``vite_escalated_signatures`` only when a fresh emission
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:1231:      (file mode reset by another process) escalates instead of
backend/tests/fixtures/merger_replay/change_883/after/backend/agents/nodes.py:1378:                        "env_fix_escalated",
backend/tests/fixtures/merger_replay/change_698/after/docs/operations/agent-rpg-system.md:506:sweep escalates. Operators tuning the noise can pass a different
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:69:    should_escalate_vite_pattern,
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:540:      * ``{"vite_escalated_signatures": [...]}`` — the freshly-escalated
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:545:    :func:`backend.web.vite_retry_budget.should_escalate_vite_pattern`
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:551:    decision = should_escalate_vite_pattern(
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:553:        already_escalated=tuple(state.vite_escalated_signatures),
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:563:        "vite_escalated_signatures": (
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:564:            list(state.vite_escalated_signatures) + [decision.pattern]
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:618:        # escalate to the operator once per (graph run, signature)
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:620:        # ``vite_escalated_signatures`` only when a fresh emission
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:1238:      (file mode reset by another process) escalates instead of
backend/tests/fixtures/merger_replay/change_883/before/backend/agents/nodes.py:1385:                        "env_fix_escalated",
backend/tests/fixtures/merger_replay/change_930/before/backend/merge_arbiter.py:20:     * ``refused_*`` (security / test / escalated) → same JIRA route +
backend/tests/fixtures/merger_replay/change_930/before/backend/merge_arbiter.py:239:    merger_refused_escalated = "merger_refused_escalated"
backend/tests/fixtures/merger_replay/change_930/before/backend/merge_arbiter.py:1171:        reason=ma.MergerReason.refused_escalated,
backend/tests/fixtures/merger_replay/change_930/before/backend/merge_arbiter.py:1380:        ma.MergerReason.refused_escalated:
backend/tests/fixtures/merger_replay/change_930/before/backend/merge_arbiter.py:1381:            ArbiterReason.merger_refused_escalated,
backend/tests/fixtures/merger_replay/change_899/after/backend/merger_agent.py:161:    refused_escalated = "refused_escalated"
backend/tests/fixtures/merger_replay/change_899/after/backend/merger_agent.py:1197:            request, MergerReason.refused_escalated,

exec
/bin/bash -lc 'rg -n "_request_idempotent|idempot|Idempot|TRANSITION_IDS|transition_to_under_review_if_needed|transition_back_to_todo|add_comment|add_label|clear_assignee|remove_label" backend/agents/jira_dispatch.py' in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
12:- ``transition_back_to_todo(key, reason)`` — In Progress → TODO (revert)
13:- ``add_comment(key, body)`` — append ADF comment
46:from backend.agents.idempotency import DEFAULT_STORE
182:    state, and idempotency DBs.
307:        headers["X-Idempotency-Key"] = idem_key
331:def _request_idempotent(
465:TRANSITION_IDS = {
701:    """Idempotent: install Gerrit commit-msg hook in worktree's COMMON git dir.
734:    Idempotent: setting same value twice is a no-op.
805:    Idempotent and best-effort: ``git worktree remove --force`` first (so git's
983:    Idempotent: clean worktrees produce no side effects or logs. If git
1079:    Idempotent: commits already containing a Change-Id are unchanged
1589:    Used by the runner's Phase 1.5 idempotency gate (OP-691): if the agent
1611:    add_comment(
1649:def transition_to_under_review_if_needed(
1669:    _request_idempotent(
1671:        {"transition": {"id": TRANSITION_IDS["to_under_review"]}},
1696:        _request_idempotent(
1700:            {"transition": {"id": TRANSITION_IDS["to_under_review"]}},
1705:        _request_idempotent(
1709:            {"transition": {"id": TRANSITION_IDS["to_approved"]}},
1714:        _request_idempotent(
1718:            {"transition": {"id": TRANSITION_IDS["to_published"]}},
1735:    :func:`transition_to_under_review_if_needed` (OP-691). Idempotent: if
1746:    transition_to_under_review_if_needed(client, key, idem_key=f"{base_key}-transition")
1779:        _request_idempotent(
1783:            {"transition": {"id": TRANSITION_IDS[transition_name]}},
1858:    Idempotent: status is re-read between steps so a partial walk (e.g.
1886:            _request_idempotent(
1890:                {"transition": {"id": TRANSITION_IDS[transition_name]}},
1903:        add_comment(
1918:    _request_idempotent(
1923:    _request_idempotent(
1925:        {"transition": {"id": TRANSITION_IDS["to_in_progress"]}},
1928:    add_comment(
1934:def transition_back_to_todo(
1987:    add_comment(
1991:    clear_assignee(client, key, idem_key=f"{base_key}-clear-assignee")
1992:    _request_idempotent(
1994:        {"transition": {"id": TRANSITION_IDS["back_to_todo"]}},
2014:def add_comment(client: DispatchClient, key: str, text: str, idem_key: str | None = None) -> None:
2016:    _request_idempotent(
2023:def clear_assignee(client: DispatchClient, key: str, idem_key: str | None = None) -> None:
2025:    _request_idempotent(
2032:def add_label(client: DispatchClient, key: str, label: str, idem_key: str | None = None) -> None:
2035:    _request_idempotent(
2042:def remove_label(client: DispatchClient, key: str, label: str, idem_key: str | None = None) -> None:
2045:    _request_idempotent(
2396:    add_comment(
2514:        add_comment(client, snapshot.key, current_comment, idem_key=f"{idem}-current")
2515:        add_comment(client, hit.other.ticket_key, other_comment, idem_key=f"{idem}-other")
2624:            add_comment(
2634:        add_comment(
2745:# ``update.labels.add`` is set-union (idempotent), not compare-and-swap,
2757:# over the readback set* (not by "did my idempotent write succeed"), two
2815:      re-claiming a ticket we already held (idempotent re-entry after a
3092:    sibling instance clears the assignee (its own revert → ``clear_assignee``
3431:    readback confirms our write landed). Idempotent for the same

exec
/bin/bash -lc "rg -n \"_default_cold_start_gateway|run_cold_start|cold_start|Startup|reconciled|swept|entered_loop|run\\(|run_forever|run_once|while\" backend/agents/pipeline_coordinator.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
15:    PipelineCoordinator  — run_once() / run_forever() + SIGTERM drain (§9 L5)
31:``run_forever`` — guarded by ``install_signals`` so test threads opt out.
83:# Interruptible sleep. Returns True if a stop was requested while waiting
103:# ADR-0021 §4, while JIRA polling remains independently gated at 60s.
486:#   Startup-1  infra verify   — deployment-audit.sh + systemctl --user start
488:#   Startup-2  JIRA reconcile — interrupted-ticket decision tree (§3.3)
489:#   Startup-3  stale sweep    — orphaned claim:* / dead waiting-* markers
490:#   Startup-4  enter loop     — log steady-state entry, hand off to run_forever
500:COLD_START_BEGAN_EVENT = "cold_start_began"
501:COLD_START_COMPLETE_EVENT = "cold_start_complete"
515:    """One expected-live unit as reported by the deployment audit (Startup-1)."""
523:    """Outcome of a Startup-1 deployment audit pass."""
534:    """A ticket left ``進行中`` by an interrupted/crashed runner (Startup-2).
549:    """The Startup-3 sweep work the gateway found, ready to apply.
570:    reconciled: list[dict[str, Any]] = field(default_factory=list)
571:    swept: list[dict[str, Any]] = field(default_factory=list)
572:    entered_loop: bool = False
584:    # ── Startup-1 (infra) ──
588:    # ── Startup-2 (JIRA reconcile) ──
597:    # ── Startup-3 (stale sweep) ──
602:    # ── shared (Startup-1 halt / ambiguous Startup-2 (d)) ──
647:    # ── Startup-1 ──
655:            proc = subprocess.run(
671:    # ── Startup-2 ──
759:                reason="[cold-start Startup-2(c)] interrupted with no commits/activity; "
765:    # ── Startup-3 ──
875:        # observe-only boot proceeds past Startup-1 to actually observe the
899:        # fire while acting=False). The intent is still captured in the log.
943:            subprocess.run(
987:    """ADR §3.3 Startup-2: a runner is live for ``key`` if a process cmdline
1024:        base = subprocess.run(
1033:        log = subprocess.run(
1078:    """Assemble the Startup-3 sweep plan from a JIRA snapshot (ADR §3.3).
1081:    in-flight ticket is never swept:
1137:def _default_cold_start_gateway(
1177:        cold_start_gateway: ColdStartGateway | None = None,
1223:        self._cold_start_gateway: ColdStartGateway = (
1224:            cold_start_gateway or _default_cold_start_gateway(config)
1264:        yields an empty graph so the daemon ticks safely while events are
1306:                return self._sprint_replan_handler.run().to_event()
1400:    def _log_cold_start(self, record: dict[str, Any]) -> None:
1413:        Order: Startup-1 (infra) → L6 crash-recovery (ADR §9 L6: "after Phase
1414:        Startup-1") → Startup-2 (JIRA reconcile) → Startup-3 (stale sweep) →
1415:        Startup-4 (log steady-state entry). If Startup-1 cannot bring the
1418:        than not running (ADR §3.3 Startup-1 failure path). :meth:`run` reads
1422:        self._log_cold_start({"event": COLD_START_BEGAN_EVENT})
1425:            self._log_cold_start(
1434:        self._log_cold_start(
1439:                "reconciled": len(report.reconciled),
1440:                "swept": len(report.swept),
1446:        """Startup-1: verify infra; start any down unit; halt if still down."""
1447:        gw = self._cold_start_gateway
1452:            self._log_cold_start(
1465:                "[cold-start Startup-1] coordinator could not bring up "
1470:            self._log_cold_start(
1512:            self._log_cold_start(entry)
1518:        gw = self._cold_start_gateway
1556:        while day <= now.date():
1580:        """Startup-2: reconcile each interrupted (進行中) ticket (ADR §3.3).
1593:        gw = self._cold_start_gateway
1612:                    "[cold-start Startup-2(d)] interrupted with no commits but "
1623:        report.reconciled.append({"ticket": key, "classification": classification, "action": action})
1624:        self._log_cold_start(entry)
1627:        """Startup-3: drop orphaned claim:* / dead waiting-* + clear assignees."""
1628:        gw = self._cold_start_gateway
1648:        report.swept.append({"ticket": key, "sweep": kind, "labels": labels})
1649:        self._log_cold_start(entry)
1652:        """Startup-4: log steady-state entry (the loop itself is run() → run_forever)."""
1653:        report.entered_loop = True
1654:        self._log_cold_start(
1662:    def run(
1671:        startup orchestrator). If Startup-1 halted on degraded infra, the loop
1673:        :meth:`run_forever` (Startup-4).
1678:                "[pipeline_coordinator] cold-start halted in Startup-1 "
1683:        return self.run_forever(install_signals=install_signals, max_ticks=max_ticks)
1687:    def run_once(self) -> DecisionResult:
1731:            loop.maybe_run(self._clock())
1758:    def run_forever(
1777:        # Startup line names the loaded rule registry when the Tier-1 engine
1795:            while not self._stop.is_set():
1796:                self.run_once()
1802:                # Interruptible: returns True if stop fired while waiting.
1896:        cold_start_gateway=_default_cold_start_gateway(config, acting=acting),
1938:        coordinator.run_once()
1940:    if args.no_cold_start:
1941:        coordinator.run_forever()
1944:    coordinator.run()

exec
/bin/bash -lc "sed -n '600,860p' backend/agents/pipeline_coordinator_rules.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
@rule(name="operator-keep-out", priority=0)
def operator_keep_out(ctx: DecisionContext) -> "Action | None":
    """§5.1 #10 — ``coord-skip`` present: take no action, ever.

    Returns a :class:`NoopAction` (not ``None``) so, sitting at priority 0,
    it short-circuits the whole registry: an operator escape-hatched ticket
    can never be touched by a lower-priority rule (ADR §6.2 self-modify ban,
    §6.3 operator override). Abstains (``None``) when there's no focal ticket
    or the label is absent, so other rules still run.
    """
    t = ctx.ticket
    if t is None or not has_label(t, COORD_SKIP_LABEL):
        return None
    return NoopAction()


@rule(name="chain-deadlock", priority=5)
def chain_deadlock(ctx: DecisionContext) -> "Action | None":
    """§5.1 #8 — A blocks B blocks A (or longer cycle): diagnose + escalate.

    Walks the blockedBy DAG from the focal ticket over ``ctx.tickets``. If
    the focal key is reachable from itself, that's a cycle — file a
    diagnostic + @-mention operator (this should never happen; if it does,
    halt rather than guess). Abstains when the graph is acyclic / unknown.
    """
    t = ctx.ticket
    if t is None:
        return None
    # DFS over blockedBy edges looking for a path back to the focal key.
    start = t.key
    stack = list(t.blocked_by)
    visited: set[str] = set()
    while stack:
        cur = stack.pop()
        if cur == start:
            return Action.mention_operator(
                start,
                message=(
                    f"[chain-deadlock] blockedBy cycle detected through {start}; "
                    f"halting automated routing — operator triage required."
                ),
                urgency="high",
            )
        if cur in visited:
            continue
        visited.add(cur)
        nxt = ctx.tickets.get(cur)
        if nxt is not None:
            stack.extend(nxt.blocked_by)
    return None


@rule(name="revert-loop-quarantine", priority=10)
def revert_loop_quarantine(ctx: DecisionContext) -> "Action | None":
    """§5.1 #5 — reverted > 5× in 24h: add ``coord-quarantine`` + @operator."""
    t = ctx.ticket
    if t is None:
        return None
    if t.revert_count_24h <= REVERT_LOOP_QUARANTINE_THRESHOLD:
        return None
    if has_label(t, COORD_QUARANTINE_LABEL):
        return None  # already quarantined — don't re-fire each tick
    return Action.relabel(
        t.key,
        add=(COORD_QUARANTINE_LABEL, NEEDS_OPERATOR_ACTION_LABEL),
    )


@rule(name="merger-repeat-fail", priority=20)
def merger_repeat_fail(ctx: DecisionContext) -> "Action | None":
    """§5.1 #4 — merger failed > 3× on one ticket: escalate with context."""
    t = ctx.ticket
    if t is None:
        return None
    if t.merger_fail_count <= MERGER_FAIL_ESCALATE_THRESHOLD:
        return None
    if has_label(t, NEEDS_OPERATOR_ACTION_LABEL):
        return None  # already escalated
    return Action.escalate(
        t.key,
        reason=(
            f"[merger-repeat-fail] merger failed {t.merger_fail_count} times on "
            f"{t.key}; conflict is not auto-resolvable — operator merge needed."
        ),
    )


@rule(name="stuck-in-progress", priority=30)
def stuck_in_progress(ctx: DecisionContext) -> "Action | None":
    """§5.1 #7 — 進行中 > 2× CLI timeout, no live runner: reconcile to To Do.

    The single-ticket equivalent of Phase Startup-2's "no commits, no recent
    activity → restart cleanly": revert to To Do + clear the claim so a fresh
    runner can re-pick it. Abstains when a runner is alive (it's handling) or
    the ticket isn't actually stale.
    """
    t = ctx.ticket
    if t is None or t.status not in IN_PROGRESS_STATUS_NAMES:
        return None
    if t.has_live_runner:
        return None
    age = _age_seconds(ctx.now, t.in_progress_since)
    if age is None or age <= _stale_threshold_seconds():
        return None
    return Action.transition(t.key, to_status="To Do")


@rule(name="stale-claim-cleanup", priority=40)
def stale_claim_cleanup(ctx: DecisionContext) -> "Action | None":
    """§5.1 #2 — ``claim:default:*`` > 2× CLI timeout, no live runner: clear.

    Removes the orphaned claim label(s) (ADR §3.3 Startup-3 stale-state sweep
    for a single ticket). Assignee clearing rides along in the action layer's
    relabel handler. Abstains while a runner is alive or the claim is fresh.
    """
    t = ctx.ticket
    if t is None or t.has_live_runner:
        return None
    claim_labels = labels_with_prefix(t, CLAIM_LABEL_PREFIX)
    if not claim_labels:
        return None
    age = _age_seconds(ctx.now, t.claim_started_at)
    if age is None or age <= _stale_threshold_seconds():
        return None
    return Action.relabel(t.key, remove=claim_labels)


@rule(name="runner-blocked-marker-stale", priority=50)
def runner_blocked_marker_stale(ctx: DecisionContext) -> "Action | None":
    """§5.1 #3 — ``runner-blocked:waiting-X`` where X is 公開済み: drop marker.

    Resolves each ``runner-blocked:waiting-<KEY>`` against ``ctx.tickets``;
    any blocker already Published is a dead wait — remove that marker so the
    blocked ticket is pickable again. Abstains if blockers are unknown or
    still open.
    """
    t = ctx.ticket
    if t is None:
        return None
    waiting = labels_with_prefix(t, DEPENDENCY_WAITING_LABEL_PREFIX)
    stale: list[str] = []
    for label in waiting:
        blocker_key = label[len(DEPENDENCY_WAITING_LABEL_PREFIX):]
        blocker = ctx.tickets.get(blocker_key)
        if blocker is not None and blocker.status in PUBLISHED_STATUS_NAMES:
            stale.append(label)
    if not stale:
        return None
    return Action.relabel(t.key, remove=tuple(stale))


@rule(name="dependency-out-of-area", priority=60)
def dependency_out_of_area(ctx: DecisionContext) -> "Action | None":
    """§5.1 #1 — runner discovered a dependency in another known area.

    Gated on ``needs-coordinator`` + a ``[runner-discovered-dependency]``
    marker in the latest comment. Parses the named area; if it's a known
    out-of-area domain, file a dependency ticket there. Unknown / unparseable
    → abstain (Tier-2 takes it).
    """
    t = ctx.ticket
    if t is None or not has_label(t, NEEDS_COORDINATOR_LABEL):
        return None
    comment = latest_comment(t)
    if comment is None or DISCOVERED_DEPENDENCY_MARKER not in comment.text:
        return None
    target_area = _parse_discovered_area(comment.text, ticket_areas=t.areas)
    if target_area is None:
        return None
    return Action.file_ticket(
        blocking=t.key,
        target_area=target_area,
        description=(
            f"Dependency discovered by runner on {t.key} requiring "
            f"area:{target_area} work. See {t.key} discovered-dependency note."
        ),
    )


def _parse_discovered_area(text: str, *, ticket_areas: tuple[str, ...]) -> str | None:
    """Extract a known out-of-area name from a discovered-dependency comment.

    Conservative: returns an area only when exactly one *known* area outside
    the ticket's own area(s) is named. Zero or multiple candidates → ``None``
    (defer to Tier-2) so we never reroute on an ambiguous mention.
    """
    own = {a.lower() for a in ticket_areas}
    lowered = text.lower()
    candidates = {
        area
        for area in KNOWN_AREAS
        if area not in own and _mentions_area(lowered, area)
    }
    if len(candidates) == 1:
        return next(iter(candidates))
    return None


def _mentions_area(lowered_text: str, area: str) -> bool:
    """True if ``area`` is named as ``area:<area>`` or a whole word."""
    if f"{AREA_LABEL_PREFIX}{area}" in lowered_text:
        return True
    import re

    return re.search(rf"\b{re.escape(area)}\b", lowered_text) is not None


@rule(name="capability-blocked-known-locale", priority=70)
def capability_blocked_known_locale(ctx: DecisionContext) -> "Action | None":
    """§5.1 #6 — ``[runner-capability-blocked]`` on a ストーリー: apply workaround.

    Until AUDIT-27 wires capability resolution properly, a capability-blocked
    Story gets the ``capability:enable=*`` operator-escape-hatch label applied
    automatically. Restricted to the Story issuetype (the observed pattern);
    other issuetypes defer to Tier-2.
    """
    t = ctx.ticket
    if t is None or t.issuetype not in STORY_ISSUETYPE_NAMES:
        return None
    comment = latest_comment(t)
    if comment is None or CAPABILITY_BLOCKED_MARKER not in comment.text:
        return None
    if has_label(t, CAPABILITY_ENABLE_LABEL):
        return None  # workaround already applied
    return Action.relabel(t.key, add=(CAPABILITY_ENABLE_LABEL,))


@rule(name="wrong-class-routing", priority=80)
def wrong_class_routing(ctx: DecisionContext) -> "Action | None":
    """§5.1 #9 — backend ticket on subscription-claude while codex idle.

    Comment-only suggestion (ADR is explicit: "don't relabel without LLM
    consult"). Fires when the focal ticket is strongly area:backend, currently
    ``class:subscription-claude``, and the capacity snapshot shows a codex
    runner with free slots. Emits a low-urgency operator note, never a
    relabel.
    """
    t = ctx.ticket
    if t is None:
        return None
    if "backend" not in {a.lower() for a in t.areas}:
        return None
    if ticket_class(t) != f"{CLASS_LABEL_PREFIX}subscription-claude":
        return None
    if not _codex_has_capacity(ctx.capacity):
        return None
    return Action.mention_operator(
        t.key,
        message=(
            f"[wrong-class-routing] {t.key} is area:backend on "
            f"class:subscription-claude while a codex runner is idle; "
            f"consider rerouting to subscription-codex (suggestion only)."
        ),
        urgency="low",
    )


def _codex_has_capacity(capacity: CapacitySnapshot) -> bool:
    for runner_class, row in capacity.runners.items():
        if "codex" in runner_class.lower() and row.free_slots > 0:
            return True

exec
/bin/bash -lc "sed -n '1128,1240p' backend/agents/pipeline_coordinator.py && sed -n '1348,1712p' backend/agents/pipeline_coordinator.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
        if dead:
            resolved_waiting[key] = tuple(dead)
    return StaleSweepPlan(
        stale_claims=stale_claims,
        orphan_assignees=tuple(orphan_assignees),
        resolved_waiting=resolved_waiting,
    )


def _default_cold_start_gateway(
    config: CoordinatorConfig, *, acting: bool = False
) -> ColdStartGateway:
    """Production gateway wiring (no I/O at construction — lazy + fail-open).

    ``acting=False`` (the default — the 29f-14 7-day shadow canary, OP-1555)
    wraps the live gateway in :class:`ShadowColdStartGateway` so cold-start
    observes the world but performs zero JIRA/git/systemctl mutation;
    ``acting=True`` returns the live gateway so the 4-phase recovery mutates as
    designed. The same single ``acting`` switch gates the engine action layer
    and the sprint re-plan handler (see :func:`build_default_coordinator`).
    """
    live = JiraDispatchColdStartGateway()
    return live if acting else ShadowColdStartGateway(live)


# ── Daemon ────────────────────────────────────────────────────────────


class PipelineCoordinator:
    """Skeleton coordinator daemon: tick → evaluate → log no-op → heartbeat.

    Collaborators are injected for deterministic tests; ``from_env`` /
    default construction wires the production defaults. The daemon holds
    no durable state of its own beyond an in-memory work-graph cache STUB
    that is rebuilt every tick (ADR-0021 §3.2 stateless-across-restarts).
    """

    def __init__(
        self,
        config: CoordinatorConfig,
        *,
        engine: DecisionEngine | None = None,
        mode_selector: ModeSelector | None = None,
        clock: Clock = _utc_now,
        heartbeat: HeartbeatWriter | None = None,
        decision_log: DecisionLog | None = None,
        capacity_provider: Callable[[], CapacitySnapshot] | None = None,
        work_graph_provider: Callable[[], WorkGraph] | None = None,
        action_executor: ActionExecutor | None = None,
        cold_start_gateway: ColdStartGateway | None = None,
        sleep: SleepFn | None = None,
        bridge_tailer: BridgeEventTailer | None = None,
        jira_poller: JiraEventPoller | None = None,
        deduper: EventDeduper | None = None,
        sprint_replan_handler: SprintReplanHandler | None = None,
        learning_loop: "Any | None" = None,
    ) -> None:
        self._config = config
        self._engine = engine or DecisionEngine()
        self._mode_selector = mode_selector or ModeSelector()
        self._clock = clock
        self._heartbeat = heartbeat or HeartbeatWriter(config.heartbeat_path, clock=clock)
        self._decision_log = decision_log or DecisionLog(config.decision_log_dir, clock=clock)
        self._capacity_provider = capacity_provider or self._default_capacity
        # 29f-3: how the daemon learns the per-tick world. Default yields an
        # empty WorkGraph (skeleton / idle); 29f-8 injects the JIRA-poll +
        # bridge-tap builder. Every rule abstains on an empty graph.
        self._work_graph_provider = work_graph_provider or self._default_work_graph
        # 29f-3: action layer. Default is shadow (observe-only) so a rule that
        # fires never mutates JIRA until the real §6 layer / acting mode lands.
        executor = action_executor or ShadowActionExecutor().execute
        self._action_executor: ActionExecutor = executor
        # 29f-6: event sources (ADR §4) — bridge JSONL tail + 60s JIRA poll +
        # hourly sweep, coalesced through a 5-min deduper. They feed the
        # per-tick raw events staged for the work-graph builder.
        self._bridge_tailer = bridge_tailer or BridgeEventTailer(
            config.bridge_events_path or (config.config_dir / "bridge-events.jsonl")
        )
        self._jira_poller = jira_poller or JiraEventPoller(
            agent_class=config.jira_agent_class,
            clock=clock,
            interval_seconds=config.jira_poll_interval_seconds,
        )
        self._deduper = deduper or EventDeduper(
            window_seconds=config.event_dedupe_seconds,
            clock=clock,
        )
        self._sprint_replan_handler = sprint_replan_handler
        self._last_sweep_at: datetime | None = self._clock()
        # 29f-7: cold-start recovery seam. Default wraps the production
        # jira_dispatch / git / systemctl adapters (lazy + fail-open) in the
        # observe-only ShadowColdStartGateway (acting=False default, OP-1555);
        # build_default_coordinator(acting=True) injects the live gateway, and
        # tests inject a fake so the 4-phase decision tree is verified in
        # isolation.
        self._cold_start_gateway: ColdStartGateway = (
            cold_start_gateway or _default_cold_start_gateway(config)
        )
        # 29f-11: learning loop (ADR §10). Default ``None`` keeps the skeleton /
        # test daemon inert; build_default_coordinator wires the Cognee
        # write-back + 24h outcome-check + weekly graduation handlers. When
        # present, every Tier-2 tick is written back within the tick ("within
        # 60s", Integration AC) and the daily/weekly schedules are gated here.
        self._learning_loop = learning_loop
        self._learning_seeded = False
        self._stop = threading.Event()
        # Interruptible sleep: default waits on the stop event so SIGTERM
        # breaks the loop within one poll instead of one tick.
        self._sleep: SleepFn = sleep or self._stop.wait
        # Current-tick world (rebuilt every tick — ADR §3.2 stateless).
        self._work_graph: WorkGraph = WorkGraph()
        # 29f-6: raw source events collected this tick, staged for the
        # events→WorkGraph builder (29f-8). Empty until the first refresh.

    # ── decision-log record (ADR-0021 §3.2 example) ──

    def _build_tick_record(
        self,
        result: DecisionResult,
        action_results: list[dict[str, Any]] | None = None,
    ) -> dict[str, Any]:
        # A no-op tick records ``actions:[]``; only real (non-noop) actions
        # are serialised into the array (skeleton emits none).
        actions = [a.to_record() for a in result.actions if not isinstance(a, NoopAction)]
        dry_run = all(a.dry_run for a in result.actions) if result.actions else True
        record: dict[str, Any] = {
            "ts": self._clock().isoformat(),
            "event": "decision_tick",
            "engine_version": result.engine_version,
            "mode": result.mode,
            "decision_id": result.decision_id,
            "actions": actions,
            "reason": result.reason,
            "dry_run": dry_run,
            "pid": os.getpid(),
        }
        # When a personality mode fired (non-idle tick), record the behavior
        # knobs that steered Tier-1 / Tier-2 so the selected mode is auditable
        # in the shadow/decision log (ADR-0021 §7.2 + Integration AC).
        if result.mode_behavior is not None:
            record["mode_behavior"] = {
                "tier1_dominant": result.mode_behavior.tier1_dominant,
                "tier2_policy": result.mode_behavior.tier2_policy,
                "llm_context_hops": result.mode_behavior.llm_context_hops,
                "llm_lessons_window": result.mode_behavior.llm_lessons_window,
            }
        # 29f-3: name the firing rule + tier (ADR Appendix C) and the action
        # layer's per-action outcomes — only when a rule actually fired, so a
        # plain no-op tick keeps the skeleton's minimal record shape.
        if result.rule_name is not None:
            record["rule_name"] = result.rule_name
        if result.tier is not None:
            record["tier"] = result.tier
        # 29f-6: the Tier-2 LLM-consultation block (ADR Appendix C) — only on a
        # tick that actually consulted (or degraded) the LLM, so a Tier-1 / no-op
        # tick keeps its minimal record shape and ``budget_cap`` events stay
        # grep-able + replayable for the daily-spend rebuild.
        if result.llm_consultation is not None:
            record["llm_consultation"] = dict(result.llm_consultation)
        if action_results:
            record["action_results"] = action_results
        return record

    # ── cold-start 4-phase recovery (ADR §3.3 + §9 L6) ──

    def _log_cold_start(self, record: dict[str, Any]) -> None:
        """Append a cold-start decision-log record, stamping ts/pid/engine."""
        base: dict[str, Any] = {
            "ts": self._clock().isoformat(),
            "engine_version": self._engine.engine_version,
            "pid": os.getpid(),
        }
        base.update(record)
        self._decision_log.append(base)

    def startup(self) -> ColdStartReport:
        """Run the 4-phase cold-start recovery (ADR §3.3). Returns a report.

        Order: Startup-1 (infra) → L6 crash-recovery (ADR §9 L6: "after Phase
        Startup-1") → Startup-2 (JIRA reconcile) → Startup-3 (stale sweep) →
        Startup-4 (log steady-state entry). If Startup-1 cannot bring the
        expected infra up, the daemon halts startup (``phase1_halted=True``)
        and does NOT proceed — entering the loop with degraded infra is worse
        than not running (ADR §3.3 Startup-1 failure path). :meth:`run` reads
        the report to decide whether to enter the loop.
        """
        report = ColdStartReport()
        self._log_cold_start({"event": COLD_START_BEGAN_EVENT})
        self._startup_1_infra(report)
        if report.phase1_halted:
            self._log_cold_start(
                {"event": COLD_START_COMPLETE_EVENT, "halted_at": "startup-1",
                 "still_down": report.still_down_units}
            )
            return report
        self._l6_crash_recovery(report)
        self._startup_2_reconcile(report)
        self._startup_3_sweep(report)
        self._startup_4_enter_loop(report)
        self._log_cold_start(
            {
                "event": COLD_START_COMPLETE_EVENT,
                "started_units": report.started_units,
                "crash_recoveries": len(report.crash_recoveries),
                "reconciled": len(report.reconciled),
                "swept": len(report.swept),
            }
        )
        return report

    def _startup_1_infra(self, report: ColdStartReport) -> None:
        """Startup-1: verify infra; start any down unit; halt if still down."""
        gw = self._cold_start_gateway
        audit = gw.audit_infra()
        for unit in audit.down:
            ok = gw.start_unit(unit)
            (report.started_units if ok else report.still_down_units).append(unit)
            self._log_cold_start(
                {"event": STARTUP_PHASE_EVENT, "phase": 1, "unit": unit,
                 "action": "start_unit", "ok": ok}
            )
        # Re-verify after starts: a unit that refused to start (or died again)
        # leaves the infra degraded → @-operator + halt (do not enter loop).
        if report.still_down_units:
            recheck = gw.audit_infra()
            report.still_down_units = list(recheck.down)
        if report.still_down_units:
            report.phase1_halted = True
            gw.mention_operator(
                "OP",  # infra-level alert is not ticket-scoped
                "[cold-start Startup-1] coordinator could not bring up "
                f"{report.still_down_units}; halting startup rather than "
                "entering the loop with degraded infra (ADR-0021 §3.3).",
                urgency="high",
            )
            self._log_cold_start(
                {"event": STARTUP_PHASE_EVENT, "phase": 1, "action": "halt",
                 "still_down": report.still_down_units}
            )

    def _l6_crash_recovery(self, report: ColdStartReport) -> None:
        """L6 (ADR §9): replay last 24h decision log; resume/complete/rollback.

        A ``shutdown_began`` with no matching ``shutdown_complete`` (paired by
        ``decision_id``) means the daemon crashed mid-drain. For each such
        crash we look at the immediately-preceding tick: if it executed a real
        (non-dry-run) action that was never confirmed complete, we resolve it
        per action kind — idempotent relabel/transition are *completed*
        (re-issued safely), file_ticket is effectively irreversible so we
        *escalate*, everything else is a logged no-op. Every resolution writes
        a ``crash_recovery_applied`` record (ADR §9 L6).
        """
        records = self._read_recent_decision_log()
        began: dict[str, dict[str, Any]] = {}
        completed: set[str] = set()
        last_real_action_tick: dict[str, Any] | None = None
        for rec in records:
            event = rec.get("event")
            if event == "decision_tick" and not rec.get("dry_run", True) and rec.get("actions"):
                last_real_action_tick = rec
            elif event == "shutdown_began":
                began[str(rec.get("decision_id", ""))] = rec
                # Snapshot which real-action tick preceded this drain.
                rec["_preceding_real_tick"] = last_real_action_tick
            elif event == "shutdown_complete":
                completed.add(str(rec.get("decision_id", "")))
        for drain_id, drain_rec in began.items():
            if drain_id in completed:
                continue  # clean shutdown — nothing to recover
            tick = drain_rec.get("_preceding_real_tick")
            resolutions = self._resolve_interrupted_actions(tick)
            entry = {
                "event": CRASH_RECOVERY_EVENT,
                "crashed_drain_id": drain_id,
                "resolutions": resolutions,
            }
            report.crash_recoveries.append(entry)
            self._log_cold_start(entry)

    def _resolve_interrupted_actions(self, tick: dict[str, Any] | None) -> list[dict[str, Any]]:
        """Map each interrupted action of a crashed tick to a recovery step."""
        if not tick:
            return [{"resolution": "noop", "reason": "no in-flight real action before crash"}]
        gw = self._cold_start_gateway
        out: list[dict[str, Any]] = []
        for action in tick.get("actions", []):
            kind = action.get("kind")
            target = action.get("target", "")
            if kind in (ACTION_RELABEL, ACTION_TRANSITION):
                # Idempotent → safe to complete (re-issue) on recovery.
                if kind == ACTION_TRANSITION and action.get("params", {}).get(
                    "to_status"
                ) == "Under Review":
                    gw.transition_under_review(target)
                resolution = "completed"
            elif kind == ACTION_FILE_TICKET:
                # Not safely reversible/repeatable — surface to operator.
                gw.mention_operator(
                    target,
                    f"[cold-start L6] crash interrupted a file_ticket action on {target}; "
                    "verify the dependency ticket exists and was not double-filed.",
                    urgency="medium",
                )
                resolution = "escalated"
            else:
                resolution = "noop"
            out.append({"kind": kind, "target": target, "resolution": resolution})
        return out

    def _read_recent_decision_log(self) -> list[dict[str, Any]]:
        """Return decision-log records from the last :data:`L6_REPLAY_WINDOW_HOURS`.

        Reads the day-partitioned JSONL files spanning the window (today + the
        days it reaches back into), in chronological order, skipping malformed
        lines so a single torn record never aborts recovery.
        """
        now = self._clock()
        window_start = now - timedelta(hours=L6_REPLAY_WINDOW_HOURS)
        records: list[dict[str, Any]] = []
        day = window_start.date()
        seen_paths: list[Path] = []
        while day <= now.date():
            path = self._decision_log.directory / f"{day.isoformat()}.jsonl"
            if path.exists():
                seen_paths.append(path)
            day = day + timedelta(days=1)
        for path in seen_paths:
            try:
                lines = path.read_text(encoding="utf-8").splitlines()
            except OSError:
                continue
            for line in lines:
                if not line.strip():
                    continue
                try:
                    rec = json.loads(line)
                except json.JSONDecodeError:
                    continue
                if isinstance(rec, dict):
                    ts = record_ts(rec)
                    if ts is None or ts >= window_start:
                        records.append(rec)
        return records

    def _startup_2_reconcile(self, report: ColdStartReport) -> None:
        """Startup-2: reconcile each interrupted (進行中) ticket (ADR §3.3).

        Decision tree per ticket with no live runner:
          (b) Gerrit change exists + mergeable      → transition Under Review
          (a) feature branch has commits             → mark resumable, leave
          (c) no commits + stale (>2× CLI timeout)   → revert To Do (clear claim)
          (d) no commits + not yet stale (ambiguous) → @-operator, leave alone
        A ticket whose runner is still alive is left untouched (it's handling
        it). (b) is checked before (a): a mergeable change means the work is
        *finished* and only missed its transition, which beats re-running it.
        (c) vs (d) hinges on staleness so a runner that only just started (and
        hasn't committed yet) is never aggressively reverted out from under.
        """
        gw = self._cold_start_gateway
        for ticket in gw.interrupted_tickets():
            key = ticket.key
            if gw.has_live_runner(key):
                self._record_reconcile(report, key, "live-runner", "leave")
            elif gw.gerrit_change_mergeable(key):
                gw.transition_under_review(key)
                self._record_reconcile(report, key, "gerrit-mergeable", "under_review")
            elif gw.branch_has_commits(key):
                gw.mark_resumable(key)
                self._record_reconcile(report, key, "branch-has-commits", "resume")
            elif ticket.stale:
                # No commits, no live runner, long-stale → abandoned work.
                gw.reset_to_todo(key)
                self._record_reconcile(report, key, "no-commits-stale", "revert_todo")
            else:
                # No commits but recent — a runner may have only just started.
                gw.mention_operator(
                    key,
                    "[cold-start Startup-2(d)] interrupted with no commits but "
                    "recent activity — ambiguous; leaving in place for operator review.",
                    urgency="medium",
                )
                self._record_reconcile(report, key, "ambiguous", "operator")

    def _record_reconcile(
        self, report: ColdStartReport, key: str, classification: str, action: str
    ) -> None:
        entry = {"event": STARTUP_PHASE_EVENT, "phase": 2, "ticket": key,
                 "classification": classification, "action": action}
        report.reconciled.append({"ticket": key, "classification": classification, "action": action})
        self._log_cold_start(entry)

    def _startup_3_sweep(self, report: ColdStartReport) -> None:
        """Startup-3: drop orphaned claim:* / dead waiting-* + clear assignees."""
        gw = self._cold_start_gateway
        plan = gw.stale_sweep_plan()
        for key, labels in plan.stale_claims.items():
            for label in labels:
                gw.remove_label(key, label)
            gw.clear_assignee(key)
            self._record_sweep(report, key, "stale-claim", list(labels))
        for key in plan.orphan_assignees:
            gw.clear_assignee(key)
            self._record_sweep(report, key, "orphan-assignee", [])
        for key, labels in plan.resolved_waiting.items():
            for label in labels:
                gw.remove_label(key, label)
            self._record_sweep(report, key, "resolved-waiting", list(labels))

    def _record_sweep(
        self, report: ColdStartReport, key: str, kind: str, labels: list[str]
    ) -> None:
        entry = {"event": STARTUP_PHASE_EVENT, "phase": 3, "ticket": key,
                 "sweep": kind, "labels": labels}
        report.swept.append({"ticket": key, "sweep": kind, "labels": labels})
        self._log_cold_start(entry)

    def _startup_4_enter_loop(self, report: ColdStartReport) -> None:
        """Startup-4: log steady-state entry (the loop itself is run() → run_forever)."""
        report.entered_loop = True
        self._log_cold_start(
            {"event": STEADY_STATE_ENTERED_EVENT, "ts_entered": self._clock().isoformat()}
        )
        logger.info(
            "[pipeline_coordinator] cold-start complete — entered steady state at %s",
            self._clock().isoformat(),
        )

    def run(
        self,
        *,
        install_signals: bool = True,
        max_ticks: int | None = None,
    ) -> int:
        """Production boot: cold-start recovery, then the steady-state loop.

        Runs :meth:`startup` first (ADR §3.3 — the coordinator is the canonical
        startup orchestrator). If Startup-1 halted on degraded infra, the loop
        is NOT entered and 0 ticks are returned. Otherwise hands off to
        :meth:`run_forever` (Startup-4).
        """
        report = self.startup()
        if report.phase1_halted:
            logger.error(
                "[pipeline_coordinator] cold-start halted in Startup-1 "
                "(infra still down: %s) — not entering steady-state loop",
                report.still_down_units,
            )
            return 0
        return self.run_forever(install_signals=install_signals, max_ticks=max_ticks)

    # ── entrypoints ──

    def run_once(self) -> DecisionResult:
        """One tick: heartbeat → refresh world → evaluate → log decision.

        Returns the engine's :class:`DecisionResult` (a no-op in the
        skeleton) so callers/tests can assert on it directly.
        """
        self._heartbeat.touch()
        self._refresh_work_graph()
        self._append_source_event_records()
        ctx = self.build_context()
        if self._config.capacity_path is not None:
            write_capacity_snapshot(ctx.capacity, self._config.capacity_path)
        result = self._engine.evaluate(ctx)
        # Hand each real (non-noop) action to the action layer. In shadow
        # mode it only records the would-be execution; the outcomes go into
        # the decision-log line so a fired rule is observable end-to-end.
        action_results = [
            self._action_executor(a, ctx)
            for a in result.actions
            if not isinstance(a, NoopAction)
        ]
        record = self._build_tick_record(result, action_results)
        self._decision_log.append(record)
        self._run_learning_loop(record, result)
        return result


exec
/bin/bash -lc "sed -n '560,920p' backend/agents/pipeline_coordinator_llm_consultation.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
        except subprocess.TimeoutExpired:
            proc.kill()
            stdout, stderr = proc.communicate()
            return LLMInvocation(
                stdout=stdout or "", stderr=stderr or "", returncode=-1, timed_out=True
            )
        return LLMInvocation(stdout=stdout or "", stderr=stderr or "", returncode=proc.returncode)


# ── Response parsing (Code AC: strict-parse; invalid → escalate) ───────


class LLMResponseInvalid(ValueError):
    """The CLI / decision JSON could not be strict-parsed."""


@dataclass(frozen=True)
class CliEnvelope:
    """The claude CLI ``--output-format json`` envelope, reduced.

    ``result_text`` is the assistant's reply (which must itself be the strict
    decision JSON); ``cost_usd`` / token counts come from the CLI's own
    accounting so cost telemetry (Exercised AC) reflects real spend.
    """

    result_text: str
    cost_usd: float
    input_tokens: int
    output_tokens: int
    is_error: bool = False


def parse_cli_envelope(stdout: str) -> CliEnvelope:
    """Parse the CLI JSON envelope; raise :class:`LLMResponseInvalid` if absent.

    A bare (non-JSON) stdout is treated as the raw result text with unknown
    cost — so a CLI configured for plain output still flows to the decision
    parser rather than hard-failing here.
    """
    text = (stdout or "").strip()
    if not text:
        raise LLMResponseInvalid("empty CLI stdout")
    try:
        envelope = json.loads(text)
    except json.JSONDecodeError:
        # Not an envelope — hand the raw text on as the candidate decision.
        return CliEnvelope(result_text=text, cost_usd=0.0, input_tokens=0, output_tokens=0)
    if not isinstance(envelope, Mapping):
        raise LLMResponseInvalid("CLI envelope is not a JSON object")
    usage = envelope.get("usage") if isinstance(envelope.get("usage"), Mapping) else {}
    return CliEnvelope(
        result_text=str(envelope.get("result", "") or ""),
        cost_usd=float(envelope.get("total_cost_usd", 0.0) or 0.0),
        input_tokens=int(usage.get("input_tokens", 0) or 0),
        output_tokens=int(usage.get("output_tokens", 0) or 0),
        is_error=bool(envelope.get("is_error", False)),
    )


@dataclass(frozen=True)
class ParsedDecision:
    """A strict-parsed + action-validated Tier-2 decision (ADR §5.2 output)."""

    decision_rationale: str
    actions: tuple[Action, ...]
    dropped_actions: tuple[Mapping[str, Any], ...]
    confidence: str
    escalate_if_wrong: bool
    learning: str


def parse_decision_json(result_text: str) -> ParsedDecision:
    """Strict-parse the LLM decision JSON; invalid shape → ``LLMResponseInvalid``.

    Tolerates the model wrapping the JSON in a ```json fenced block but
    nothing looser — a response without a parseable top-level object raises,
    and the consultant turns that into an operator escalation (Code AC:
    "JSON response strict-parsed; invalid → escalate operator").
    """
    candidate = _strip_code_fence(result_text)
    try:
        payload = json.loads(candidate)
    except json.JSONDecodeError as exc:
        raise LLMResponseInvalid(f"decision is not valid JSON: {exc}") from exc
    if not isinstance(payload, Mapping):
        raise LLMResponseInvalid("decision JSON is not an object")
    raw_actions = payload.get("actions", [])
    if not isinstance(raw_actions, list):
        raise LLMResponseInvalid("decision 'actions' must be a list")
    actions, dropped = validate_actions(raw_actions)
    confidence = str(payload.get("confidence", "")).strip().lower()
    if confidence not in _VALID_CONFIDENCE:
        confidence = "low"  # unrecognised → treat conservatively, never crash
    return ParsedDecision(
        decision_rationale=str(payload.get("decision_rationale", "") or ""),
        actions=actions,
        dropped_actions=tuple(dropped),
        confidence=confidence,
        escalate_if_wrong=bool(payload.get("escalate_if_wrong", False)),
        learning=str(payload.get("learning", "") or ""),
    )


def _strip_code_fence(text: str) -> str:
    stripped = (text or "").strip()
    if stripped.startswith("```"):
        # Drop the opening fence (``` or ```json) and the closing fence.
        body = stripped.split("\n", 1)[1] if "\n" in stripped else ""
        if body.rstrip().endswith("```"):
            body = body.rstrip()[: -3]
        return body.strip()
    return stripped


def validate_actions(
    raw_actions: Sequence[Any],
) -> tuple[tuple[Action, ...], list[Mapping[str, Any]]]:
    """Map LLM action dicts to §6.1 :class:`Action`s; drop everything else.

    Code AC ("only ADR §6.1 allowed actions executed"): an action whose name
    is outside :data:`_LLM_ACTION_NAME_TO_KIND`, or that fails the
    :class:`Action` envelope's own §6.1 kind check, is dropped (not executed)
    and returned in the rejects list so the decision log records the attempt.
    A bare ``noop`` is dropped from the executable set (it carries no effect).
    """
    accepted: list[Action] = []
    dropped: list[Mapping[str, Any]] = []
    for raw in raw_actions:
        if not isinstance(raw, Mapping):
            dropped.append({"reason": "not_an_object", "raw": repr(raw)})
            continue
        name = str(raw.get("action", "")).strip().lower()
        kind = _LLM_ACTION_NAME_TO_KIND.get(name)
        if kind is None or kind not in ALLOWED_ACTION_KINDS:
            dropped.append({"reason": "unknown_action", "action": name})
            continue
        if kind == ACTION_NOOP:
            # A noop instruction is a no-action; nothing to execute.
            continue
        ticket = str(raw.get("ticket", "") or "")
        params = raw.get("params", {})
        if not isinstance(params, Mapping):
            params = {}
        try:
            accepted.append(Action(kind=kind, target=ticket, params=dict(params)))
        except ValueError as exc:
            dropped.append({"reason": f"invalid_action: {exc}", "action": name})
    return tuple(accepted), dropped


# ── Tier-2 consultant ──────────────────────────────────────────────────


@dataclass(frozen=True)
class Tier2Outcome:
    """What a single :meth:`Tier2Consultant.consult` produced.

    ``actions`` are the validated §6.1 actions to execute; ``llm_consultation``
    is the Appendix-C metadata block embedded in the decision-log line; and
    ``reason`` is the grep-able reason string. ``degraded`` flags the
    budget-cap path (Tier-1-only + operator alert).
    """

    actions: tuple[Action, ...]
    reason: str
    llm_consultation: Mapping[str, Any]
    degraded: bool = False
    learning: str = ""


class Tier2Consultant:
    """Assemble context → invoke claude CLI → parse → validate → DecisionResult.

    Collaborators are injected for deterministic tests; production defaults
    wire the real CLI runner + Cognee lesson recall + budget guard. The
    consultant never mutates JIRA itself — it only *produces* §6.1 actions for
    the daemon's action layer (shadow in the 29f-14 canary, real later), so
    Tier-2 output flows through the exact same action path as Tier-1
    (Integration AC).
    """

    engine_version: str = TIER2_ENGINE_VERSION

    def __init__(
        self,
        *,
        config: LLMConsultationConfig | None = None,
        runner: LLMRunner | None = None,
        budget: BudgetGuard | None = None,
        lessons_provider: LessonsProvider | None = None,
        operator_prefs_provider: OperatorPrefsProvider | None = None,
        recent_decisions_provider: RecentDecisionsProvider | None = None,
        clock: Clock = _utc_now,
    ) -> None:
        self._config = config or LLMConsultationConfig.from_env()
        self._runner = runner or CliLLMRunner(self._config)
        self._budget = budget if budget is not None else BudgetGuard(
            self._config.daily_budget_usd, clock=clock
        )
        self._lessons_provider = lessons_provider or _default_lessons_provider
        self._operator_prefs_provider = operator_prefs_provider or (lambda _ctx: {})
        self._recent_decisions_provider = recent_decisions_provider or (lambda _ctx: ())
        self._clock = clock

    @property
    def budget(self) -> BudgetGuard:
        return self._budget

    def consult(
        self,
        ctx: DecisionContext,
        *,
        behavior: PersonalityMode | None = None,
        trigger: str = "tier1_no_match",
    ) -> Tier2Outcome:
        """Run one Tier-2 consultation for ``ctx`` (or degrade on budget)."""
        # Budget pre-check (§5.3): refuse a consult that would breach the cap;
        # degrade to Tier-1-only + alert the operator instead of spending.
        if self._budget.exhausted or not self._budget.can_afford(
            self._config.per_decision_estimate_usd
        ):
            return self._degrade_for_budget(ctx)

        window = behavior.llm_lessons_window if behavior is not None else 5
        lessons = tuple(self._safe_recall(ctx, window))
        bundle = build_context_bundle(
            ctx,
            mode_behavior=behavior,
            lessons=lessons,
            operator_prefs=self._operator_prefs_provider(ctx),
            recent_decisions=self._recent_decisions_provider(ctx),
        )
        lesson_ids = [lesson.identifier for lesson in lessons]
        logger.info(
            "[coord-llm] Tier-2 consult trigger=%s mode=%s lessons=%d cited=[%s]",
            trigger,
            ctx.mode,
            len(lessons),
            ",".join(lesson_ids),
        )

        invocation = self._runner.invoke(bundle.render())
        if not invocation.ok:
            return self._escalate_invocation_failure(ctx, invocation, lesson_ids)

        try:
            envelope = parse_cli_envelope(invocation.stdout)
            parsed = parse_decision_json(envelope.result_text)
        except LLMResponseInvalid as exc:
            return self._escalate_parse_failure(ctx, invocation, lesson_ids, exc)

        cost = envelope.cost_usd or self._config.per_decision_estimate_usd
        self._budget.record(cost)
        return self._build_consulted_outcome(
            ctx, bundle, envelope, parsed, cost, lesson_ids, trigger
        )

    # ── outcome builders ──

    def _build_consulted_outcome(
        self,
        ctx: DecisionContext,
        bundle: ContextBundle,
        envelope: CliEnvelope,
        parsed: ParsedDecision,
        cost: float,
        lesson_ids: list[str],
        trigger: str,
    ) -> Tier2Outcome:
        actions = parsed.actions
        # A consult that returned zero executable actions but asked to escalate
        # (or simply produced nothing) still surfaces to the operator so a
        # Tier-2 tick is never silently a no-op when the model wanted action.
        if not actions and parsed.escalate_if_wrong:
            actions = (
                Action.escalate(
                    _focal_key(ctx),
                    reason=f"[tier2] low-confidence consult, no safe action: "
                    f"{parsed.decision_rationale[:200]}",
                ),
            )
        meta = {
            "trigger": trigger,
            "context_size_tokens": envelope.input_tokens or _estimate_tokens(bundle.render()),
            "response_tokens": envelope.output_tokens,
            "cost_usd": round(cost, 6),
            "estimate_usd": round(self._config.per_decision_estimate_usd, 6),
            "confidence": parsed.confidence,
            "decision_rationale": parsed.decision_rationale,
            # 29f-11: the one-line lesson the learning loop writes back to
            # Cognee when this decision's 24h outcome-check succeeds (§10.1).
            "learning": parsed.learning,
            "lessons_cited": lesson_ids,
            "model": self._config.model,
            "budget_remaining_usd": round(self._budget.remaining_usd(), 6),
        }
        if parsed.dropped_actions:
            meta["dropped_actions"] = list(parsed.dropped_actions)
        return Tier2Outcome(
            actions=actions,
            reason=TIER2_CONSULTED_REASON,
            llm_consultation=meta,
            learning=parsed.learning,
        )

    def _degrade_for_budget(self, ctx: DecisionContext) -> Tier2Outcome:
        msg = (
            f"[coord-budget] daily Tier-2 LLM budget "
            f"${self._budget.cap_usd:.2f} exhausted "
            f"(spent ${self._budget.spent_usd():.2f}); degrading to Tier-1-only "
            f"until UTC day rollover."
        )
        logger.warning("[coord-llm] %s", msg)
        return Tier2Outcome(
            actions=(Action.mention_operator(_focal_key(ctx), message=msg, urgency="high"),),
            reason=TIER2_DEGRADED_BUDGET_REASON,
            llm_consultation={
                "degraded": "budget_cap",
                "daily_budget_usd": round(self._budget.cap_usd, 6),
                "spent_usd": round(self._budget.spent_usd(), 6),
                "cost_usd": 0.0,
            },
            degraded=True,
        )

    def _escalate_invocation_failure(
        self,
        ctx: DecisionContext,
        invocation: LLMInvocation,
        lesson_ids: list[str],
    ) -> Tier2Outcome:
        detail = "timeout" if invocation.timed_out else f"exit={invocation.returncode}"
        msg = f"[tier2] claude CLI consultation failed ({detail}); operator triage needed."
        logger.warning("[coord-llm] %s stderr=%s", msg, (invocation.stderr or "")[:300])
        return Tier2Outcome(
            actions=(Action.escalate(_focal_key(ctx), reason=msg),),
            reason=TIER2_INVOCATION_FAILED_REASON,
            llm_consultation={
                "error": detail,
                "cost_usd": 0.0,
                "lessons_cited": lesson_ids,
            },
        )

    def _escalate_parse_failure(
        self,
        ctx: DecisionContext,
        invocation: LLMInvocation,
        lesson_ids: list[str],
        exc: Exception,
    ) -> Tier2Outcome:
        msg = f"[tier2] LLM response was not valid decision JSON ({exc}); operator triage needed."
        logger.warning("[coord-llm] %s", msg)
        return Tier2Outcome(
            actions=(Action.escalate(_focal_key(ctx), reason=msg),),
            reason=TIER2_PARSE_FAILED_REASON,
            llm_consultation={
                "error": "parse_failed",
                "detail": str(exc),
                "cost_usd": 0.0,
                "lessons_cited": lesson_ids,

exec
/bin/bash -lc "sed -n '300,345p' backend/agents/jira_dispatch.py && sed -n '1640,1680p' backend/agents/jira_dispatch.py && sed -n '1930,2000p' backend/agents/jira_dispatch.py" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
    headers = {
        "Authorization": auth_header,
        "Content-Type": "application/json",
        "Accept": "application/json",
    }
    if idem_key:
        headers["X-Atlassian-Token"] = "no-check"
        headers["X-Idempotency-Key"] = idem_key
    req = urllib.request.Request(
        url, data=data, method=method,
        headers=headers,
    )
    try:
        with BREAKERS["jira_rest"].call(urllib.request.urlopen, req, timeout=30) as resp:
            payload = resp.read().decode()
            return json.loads(payload) if payload else {}
    except urllib.error.HTTPError as e:
        body_text = e.read().decode() if e.fp else ""
        raise RuntimeError(f"{method} {url} → {e.code}: {body_text}") from e


def _request(
    client: DispatchClient,
    method: str,
    path: str,
    body: dict | None = None,
    idem_key: str | None = None,
) -> dict:
    return _request_raw(method, client.base_url + path, client.auth_header, body, idem_key)


def _request_idempotent(
    client: DispatchClient,
    method: str,
    path: str,
    body: dict | None,
    idem_key: str,
) -> dict:
    return DEFAULT_STORE.run(idem_key, lambda: _request(client, method, path, body, idem_key))


# ── ADF helpers ───────────────────────────────────────────────────


def _adf_paragraph(text: str) -> dict:
    return {
        material = json.dumps(
            {"target_status": "under-review", "patchset": None},
            sort_keys=True,
            separators=(",", ":"),
        )
        args_hash = hashlib.sha256(material.encode()).hexdigest()[:12]
    return f"{key}:{operation_type}:{args_hash}"


def transition_to_under_review_if_needed(
    client: "DispatchClient",
    key: str,
    idem_key: str | None = None,
    change_number: int | None = None,
) -> bool:
    """In Progress → Under Review, but only if not already there.

    Returns True if a transition POST was issued, False if the ticket was
    already in Under Review and the call was a no-op. Raises RuntimeError
    on any non-4xx HTTP failure of the transition POST itself; the runner
    catches and downgrades to a skip-comment per OP-691 AC.
    """
    if get_issue_status(client, key) == UNDER_REVIEW_STATUS_NAME:
        return False
    idem_key = idem_key or (
        _under_review_idem_key(key, change_number=change_number)
        if change_number is not None
        else f"transition-{key}-under-review-{uuid.uuid4().hex[:12]}"
    )
    _request_idempotent(
        client, "POST", f"/issue/{key}/transitions",
        {"transition": {"id": TRANSITION_IDS["to_under_review"]}},
        idem_key,
    )
    return True


def force_walk_to_published(
    client: "DispatchClient",
    key: str,
    idem_key: str | None = None,
        idem_key=f"{base_key}-comment",
    )


def transition_back_to_todo(
    client: DispatchClient,
    key: str,
    reason: str,
    idem_key: str | None = None,
    *,
    failure_class: "str | None" = None,
    raw_traceback: str = "",
    mutex_label: str | None = None,
    area: str | None = None,
) -> None:
    """In Progress → TODO with reason comment + clear assignee.

    OP-1541 invariant: this is a destructive recovery — it clears the
    assignee. Under a shared JIRA account ONLY the current winning
    fencing-token claim holder may revert. Callers on the TOCTOU /
    assignee-divergence path MUST first prove claim ownership (see
    :func:`is_winning_claim_owner` and ``auto-runner-jira._handle_toctou_abort``)
    so a sibling instance's assignee clear does not cascade into a false
    revert (OP-1533 burst → stoploss). The non-recovery callers (CLI
    failure, capability gate, discovered dependency, pre-claim cwd-unsafe)
    are unaffected — they own or predate the claim.

    OP-854 (C2): when ``failure_class`` is supplied, an incident row
    lands in ``runner_incidents`` before the transition fires so the
    C8 failure-graph and the C2 recall path can pick it up next time
    the ticket is attempted. Recording is best-effort — a recorder
    fault never blocks the JIRA transition (the operator still needs
    the ticket reverted even if the audit shim is wedged).
    """
    base_key = idem_key or f"transition-{key}-back-to-todo-{uuid.uuid4().hex[:12]}"
    if failure_class is not None:
        try:
            # Local import keeps jira_dispatch importable without the
            # incident_recorder + failure_class chain (e.g. early-boot
            # JQL-only smoke tests).
            from backend.agents.incident_recorder import record_runner_incident

            record_runner_incident(
                ticket_key=key,
                failure_class=failure_class,
                summary=reason.splitlines()[0] if reason else "",
                raw_traceback=raw_traceback,
                runner_class=getattr(client, "agent_class", "unknown"),
                mutex_label=mutex_label,
                area=area,
            )
        except Exception as exc:  # noqa: BLE001 — best-effort wiring
            log.warning(
                "jira_dispatch.record_runner_incident_failed key=%s err=%s",
                key,
                exc,
            )
    add_comment(
        client, key, f"Reverting to TODO. Reason:\n{reason}",
        idem_key=f"{base_key}-comment",
    )
    clear_assignee(client, key, idem_key=f"{base_key}-clear-assignee")
    _request_idempotent(
        client, "POST", f"/issue/{key}/transitions",
        {"transition": {"id": TRANSITION_IDS["back_to_todo"]}},
        f"{base_key}-transition",
    )

    # OP-1140: stoploss circuit-breaker recording — best-effort.
    # A recorder fault (network blip, label-fetch 404 on a freshly-created
    # ticket, ...) must NEVER prevent the §11-revert from completing; the

exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator.py | sed -n '260,305p;576,612p;735,812p;1137,1150p;1413,1710p;1862,1945p'" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator_rules.py | sed -n '55,82p;177,212p;618,682p;696,728p;738,752p;818,852p'" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator_llm_consultation.py | sed -n '116,126p;650,690p;830,915p'" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "nl -ba backend/agents/jira_dispatch.py | sed -n '300,335p;1649,1675p;1934,1996p;2014,2048p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 105ms:
    55	# The *closed* set of action kinds a rule (or the Tier-2 LLM) may emit.
    56	# §6.1 deliberately keeps this list closed for safety; new kinds are added
    57	# by reviewable operator patch (§12 open-question 6), never by an LLM
    58	# proposal. Anything outside this set is dropped by the action layer.
    59	ACTION_NOOP = "noop"
    60	ACTION_FILE_TICKET = "file_ticket"
    61	ACTION_RELABEL = "relabel"
    62	ACTION_TRANSITION = "transition"
    63	ACTION_MENTION_OPERATOR = "mention_operator"
    64	ACTION_MARK_FOR_FOLLOWUP = "mark_for_followup"
    65	ACTION_ESCALATE = "escalate"
    66	
    67	ALLOWED_ACTION_KINDS: frozenset[str] = frozenset(
    68	    {
    69	        ACTION_NOOP,
    70	        ACTION_FILE_TICKET,
    71	        ACTION_RELABEL,
    72	        ACTION_TRANSITION,
    73	        ACTION_MENTION_OPERATOR,
    74	        ACTION_MARK_FOR_FOLLOWUP,
    75	        ACTION_ESCALATE,
    76	    }
    77	)
    78	
    79	
    80	@dataclass(frozen=True)
    81	class Action:
    82	    """A single thing the coordinator decides to do.
   177	    def mention_operator(
   178	        cls,
   179	        ticket: str,
   180	        *,
   181	        message: str,
   182	        urgency: str = "medium",
   183	        dry_run: bool = True,
   184	    ) -> "Action":
   185	        """§6.1 mention_operator — @-mention operator; high urgency tags too."""
   186	        return cls(
   187	            kind=ACTION_MENTION_OPERATOR,
   188	            target=ticket,
   189	            params={"message": message, "urgency": urgency},
   190	            dry_run=dry_run,
   191	        )
   192	
   193	    @classmethod
   194	    def mark_for_followup(
   195	        cls, ticket: str, *, when: str, why: str, dry_run: bool = True
   196	    ) -> "Action":
   197	        """§6.1 mark_for_followup — defer attention via coord-resume-after."""
   198	        return cls(
   199	            kind=ACTION_MARK_FOR_FOLLOWUP,
   200	            target=ticket,
   201	            params={"when": when, "why": why},
   202	            dry_run=dry_run,
   203	        )
   204	
   205	    @classmethod
   206	    def escalate(cls, ticket: str, *, reason: str, dry_run: bool = True) -> "Action":
   207	        """§6.1 escalate — needs-operator-action label + reason comment."""
   208	        return cls(
   209	            kind=ACTION_ESCALATE,
   210	            target=ticket,
   211	            params={"reason": reason},
   212	            dry_run=dry_run,
   618	    """§5.1 #8 — A blocks B blocks A (or longer cycle): diagnose + escalate.
   619	
   620	    Walks the blockedBy DAG from the focal ticket over ``ctx.tickets``. If
   621	    the focal key is reachable from itself, that's a cycle — file a
   622	    diagnostic + @-mention operator (this should never happen; if it does,
   623	    halt rather than guess). Abstains when the graph is acyclic / unknown.
   624	    """
   625	    t = ctx.ticket
   626	    if t is None:
   627	        return None
   628	    # DFS over blockedBy edges looking for a path back to the focal key.
   629	    start = t.key
   630	    stack = list(t.blocked_by)
   631	    visited: set[str] = set()
   632	    while stack:
   633	        cur = stack.pop()
   634	        if cur == start:
   635	            return Action.mention_operator(
   636	                start,
   637	                message=(
   638	                    f"[chain-deadlock] blockedBy cycle detected through {start}; "
   639	                    f"halting automated routing — operator triage required."
   640	                ),
   641	                urgency="high",
   642	            )
   643	        if cur in visited:
   644	            continue
   645	        visited.add(cur)
   646	        nxt = ctx.tickets.get(cur)
   647	        if nxt is not None:
   648	            stack.extend(nxt.blocked_by)
   649	    return None
   650	
   651	
   652	@rule(name="revert-loop-quarantine", priority=10)
   653	def revert_loop_quarantine(ctx: DecisionContext) -> "Action | None":
   654	    """§5.1 #5 — reverted > 5× in 24h: add ``coord-quarantine`` + @operator."""
   655	    t = ctx.ticket
   656	    if t is None:
   657	        return None
   658	    if t.revert_count_24h <= REVERT_LOOP_QUARANTINE_THRESHOLD:
   659	        return None
   660	    if has_label(t, COORD_QUARANTINE_LABEL):
   661	        return None  # already quarantined — don't re-fire each tick
   662	    return Action.relabel(
   663	        t.key,
   664	        add=(COORD_QUARANTINE_LABEL, NEEDS_OPERATOR_ACTION_LABEL),
   665	    )
   666	
   667	
   668	@rule(name="merger-repeat-fail", priority=20)
   669	def merger_repeat_fail(ctx: DecisionContext) -> "Action | None":
   670	    """§5.1 #4 — merger failed > 3× on one ticket: escalate with context."""
   671	    t = ctx.ticket
   672	    if t is None:
   673	        return None
   674	    if t.merger_fail_count <= MERGER_FAIL_ESCALATE_THRESHOLD:
   675	        return None
   676	    if has_label(t, NEEDS_OPERATOR_ACTION_LABEL):
   677	        return None  # already escalated
   678	    return Action.escalate(
   679	        t.key,
   680	        reason=(
   681	            f"[merger-repeat-fail] merger failed {t.merger_fail_count} times on "
   682	            f"{t.key}; conflict is not auto-resolvable — operator merge needed."
   696	    t = ctx.ticket
   697	    if t is None or t.status not in IN_PROGRESS_STATUS_NAMES:
   698	        return None
   699	    if t.has_live_runner:
   700	        return None
   701	    age = _age_seconds(ctx.now, t.in_progress_since)
   702	    if age is None or age <= _stale_threshold_seconds():
   703	        return None
   704	    return Action.transition(t.key, to_status="To Do")
   705	
   706	
   707	@rule(name="stale-claim-cleanup", priority=40)
   708	def stale_claim_cleanup(ctx: DecisionContext) -> "Action | None":
   709	    """§5.1 #2 — ``claim:default:*`` > 2× CLI timeout, no live runner: clear.
   710	
   711	    Removes the orphaned claim label(s) (ADR §3.3 Startup-3 stale-state sweep
   712	    for a single ticket). Assignee clearing rides along in the action layer's
   713	    relabel handler. Abstains while a runner is alive or the claim is fresh.
   714	    """
   715	    t = ctx.ticket
   716	    if t is None or t.has_live_runner:
   717	        return None
   718	    claim_labels = labels_with_prefix(t, CLAIM_LABEL_PREFIX)
   719	    if not claim_labels:
   720	        return None
   721	    age = _age_seconds(ctx.now, t.claim_started_at)
   722	    if age is None or age <= _stale_threshold_seconds():
   723	        return None
   724	    return Action.relabel(t.key, remove=claim_labels)
   725	
   726	
   727	@rule(name="runner-blocked-marker-stale", priority=50)
   728	def runner_blocked_marker_stale(ctx: DecisionContext) -> "Action | None":
   738	        return None
   739	    waiting = labels_with_prefix(t, DEPENDENCY_WAITING_LABEL_PREFIX)
   740	    stale: list[str] = []
   741	    for label in waiting:
   742	        blocker_key = label[len(DEPENDENCY_WAITING_LABEL_PREFIX):]
   743	        blocker = ctx.tickets.get(blocker_key)
   744	        if blocker is not None and blocker.status in PUBLISHED_STATUS_NAMES:
   745	            stale.append(label)
   746	    if not stale:
   747	        return None
   748	    return Action.relabel(t.key, remove=tuple(stale))
   749	
   750	
   751	@rule(name="dependency-out-of-area", priority=60)
   752	def dependency_out_of_area(ctx: DecisionContext) -> "Action | None":
   818	        return None
   819	    comment = latest_comment(t)
   820	    if comment is None or CAPABILITY_BLOCKED_MARKER not in comment.text:
   821	        return None
   822	    if has_label(t, CAPABILITY_ENABLE_LABEL):
   823	        return None  # workaround already applied
   824	    return Action.relabel(t.key, add=(CAPABILITY_ENABLE_LABEL,))
   825	
   826	
   827	@rule(name="wrong-class-routing", priority=80)
   828	def wrong_class_routing(ctx: DecisionContext) -> "Action | None":
   829	    """§5.1 #9 — backend ticket on subscription-claude while codex idle.
   830	
   831	    Comment-only suggestion (ADR is explicit: "don't relabel without LLM
   832	    consult"). Fires when the focal ticket is strongly area:backend, currently
   833	    ``class:subscription-claude``, and the capacity snapshot shows a codex
   834	    runner with free slots. Emits a low-urgency operator note, never a
   835	    relabel.
   836	    """
   837	    t = ctx.ticket
   838	    if t is None:
   839	        return None
   840	    if "backend" not in {a.lower() for a in t.areas}:
   841	        return None
   842	    if ticket_class(t) != f"{CLASS_LABEL_PREFIX}subscription-claude":
   843	        return None
   844	    if not _codex_has_capacity(ctx.capacity):
   845	        return None
   846	    return Action.mention_operator(
   847	        t.key,
   848	        message=(
   849	            f"[wrong-class-routing] {t.key} is area:backend on "
   850	            f"class:subscription-claude while a codex runner is idle; "
   851	            f"consider rerouting to subscription-codex (suggestion only)."
   852	        ),

 succeeded in 119ms:
   260	        return None
   261	
   262	
   263	# ── Action layer (ADR-0021 §6) — shadow stub for 29f-3 ────────────────
   264	
   265	
   266	class ShadowActionExecutor:
   267	    """Records actions instead of executing them (ADR §6 / shadow mode).
   268	
   269	    The real §6 action layer (JIRA mutate / @-operator) lands in a later
   270	    phase; until then — and during the 29f-14 7-day shadow canary — the
   271	    coordinator must *observe* what a rule would do without touching JIRA.
   272	    This executor returns a per-action result record (``executed=False``)
   273	    that the daemon folds into the decision-log line, so the integration AC
   274	    ("rule returns Action → action layer executes it (mock if not yet built)
   275	    → action recorded in decision log") is satisfied without any mutation.
   276	
   277	    A real executor is a drop-in: same ``execute`` signature, returning
   278	    ``executed=True`` plus the mutation outcome.
   279	    """
   280	
   281	    #: Stamped into each result so a decision-log reader can tell shadow
   282	    #: (observed-only) executions from real ones.
   283	    executed: bool = False
   284	
   285	    def execute(self, action: Action, ctx: DecisionContext) -> dict[str, Any]:
   286	        return {
   287	            "kind": action.kind,
   288	            "target": action.target,
   289	            "executed": self.executed,
   290	            "shadow": True,
   291	        }
   292	
   293	
   294	# Action executor seam: anything with ``execute(action, ctx) -> dict``.
   295	ActionExecutor = Callable[[Action, DecisionContext], dict]
   296	
   297	
   298	# ── Event sources (ADR-0021 §4) ──────────────────────────────────────
   299	
   300	
   301	class EventDeduper:
   302	    """Five-minute coalescing window for identical source events."""
   303	
   304	    def __init__(
   305	        self,
   576	class ColdStartGateway(Protocol):
   577	    """The infra / JIRA / git seam the 4-phase recovery drives (ADR §3.3).
   578	
   579	    Every method is best-effort in production: an adapter that cannot reach
   580	    its backend returns an empty / conservative answer rather than raising,
   581	    so a degraded environment can never wedge the daemon before its loop.
   582	    """
   583	
   584	    # ── Startup-1 (infra) ──
   585	    def audit_infra(self) -> InfraAuditResult: ...
   586	    def start_unit(self, unit: str) -> bool: ...
   587	
   588	    # ── Startup-2 (JIRA reconcile) ──
   589	    def interrupted_tickets(self) -> list[InterruptedTicket]: ...
   590	    def has_live_runner(self, key: str) -> bool: ...
   591	    def gerrit_change_mergeable(self, key: str) -> bool: ...
   592	    def branch_has_commits(self, key: str) -> bool: ...
   593	    def mark_resumable(self, key: str) -> None: ...
   594	    def transition_under_review(self, key: str) -> None: ...
   595	    def reset_to_todo(self, key: str) -> None: ...
   596	
   597	    # ── Startup-3 (stale sweep) ──
   598	    def stale_sweep_plan(self) -> StaleSweepPlan: ...
   599	    def remove_label(self, key: str, label: str) -> None: ...
   600	    def clear_assignee(self, key: str) -> None: ...
   601	
   602	    # ── shared (Startup-1 halt / ambiguous Startup-2 (d)) ──
   603	    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None: ...
   604	
   605	
   606	class JiraDispatchColdStartGateway:
   607	    """Production :class:`ColdStartGateway` over ``jira_dispatch`` + git + shell.
   608	
   609	    Heavy collaborators (``jira_dispatch.make_client``, the Gerrit SSH path)
   610	    are imported + constructed lazily and wrapped so a failure degrades to a
   611	    safe default — the daemon must reach its loop even on a half-up host.
   612	    The agent class used for the JIRA/Gerrit identity defaults to the
   735	    def mark_resumable(self, key: str) -> None:
   736	        self._add_label(key, RESUME_FROM_FEATURE_LABEL)
   737	
   738	    def transition_under_review(self, key: str) -> None:
   739	        client = self._jira()
   740	        if client is None:
   741	            return
   742	        try:
   743	            from backend.agents import jira_dispatch
   744	
   745	            # Idempotent per OP-691: skips when already Under Review.
   746	            jira_dispatch.transition_to_under_review_if_needed(client, key)
   747	        except Exception as exc:  # noqa: BLE001
   748	            logger.warning("[pipeline_coordinator] transition_under_review(%s) failed: %s", key, exc)
   749	
   750	    def reset_to_todo(self, key: str) -> None:
   751	        client = self._jira()
   752	        if client is None:
   753	            return
   754	        try:
   755	            from backend.agents import jira_dispatch
   756	
   757	            jira_dispatch.transition_back_to_todo(
   758	                client, key,
   759	                reason="[cold-start Startup-2(c)] interrupted with no commits/activity; "
   760	                       "reverting to To Do for clean re-pickup.",
   761	            )
   762	        except Exception as exc:  # noqa: BLE001
   763	            logger.warning("[pipeline_coordinator] reset_to_todo(%s) failed: %s", key, exc)
   764	
   765	    # ── Startup-3 ──
   766	    def stale_sweep_plan(self) -> StaleSweepPlan:
   767	        client = self._jira()
   768	        if client is None:
   769	            return StaleSweepPlan()
   770	        try:
   771	            return _build_stale_sweep_plan(client, repo_root=self._repo_root)
   772	        except Exception as exc:  # noqa: BLE001
   773	            logger.warning("[pipeline_coordinator] stale-sweep plan failed: %s", exc)
   774	            return StaleSweepPlan()
   775	
   776	    def remove_label(self, key: str, label: str) -> None:
   777	        client = self._jira()
   778	        if client is None:
   779	            return
   780	        try:
   781	            from backend.agents import jira_dispatch
   782	
   783	            jira_dispatch.remove_label(client, key, label)
   784	        except Exception as exc:  # noqa: BLE001
   785	            logger.warning("[pipeline_coordinator] remove_label(%s, %s) failed: %s", key, label, exc)
   786	
   787	    def clear_assignee(self, key: str) -> None:
   788	        client = self._jira()
   789	        if client is None:
   790	            return
   791	        try:
   792	            from backend.agents import jira_dispatch
   793	
   794	            jira_dispatch.clear_assignee(client, key)
   795	        except Exception as exc:  # noqa: BLE001
   796	            logger.warning("[pipeline_coordinator] clear_assignee(%s) failed: %s", key, exc)
   797	
   798	    # ── shared ──
   799	    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None:
   800	        client = self._jira()
   801	        if client is None:
   802	            logger.error("[pipeline_coordinator] OPERATOR-ALERT (JIRA down) %s: %s", key, message)
   803	            return
   804	        try:
   805	            from backend.agents import jira_dispatch
   806	
   807	            jira_dispatch.add_comment(client, key, f"@nanakusa-sora {message}")
   808	            if urgency == "high":
   809	                jira_dispatch.add_label(client, key, "needs-operator-action")
   810	        except Exception as exc:  # noqa: BLE001
   811	            logger.warning("[pipeline_coordinator] mention_operator(%s) failed: %s", key, exc)
   812	
  1137	def _default_cold_start_gateway(
  1138	    config: CoordinatorConfig, *, acting: bool = False
  1139	) -> ColdStartGateway:
  1140	    """Production gateway wiring (no I/O at construction — lazy + fail-open).
  1141	
  1142	    ``acting=False`` (the default — the 29f-14 7-day shadow canary, OP-1555)
  1143	    wraps the live gateway in :class:`ShadowColdStartGateway` so cold-start
  1144	    observes the world but performs zero JIRA/git/systemctl mutation;
  1145	    ``acting=True`` returns the live gateway so the 4-phase recovery mutates as
  1146	    designed. The same single ``acting`` switch gates the engine action layer
  1147	    and the sprint re-plan handler (see :func:`build_default_coordinator`).
  1148	    """
  1149	    live = JiraDispatchColdStartGateway()
  1150	    return live if acting else ShadowColdStartGateway(live)
  1413	        Order: Startup-1 (infra) → L6 crash-recovery (ADR §9 L6: "after Phase
  1414	        Startup-1") → Startup-2 (JIRA reconcile) → Startup-3 (stale sweep) →
  1415	        Startup-4 (log steady-state entry). If Startup-1 cannot bring the
  1416	        expected infra up, the daemon halts startup (``phase1_halted=True``)
  1417	        and does NOT proceed — entering the loop with degraded infra is worse
  1418	        than not running (ADR §3.3 Startup-1 failure path). :meth:`run` reads
  1419	        the report to decide whether to enter the loop.
  1420	        """
  1421	        report = ColdStartReport()
  1422	        self._log_cold_start({"event": COLD_START_BEGAN_EVENT})
  1423	        self._startup_1_infra(report)
  1424	        if report.phase1_halted:
  1425	            self._log_cold_start(
  1426	                {"event": COLD_START_COMPLETE_EVENT, "halted_at": "startup-1",
  1427	                 "still_down": report.still_down_units}
  1428	            )
  1429	            return report
  1430	        self._l6_crash_recovery(report)
  1431	        self._startup_2_reconcile(report)
  1432	        self._startup_3_sweep(report)
  1433	        self._startup_4_enter_loop(report)
  1434	        self._log_cold_start(
  1435	            {
  1436	                "event": COLD_START_COMPLETE_EVENT,
  1437	                "started_units": report.started_units,
  1438	                "crash_recoveries": len(report.crash_recoveries),
  1439	                "reconciled": len(report.reconciled),
  1440	                "swept": len(report.swept),
  1441	            }
  1442	        )
  1443	        return report
  1444	
  1445	    def _startup_1_infra(self, report: ColdStartReport) -> None:
  1446	        """Startup-1: verify infra; start any down unit; halt if still down."""
  1447	        gw = self._cold_start_gateway
  1448	        audit = gw.audit_infra()
  1449	        for unit in audit.down:
  1450	            ok = gw.start_unit(unit)
  1451	            (report.started_units if ok else report.still_down_units).append(unit)
  1452	            self._log_cold_start(
  1453	                {"event": STARTUP_PHASE_EVENT, "phase": 1, "unit": unit,
  1454	                 "action": "start_unit", "ok": ok}
  1455	            )
  1456	        # Re-verify after starts: a unit that refused to start (or died again)
  1457	        # leaves the infra degraded → @-operator + halt (do not enter loop).
  1458	        if report.still_down_units:
  1459	            recheck = gw.audit_infra()
  1460	            report.still_down_units = list(recheck.down)
  1461	        if report.still_down_units:
  1462	            report.phase1_halted = True
  1463	            gw.mention_operator(
  1464	                "OP",  # infra-level alert is not ticket-scoped
  1465	                "[cold-start Startup-1] coordinator could not bring up "
  1466	                f"{report.still_down_units}; halting startup rather than "
  1467	                "entering the loop with degraded infra (ADR-0021 §3.3).",
  1468	                urgency="high",
  1469	            )
  1470	            self._log_cold_start(
  1471	                {"event": STARTUP_PHASE_EVENT, "phase": 1, "action": "halt",
  1472	                 "still_down": report.still_down_units}
  1473	            )
  1474	
  1475	    def _l6_crash_recovery(self, report: ColdStartReport) -> None:
  1476	        """L6 (ADR §9): replay last 24h decision log; resume/complete/rollback.
  1477	
  1478	        A ``shutdown_began`` with no matching ``shutdown_complete`` (paired by
  1479	        ``decision_id``) means the daemon crashed mid-drain. For each such
  1480	        crash we look at the immediately-preceding tick: if it executed a real
  1481	        (non-dry-run) action that was never confirmed complete, we resolve it
  1482	        per action kind — idempotent relabel/transition are *completed*
  1483	        (re-issued safely), file_ticket is effectively irreversible so we
  1484	        *escalate*, everything else is a logged no-op. Every resolution writes
  1485	        a ``crash_recovery_applied`` record (ADR §9 L6).
  1486	        """
  1487	        records = self._read_recent_decision_log()
  1488	        began: dict[str, dict[str, Any]] = {}
  1489	        completed: set[str] = set()
  1490	        last_real_action_tick: dict[str, Any] | None = None
  1491	        for rec in records:
  1492	            event = rec.get("event")
  1493	            if event == "decision_tick" and not rec.get("dry_run", True) and rec.get("actions"):
  1494	                last_real_action_tick = rec
  1495	            elif event == "shutdown_began":
  1496	                began[str(rec.get("decision_id", ""))] = rec
  1497	                # Snapshot which real-action tick preceded this drain.
  1498	                rec["_preceding_real_tick"] = last_real_action_tick
  1499	            elif event == "shutdown_complete":
  1500	                completed.add(str(rec.get("decision_id", "")))
  1501	        for drain_id, drain_rec in began.items():
  1502	            if drain_id in completed:
  1503	                continue  # clean shutdown — nothing to recover
  1504	            tick = drain_rec.get("_preceding_real_tick")
  1505	            resolutions = self._resolve_interrupted_actions(tick)
  1506	            entry = {
  1507	                "event": CRASH_RECOVERY_EVENT,
  1508	                "crashed_drain_id": drain_id,
  1509	                "resolutions": resolutions,
  1510	            }
  1511	            report.crash_recoveries.append(entry)
  1512	            self._log_cold_start(entry)
  1513	
  1514	    def _resolve_interrupted_actions(self, tick: dict[str, Any] | None) -> list[dict[str, Any]]:
  1515	        """Map each interrupted action of a crashed tick to a recovery step."""
  1516	        if not tick:
  1517	            return [{"resolution": "noop", "reason": "no in-flight real action before crash"}]
  1518	        gw = self._cold_start_gateway
  1519	        out: list[dict[str, Any]] = []
  1520	        for action in tick.get("actions", []):
  1521	            kind = action.get("kind")
  1522	            target = action.get("target", "")
  1523	            if kind in (ACTION_RELABEL, ACTION_TRANSITION):
  1524	                # Idempotent → safe to complete (re-issue) on recovery.
  1525	                if kind == ACTION_TRANSITION and action.get("params", {}).get(
  1526	                    "to_status"
  1527	                ) == "Under Review":
  1528	                    gw.transition_under_review(target)
  1529	                resolution = "completed"
  1530	            elif kind == ACTION_FILE_TICKET:
  1531	                # Not safely reversible/repeatable — surface to operator.
  1532	                gw.mention_operator(
  1533	                    target,
  1534	                    f"[cold-start L6] crash interrupted a file_ticket action on {target}; "
  1535	                    "verify the dependency ticket exists and was not double-filed.",
  1536	                    urgency="medium",
  1537	                )
  1538	                resolution = "escalated"
  1539	            else:
  1540	                resolution = "noop"
  1541	            out.append({"kind": kind, "target": target, "resolution": resolution})
  1542	        return out
  1543	
  1544	    def _read_recent_decision_log(self) -> list[dict[str, Any]]:
  1545	        """Return decision-log records from the last :data:`L6_REPLAY_WINDOW_HOURS`.
  1546	
  1547	        Reads the day-partitioned JSONL files spanning the window (today + the
  1548	        days it reaches back into), in chronological order, skipping malformed
  1549	        lines so a single torn record never aborts recovery.
  1550	        """
  1551	        now = self._clock()
  1552	        window_start = now - timedelta(hours=L6_REPLAY_WINDOW_HOURS)
  1553	        records: list[dict[str, Any]] = []
  1554	        day = window_start.date()
  1555	        seen_paths: list[Path] = []
  1556	        while day <= now.date():
  1557	            path = self._decision_log.directory / f"{day.isoformat()}.jsonl"
  1558	            if path.exists():
  1559	                seen_paths.append(path)
  1560	            day = day + timedelta(days=1)
  1561	        for path in seen_paths:
  1562	            try:
  1563	                lines = path.read_text(encoding="utf-8").splitlines()
  1564	            except OSError:
  1565	                continue
  1566	            for line in lines:
  1567	                if not line.strip():
  1568	                    continue
  1569	                try:
  1570	                    rec = json.loads(line)
  1571	                except json.JSONDecodeError:
  1572	                    continue
  1573	                if isinstance(rec, dict):
  1574	                    ts = record_ts(rec)
  1575	                    if ts is None or ts >= window_start:
  1576	                        records.append(rec)
  1577	        return records
  1578	
  1579	    def _startup_2_reconcile(self, report: ColdStartReport) -> None:
  1580	        """Startup-2: reconcile each interrupted (進行中) ticket (ADR §3.3).
  1581	
  1582	        Decision tree per ticket with no live runner:
  1583	          (b) Gerrit change exists + mergeable      → transition Under Review
  1584	          (a) feature branch has commits             → mark resumable, leave
  1585	          (c) no commits + stale (>2× CLI timeout)   → revert To Do (clear claim)
  1586	          (d) no commits + not yet stale (ambiguous) → @-operator, leave alone
  1587	        A ticket whose runner is still alive is left untouched (it's handling
  1588	        it). (b) is checked before (a): a mergeable change means the work is
  1589	        *finished* and only missed its transition, which beats re-running it.
  1590	        (c) vs (d) hinges on staleness so a runner that only just started (and
  1591	        hasn't committed yet) is never aggressively reverted out from under.
  1592	        """
  1593	        gw = self._cold_start_gateway
  1594	        for ticket in gw.interrupted_tickets():
  1595	            key = ticket.key
  1596	            if gw.has_live_runner(key):
  1597	                self._record_reconcile(report, key, "live-runner", "leave")
  1598	            elif gw.gerrit_change_mergeable(key):
  1599	                gw.transition_under_review(key)
  1600	                self._record_reconcile(report, key, "gerrit-mergeable", "under_review")
  1601	            elif gw.branch_has_commits(key):
  1602	                gw.mark_resumable(key)
  1603	                self._record_reconcile(report, key, "branch-has-commits", "resume")
  1604	            elif ticket.stale:
  1605	                # No commits, no live runner, long-stale → abandoned work.
  1606	                gw.reset_to_todo(key)
  1607	                self._record_reconcile(report, key, "no-commits-stale", "revert_todo")
  1608	            else:
  1609	                # No commits but recent — a runner may have only just started.
  1610	                gw.mention_operator(
  1611	                    key,
  1612	                    "[cold-start Startup-2(d)] interrupted with no commits but "
  1613	                    "recent activity — ambiguous; leaving in place for operator review.",
  1614	                    urgency="medium",
  1615	                )
  1616	                self._record_reconcile(report, key, "ambiguous", "operator")
  1617	
  1618	    def _record_reconcile(
  1619	        self, report: ColdStartReport, key: str, classification: str, action: str
  1620	    ) -> None:
  1621	        entry = {"event": STARTUP_PHASE_EVENT, "phase": 2, "ticket": key,
  1622	                 "classification": classification, "action": action}
  1623	        report.reconciled.append({"ticket": key, "classification": classification, "action": action})
  1624	        self._log_cold_start(entry)
  1625	
  1626	    def _startup_3_sweep(self, report: ColdStartReport) -> None:
  1627	        """Startup-3: drop orphaned claim:* / dead waiting-* + clear assignees."""
  1628	        gw = self._cold_start_gateway
  1629	        plan = gw.stale_sweep_plan()
  1630	        for key, labels in plan.stale_claims.items():
  1631	            for label in labels:
  1632	                gw.remove_label(key, label)
  1633	            gw.clear_assignee(key)
  1634	            self._record_sweep(report, key, "stale-claim", list(labels))
  1635	        for key in plan.orphan_assignees:
  1636	            gw.clear_assignee(key)
  1637	            self._record_sweep(report, key, "orphan-assignee", [])
  1638	        for key, labels in plan.resolved_waiting.items():
  1639	            for label in labels:
  1640	                gw.remove_label(key, label)
  1641	            self._record_sweep(report, key, "resolved-waiting", list(labels))
  1642	
  1643	    def _record_sweep(
  1644	        self, report: ColdStartReport, key: str, kind: str, labels: list[str]
  1645	    ) -> None:
  1646	        entry = {"event": STARTUP_PHASE_EVENT, "phase": 3, "ticket": key,
  1647	                 "sweep": kind, "labels": labels}
  1648	        report.swept.append({"ticket": key, "sweep": kind, "labels": labels})
  1649	        self._log_cold_start(entry)
  1650	
  1651	    def _startup_4_enter_loop(self, report: ColdStartReport) -> None:
  1652	        """Startup-4: log steady-state entry (the loop itself is run() → run_forever)."""
  1653	        report.entered_loop = True
  1654	        self._log_cold_start(
  1655	            {"event": STEADY_STATE_ENTERED_EVENT, "ts_entered": self._clock().isoformat()}
  1656	        )
  1657	        logger.info(
  1658	            "[pipeline_coordinator] cold-start complete — entered steady state at %s",
  1659	            self._clock().isoformat(),
  1660	        )
  1661	
  1662	    def run(
  1663	        self,
  1664	        *,
  1665	        install_signals: bool = True,
  1666	        max_ticks: int | None = None,
  1667	    ) -> int:
  1668	        """Production boot: cold-start recovery, then the steady-state loop.
  1669	
  1670	        Runs :meth:`startup` first (ADR §3.3 — the coordinator is the canonical
  1671	        startup orchestrator). If Startup-1 halted on degraded infra, the loop
  1672	        is NOT entered and 0 ticks are returned. Otherwise hands off to
  1673	        :meth:`run_forever` (Startup-4).
  1674	        """
  1675	        report = self.startup()
  1676	        if report.phase1_halted:
  1677	            logger.error(
  1678	                "[pipeline_coordinator] cold-start halted in Startup-1 "
  1679	                "(infra still down: %s) — not entering steady-state loop",
  1680	                report.still_down_units,
  1681	            )
  1682	            return 0
  1683	        return self.run_forever(install_signals=install_signals, max_ticks=max_ticks)
  1684	
  1685	    # ── entrypoints ──
  1686	
  1687	    def run_once(self) -> DecisionResult:
  1688	        """One tick: heartbeat → refresh world → evaluate → log decision.
  1689	
  1690	        Returns the engine's :class:`DecisionResult` (a no-op in the
  1691	        skeleton) so callers/tests can assert on it directly.
  1692	        """
  1693	        self._heartbeat.touch()
  1694	        self._refresh_work_graph()
  1695	        self._append_source_event_records()
  1696	        ctx = self.build_context()
  1697	        if self._config.capacity_path is not None:
  1698	            write_capacity_snapshot(ctx.capacity, self._config.capacity_path)
  1699	        result = self._engine.evaluate(ctx)
  1700	        # Hand each real (non-noop) action to the action layer. In shadow
  1701	        # mode it only records the would-be execution; the outcomes go into
  1702	        # the decision-log line so a fired rule is observable end-to-end.
  1703	        action_results = [
  1704	            self._action_executor(a, ctx)
  1705	            for a in result.actions
  1706	            if not isinstance(a, NoopAction)
  1707	        ]
  1708	        record = self._build_tick_record(result, action_results)
  1709	        self._decision_log.append(record)
  1710	        self._run_learning_loop(record, result)
  1862	def build_default_coordinator(
  1863	    config: CoordinatorConfig | None = None,
  1864	    *,
  1865	    acting: bool = False,
  1866	    action_executor: ActionExecutor | None = None,
  1867	) -> PipelineCoordinator:
  1868	    """Production wiring: env config, hybrid Tier-1+Tier-2 engine, shadow layer.
  1869	
  1870	    29f-3 shipped the Tier-1 rule engine; 29f-6 fronts it with the Tier-2 LLM
  1871	    consultant (:class:`HybridDecisionEngine`) so a tick that no Tier-1 rule
  1872	    matches (or that the personality mode escalates) consults the claude CLI
  1873	    under a daily budget cap. Constructing the engine logs the loaded rule
  1874	    registry (Deploy AC). The budget guard rebuilds the day's spend from the
  1875	    decision log (ADR §3.2 stateless-across-restarts). The action layer
  1876	    defaults to shadow (observe-only) so this stays safe for the 29f-14 7-day
  1877	    shadow canary. ``acting=True`` is the single switch that permits the main
  1878	    action executor, the hourly sprint re-plan handler, AND the 4-phase
  1879	    cold-start recovery to mutate; ``acting=False`` wires every one of them
  1880	    observe-only (cold-start via :class:`ShadowColdStartGateway`, OP-1555).
  1881	    """
  1882	    config = config or CoordinatorConfig.from_env()
  1883	    if acting and action_executor is None:
  1884	        raise ValueError("acting=True requires an explicit action_executor")
  1885	    if not acting and action_executor is not None:
  1886	        raise ValueError("action_executor requires acting=True")
  1887	    decision_log = DecisionLog(config.decision_log_dir)
  1888	    # 29f-11: the learning loop reads + appends to the same decision log; build
  1889	    # it from the log so its daily/weekly schedule survives a restart (§3.2).
  1890	    from backend.agents.learning_loop import build_default_learning_loop
  1891	
  1892	    return PipelineCoordinator(
  1893	        config,
  1894	        engine=build_hybrid_engine(decision_log_dir=config.decision_log_dir),
  1895	        action_executor=action_executor,
  1896	        cold_start_gateway=_default_cold_start_gateway(config, acting=acting),
  1897	        sprint_replan_handler=build_default_sprint_replan_handler(
  1898	            config_dir=config.config_dir,
  1899	            decision_log_dir=config.decision_log_dir,
  1900	            capacity_path=config.capacity_path,
  1901	            agent_class=config.jira_agent_class,
  1902	            shadow=not acting,
  1903	        ),
  1904	        decision_log=decision_log,
  1905	        learning_loop=build_default_learning_loop(decision_log),
  1906	    )
  1907	
  1908	
  1909	def main(argv: list[str] | None = None) -> int:
  1910	    """CLI entrypoint. ``--once`` runs a single tick; default runs forever."""
  1911	    import argparse
  1912	
  1913	    parser = argparse.ArgumentParser(description=__doc__)
  1914	    parser.add_argument(
  1915	        "--once",
  1916	        action="store_true",
  1917	        help="run a single tick (heartbeat + one no-op decision) and exit "
  1918	             "(skips cold-start recovery — a diagnostic tick, not a boot)",
  1919	    )
  1920	    parser.add_argument(
  1921	        "--no-cold-start",
  1922	        action="store_true",
  1923	        help="enter the steady-state loop directly, skipping the 4-phase "
  1924	             "cold-start recovery (ADR §3.3) — for local debugging only",
  1925	    )
  1926	    parser.add_argument(
  1927	        "--config-dir",
  1928	        type=Path,
  1929	        default=None,
  1930	        help="override the coordinator config dir (default ~/.config/omnisight/coordinator)",
  1931	    )
  1932	    args = parser.parse_args(argv)
  1933	
  1934	    logging.basicConfig(level=logging.INFO)
  1935	    config = CoordinatorConfig.from_env(config_dir=args.config_dir)
  1936	    coordinator = build_default_coordinator(config)
  1937	    if args.once:
  1938	        coordinator.run_once()
  1939	        return 0
  1940	    if args.no_cold_start:
  1941	        coordinator.run_forever()
  1942	        return 0
  1943	    # Production boot: 4-phase cold-start recovery, then steady-state loop.
  1944	    coordinator.run()
  1945	    return 0

 succeeded in 152ms:
   116	# Maps each name to the §6.1 allowed-kind constant; anything outside this map
   117	# is dropped by :func:`validate_actions` (Code AC: only §6.1 actions execute).
   118	_LLM_ACTION_NAME_TO_KIND: Mapping[str, str] = {
   119	    "file_ticket": ACTION_FILE_TICKET,
   120	    "relabel": ACTION_RELABEL,
   121	    "transition": ACTION_TRANSITION,
   122	    "mention_operator": ACTION_MENTION_OPERATOR,
   123	    "mark_for_followup": ACTION_MARK_FOR_FOLLOWUP,
   124	    "escalate": ACTION_ESCALATE,
   125	    "noop": ACTION_NOOP,
   126	}
   650	    confidence = str(payload.get("confidence", "")).strip().lower()
   651	    if confidence not in _VALID_CONFIDENCE:
   652	        confidence = "low"  # unrecognised → treat conservatively, never crash
   653	    return ParsedDecision(
   654	        decision_rationale=str(payload.get("decision_rationale", "") or ""),
   655	        actions=actions,
   656	        dropped_actions=tuple(dropped),
   657	        confidence=confidence,
   658	        escalate_if_wrong=bool(payload.get("escalate_if_wrong", False)),
   659	        learning=str(payload.get("learning", "") or ""),
   660	    )
   661	
   662	
   663	def _strip_code_fence(text: str) -> str:
   664	    stripped = (text or "").strip()
   665	    if stripped.startswith("```"):
   666	        # Drop the opening fence (``` or ```json) and the closing fence.
   667	        body = stripped.split("\n", 1)[1] if "\n" in stripped else ""
   668	        if body.rstrip().endswith("```"):
   669	            body = body.rstrip()[: -3]
   670	        return body.strip()
   671	    return stripped
   672	
   673	
   674	def validate_actions(
   675	    raw_actions: Sequence[Any],
   676	) -> tuple[tuple[Action, ...], list[Mapping[str, Any]]]:
   677	    """Map LLM action dicts to §6.1 :class:`Action`s; drop everything else.
   678	
   679	    Code AC ("only ADR §6.1 allowed actions executed"): an action whose name
   680	    is outside :data:`_LLM_ACTION_NAME_TO_KIND`, or that fails the
   681	    :class:`Action` envelope's own §6.1 kind check, is dropped (not executed)
   682	    and returned in the rejects list so the decision log records the attempt.
   683	    A bare ``noop`` is dropped from the executable set (it carries no effect).
   684	    """
   685	    accepted: list[Action] = []
   686	    dropped: list[Mapping[str, Any]] = []
   687	    for raw in raw_actions:
   688	        if not isinstance(raw, Mapping):
   689	            dropped.append({"reason": "not_an_object", "raw": repr(raw)})
   690	            continue
   830	        # A consult that returned zero executable actions but asked to escalate
   831	        # (or simply produced nothing) still surfaces to the operator so a
   832	        # Tier-2 tick is never silently a no-op when the model wanted action.
   833	        if not actions and parsed.escalate_if_wrong:
   834	            actions = (
   835	                Action.escalate(
   836	                    _focal_key(ctx),
   837	                    reason=f"[tier2] low-confidence consult, no safe action: "
   838	                    f"{parsed.decision_rationale[:200]}",
   839	                ),
   840	            )
   841	        meta = {
   842	            "trigger": trigger,
   843	            "context_size_tokens": envelope.input_tokens or _estimate_tokens(bundle.render()),
   844	            "response_tokens": envelope.output_tokens,
   845	            "cost_usd": round(cost, 6),
   846	            "estimate_usd": round(self._config.per_decision_estimate_usd, 6),
   847	            "confidence": parsed.confidence,
   848	            "decision_rationale": parsed.decision_rationale,
   849	            # 29f-11: the one-line lesson the learning loop writes back to
   850	            # Cognee when this decision's 24h outcome-check succeeds (§10.1).
   851	            "learning": parsed.learning,
   852	            "lessons_cited": lesson_ids,
   853	            "model": self._config.model,
   854	            "budget_remaining_usd": round(self._budget.remaining_usd(), 6),
   855	        }
   856	        if parsed.dropped_actions:
   857	            meta["dropped_actions"] = list(parsed.dropped_actions)
   858	        return Tier2Outcome(
   859	            actions=actions,
   860	            reason=TIER2_CONSULTED_REASON,
   861	            llm_consultation=meta,
   862	            learning=parsed.learning,
   863	        )
   864	
   865	    def _degrade_for_budget(self, ctx: DecisionContext) -> Tier2Outcome:
   866	        msg = (
   867	            f"[coord-budget] daily Tier-2 LLM budget "
   868	            f"${self._budget.cap_usd:.2f} exhausted "
   869	            f"(spent ${self._budget.spent_usd():.2f}); degrading to Tier-1-only "
   870	            f"until UTC day rollover."
   871	        )
   872	        logger.warning("[coord-llm] %s", msg)
   873	        return Tier2Outcome(
   874	            actions=(Action.mention_operator(_focal_key(ctx), message=msg, urgency="high"),),
   875	            reason=TIER2_DEGRADED_BUDGET_REASON,
   876	            llm_consultation={
   877	                "degraded": "budget_cap",
   878	                "daily_budget_usd": round(self._budget.cap_usd, 6),
   879	                "spent_usd": round(self._budget.spent_usd(), 6),
   880	                "cost_usd": 0.0,
   881	            },
   882	            degraded=True,
   883	        )
   884	
   885	    def _escalate_invocation_failure(
   886	        self,
   887	        ctx: DecisionContext,
   888	        invocation: LLMInvocation,
   889	        lesson_ids: list[str],
   890	    ) -> Tier2Outcome:
   891	        detail = "timeout" if invocation.timed_out else f"exit={invocation.returncode}"
   892	        msg = f"[tier2] claude CLI consultation failed ({detail}); operator triage needed."
   893	        logger.warning("[coord-llm] %s stderr=%s", msg, (invocation.stderr or "")[:300])
   894	        return Tier2Outcome(
   895	            actions=(Action.escalate(_focal_key(ctx), reason=msg),),
   896	            reason=TIER2_INVOCATION_FAILED_REASON,
   897	            llm_consultation={
   898	                "error": detail,
   899	                "cost_usd": 0.0,
   900	                "lessons_cited": lesson_ids,
   901	            },
   902	        )
   903	
   904	    def _escalate_parse_failure(
   905	        self,
   906	        ctx: DecisionContext,
   907	        invocation: LLMInvocation,
   908	        lesson_ids: list[str],
   909	        exc: Exception,
   910	    ) -> Tier2Outcome:
   911	        msg = f"[tier2] LLM response was not valid decision JSON ({exc}); operator triage needed."
   912	        logger.warning("[coord-llm] %s", msg)
   913	        return Tier2Outcome(
   914	            actions=(Action.escalate(_focal_key(ctx), reason=msg),),
   915	            reason=TIER2_PARSE_FAILED_REASON,

 succeeded in 142ms:
   300	    headers = {
   301	        "Authorization": auth_header,
   302	        "Content-Type": "application/json",
   303	        "Accept": "application/json",
   304	    }
   305	    if idem_key:
   306	        headers["X-Atlassian-Token"] = "no-check"
   307	        headers["X-Idempotency-Key"] = idem_key
   308	    req = urllib.request.Request(
   309	        url, data=data, method=method,
   310	        headers=headers,
   311	    )
   312	    try:
   313	        with BREAKERS["jira_rest"].call(urllib.request.urlopen, req, timeout=30) as resp:
   314	            payload = resp.read().decode()
   315	            return json.loads(payload) if payload else {}
   316	    except urllib.error.HTTPError as e:
   317	        body_text = e.read().decode() if e.fp else ""
   318	        raise RuntimeError(f"{method} {url} → {e.code}: {body_text}") from e
   319	
   320	
   321	def _request(
   322	    client: DispatchClient,
   323	    method: str,
   324	    path: str,
   325	    body: dict | None = None,
   326	    idem_key: str | None = None,
   327	) -> dict:
   328	    return _request_raw(method, client.base_url + path, client.auth_header, body, idem_key)
   329	
   330	
   331	def _request_idempotent(
   332	    client: DispatchClient,
   333	    method: str,
   334	    path: str,
   335	    body: dict | None,
  1649	def transition_to_under_review_if_needed(
  1650	    client: "DispatchClient",
  1651	    key: str,
  1652	    idem_key: str | None = None,
  1653	    change_number: int | None = None,
  1654	) -> bool:
  1655	    """In Progress → Under Review, but only if not already there.
  1656	
  1657	    Returns True if a transition POST was issued, False if the ticket was
  1658	    already in Under Review and the call was a no-op. Raises RuntimeError
  1659	    on any non-4xx HTTP failure of the transition POST itself; the runner
  1660	    catches and downgrades to a skip-comment per OP-691 AC.
  1661	    """
  1662	    if get_issue_status(client, key) == UNDER_REVIEW_STATUS_NAME:
  1663	        return False
  1664	    idem_key = idem_key or (
  1665	        _under_review_idem_key(key, change_number=change_number)
  1666	        if change_number is not None
  1667	        else f"transition-{key}-under-review-{uuid.uuid4().hex[:12]}"
  1668	    )
  1669	    _request_idempotent(
  1670	        client, "POST", f"/issue/{key}/transitions",
  1671	        {"transition": {"id": TRANSITION_IDS["to_under_review"]}},
  1672	        idem_key,
  1673	    )
  1674	    return True
  1675	
  1934	def transition_back_to_todo(
  1935	    client: DispatchClient,
  1936	    key: str,
  1937	    reason: str,
  1938	    idem_key: str | None = None,
  1939	    *,
  1940	    failure_class: "str | None" = None,
  1941	    raw_traceback: str = "",
  1942	    mutex_label: str | None = None,
  1943	    area: str | None = None,
  1944	) -> None:
  1945	    """In Progress → TODO with reason comment + clear assignee.
  1946	
  1947	    OP-1541 invariant: this is a destructive recovery — it clears the
  1948	    assignee. Under a shared JIRA account ONLY the current winning
  1949	    fencing-token claim holder may revert. Callers on the TOCTOU /
  1950	    assignee-divergence path MUST first prove claim ownership (see
  1951	    :func:`is_winning_claim_owner` and ``auto-runner-jira._handle_toctou_abort``)
  1952	    so a sibling instance's assignee clear does not cascade into a false
  1953	    revert (OP-1533 burst → stoploss). The non-recovery callers (CLI
  1954	    failure, capability gate, discovered dependency, pre-claim cwd-unsafe)
  1955	    are unaffected — they own or predate the claim.
  1956	
  1957	    OP-854 (C2): when ``failure_class`` is supplied, an incident row
  1958	    lands in ``runner_incidents`` before the transition fires so the
  1959	    C8 failure-graph and the C2 recall path can pick it up next time
  1960	    the ticket is attempted. Recording is best-effort — a recorder
  1961	    fault never blocks the JIRA transition (the operator still needs
  1962	    the ticket reverted even if the audit shim is wedged).
  1963	    """
  1964	    base_key = idem_key or f"transition-{key}-back-to-todo-{uuid.uuid4().hex[:12]}"
  1965	    if failure_class is not None:
  1966	        try:
  1967	            # Local import keeps jira_dispatch importable without the
  1968	            # incident_recorder + failure_class chain (e.g. early-boot
  1969	            # JQL-only smoke tests).
  1970	            from backend.agents.incident_recorder import record_runner_incident
  1971	
  1972	            record_runner_incident(
  1973	                ticket_key=key,
  1974	                failure_class=failure_class,
  1975	                summary=reason.splitlines()[0] if reason else "",
  1976	                raw_traceback=raw_traceback,
  1977	                runner_class=getattr(client, "agent_class", "unknown"),
  1978	                mutex_label=mutex_label,
  1979	                area=area,
  1980	            )
  1981	        except Exception as exc:  # noqa: BLE001 — best-effort wiring
  1982	            log.warning(
  1983	                "jira_dispatch.record_runner_incident_failed key=%s err=%s",
  1984	                key,
  1985	                exc,
  1986	            )
  1987	    add_comment(
  1988	        client, key, f"Reverting to TODO. Reason:\n{reason}",
  1989	        idem_key=f"{base_key}-comment",
  1990	    )
  1991	    clear_assignee(client, key, idem_key=f"{base_key}-clear-assignee")
  1992	    _request_idempotent(
  1993	        client, "POST", f"/issue/{key}/transitions",
  1994	        {"transition": {"id": TRANSITION_IDS["back_to_todo"]}},
  1995	        f"{base_key}-transition",
  1996	    )
  2014	def add_comment(client: DispatchClient, key: str, text: str, idem_key: str | None = None) -> None:
  2015	    idem_key = idem_key or f"comment-{key}-{uuid.uuid4().hex[:12]}"
  2016	    _request_idempotent(
  2017	        client, "POST", f"/issue/{key}/comment",
  2018	        {"body": _adf_paragraph(text)},
  2019	        idem_key,
  2020	    )
  2021	
  2022	
  2023	def clear_assignee(client: DispatchClient, key: str, idem_key: str | None = None) -> None:
  2024	    idem_key = idem_key or f"clear-assignee-{key}-{uuid.uuid4().hex[:12]}"
  2025	    _request_idempotent(
  2026	        client, "PUT", f"/issue/{key}",
  2027	        {"fields": {"assignee": None}},
  2028	        idem_key,
  2029	    )
  2030	
  2031	
  2032	def add_label(client: DispatchClient, key: str, label: str, idem_key: str | None = None) -> None:
  2033	    """Add one JIRA label without replacing the existing label set."""
  2034	    idem_key = idem_key or f"label-add-{key}-{label}-{uuid.uuid4().hex[:12]}"
  2035	    _request_idempotent(
  2036	        client, "PUT", f"/issue/{key}",
  2037	        {"update": {"labels": [{"add": label}]}},
  2038	        idem_key,
  2039	    )
  2040	
  2041	
  2042	def remove_label(client: DispatchClient, key: str, label: str, idem_key: str | None = None) -> None:
  2043	    """Remove one JIRA label if present; JIRA treats absent labels as a no-op."""
  2044	    idem_key = idem_key or f"label-remove-{key}-{label}-{uuid.uuid4().hex[:12]}"
  2045	    _request_idempotent(
  2046	        client, "PUT", f"/issue/{key}",
  2047	        {"update": {"labels": [{"remove": label}]}},
  2048	        idem_key,

exec
/bin/bash -lc "nl -ba backend/agents/rate_limiter.py | sed -n '431,520p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   431	class RetryableExecutor:
   432	    """Execute an async API call with retries + DLQ.
   433	
   434	    Caller provides:
   435	      - ``call_factory``: zero-arg coroutine factory; called once per attempt
   436	      - ``status_extractor``: optional fn that extracts HTTP status from
   437	        an exception (Anthropic SDK puts it on .status_code attribute)
   438	      - ``retry_after_extractor``: optional fn that extracts the
   439	        Retry-After header value from an exception (None → use backoff)
   440	
   441	    Behaviour:
   442	      - Attempt 1..max_retries+1 with exponential backoff + jitter
   443	      - Honours `retry-after` from rate-limit responses
   444	      - Records to RateLimitTracker on success
   445	      - Deposits to DLQ on max-retry exhaustion or non_retryable error
   446	    """
   447	
   448	    def __init__(
   449	        self,
   450	        *,
   451	        policy: RetryPolicy | None = None,
   452	        tracker: RateLimitTracker | None = None,
   453	        dlq: DeadLetterQueue | None = None,
   454	        sleep: Callable[[float], Awaitable[None]] = asyncio.sleep,
   455	    ) -> None:
   456	        # Use ``is None`` checks rather than ``or``: an empty
   457	        # InMemoryDeadLetterQueue is falsy via ``__len__`` and would
   458	        # silently get replaced with a fresh one, dropping the caller's
   459	        # reference (and its inspection ability).
   460	        self.policy = policy if policy is not None else RetryPolicy()
   461	        self.tracker = tracker if tracker is not None else RateLimitTracker()
   462	        self.dlq: DeadLetterQueue = (
   463	            dlq if dlq is not None else InMemoryDeadLetterQueue()
   464	        )
   465	        self._sleep = sleep
   466	
   467	    async def execute(
   468	        self,
   469	        call_factory: CallableAsync,
   470	        *,
   471	        workspace: str,
   472	        model: str,
   473	        input_tokens_estimated: int = 0,
   474	        output_tokens_estimated: int = 0,
   475	        request_metadata: dict[str, Any] | None = None,
   476	        status_extractor: Callable[[BaseException], int | None] = lambda _: None,
   477	        retry_after_extractor: Callable[[BaseException], str | None] = lambda _: None,
   478	    ) -> Any:
   479	        """Run with retries. Returns the call result on success.
   480	
   481	        On final failure: deposits DLQ entry + re-raises the last
   482	        exception so the caller can choose to handle / propagate.
   483	        """
   484	        last_exception: BaseException | None = None
   485	        last_classification: RetryClassification = "retryable"
   486	        last_status_code: int | None = None
   487	        last_reason: str = ""
   488	
   489	        # Pre-flight: tracker check.
   490	        breached, reason = self.tracker.would_exceed(
   491	            workspace=workspace, model=model,
   492	            input_tokens_estimated=input_tokens_estimated,
   493	            output_tokens_estimated=output_tokens_estimated,
   494	        )
   495	        if breached:
   496	            # Treat predicted breach as a synthetic 429 — backoff + retry.
   497	            logger.info(
   498	                "RateLimitTracker preflight: workspace=%s model=%s would breach (%s); "
   499	                "delaying before submit",
   500	                workspace, model, reason,
   501	            )
   502	            await self._sleep(
   503	                compute_backoff(0, self.policy, retry_after=None)
   504	            )
   505	
   506	        for attempt in range(self.policy.max_retries + 1):
   507	            try:
   508	                result = await call_factory()
   509	            except BaseException as e:  # noqa: BLE001
   510	                last_exception = e
   511	                last_status_code = status_extractor(e)
   512	                last_classification = classify_error(
   513	                    status_code=last_status_code, exception=e
   514	                )
   515	                last_reason = f"{type(e).__name__}: {e}"
   516	
   517	                if last_classification == "non_retryable":
   518	                    # No retry — straight to DLQ.
   519	                    await self._deposit_dlq(
   520	                        workspace=workspace,

exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator.py | sed -n '1710,1810p'" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "nl -ba backend/agents/jira_dispatch.py | sed -n '331,340p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   331	def _request_idempotent(
   332	    client: DispatchClient,
   333	    method: str,
   334	    path: str,
   335	    body: dict | None,
   336	    idem_key: str,
   337	) -> dict:
   338	    return DEFAULT_STORE.run(idem_key, lambda: _request(client, method, path, body, idem_key))
   339	
   340	

 succeeded in 0ms:
  1710	        self._run_learning_loop(record, result)
  1711	        return result
  1712	
  1713	    def _run_learning_loop(self, record: dict[str, Any], result: DecisionResult) -> None:
  1714	        """Write back a Tier-2 decision + fire any due daily/weekly handler.
  1715	
  1716	        Gated on a wired learning loop (29f-11) so the skeleton / test daemon is
  1717	        unaffected. Write-back happens in-tick so a Tier-2 decision reaches
  1718	        Cognee well within the 60s Integration-AC budget; the daily + weekly
  1719	        schedules are gated by the loop's own cadence (mirrors the hourly
  1720	        sweep). Fail-open: a learning-loop error never breaks a decision tick.
  1721	        """
  1722	        loop = self._learning_loop
  1723	        if loop is None:
  1724	            return
  1725	        try:
  1726	            if not self._learning_seeded:
  1727	                loop.seed_schedule(self._clock())
  1728	                self._learning_seeded = True
  1729	            if result.tier == 2:
  1730	                loop.write_back_decision(record)
  1731	            loop.maybe_run(self._clock())
  1732	        except Exception as exc:  # noqa: BLE001 — learning loop is best-effort
  1733	            logger.warning("[pipeline_coordinator] learning loop error: %s", exc)
  1734	
  1735	    def _append_source_event_records(self) -> None:
  1736	        for event in self._pending_events:
  1737	            self._decision_log.append(
  1738	                {
  1739	                    "ts": self._clock().isoformat(),
  1740	                    "event": "coordinator_source_event",
  1741	                    "trigger": event.get("trigger"),
  1742	                    "source": event.get("source"),
  1743	                    "ticket_key": event.get("ticket_key"),
  1744	                    "payload": event.get("payload"),
  1745	                    "dry_run": True,
  1746	                    "pid": os.getpid(),
  1747	                }
  1748	            )
  1749	
  1750	    def request_stop(self) -> None:
  1751	        """Ask the steady-state loop to stop after the current tick."""
  1752	        self._stop.set()
  1753	
  1754	    @property
  1755	    def stopping(self) -> bool:
  1756	        return self._stop.is_set()
  1757	
  1758	    def run_forever(
  1759	        self,
  1760	        *,
  1761	        install_signals: bool = True,
  1762	        max_ticks: int | None = None,
  1763	    ) -> int:
  1764	        """Steady-state loop: tick, then sleep one interval, until stopped.
  1765	
  1766	        On stop (SIGTERM via the installed handler, ``request_stop()``, or
  1767	        ``max_ticks`` reached) the loop drains cleanly: writes
  1768	        ``shutdown_began`` + ``shutdown_complete`` to the decision log,
  1769	        then returns the number of ticks executed (ADR-0021 §9 L5).
  1770	
  1771	        ``install_signals`` is False-able so tests can drive the loop from
  1772	        a non-main thread (where ``signal.signal`` would raise).
  1773	        ``max_ticks`` bounds the loop for tests; production passes None.
  1774	        """
  1775	        if install_signals:
  1776	            self._install_signal_handlers()
  1777	        # Startup line names the loaded rule registry when the Tier-1 engine
  1778	        # is wired (Deploy AC: registry visible at startup). The engine also
  1779	        # logs its full rule list at construction.
  1780	        rule_names = getattr(self._engine, "rule_names", None)
  1781	        rules_desc = ",".join(rule_names()) if callable(rule_names) else "n/a"
  1782	        logger.info(
  1783	            "[pipeline_coordinator] entering steady state "
  1784	            "(engine=%s mode=%s tick=%.0fs heartbeat=%.0fs "
  1785	            "capacity_tracking=active capacity_path=%s rules=[%s])",
  1786	            self._engine.engine_version,
  1787	            self._mode_selector.select(),
  1788	            self._config.tick_interval_seconds,
  1789	            self._config.heartbeat_interval_seconds,
  1790	            self._config.capacity_path,
  1791	            rules_desc,
  1792	        )
  1793	        ticks = 0
  1794	        try:
  1795	            while not self._stop.is_set():
  1796	                self.run_once()
  1797	                ticks += 1
  1798	                if max_ticks is not None and ticks >= max_ticks:
  1799	                    break
  1800	                if self._stop.is_set():
  1801	                    break
  1802	                # Interruptible: returns True if stop fired while waiting.
  1803	                if self._sleep(self._config.tick_interval_seconds):
  1804	                    break
  1805	        finally:
  1806	            self._drain()
  1807	        return ticks
  1808	
  1809	    # ── L5 drain on shutdown (ADR-0021 §9 L5) ──
  1810	

exec
/bin/bash -lc "nl -ba backend/tests/test_pipeline_coordinator_integration.py | sed -n '440,485p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   440	            r.get("rule_name"),
   441	            r.get("tier"),
   442	            r.get("mode"),
   443	        )
   444	        for r in records
   445	        if r["event"] in ("startup_phase", "coordinator_source_event", "decision_tick")
   446	    ]
   447	    assert sequence == [
   448	        ("startup_phase", "OP-C", None, None, None),
   449	        ("coordinator_source_event", "OP-A", None, None, None),
   450	        (
   451	            "decision_tick",
   452	            None,
   453	            "capability-blocked-known-locale",
   454	            1,
   455	            "ExecutionMode",
   456	        ),
   457	        ("coordinator_source_event", "OP-B", None, None, None),
   458	        ("decision_tick", None, None, 2, RESCUE_MODE),
   459	    ]
   460	
   461	    tick_a, tick_b = [r for r in records if r["event"] == "decision_tick"]
   462	    assert tick_a["engine_version"] == HYBRID_ENGINE_VERSION
   463	    assert tick_a["actions"][0]["params"]["add"] == [CAPABILITY_ENABLE_LABEL]
   464	    assert tick_a["action_results"] == [
   465	        {"kind": ACTION_RELABEL, "target": "OP-A", "executed": True, "shadow": False}
   466	    ]
   467	    assert tick_b["mode_behavior"]["tier2_policy"] == "always"
   468	    assert tick_b["mode_behavior"]["llm_context_hops"] == 10
   469	    assert tick_b["llm_consultation"]["trigger"] == "mode_requires_tier2"
   470	    assert tick_b["llm_consultation"]["lessons_cited"] == ["L-OP-870-blockedby.md"]
   471	    assert tick_b["action_results"] == [
   472	        {"kind": ACTION_ESCALATE, "target": "OP-B", "executed": True, "shadow": False}
   473	    ]
   474	    assert world.operator_mentions == [
   475	        (
   476	            "OP-B",
   477	            "[merger-repeat-fail] OP-B merger failed 4 times; full context: "
   478	            "change Iabcdef, conflicts in src/a.py, mode=RescueMode, "
   479	            "free_codex_slots=1",
   480	            "high",
   481	        )
   482	    ]

exec
/bin/bash -lc "nl -ba backend/tests/test_pipeline_coordinator_integration.py | sed -n '300,375p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   300	        assert ctx.capacity.runners["subscription-codex"].free_slots == 1
   301	        reason = (
   302	            "[merger-repeat-fail] OP-B merger failed 4 times; "
   303	            "full context: change Iabcdef, conflicts in src/a.py, "
   304	            "mode=RescueMode, free_codex_slots=1"
   305	        )
   306	        return Tier2Outcome(
   307	            actions=(Action.escalate(ctx.ticket.key, reason=reason),),
   308	            reason=TIER2_CONSULTED_REASON,
   309	            llm_consultation={
   310	                "trigger": trigger,
   311	                "confidence": "high",
   312	                "decision_rationale": reason,
   313	                "cost_usd": 0.01,
   314	                "lessons_cited": ["L-OP-870-blockedby.md"],
   315	            },
   316	        )
   317	
   318	
   319	def _config(tmp_path: Path) -> CoordinatorConfig:
   320	    base = tmp_path / "coordinator"
   321	    return CoordinatorConfig(
   322	        config_dir=base,
   323	        heartbeat_path=base / "heartbeat",
   324	        decision_log_dir=base / "decision-log",
   325	        heartbeat_interval_seconds=60.0,
   326	        tick_interval_seconds=60.0,
   327	        sweep_interval_seconds=3600.0,
   328	    )
   329	
   330	
   331	def _read_records(directory: Path) -> list[dict[str, Any]]:
   332	    records: list[dict[str, Any]] = []
   333	    for path in sorted(directory.glob("*.jsonl")):
   334	        for line in path.read_text(encoding="utf-8").splitlines():
   335	            if line.strip():
   336	                records.append(json.loads(line))
   337	    return records
   338	
   339	
   340	def test_full_pipeline_scenarios_flow_through_coordinator(tmp_path: Path) -> None:
   341	    """Scenarios A/B/C run through startup, event subscribers, engine, actions.
   342	
   343	    Sequence:
   344	      1. startup cleans OP-C's dead claim so it is pickable again;
   345	      2. bridge event for OP-A drives a Tier-1 capability-enable relabel;
   346	      3. JIRA event for OP-B drives RescueMode Tier-2 operator escalation.
   347	    """
   348	    clock = FakeClock()
   349	    world = PipelineWorld()
   350	    coord_ref: dict[str, PipelineCoordinator] = {}
   351	
   352	    def _work_graph() -> WorkGraph:
   353	        return world.graph_for_events(coord_ref["coord"]._pending_events)
   354	
   355	    def _capacity() -> CapacitySnapshot:
   356	        return CapacitySnapshot(
   357	            captured_at=clock(),
   358	            runners={
   359	                "subscription-codex": RunnerCapacity(
   360	                    "subscription-codex",
   361	                    free_slots=1,
   362	                    budget_remaining=12.0,
   363	                    tokens_in_current_week=1200,
   364	                    weekly_cap=100000,
   365	                )
   366	            },
   367	        )
   368	
   369	    engine = HybridDecisionEngine(consultant=ContextEscalatingConsultant())
   370	    coord = PipelineCoordinator(
   371	        _config(tmp_path),
   372	        engine=engine,
   373	        clock=clock,
   374	        capacity_provider=_capacity,
   375	        work_graph_provider=_work_graph,

exec
/bin/bash -lc 'rg -n "class .*Action|def .*execute|ACTION_ESCALATE|ACTION_RELABEL|ACTION_TRANSITION|needs-operator-action" backend/tests/test_pipeline_coordinator_integration.py backend/tests/test_pipeline_coordinator_rules.py backend/tests/test_pipeline_coordinator_llm_consultation.py' in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
backend/tests/test_pipeline_coordinator_llm_consultation.py:54:    ACTION_ESCALATE,
backend/tests/test_pipeline_coordinator_llm_consultation.py:56:    ACTION_RELABEL,
backend/tests/test_pipeline_coordinator_llm_consultation.py:245:    assert parsed.actions[0].kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_llm_consultation.py:287:    assert {a.kind for a in actions} == {ACTION_RELABEL, ACTION_ESCALATE}
backend/tests/test_pipeline_coordinator_llm_consultation.py:397:    assert outcome.actions[0].kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_llm_consultation.py:425:    assert outcome.actions[0].kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_llm_consultation.py:433:    assert outcome.actions[0].kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_llm_consultation.py:460:    assert [a.kind for a in outcome.actions] == [ACTION_RELABEL]
backend/tests/test_pipeline_coordinator_llm_consultation.py:626:    assert ACTION_RELABEL in kinds            # the Tier-1 deterministic action
backend/tests/test_pipeline_coordinator_rules.py:32:    ACTION_ESCALATE,
backend/tests/test_pipeline_coordinator_rules.py:35:    ACTION_RELABEL,
backend/tests/test_pipeline_coordinator_rules.py:36:    ACTION_TRANSITION,
backend/tests/test_pipeline_coordinator_rules.py:72:    assert Action.relabel("OP-1", add=("x",)).kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:74:    assert Action.transition("OP-1", to_status="To Do").kind == ACTION_TRANSITION
backend/tests/test_pipeline_coordinator_rules.py:76:    assert Action.escalate("OP-1", reason="r").kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_rules.py:129:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:147:    assert action is not None and action.kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_rules.py:160:    assert action is not None and action.kind == ACTION_TRANSITION
backend/tests/test_pipeline_coordinator_rules.py:184:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:209:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:280:    assert action is not None and action.kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_rules.py:425:    assert record["actions"][0]["kind"] == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_integration.py:49:    ACTION_ESCALATE,
backend/tests/test_pipeline_coordinator_integration.py:50:    ACTION_RELABEL,
backend/tests/test_pipeline_coordinator_integration.py:171:    def execute(self, action: Action, _ctx: DecisionContext) -> dict[str, Any]:
backend/tests/test_pipeline_coordinator_integration.py:172:        if action.kind == ACTION_RELABEL:
backend/tests/test_pipeline_coordinator_integration.py:195:        elif action.kind == ACTION_ESCALATE:
backend/tests/test_pipeline_coordinator_integration.py:426:    assert scenario_a.actions[0].kind == ACTION_RELABEL
backend/tests/test_pipeline_coordinator_integration.py:432:    assert scenario_b.actions[0].kind == ACTION_ESCALATE
backend/tests/test_pipeline_coordinator_integration.py:465:        {"kind": ACTION_RELABEL, "target": "OP-A", "executed": True, "shadow": False}
backend/tests/test_pipeline_coordinator_integration.py:472:        {"kind": ACTION_ESCALATE, "target": "OP-B", "executed": True, "shadow": False}

exec
/bin/bash -lc "nl -ba backend/agents/sprint_replan.py | sed -n '120,152p;360,378p;450,490p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   120	
   121	    def remove_label(self, key: str, label: str) -> None: ...
   122	
   123	    def file_scope_review_ticket(
   124	        self,
   125	        *,
   126	        source_key: str,
   127	        target_area: str,
   128	        description: str,
   129	    ) -> str: ...
   130	
   131	    def mention_operator(self, key: str, message: str, *, urgency: str) -> None: ...
   132	
   133	
   134	class SprintReplanActionLayer:
   135	    """Execute only validated ADR §6.1 actions for sprint re-planning."""
   136	
   137	    def __init__(self, gateway: SprintReplanGateway, *, shadow: bool = True) -> None:
   138	        self._gateway = gateway
   139	        self._shadow = shadow
   140	
   141	    def execute(self, action: Action) -> dict[str, Any]:
   142	        if action.kind == ACTION_RELABEL:
   143	            return self._execute_relabel(action)
   144	        if action.kind == ACTION_FILE_TICKET:
   145	            return self._execute_file_ticket(action)
   146	        if action.kind == ACTION_MENTION_OPERATOR:
   147	            return self._execute_mention(action)
   148	        return {
   149	            "kind": action.kind,
   150	            "target": action.target,
   151	            "executed": False,
   152	            "reason": "unsupported_for_sprint_replan",
   360	            }
   361	        }
   362	        resp = jira_dispatch._request(self.client, "POST", "/issue", body)
   363	        key = str(resp.get("key") or "")
   364	        if not key:
   365	            raise RuntimeError(f"JIRA POST /issue returned no key: {resp!r}")
   366	        return key
   367	
   368	    def mention_operator(self, key: str, message: str, *, urgency: str) -> None:
   369	        from backend.agents import jira_dispatch
   370	
   371	        prefix = "@nanakusa-sora "
   372	        jira_dispatch.add_comment(self.client, key, f"{prefix}[{urgency}] {message}")
   373	
   374	
   375	def build_default_handler(
   376	    *,
   377	    config_dir: Path,
   378	    decision_log_dir: Path,
   450	    pickable: Sequence[PickableTicket],
   451	    *,
   452	    capacity: CapacitySnapshot,
   453	    llm_actions: Sequence[Action],
   454	) -> tuple[Action, ...]:
   455	    actions: list[Action] = []
   456	    seen: set[tuple[str, str, str]] = set()
   457	    pickable_keys = {ticket.key for ticket in pickable}
   458	    for action in llm_actions:
   459	        if action.kind not in {ACTION_RELABEL, ACTION_FILE_TICKET, ACTION_MENTION_OPERATOR}:
   460	            continue
   461	        normalised = action
   462	        if action.kind == ACTION_MENTION_OPERATOR and action.target == SPRINT_REPLAN_FOCAL_KEY:
   463	            normalised = Action.mention_operator(
   464	                DEFAULT_OPERATOR_TICKET,
   465	                message=str(action.params.get("message") or ""),
   466	                urgency=str(action.params.get("urgency") or "medium"),
   467	                dry_run=action.dry_run,
   468	            )
   469	        if (
   470	            normalised.target
   471	            and normalised.target not in pickable_keys
   472	            and normalised.target != DEFAULT_OPERATOR_TICKET
   473	        ):
   474	            continue
   475	        marker = (
   476	            normalised.kind,
   477	            normalised.target,
   478	            repr(sorted(dict(normalised.params).items())),
   479	        )
   480	        if marker in seen:
   481	            continue
   482	        seen.add(marker)
   483	        actions.append(normalised)
   484	
   485	    if capacity.total_free_slots < len(pickable):
   486	        actions.append(
   487	            Action.mention_operator(
   488	                DEFAULT_OPERATOR_TICKET,
   489	                message=(
   490	                    "[sprint-replan] capacity insufficient: "

exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator.py | sed -n '120,175p;1810,1840p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   120	    the bridge uses so both agents share one audit trail.
   121	    """
   122	
   123	    config_dir: Path
   124	    heartbeat_path: Path
   125	    decision_log_dir: Path
   126	    capacity_path: Path | None = None
   127	    bridge_events_path: Path | None = None
   128	    jira_agent_class: str = "subscription-claude"
   129	    heartbeat_interval_seconds: float = DEFAULT_HEARTBEAT_INTERVAL_SECONDS
   130	    tick_interval_seconds: float = DEFAULT_TICK_INTERVAL_SECONDS
   131	    jira_poll_interval_seconds: float = DEFAULT_JIRA_POLL_INTERVAL_SECONDS
   132	    sweep_interval_seconds: float = DEFAULT_SWEEP_INTERVAL_SECONDS
   133	    event_dedupe_seconds: float = DEFAULT_EVENT_DEDUPE_SECONDS
   134	
   135	    def __post_init__(self) -> None:
   136	        if self.capacity_path is None:
   137	            object.__setattr__(
   138	                self,
   139	                "capacity_path",
   140	                capacity_path_from_env(self.config_dir),
   141	            )
   142	        if self.bridge_events_path is None:
   143	            object.__setattr__(
   144	                self,
   145	                "bridge_events_path",
   146	                self.config_dir / "bridge-events.jsonl",
   147	            )
   148	
   149	    @classmethod
   150	    def from_env(
   151	        cls,
   152	        env: dict[str, str] | None = None,
   153	        *,
   154	        config_dir: Path | None = None,
   155	    ) -> "CoordinatorConfig":
   156	        env = env if env is not None else dict(os.environ)
   157	        base = (
   158	            Path(config_dir)
   159	            if config_dir is not None
   160	            else Path(env.get(CONFIG_DIR_ENV, str(DEFAULT_CONFIG_DIR))).expanduser()
   161	        )
   162	        decision_log_dir = Path(
   163	            env.get(DECISION_LOG_DIR_ENV, str(base / "decision-log"))
   164	        ).expanduser()
   165	        return cls(
   166	            config_dir=base,
   167	            heartbeat_path=base / "heartbeat",
   168	            decision_log_dir=decision_log_dir,
   169	            capacity_path=capacity_path_from_env(base, env),
   170	            bridge_events_path=Path(
   171	                env.get(BRIDGE_EVENTS_FILE_ENV, str(base / "bridge-events.jsonl"))
   172	            ).expanduser(),
   173	            jira_agent_class=env.get(JIRA_AGENT_CLASS_ENV, "subscription-claude"),
   174	        )
   175	
  1810	
  1811	    def _drain(self) -> None:
  1812	        """Write the shutdown markers and exit cleanly.
  1813	
  1814	        The skeleton has no in-flight LLM consultation to finish (§9 L5
  1815	        step 1 is a no-op here); it writes the ``shutdown_began`` /
  1816	        ``shutdown_complete`` pair (step 2) so L6 crash-recovery can tell a
  1817	        clean shutdown from a crash.
  1818	        """
  1819	        drain_id = uuid.uuid4().hex
  1820	        self._decision_log.append(
  1821	            {
  1822	                "ts": self._clock().isoformat(),
  1823	                "event": "shutdown_began",
  1824	                "engine_version": self._engine.engine_version,
  1825	                "decision_id": drain_id,
  1826	                "pid": os.getpid(),
  1827	            }
  1828	        )
  1829	        # (29f: finish in-flight LLM consultation here, max-wait 60s.)
  1830	        self._decision_log.append(
  1831	            {
  1832	                "ts": self._clock().isoformat(),
  1833	                "event": "shutdown_complete",
  1834	                "engine_version": self._engine.engine_version,
  1835	                "decision_id": drain_id,
  1836	                "pid": os.getpid(),
  1837	            }
  1838	        )
  1839	        logger.info("[pipeline_coordinator] drain complete — exiting cleanly")
  1840	

exec
/bin/bash -lc "nl -ba backend/tests/test_pipeline_coordinator_integration.py | sed -n '150,210p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   150	        if "coord-mode:rescue" in ticket.labels:
   151	            return (
   152	                SituationProfile(
   153	                    urgency=Level.HIGH,
   154	                    risk=Level.MEDIUM,
   155	                    novelty=Level.MEDIUM,
   156	                    reversibility=Level.LOW,
   157	                    runner_stuck_hours=25.0,
   158	                ),
   159	                RESCUE_MODE,
   160	            )
   161	        return (
   162	            SituationProfile(
   163	                urgency=Level.HIGH,
   164	                risk=Level.LOW,
   165	                novelty=Level.LOW,
   166	                reversibility=Level.HIGH,
   167	            ),
   168	            None,
   169	        )
   170	
   171	    def execute(self, action: Action, _ctx: DecisionContext) -> dict[str, Any]:
   172	        if action.kind == ACTION_RELABEL:
   173	            ticket = self.tickets[action.target]
   174	            labels = set(ticket.labels)
   175	            for label in action.params.get("remove", []):
   176	                labels.discard(label)
   177	                self.removed_labels.append((action.target, label))
   178	            for label in action.params.get("add", []):
   179	                labels.add(label)
   180	            self.tickets[action.target] = Ticket(
   181	                key=ticket.key,
   182	                status=ticket.status,
   183	                issuetype=ticket.issuetype,
   184	                areas=ticket.areas,
   185	                labels=tuple(sorted(labels)),
   186	                assignee=ticket.assignee,
   187	                comments=ticket.comments,
   188	                in_progress_since=ticket.in_progress_since,
   189	                claim_started_at=ticket.claim_started_at,
   190	                has_live_runner=ticket.has_live_runner,
   191	                blocked_by=ticket.blocked_by,
   192	                merger_fail_count=ticket.merger_fail_count,
   193	                revert_count_24h=ticket.revert_count_24h,
   194	            )
   195	        elif action.kind == ACTION_ESCALATE:
   196	            self.operator_mentions.append(
   197	                (action.target, str(action.params.get("reason", "")), "high")
   198	            )
   199	        result = {
   200	            "kind": action.kind,
   201	            "target": action.target,
   202	            "executed": True,
   203	            "shadow": False,
   204	        }
   205	        self.action_results.append(result)
   206	        return result
   207	
   208	    # ── ColdStartGateway subset used by startup() ──
   209	    def audit_infra(self) -> InfraAuditResult:
   210	        return InfraAuditResult()

exec
/bin/bash -lc "nl -ba backend/agents/idempotency.py | sed -n '1,220p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
     1	"""SQLite-backed idempotency keys for runner external mutations.
     2	
     3	The runner retries whole ticks.  Without a local dedup store, retried
     4	JIRA comments and transitions can create duplicate operator-visible
     5	side effects.  This module keeps successful mutation responses for 24h
     6	under ``~/.config/omnisight/idem-keys.db`` and returns the cached
     7	response when the same key is seen again.
     8	"""
     9	
    10	from __future__ import annotations
    11	
    12	import functools
    13	import json
    14	import os
    15	import sqlite3
    16	import time
    17	from pathlib import Path
    18	from typing import Any, Callable, TypeVar
    19	
    20	
    21	def _resolve_idempotency_db_path() -> Path:
    22	    """Resolve the SQLite path from env override or fall back to the legacy default.
    23	
    24	    OP-783: a multi-instance runner host runs several runner processes in
    25	    parallel; each one needs its own idempotency DB so that two
    26	    in-flight transitions for the same JIRA key (one per instance, both
    27	    targeting different tickets) don't share a row and silently dedup
    28	    each other. The launcher script sets ``OMNISIGHT_IDEMPOTENCY_DB_PATH``
    29	    to ``~/.config/omnisight/idem-keys-<bot>.db``; default-instance
    30	    runners and library callers without the env var keep the legacy
    31	    ``~/.config/omnisight/idem-keys.db`` path (backwards-compat).
    32	    """
    33	    override = os.environ.get("OMNISIGHT_IDEMPOTENCY_DB_PATH", "").strip()
    34	    if override:
    35	        return Path(override).expanduser()
    36	    return Path("~/.config/omnisight/idem-keys.db").expanduser()
    37	
    38	
    39	IDEMPOTENCY_DB = _resolve_idempotency_db_path()
    40	TTL_SECONDS = 24 * 60 * 60
    41	
    42	F = TypeVar("F", bound=Callable[..., Any])
    43	
    44	
    45	class IdempotencyStore:
    46	    """Tiny SQLite cache keyed by caller-provided idempotency strings."""
    47	
    48	    def __init__(self, path: Path = IDEMPOTENCY_DB, ttl_seconds: int = TTL_SECONDS) -> None:
    49	        self.path = path
    50	        self.ttl_seconds = ttl_seconds
    51	
    52	    def _connect(self) -> sqlite3.Connection:
    53	        self.path.parent.mkdir(parents=True, exist_ok=True)
    54	        conn = sqlite3.connect(self.path)
    55	        conn.execute(
    56	            "CREATE TABLE IF NOT EXISTS idempotency_keys ("
    57	            "key TEXT PRIMARY KEY, response TEXT NOT NULL, ts REAL NOT NULL)"
    58	        )
    59	        return conn
    60	
    61	    def prune(self, now: float | None = None) -> None:
    62	        cutoff = (now if now is not None else time.time()) - self.ttl_seconds
    63	        with self._connect() as conn:
    64	            conn.execute("DELETE FROM idempotency_keys WHERE ts < ?", (cutoff,))
    65	
    66	    def get(self, key: str, now: float | None = None) -> Any | None:
    67	        current = now if now is not None else time.time()
    68	        with self._connect() as conn:
    69	            row = conn.execute(
    70	                "SELECT response, ts FROM idempotency_keys WHERE key = ?",
    71	                (key,),
    72	            ).fetchone()
    73	            if row is None:
    74	                return None
    75	            response, ts = row
    76	            if ts < current - self.ttl_seconds:
    77	                conn.execute("DELETE FROM idempotency_keys WHERE key = ?", (key,))
    78	                return None
    79	            return json.loads(response)
    80	
    81	    def put(self, key: str, response: Any, now: float | None = None) -> None:
    82	        current = now if now is not None else time.time()
    83	        with self._connect() as conn:
    84	            conn.execute(
    85	                "INSERT OR REPLACE INTO idempotency_keys (key, response, ts) "
    86	                "VALUES (?, ?, ?)",
    87	                (key, json.dumps(response, sort_keys=True, default=str), current),
    88	            )
    89	
    90	    def run(self, key: str, fn: Callable[[], Any]) -> Any:
    91	        self.prune()
    92	        cached = self.get(key)
    93	        if cached is not None:
    94	            return cached
    95	        response = fn()
    96	        self.put(key, response if response is not None else {})
    97	        return response
    98	
    99	
   100	DEFAULT_STORE = IdempotencyStore()
   101	
   102	
   103	def idempotent(key_arg: str = "idem_key", store: IdempotencyStore = DEFAULT_STORE) -> Callable[[F], F]:
   104	    """Decorator for functions that accept an ``idem_key`` keyword."""
   105	
   106	    def decorate(fn: F) -> F:
   107	        @functools.wraps(fn)
   108	        def wrapped(*args: Any, **kwargs: Any) -> Any:
   109	            key = kwargs.get(key_arg)
   110	            if not key:
   111	                return fn(*args, **kwargs)
   112	            return store.run(str(key), lambda: fn(*args, **kwargs))
   113	
   114	        return wrapped  # type: ignore[return-value]
   115	
   116	    return decorate

exec
/bin/bash -lc 'rg -n "DEFAULT_STORE|IDEMP|idempot" backend/agents/idempotency.py backend/agents/jira_dispatch.py backend/tests/test_idempotency.py' in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
backend/tests/test_idempotency.py:1:"""OP-749 runner idempotency tests."""
backend/tests/test_idempotency.py:10:from backend.agents.idempotency import IdempotencyStore
backend/tests/test_idempotency.py:21:def test_idempotency_store_replays_cached_success(tmp_path: Path) -> None:
backend/tests/test_idempotency.py:34:def test_idempotency_store_prunes_after_24h(tmp_path: Path) -> None:
backend/tests/test_idempotency.py:48:    monkeypatch.setattr(jd, "_request_idempotent", fake_request)
backend/agents/idempotency.py:1:"""SQLite-backed idempotency keys for runner external mutations.
backend/agents/idempotency.py:21:def _resolve_idempotency_db_path() -> Path:
backend/agents/idempotency.py:25:    parallel; each one needs its own idempotency DB so that two
backend/agents/idempotency.py:28:    each other. The launcher script sets ``OMNISIGHT_IDEMPOTENCY_DB_PATH``
backend/agents/idempotency.py:33:    override = os.environ.get("OMNISIGHT_IDEMPOTENCY_DB_PATH", "").strip()
backend/agents/idempotency.py:39:IDEMPOTENCY_DB = _resolve_idempotency_db_path()
backend/agents/idempotency.py:46:    """Tiny SQLite cache keyed by caller-provided idempotency strings."""
backend/agents/idempotency.py:48:    def __init__(self, path: Path = IDEMPOTENCY_DB, ttl_seconds: int = TTL_SECONDS) -> None:
backend/agents/idempotency.py:56:            "CREATE TABLE IF NOT EXISTS idempotency_keys ("
backend/agents/idempotency.py:64:            conn.execute("DELETE FROM idempotency_keys WHERE ts < ?", (cutoff,))
backend/agents/idempotency.py:70:                "SELECT response, ts FROM idempotency_keys WHERE key = ?",
backend/agents/idempotency.py:77:                conn.execute("DELETE FROM idempotency_keys WHERE key = ?", (key,))
backend/agents/idempotency.py:85:                "INSERT OR REPLACE INTO idempotency_keys (key, response, ts) "
backend/agents/idempotency.py:100:DEFAULT_STORE = IdempotencyStore()
backend/agents/idempotency.py:103:def idempotent(key_arg: str = "idem_key", store: IdempotencyStore = DEFAULT_STORE) -> Callable[[F], F]:
backend/agents/jira_dispatch.py:46:from backend.agents.idempotency import DEFAULT_STORE
backend/agents/jira_dispatch.py:182:    state, and idempotency DBs.
backend/agents/jira_dispatch.py:331:def _request_idempotent(
backend/agents/jira_dispatch.py:338:    return DEFAULT_STORE.run(idem_key, lambda: _request(client, method, path, body, idem_key))
backend/agents/jira_dispatch.py:1589:    Used by the runner's Phase 1.5 idempotency gate (OP-691): if the agent
backend/agents/jira_dispatch.py:1669:    _request_idempotent(
backend/agents/jira_dispatch.py:1696:        _request_idempotent(
backend/agents/jira_dispatch.py:1705:        _request_idempotent(
backend/agents/jira_dispatch.py:1714:        _request_idempotent(
backend/agents/jira_dispatch.py:1779:        _request_idempotent(
backend/agents/jira_dispatch.py:1886:            _request_idempotent(
backend/agents/jira_dispatch.py:1918:    _request_idempotent(
backend/agents/jira_dispatch.py:1923:    _request_idempotent(
backend/agents/jira_dispatch.py:1992:    _request_idempotent(
backend/agents/jira_dispatch.py:2016:    _request_idempotent(
backend/agents/jira_dispatch.py:2025:    _request_idempotent(
backend/agents/jira_dispatch.py:2035:    _request_idempotent(
backend/agents/jira_dispatch.py:2045:    _request_idempotent(
backend/agents/jira_dispatch.py:2745:# ``update.labels.add`` is set-union (idempotent), not compare-and-swap,
backend/agents/jira_dispatch.py:2757:# over the readback set* (not by "did my idempotent write succeed"), two
backend/agents/jira_dispatch.py:2815:      re-claiming a ticket we already held (idempotent re-entry after a

exec
/bin/bash -lc "nl -ba backend/tests/test_idempotency.py | sed -n '1,90p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
     1	"""OP-749 runner idempotency tests."""
     2	
     3	from __future__ import annotations
     4	
     5	from pathlib import Path
     6	
     7	import pytest
     8	
     9	from backend import audit
    10	from backend.agents.idempotency import IdempotencyStore
    11	from backend.agents import jira_dispatch as jd
    12	
    13	
    14	class _Client:
    15	    agent_class = "subscription-codex"
    16	    base_url = "https://jira.example/rest/api/3"
    17	    auth_header = "Basic x"
    18	    bot_account_id = "acct-1"
    19	
    20	
    21	def test_idempotency_store_replays_cached_success(tmp_path: Path) -> None:
    22	    store = IdempotencyStore(tmp_path / "idem-keys.db")
    23	    calls = {"count": 0}
    24	
    25	    def mutate() -> dict[str, str]:
    26	        calls["count"] += 1
    27	        return {"ok": "yes"}
    28	
    29	    assert store.run("comment-OP-749-fixed", mutate) == {"ok": "yes"}
    30	    assert store.run("comment-OP-749-fixed", mutate) == {"ok": "yes"}
    31	    assert calls["count"] == 1
    32	
    33	
    34	def test_idempotency_store_prunes_after_24h(tmp_path: Path) -> None:
    35	    store = IdempotencyStore(tmp_path / "idem-keys.db")
    36	    store.put("stale", {"ok": True}, now=100)
    37	
    38	    assert store.get("stale", now=100 + store.ttl_seconds + 1) is None
    39	
    40	
    41	def test_jira_mutations_share_stable_subkeys(monkeypatch) -> None:
    42	    calls: list[tuple[str, str, str]] = []
    43	
    44	    def fake_request(client, method, path, body, idem_key):
    45	        calls.append((method, path, idem_key))
    46	        return {}
    47	
    48	    monkeypatch.setattr(jd, "_request_idempotent", fake_request)
    49	    monkeypatch.setattr(jd, "get_issue_status", lambda client, key: "In Progress")
    50	
    51	    jd.transition_to_in_progress(_Client(), "OP-749", idem_key="pickup-1")
    52	    jd.transition_back_to_todo(_Client(), "OP-749", "reason", idem_key="todo-1")
    53	    jd.transition_to_under_review_if_needed(_Client(), "OP-749", idem_key="review-1")
    54	    jd.add_comment(_Client(), "OP-749", "done", idem_key="comment-1")
    55	    jd.clear_assignee(_Client(), "OP-749", idem_key="clear-1")
    56	
    57	    assert ("PUT", "/issue/OP-749", "pickup-1-assign") in calls
    58	    assert ("POST", "/issue/OP-749/transitions", "pickup-1-transition") in calls
    59	    assert ("POST", "/issue/OP-749/comment", "pickup-1-comment") in calls
    60	    assert ("PUT", "/issue/OP-749", "todo-1-clear-assignee") in calls
    61	    assert ("POST", "/issue/OP-749/transitions", "todo-1-transition") in calls
    62	    assert ("POST", "/issue/OP-749/transitions", "review-1") in calls
    63	    assert ("POST", "/issue/OP-749/comment", "comment-1") in calls
    64	    assert ("PUT", "/issue/OP-749", "clear-1") in calls
    65	
    66	
    67	@pytest.mark.asyncio
    68	async def test_audit_log_dedups_same_action_entity_second() -> None:
    69	    class FakeConn:
    70	        def __init__(self) -> None:
    71	            self.fetchrow_calls = 0
    72	            self.insert_seen = False
    73	
    74	        async def execute(self, *args, **kwargs):
    75	            return None
    76	
    77	        async def fetchrow(self, query, *args):
    78	            self.fetchrow_calls += 1
    79	            if "ORDER BY id DESC LIMIT 1" in query:
    80	                return None
    81	            if "SELECT id FROM audit_log" in query:
    82	                return {"id": 7}
    83	            if "INSERT INTO audit_log" in query:
    84	                self.insert_seen = True
    85	                return {"id": 8}
    86	            raise AssertionError(query)
    87	
    88	    conn = FakeConn()
    89	
    90	    row_id = await audit._log_impl(

codex
**BLOCKER Findings**

1. **Dispatch table does not match the actual action vocabulary.**  
   The design maps `clear_assignee`, `reset_to_todo`, `transition_under_review`, and `mark_resumable`, but the executable closed set is `file_ticket`, `relabel`, `transition`, `mention_operator`, `mark_for_followup`, `escalate`, `noop` in [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:59). Rules emit `Action.transition(... to_status="To Do")` for stuck tickets, `Action.relabel(...)` for stale claims/waiting labels, and `Action.escalate(...)` for merger-repeat-fail, not the gateway method names: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:678), [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:704), [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:724). A LiveActionExecutor built from the design would fail to execute most real Tier-1/Tier-2 actions unless it translates ADR actions to gateway ops.

2. **Q1: `escalate` is emitted today, and `urgency="critical"` would silently miss the operator-action label.**  
   `Action.escalate` is documented as “needs-operator-action label + reason comment” in [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:206). It is emitted by deterministic `merger-repeat-fail` at [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:668), by Tier-2 accepted LLM actions via `"escalate"` mapping at [pipeline_coordinator_llm_consultation.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_llm_consultation.py:118), and by Tier-2 failure paths at [pipeline_coordinator_llm_consultation.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_llm_consultation.py:895) and [pipeline_coordinator_llm_consultation.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_llm_consultation.py:914). `JiraDispatchColdStartGateway.mention_operator` only adds `needs-operator-action` when `urgency == "high"`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:807). So the proposed `critical` urgency would add only a comment, violating `Action.escalate` semantics. Implement `escalate` as a distinct path or map it to `mention_operator(..., urgency="high")` plus explicit result metadata.

3. **Q2: gateway ops do not accept stable idempotency keys, so restarts can double-apply visible side effects.**  
   `JiraDispatchColdStartGateway.mark_resumable`, `transition_under_review`, `reset_to_todo`, `clear_assignee`, and `mention_operator` take only key/message args and call `jira_dispatch` without idem keys: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:735), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:746), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:757), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:794), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:807). The underlying helpers default to random UUID-based idem keys when none is passed: [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:1664), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:1964), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:2014), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:2023), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:2032). The durable idempotency store works only when the same key is reused: [idempotency.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/idempotency.py:90). D2 cannot be achieved through this gateway unchanged.

4. **Q3: no current per-tick mutation cap; `RetryableExecutor` is the wrong abstraction.**  
   `run_once` executes every non-noop action returned by the engine with an unbounded list comprehension: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1699). `RetryableExecutor` is an async API retry/DLQ wrapper with token preflight, not a blast-radius limiter for synchronous JIRA mutations: [rate_limiter.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/rate_limiter.py:431). For first live flip, safe ceiling should be **1 executed coordinator action per tick**, with a separate allowlist/count for sub-REST calls hidden inside composite actions like `reset_to_todo`. Anything more should require explicit config after canary evidence.

5. **Q4/reuse: `JiraDispatchColdStartGateway` is not cleanly safe as the steady-state backend without adaptation.**  
   It encodes cold-start semantics in destructive operations: `reset_to_todo` always comments with a cold-start Startup-2(c) reason: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:757). It is also intentionally fail-open and swallows operation exceptions internally: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:747), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:762), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:810). That means a LiveActionExecutor cannot reliably return `executed=True` vs `executed=False` from gateway return values. Reuse the low-level `jira_dispatch` helpers or wrap a revised gateway that accepts action-specific reasons, idem keys, and propagates outcome.

6. **Q5: acting=True first boot combines live cold-start and live steady-state with no shared mutation budget.**  
   `build_default_coordinator(acting=True)` flips action executor, sprint replan, and cold-start gateway together: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1877), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1895), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1896), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1902). Startup can mutate many tickets in Startup-2 and Startup-3 loops before steady-state begins: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1594), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1630). First live boot needs a global boot cap or phased rollout; `cold_start_complete=[]` in one environment is not a design safety property.

**SHOULD-FIX Findings**

7. **D1 fail-open is too broad for autonomous live mutation.**  
   Continuing after one failed action is reasonable, but reporting success is impossible if gateway methods swallow exceptions and return `None`. Current daemon does not catch executor exceptions either; failures in `self._action_executor` would abort the tick before logging: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1703). The executor should catch per action, but gateway mutation wrappers should expose success/failure.

8. **D4 env gate is not implemented in CLI.**  
   `main()` always calls `build_default_coordinator(config)` with default `acting=False`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1935). `CoordinatorConfig.from_env` reads config paths and `jira_agent_class`, not `OMNISIGHT_COORDINATOR_ACTING`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:150). Default safety is currently safe, but the planned flip path does not exist.

9. **Cold-start gateway ignores configured JIRA agent class.**  
   `_default_cold_start_gateway(config, acting=...)` constructs `JiraDispatchColdStartGateway()` without passing `config.jira_agent_class`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1149). The gateway default is `agent_class="claude"`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:625), while `CoordinatorConfig` defaults to `"subscription-claude"`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:128). Reusing it for steady-state would preserve that identity mismatch.

10. **Crash recovery already assumes only ADR `relabel`/`transition` are recoverable, which conflicts with the design’s gateway-kind table.**  
    L6 replays logged `ACTION_RELABEL`/`ACTION_TRANSITION`, not `reset_to_todo`/`mark_resumable`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1523). It only reissues transition when `to_status == "Under Review"` and marks all other relabel/transition as completed without doing anything: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1525). Live executor semantics and crash recovery semantics need to be aligned before acting=True.

**NICE / Missed By Design (Q6)**

11. **No handling for `file_ticket`, `relabel`, `transition`, or `mark_for_followup` despite all being allowed actions.**  
    These are in `ALLOWED_ACTION_KINDS`: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:67). Sprint replan already has a narrower action layer pattern that rejects unsupported kinds explicitly: [sprint_replan.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/sprint_replan.py:141). Live executor needs the same explicit allow/reject behavior plus tests.

12. **`dry_run` is not enforced by the action layer.**  
    Actions default `dry_run=True`: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:83), and the daemon records aggregate `dry_run`, but still passes all non-noop actions to the executor: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1703). Acting mode must define whether executor ignores `dry_run`, flips it, or refuses dry-run actions.

13. **Operator override escape hatch must be enforced in executor, not only rules.**  
    `coord-skip` short-circuits Tier-1 rules: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:618). Tier-2 accepted actions can still target a skipped ticket unless the live executor rechecks current labels before mutation.

Bottom line: reusing cold-start code as an implementation reference is useful, but reusing `JiraDispatchColdStartGateway` directly as the live steady-state executor is not correct yet. The live layer needs ADR-action dispatch, stable idem keys, outcome propagation, an initial 1-action/tick cap, explicit `escalate` semantics, and a separate first-boot budget for cold-start mutations.
tokens used
94,977
**BLOCKER Findings**

1. **Dispatch table does not match the actual action vocabulary.**  
   The design maps `clear_assignee`, `reset_to_todo`, `transition_under_review`, and `mark_resumable`, but the executable closed set is `file_ticket`, `relabel`, `transition`, `mention_operator`, `mark_for_followup`, `escalate`, `noop` in [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:59). Rules emit `Action.transition(... to_status="To Do")` for stuck tickets, `Action.relabel(...)` for stale claims/waiting labels, and `Action.escalate(...)` for merger-repeat-fail, not the gateway method names: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:678), [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:704), [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:724). A LiveActionExecutor built from the design would fail to execute most real Tier-1/Tier-2 actions unless it translates ADR actions to gateway ops.

2. **Q1: `escalate` is emitted today, and `urgency="critical"` would silently miss the operator-action label.**  
   `Action.escalate` is documented as “needs-operator-action label + reason comment” in [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:206). It is emitted by deterministic `merger-repeat-fail` at [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:668), by Tier-2 accepted LLM actions via `"escalate"` mapping at [pipeline_coordinator_llm_consultation.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_llm_consultation.py:118), and by Tier-2 failure paths at [pipeline_coordinator_llm_consultation.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_llm_consultation.py:895) and [pipeline_coordinator_llm_consultation.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_llm_consultation.py:914). `JiraDispatchColdStartGateway.mention_operator` only adds `needs-operator-action` when `urgency == "high"`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:807). So the proposed `critical` urgency would add only a comment, violating `Action.escalate` semantics. Implement `escalate` as a distinct path or map it to `mention_operator(..., urgency="high")` plus explicit result metadata.

3. **Q2: gateway ops do not accept stable idempotency keys, so restarts can double-apply visible side effects.**  
   `JiraDispatchColdStartGateway.mark_resumable`, `transition_under_review`, `reset_to_todo`, `clear_assignee`, and `mention_operator` take only key/message args and call `jira_dispatch` without idem keys: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:735), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:746), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:757), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:794), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:807). The underlying helpers default to random UUID-based idem keys when none is passed: [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:1664), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:1964), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:2014), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:2023), [jira_dispatch.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/jira_dispatch.py:2032). The durable idempotency store works only when the same key is reused: [idempotency.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/idempotency.py:90). D2 cannot be achieved through this gateway unchanged.

4. **Q3: no current per-tick mutation cap; `RetryableExecutor` is the wrong abstraction.**  
   `run_once` executes every non-noop action returned by the engine with an unbounded list comprehension: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1699). `RetryableExecutor` is an async API retry/DLQ wrapper with token preflight, not a blast-radius limiter for synchronous JIRA mutations: [rate_limiter.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/rate_limiter.py:431). For first live flip, safe ceiling should be **1 executed coordinator action per tick**, with a separate allowlist/count for sub-REST calls hidden inside composite actions like `reset_to_todo`. Anything more should require explicit config after canary evidence.

5. **Q4/reuse: `JiraDispatchColdStartGateway` is not cleanly safe as the steady-state backend without adaptation.**  
   It encodes cold-start semantics in destructive operations: `reset_to_todo` always comments with a cold-start Startup-2(c) reason: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:757). It is also intentionally fail-open and swallows operation exceptions internally: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:747), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:762), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:810). That means a LiveActionExecutor cannot reliably return `executed=True` vs `executed=False` from gateway return values. Reuse the low-level `jira_dispatch` helpers or wrap a revised gateway that accepts action-specific reasons, idem keys, and propagates outcome.

6. **Q5: acting=True first boot combines live cold-start and live steady-state with no shared mutation budget.**  
   `build_default_coordinator(acting=True)` flips action executor, sprint replan, and cold-start gateway together: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1877), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1895), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1896), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1902). Startup can mutate many tickets in Startup-2 and Startup-3 loops before steady-state begins: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1594), [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1630). First live boot needs a global boot cap or phased rollout; `cold_start_complete=[]` in one environment is not a design safety property.

**SHOULD-FIX Findings**

7. **D1 fail-open is too broad for autonomous live mutation.**  
   Continuing after one failed action is reasonable, but reporting success is impossible if gateway methods swallow exceptions and return `None`. Current daemon does not catch executor exceptions either; failures in `self._action_executor` would abort the tick before logging: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1703). The executor should catch per action, but gateway mutation wrappers should expose success/failure.

8. **D4 env gate is not implemented in CLI.**  
   `main()` always calls `build_default_coordinator(config)` with default `acting=False`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1935). `CoordinatorConfig.from_env` reads config paths and `jira_agent_class`, not `OMNISIGHT_COORDINATOR_ACTING`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:150). Default safety is currently safe, but the planned flip path does not exist.

9. **Cold-start gateway ignores configured JIRA agent class.**  
   `_default_cold_start_gateway(config, acting=...)` constructs `JiraDispatchColdStartGateway()` without passing `config.jira_agent_class`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1149). The gateway default is `agent_class="claude"`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:625), while `CoordinatorConfig` defaults to `"subscription-claude"`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:128). Reusing it for steady-state would preserve that identity mismatch.

10. **Crash recovery already assumes only ADR `relabel`/`transition` are recoverable, which conflicts with the design’s gateway-kind table.**  
    L6 replays logged `ACTION_RELABEL`/`ACTION_TRANSITION`, not `reset_to_todo`/`mark_resumable`: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1523). It only reissues transition when `to_status == "Under Review"` and marks all other relabel/transition as completed without doing anything: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1525). Live executor semantics and crash recovery semantics need to be aligned before acting=True.

**NICE / Missed By Design (Q6)**

11. **No handling for `file_ticket`, `relabel`, `transition`, or `mark_for_followup` despite all being allowed actions.**  
    These are in `ALLOWED_ACTION_KINDS`: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:67). Sprint replan already has a narrower action layer pattern that rejects unsupported kinds explicitly: [sprint_replan.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/sprint_replan.py:141). Live executor needs the same explicit allow/reject behavior plus tests.

12. **`dry_run` is not enforced by the action layer.**  
    Actions default `dry_run=True`: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:83), and the daemon records aggregate `dry_run`, but still passes all non-noop actions to the executor: [pipeline_coordinator.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator.py:1703). Acting mode must define whether executor ignores `dry_run`, flips it, or refuses dry-run actions.

13. **Operator override escape hatch must be enforced in executor, not only rules.**  
    `coord-skip` short-circuits Tier-1 rules: [pipeline_coordinator_rules.py](/home/user/work/sora/OmniSight-Productizer/backend/agents/pipeline_coordinator_rules.py:618). Tier-2 accepted actions can still target a skipped ticket unless the live executor rechecks current labels before mutation.

Bottom line: reusing cold-start code as an implementation reference is useful, but reusing `JiraDispatchColdStartGateway` directly as the live steady-state executor is not correct yet. The live layer needs ADR-action dispatch, stable idem keys, outcome propagation, an initial 1-action/tick cap, explicit `escalate` semantics, and a separate first-boot budget for cold-start mutations.
