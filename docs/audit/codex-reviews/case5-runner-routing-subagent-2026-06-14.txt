Sub-agent adversarial audit — Case 5 runner routing (2026-06-14)
VERDICT: NEEDS-WORK
1. BLOCKER — self-tenant push goes to PRODUCTIZER not conference-appliance: resolve_gerrit_push_identity for self-tenant hardcodes project=GERRIT_PROJECT_PATH=omnisight/OmniSight-Productizer (jira_dispatch.py:842-851, const :502). conf tickets have no tenant: label → default omnisight-self → pushes to productizer. project_key route is JIRA key (OP) → all 21 conf tickets in OP → resolves to productizer default (delivery_target.py:132-134). Fix: route by repo: LABEL, thread routed descriptor straight into push, bypass resolve_gerrit_push_identity; customer-tenant path REFUSES shared bot key + refuses project==productizer.
2. BLOCKER — dispatch seam runs agent in wrapper's PRODUCTIZER clone; design forgot ephemeral-wrapper layer. Must use contribution_runner self-clone pattern (own clone of conference-appliance). Add assertion.
3. MAJOR — --reference mirror is productizer-only; contribution_runner does plain full clone (no --reference/--depth); _repo_dir_name keys worktree on repo URL only → 2 concurrent conf tickets in same workspace_root collide. Implement per-instance path NOW; verify orphan-reaper/workspace-safety don't reap it.
4. MAJOR — claim labels/idem/assignee/metrics keyed by ticket+bot NOT project; no collision (OP keys unique) BUT fleet contention real: conf + productizer compete for same fixed pool + provider quota. "0 new runners" undersells the trade-off.
5. MAJOR — fail-closed NOT achievable at seam as designed: a typo'd repo: label fails the positive predicate and falls through to _invoke_cli on productizer workspace (silent fallback). Need explicit: repo: present AND unresolved → hard abstain BEFORE normal path.
6. MINOR — co-author trailer + body camviewpro/GitHub-hardcoded; Gerrit lane needs Change-Id hook + dual co-author trailers, all absent from contribution lane.
7. MINOR — Case 5 epic correctly fences hw/partnership gates but OP-2180 integration ticket not in the EPIC tree doc.
