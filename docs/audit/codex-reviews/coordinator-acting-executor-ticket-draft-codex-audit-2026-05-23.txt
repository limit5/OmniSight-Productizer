Reading prompt from stdin...
OpenAI Codex v0.128.0 (research preview)
--------
workdir: /home/user/work/sora/OmniSight-Productizer
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, /home/user/.codex/memories]
reasoning effort: none
reasoning summaries: none
session id: 019e5076-5a76-73a0-94bc-2e5b38deb451
--------
user
Audit a TICKET DRAFT for coverage + consistency against a file-ready design. This is NOT a code audit — it is a coverage/completeness audit.

READ:
- docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md  (the ticket draft — your target; META + T1-T7 + a coverage map)
- docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md  (the file-ready design — the source of truth; includes a "Round-4 verdict: file-ready-with-conditions" section with 3 conditions)
- docs/audit/codex-reviews/coordinator-acting-executor-design-v4-codex-audit-2026-05-22.txt  (your own round-4 audit + the 3 implementation conditions you set)

You set the v4 conditions yourself in round 4. Now verify the ticket draft would, if filed and implemented, fully realize v4 with nothing missing and nothing extra.

ANSWER, as a prioritized checklist (each item: GAP / SCOPE-CREEP / CONVENTION / OK, with the ticket id + v4 section):

1. 1:1 COVERAGE: Does every v4 deliverable map to a ticket AC? Walk the v4 doc section by section (LiveActionExecutor dispatch, Idempotency, dry_run Option B, Blast-radius, Cold-start budget, Crash recovery, coord-skip/destructive recheck, Activation, Tests, file_ticket accounting, capability scope) AND the 3 round-4 conditions (Option B rebuild DecisionResult + preserve decision_id; destructive recheck fail-closed on probe uncertainty; tick_intent stores final stable idem keys + 24h replay window). Flag any v4 item with NO ticket. Cross-check the draft's own coverage map for honesty.

2. SCOPE CREEP: Any ticket AC that introduces work NOT in v4? Flag it.

3. FILING CONVENTIONS (verify against the stated rules in the draft): 4-AC completeness per ticket (Code/Deploy/Integration/Exercised + Go-Live); area:* spans all areas each ticket's ACs touch (9-value whitelist backend/db/devops/docs/embedded/frontend/security/tests/tooling); tier correctness; T7 correctly tier:X + requires:operator-approval as the only HUMAN-ONLY ticket.

4. DEPENDENCY ORDERING: Is the blockedBy graph (T2,T3,T4,T6 ← T1; T5 ← T1+T3; T7 ← all + OP-1555) correct and complete? Any missing edge (e.g. should T4 fairness-cursor or T5 intent-log depend on T3's DecisionResult/log-shape changes)? Any ticket that should be split or merged?

5. Verify the code anchors the ACs cite still exist and are accurate (spot-check: run_once :1699-1703/:1708, _build_tick_record :1394, L6 :1514/:1493, from_env :149, main :1936, _default_cold_start_gateway :1149, fetch_ticket_labels jira_dispatch.py:1826, get_issue_status :1586, _ticket_has_live_runner :986-990, idempotency TTL :40, _request_idempotent :331, sprint_replan :141, pipeline-coordinator.service :29-32, the 3 test files).

Output a fix checklist. FINAL VERDICT (one line): file-ready / needs-revision. Audit only, modify nothing.

warning: Codex could not find bubblewrap on PATH. Install bubblewrap with your OS package manager. See the sandbox prerequisites: https://developers.openai.com/codex/concepts/sandboxing#prerequisites. Codex will use the vendored bubblewrap in the meantime.
codex
I’ll audit only and leave the worktree untouched. I’m going to read the three source documents first, then spot-check the cited code anchors before producing the prioritized fix checklist.
exec
/bin/bash -lc "sed -n '1,260p' docs/audit/codex-reviews/coordinator-acting-executor-design-v4-codex-audit-2026-05-22.txt" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "sed -n '1,260p' docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
# Coordinator §6 LiveActionExecutor + acting-flip — TICKET DRAFT (for codex coverage audit)

**Status**: DRAFT (2026-05-23) for codex coverage audit BEFORE filing. Source of
truth = `docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md`
(codex round-4 verdict: file-ready-with-conditions). This draft decomposes v4 into
a META + 7 children. Codex is asked to confirm **1:1 coverage** (every v4
deliverable + each of the 3 file-ready conditions maps to a ticket AC), **no
scope creep** beyond v4, and **filing-convention correctness** (4-AC, area labels,
tier, dependency order). Nothing is filed until that audit passes.

Conventions applied (project memory):
- **4-AC discipline**: every impl ticket has Code / Deploy / Integration /
  Exercised AC + a Go-Live target (a Code-AC-only ticket = shipped-but-not-deployed
  by design → rejected at filing).
- **area:* whitelist** (9 values): backend / db / devops / docs / embedded /
  frontend / security / tests / tooling. A ticket's area:* must span ALL areas its
  4 ACs touch (else §11 revert loop).
- **action-tag**: `[META|OP|GATE][coord-acting] Title`.
- **tier**: S/M/L/X. Tier:S backend tickets need `capability:enable=gerrit_push`.
  HUMAN-ONLY / operator-approval tickets MUST be **tier:X** (runner JQL only
  excludes tier:X).
- Default code posture stays `acting=False` (shadow) until the GATE ticket (T7).

---

## META — [META][coord-acting] Coordinator §6 live action executor + acting-flip (ADR-0021)

- **type**: meta (has real children — safe per type:meta routing rule)
- **labels**: `scope:coord-acting`, `area:backend`, fixVersion TBD
- **Goal**: build the unbuilt ADR-0021 §6 autonomous action layer so the
  coordinator can, when `acting=True`, mutate live JIRA + steer the runner fleet —
  default-safe (shadow) until the operator flip. Closes the `build_default_coordinator(acting=True)`
  ValueError gap (pipeline_coordinator.py:1883).
- **Children**: T1→T7 (below). **Blocks**: relates to OP-1555 (coordinator
  maint-window activation) — T7 is blockedBy OP-1555.
- **DoD**: all children Done; codex round-4 conditions evidenced; decision-log
  shows `executed:True` actions matching intent in a phased first-flip.

---

## T1 — [OP][coord-acting] LiveActionExecutor core (dispatch + idempotency + outcomes)
- **tier**: M · **area**: `area:backend`, `area:tests` · **agent**: auto
- **v4 coverage**: §LiveActionExecutor dispatch table, §Idempotency, B1/B2/B5/N11, file_ticket accounting, S9 client-from-jira_agent_class.
- **Code AC**:
  - New `LiveActionExecutor` (pipeline_coordinator.py, drop-in for the
    `ActionExecutor` seam :295) dispatching on `action.kind` (the ADR closed set
    rules.py:67), mirroring `SprintReplanActionLayer.execute` (sprint_replan.py:141).
  - Calls **low-level `jira_dispatch` helpers** (NOT the fail-open cold-start
    gateway): transition_back_to_todo / transition_to_under_review_if_needed /
    add_label / remove_label / add_comment / clear_assignee.
  - `escalate` = add `needs-operator-action` label **+** reason comment, always
    both (NOT urgency-string driven) (B2).
  - Client built from `config.jira_agent_class` (S9).
  - **Idempotency**: key `coord:{kind}:{target}:{content_hash}` where content_hash
    = stable digest of canonical semantic params **excluding volatile fields**
    (decision_id, timestamps, transient observations); operator-visible comment
    text included so a reworded comment is a new action. Per-REST suffixes
    `:add:<l>` / `:remove:<l>` / `:clear-assignee` / `:transition` / `:comment` /
    `:label` (one key per PUT/POST; `_request_idempotent` caches by key
    jira_dispatch.py:331).
  - `file_ticket` NOT auto-executed → `mention_operator` + **two** result lines
    (the `executed:False (file_ticket,requires_operator)` AND the mention side
    effect) so decision-log readers don't misread (v4 NICE).
  - Result envelope matches `ShadowActionExecutor` shape (`executed`, `kind`,
    `target`, `outcome`/`error`) for shadow-vs-live diff.
- **Deploy AC**: ships in the coordinator package; default wiring stays
  `acting=False` (shadow) → no behavior change in prod until T7. `deployed:` =
  present in the next coordinator image/package build.
- **Integration AC**: in a unit harness, a rule emitting each `Action.kind` →
  the correct low-level `jira_dispatch` call with the correct idem key (mock
  dispatch); unknown kind → `executed:False,reason:"unsupported"`.
- **Exercised AC**: tests — per-kind dispatch; escalate adds label+comment; idem
  key stable across two ticks with different `decision_id` → one mutation; a
  reworded comment → second mutation (not deduped); composite relabel (2 labels)
  → 2 distinct PUTs neither deduped.
- **Go-Live**: shadow-safe; exercised in CI.

## T2 — [OP][coord-acting] Pre-mutation safety rechecks (coord-skip + destructive fail-closed)
- **tier**: M · **area**: `area:backend`, `area:tests` · **agent**: auto · **blockedBy**: T1
- **v4 coverage**: §coord-skip/destructive recheck, N13, condition #2.
- **Code AC**:
  - Before any mutation, executor reads live labels via
    **`fetch_ticket_labels(client, target)`** (jira_dispatch.py:1826); on
    exception → `executed:False,reason:"label_read_failed"` (**fail-CLOSED** — never
    mutate on unknown state). `coord-skip`/`coord-quarantine` present →
    `executed:False,reason:"coord_skip"`.
  - **For `transition → To Do` only** (destructive — clears assignee
    jira_dispatch.py:1991): a STRICT recheck wrapper requiring
    `get_issue_status(client, target)` (jira_dispatch.py:1586) still ∈ IN_PROGRESS
    names AND `not _ticket_has_live_runner(target)` (pipeline_coordinator.py:986).
    **`_ticket_has_live_runner` treats probe errors as "not live" (:987-990); the
    wrapper MUST flip that to fail-closed** → an unknown/failed probe abstains
    `executed:False,reason:"context_stale"` (condition #2).
- **Deploy AC**: rides in the same package; default acting=False unchanged.
- **Integration AC**: target carrying `coord-skip` → no mutation; status changed
  / runner now live before a `To Do` transition → abstains.
- **Exercised AC**: tests — coord-skip blocks; `fetch_ticket_labels` raises → no
  mutation (fail-closed); destructive recheck with now-live runner OR probe error
  → `context_stale`.
- **Go-Live**: shadow-safe; exercised in CI.

## T3 — [OP][coord-acting] run_once acting dry_run flip (Option B)
- **tier**: M · **area**: `area:backend`, `area:tests` · **agent**: auto · **blockedBy**: T1
- **v4 coverage**: §dry_run Option B wiring, Q7 ruling, condition #1.
- **Code AC**:
  - In `run_once` between `engine.evaluate` and the executor (:1699-1703): when
    `self._acting`, rewrite each non-noop action via
    `dataclasses.replace(a, dry_run=False)` (NoopAction skipped); `Action` is a
    frozen dataclass (rules.py:80).
  - **Rebuild the `DecisionResult`**: `result = dataclasses.replace(result,
    actions=actions)` so `_build_tick_record()` (:1394) AND the learning-loop
    writeback see `dry_run=false`; **preserve the same `decision_id`** (condition
    #1). Rewriting only a local tuple leaves the log reading `dry_run:true`.
  - Executor mutates iff `dry_run=False` (default-deny preserved: a stray
    `dry_run=True` cannot mutate).
- **Deploy AC**: default acting=False → flip is inert until T7.
- **Integration AC**: acting=True → every non-noop action reaches the executor
  with `dry_run=False` AND the logged tick record reads `dry_run:false`.
- **Exercised AC**: tests — acting flip rewrites + DecisionResult rebuilt +
  decision_id preserved + logged dry_run:false; acting=False leaves dry_run:true.
- **Go-Live**: shadow-safe.

## T4 — [OP][coord-acting] run_once blast-radius cap + fairness + kill-switch
- **tier**: M · **area**: `area:backend`, `area:tests` · **agent**: auto · **blockedBy**: T1
- **v4 coverage**: §Blast-radius, B4/S7, fairness cursor (Q13/Q17).
- **Code AC**:
  - `run_once` caps executed actions at `OMNISIGHT_COORDINATOR_MAX_ACTIONS_PER_TICK`
    (**default 1**); excess → `executed:False,reason:"tick_cap"`, logged.
  - **Per-action `try/except`** around each executor call so one failure logs
    `executed:False,error` and the tick STILL appends the record (fixes the
    current list-comprehension that aborts before :1709) (S7).
  - **Kill-switch** `OMNISIGHT_COORDINATOR_ACTING_KILL=1` → observe-only without
    restart.
  - **Fairness cursor**: round-robin over targets so capped ticks drain a backlog;
    cursor state **rebuilt from a bounded decision-log tail across all date files
    in the 24h window** (NOT a new opaque file — ADR §3.2 / ADR-0021:105), with a
    record-count cap as a perf guard (Q17).
- **Deploy AC**: default acting=False unchanged; cap default 1.
- **Integration AC**: > cap actions in one tick → capped + excess logged; a raising
  executor → tick still records; kill-switch forces observe-only.
- **Exercised AC**: tests — cap; per-action try/except keeps log; kill-switch;
  fairness cursor advances across capped ticks.
- **Go-Live**: shadow-safe.

## T5 — [OP][coord-acting] Crash-safe intent log + L6 replay
- **tier**: M · **area**: `area:backend`, `area:tests` · **agent**: auto · **blockedBy**: T1, T3
- **v4 coverage**: §Crash recovery, S10/Q11, condition #3, Q16.
- **Code AC**:
  - `run_once` writes a `tick_intent` record **before** executing (closes the
    execute-before-append window :1703-before-:1708): `{decision_id, planned:
    [{kind,target,idem_keys:[...]}], ts}` carrying the **final resolved stable idem
    keys** (condition #3, not just payloads). Outcome `decision_tick` record
    written after, same `decision_id` (correlation key — Q16; add `intent_seq`
    only if one decision ever emits multiple intents).
  - Persist `action_results` alongside `actions` in the tick record (:1394).
  - Extend L6 `_resolve_interrupted_actions` (:1514; log scan :1493 currently
    `decision_tick`+shutdown) to also scan `tick_intent`: a `tick_intent` whose
    matching `decision_tick` is absent → replay its planned actions under the SAME
    idem keys (idempotent — already-applied subcalls deduped by idempotency store
    :90). **Replay ONLY within the 24h idem TTL window** (idempotency.py:40);
    beyond it the store has expired → do NOT replay (condition #3).
- **Deploy AC**: default acting=False; intent records are harmless in shadow.
- **Integration AC**: simulate crash (intent written, no outcome) → L6 replays
  exactly the missing subcalls, no double-apply; intent older than 24h → skipped.
- **Exercised AC**: tests — intent-before-execute ordering; replay-on-missing-outcome
  idempotent; 24h-window cutoff; **update `test_l6_resolves_interrupted_actions_by_kind`
  (test_pipeline_coordinator_coldstart.py:473) to handle tick_intent**.
- **Go-Live**: shadow-safe.

## T6 — [OP][coord-acting] env-gate wiring + cold-start agent_class + per-phase caps
- **tier**: M · **area**: `area:backend`, `area:devops`, `area:tests` · **agent**: auto · **blockedBy**: T1
- **v4 coverage**: §env-gate (S8/D4), S9 agent_class, §Cold-start budget (B6/Q14), capability-scope clarification.
- **Code AC**:
  - `CoordinatorConfig.from_env` (:149) parses `OMNISIGHT_COORDINATOR_ACTING`
    (truthy → acting) into a new `acting: bool=False` field; `main` (:1936) builds
    `LiveActionExecutor` + `build_default_coordinator(acting=True, action_executor=...)`
    when set, else today's shadow path. `self._acting` is the single source of truth.
  - `_default_cold_start_gateway` (:1149) + `LiveActionExecutor` receive
    `config.jira_agent_class` (fixes S9: gateway default "claude" :621 vs config
    "subscription-claude" :128).
  - Per-phase cold-start mutation caps (env
    `OMNISIGHT_COORDINATOR_COLD_START_MAX_{INFRA,RECONCILE,SWEEP}`, **defaults
    1/1/1**): Startup-1 start_unit (:653), Startup-2 transitions/labels (:735),
    Startup-3 sweep (:776).
- **Deploy AC**: env var documented; **NO behavior change unless the env is set**
  (default-safe). Coordinator daemon is NOT capability-matrix gated (the flip is a
  daemon env authorization, not runner pickup — capability_matrix.yaml:27 is a
  different actor) — documented in the ticket.
- **Integration AC**: env unset → shadow (acting=False, ShadowActionExecutor); set
  → acting=True + LiveActionExecutor; `acting=True` without executor still raises
  (:1884). Cold-start caps enforced per phase.
- **Exercised AC**: tests — env-gate matrix; agent_class threaded (**update
  `test_default_cold_start_gateway_shadow_vs_acting` coldstart:954**); per-phase
  cap; **update `test_pipeline_coordinator_systemd.py:68` for new env**.
- **Go-Live**: shadow-safe (flip happens only in T7).

## T7 — [GATE][coord-acting] Activate acting=True (phased first live flip) 🔒 HUMAN-ONLY
- **tier**: **X** · **area**: `area:devops`, `area:docs` · **agent**: none (operator) · **labels**: `requires:operator-approval`
- **blockedBy**: T1, T2, T3, T4, T5, T6, **OP-1555**
- **v4 coverage**: §Activation, D5 budget coupling, phased rollout.
- **Code/Config AC**: systemd **drop-in** via `systemctl --user edit
  pipeline-coordinator` adding `Environment=OMNISIGHT_COORDINATOR_ACTING=1` (NOT an
  env.conf — unit uses inline `Environment=`, no EnvironmentFile;
  pipeline-coordinator.service:29-32, runbook:53); **simultaneously remove the
  `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` override** (D5 — shadow pays for
  un-executed LLM calls otherwise). Confirm `MAX_ACTIONS_PER_TICK=1`.
- **Deploy AC**: phased — first flip in an env with a known-small interrupted-ticket
  set; per-phase cold-start caps (1/1/1) not exceeded; `daemon-reload` + restart.
- **Integration AC**: decision-log shows `executed:True` actions matching intent;
  rollback verified = `OMNISIGHT_COORDINATOR_ACTING_KILL=1` (no restart) OR revert
  drop-in + restart.
- **Exercised AC**: a live action observed end-to-end + a deliberate kill-switch
  rollback exercised; update `docs/operations/coordinator-runbook.md` activation +
  rollback section.
- **Go-Live**: this IS the go-live (operator-gated).

---

## Coverage map — every v4 deliverable → ticket (for codex to verify 1:1)

| v4 section / item | ticket |
|---|---|
| §LiveActionExecutor dispatch table (B1) | T1 |
| escalate distinct path (B2) | T1 |
| low-level helpers, not fail-open gateway (B5) | T1 |
| all allowed kinds + unsupported (N11) | T1 |
| file_ticket two-line accounting (NICE) | T1 |
| §Idempotency content_hash + suffixes (B3/Q12) | T1 |
| coord-skip fail-closed `fetch_ticket_labels` (N13) | T2 |
| destructive transition strict recheck (condition #2) | T2 |
| §dry_run Option B + DecisionResult rebuild (Q7/condition #1) | T3 |
| §Blast-radius cap + try/except + kill-switch (B4/S7) | T4 |
| fairness cursor decision-log-derived (Q13/Q17) | T4 |
| §Crash recovery intent-log + replay (S10/Q11/condition #3/Q16) | T5 |
| persist action_results (:1394) | T5 |
| env-gate from_env+main (S8/D4) | T6 |
| agent_class plumb (S9) | T6 |
| per-phase cold-start caps (B6/Q14) | T6 |
| capability-scope clarification | T6 |
| §Activation systemd drop-in + D5 + phased (operator) | T7 |
| update test_l6_resolves_interrupted_actions_by_kind | T5 |
| update test_default_cold_start_gateway_shadow_vs_acting | T6 |
| update test_pipeline_coordinator_systemd.py:68 | T6 |

## Open questions for codex (this audit)
- COV1: is the T1-T7 split right, or should any be merged/split (e.g. idem grain
  as its own ticket; T5 depends on both T1 and T3 — correct ordering?).
- COV2: any v4 deliverable or condition NOT in the coverage map (a gap)?
- COV3: any ticket AC that goes BEYOND v4 (scope creep)?
- COV4: area-label correctness — does any AC touch an area not in its `area:*`
  set (the §11-revert trap)?
- COV5: is T7 correctly the only HUMAN-ONLY/tier:X ticket, and is the blockedBy
  chain (incl OP-1555) complete?

## Audit ask
Confirm: (1) 1:1 coverage of v4 + 3 conditions, (2) no scope creep, (3) filing-
convention correctness, (4) dependency ordering. Output gaps/fixes as a checklist.
Verdict: file-ready / needs-revision. Audit only, modify nothing.

exec
/bin/bash -lc "sed -n '1,320p' docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
Reading prompt from stdin...
OpenAI Codex v0.128.0 (research preview)
--------
workdir: /home/user/work/sora/OmniSight-Productizer
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, /home/user/.codex/memories]
reasoning effort: none
reasoning summaries: none
session id: 019e505c-78f0-7cf0-a035-bcce3f81c672
--------
user
Confirmation audit (round 4) of DESIGN v4 of the OmniSight pipeline-coordinator §6 live action executor + acting-flip.

READ:
- docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md  (v4 — your target)
- docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md  (v3, context)
- docs/audit/codex-reviews/coordinator-acting-executor-design-v3-codex-audit-2026-05-22.txt  (your round-3 findings; verdict needs-v4; you ruled Q7=Option B)

v4 claims to PIN every round-3 answer mechanically (no redesign). Verify each against code; do NOT trust the doc's line numbers blindly.

CODE ANCHORS:
- backend/agents/pipeline_coordinator.py — run_once :1688, dry_run logging :1358, tick-record build :1394, L6 _resolve_interrupted_actions :1514 + log scan :1493, _ticket_has_live_runner :986, start_unit :653, sweep :776
- backend/agents/pipeline_coordinator_rules.py — Action frozen dataclass :80, NoopAction, dry_run default :100, decision_id-per-tick :266, coord-skip :611
- backend/agents/pipeline_coordinator_llm_consultation.py — Tier-2 pass-through :1073
- backend/agents/jira_dispatch.py — fetch_ticket_labels :1826, get_issue_status :1586, transition_back_to_todo clears assignee :1991, idem_key params :1649/:1934/:2014, _request_idempotent :331
- backend/agents/idempotency.py :90
- docs/adr/ADR-0021-release-pipeline-coordinator.md :105 (blessed state artifacts: decision-log, heartbeat, state/ dir)
- deploy/systemd/pipeline-coordinator.service :29-32, docs/operations/coordinator-runbook.md :53

CONFIRM (RESOLVED / PARTIAL / NOT-RESOLVED, file:line each):
1. Q7 Option B wiring: `dataclasses.replace(action, dry_run=False)` at the run_once seam when acting, NoopAction skipped, dry_run logging (:1358) now truthful, default-deny preserved. Is the wiring correct and complete?
2. idem hash: content_hash over canonical semantic params EXCLUDING volatile (decision_id/timestamps/transient), comment text included; key + per-REST suffixes. Correct?
3. N13: `fetch_ticket_labels` (:1826) is the right helper, fail-closed on exception. Correct?
4. destructive recheck: `get_issue_status` (:1586) + `_ticket_has_live_runner` (:986) before transition→To Do. Correct?
5. cold-start defaults INFRA/RECONCILE/SWEEP=1. Reasonable?
6. fairness cursor decision-log-derived per ADR §3.2 (:105) — note ADR also blesses a `state/` dir; is decision-log-derived right, or should it use state/?
7. crash correlation: decision_id as tick_intent↔decision_tick key; replay-when-outcome-absent; idempotent-by-stable-key (no per-subcall bookkeeping). Sound? Any hole (e.g. decision_id reuse, log rotation across YYYY-MM-DD files)?

Then answer v4 Q16-Q18.

FINAL VERDICT (one line): file-ready / file-ready-with-conditions / needs-v5. If file-ready-with-conditions, list the conditions as a short implementation checklist. Audit only, modify nothing.

warning: Codex could not find bubblewrap on PATH. Install bubblewrap with your OS package manager. See the sandbox prerequisites: https://developers.openai.com/codex/concepts/sandboxing#prerequisites. Codex will use the vendored bubblewrap in the meantime.
codex
I’ll audit this read-only and verify the design against the current code, not the cited line numbers. I’ll first locate the relevant sections and branch state, then cross-check each claim and return findings with exact file-line references.
exec
/bin/bash -lc 'rg -n "run_once|dry_run|tick|_resolve_interrupted_actions|_ticket_has_live_runner|def start_unit|def sweep|fetch_ticket_labels|get_issue_status|transition_back_to_todo|idem_key|_request_idempotent|class Action|NoopAction|decision_id|coord-skip|Tier-2|state/|decision-log|INFRA|RECONCILE|SWEEP|Q16|Q17|Q18|content_hash|canonical" docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md docs/audit/codex-reviews/coordinator-acting-executor-design-v3-codex-audit-2026-05-22.txt backend/agents/pipeline_coordinator.py backend/agents/pipeline_coordinator_rules.py backend/agents/pipeline_coordinator_llm_consultation.py backend/agents/jira_dispatch.py backend/agents/idempotency.py docs/adr/ADR-0021-release-pipeline-coordinator.md deploy/systemd/pipeline-coordinator.service docs/operations/coordinator-runbook.md' in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
deploy/systemd/pipeline-coordinator.service:32:Environment=OMNISIGHT_COORDINATOR_DECISION_LOG_DIR=/home/user/.config/omnisight/coordinator/decision-log
deploy/systemd/pipeline-coordinator.service:36:; JIRA work. The decision-log dir (OMNISIGHT_COORDINATOR_DECISION_LOG_DIR,
docs/operations/coordinator-runbook.md:11:> **Status**: canonical operator artifact for the coordinator daemon.
docs/operations/coordinator-runbook.md:12:> Reflects the code as shipped through AUDIT-29f-6 (Tier-2 LLM consult)
docs/operations/coordinator-runbook.md:21:(`backend.agents.pipeline_coordinator`) that, every tick: writes a
docs/operations/coordinator-runbook.md:24:Tier-2 LLM consult), and appends one JSON line per decision to the
docs/operations/coordinator-runbook.md:35:   a "scary" decision-log line is an *observation*, not an action.
docs/operations/coordinator-runbook.md:37:   (ADR §3.3 / `pipeline_coordinator.py:475+`) transitions tickets,
docs/operations/coordinator-runbook.md:49:| Decision log | `…/coordinator/decision-log/YYYY-MM-DD.jsonl` (0600) |
docs/operations/coordinator-runbook.md:80:# Live daemon log (cold-start phases, tick reasons, drain):
docs/operations/coordinator-runbook.md:84:tail -f /home/user/.config/omnisight/coordinator/decision-log/$(date -u +%F).jsonl | jq .
docs/operations/coordinator-runbook.md:91:- `entering steady state` / per-tick `decision_tick` records.
docs/operations/coordinator-runbook.md:99:# If you ever see executed=true on a *decision_tick* (not a startup_phase),
docs/operations/coordinator-runbook.md:101:jq -c 'select(.event=="decision_tick") | {ts, mode, reason, dry_run, actions}' \
docs/operations/coordinator-runbook.md:102:   /home/user/.config/omnisight/coordinator/decision-log/$(date -u +%F).jsonl
docs/operations/coordinator-runbook.md:113:| `coord-skip` | operator → coord | Coordinator never touches the ticket. Excluded in `coordinator_jql()` (`pipeline_coordinator.py:463`) **and** vetoed by the `operator-keep-out` rule (`pipeline_coordinator_rules.py`, `COORD_SKIP_LABEL`). Operator escape hatch — the coordinator may not remove it from itself (ADR §6.2). |
docs/operations/coordinator-runbook.md:114:| `coord-mode:<mode>` | operator → coord | Forces a personality mode for any decision on that ticket. `<mode>` is a slug (`execution`, `investigation`, `rescue`, `triage`, `architecture`) **or** the canonical name (`ExecutionMode`…), case-insensitive (`pipeline_coordinator_modes.py:86`, `_OVERRIDE_SLUGS`). Phase 6 ships Execution + Investigation + Rescue behaviors. |
docs/operations/coordinator-runbook.md:117:| `coord-quarantine` | coord → operator | Added when a ticket reverts > 5× in 24 h (`revert-loop-quarantine` rule), paired with an operator @-mention. |
docs/operations/coordinator-runbook.md:119:| `runner:resume-from-feature-branch` | coord → runner | Cold-start Startup-2(a): an interrupted ticket whose feature branch has commits is left In Progress with this label for the runner to resume (`RESUME_FROM_FEATURE_LABEL`). |
docs/operations/coordinator-runbook.md:131:| `OMNISIGHT_COORDINATOR_CONFIG_DIR` | `~/.config/omnisight/coordinator` | Base config dir (heartbeat, decision-log, capacity, bridge-events live under it). |
docs/operations/coordinator-runbook.md:132:| `OMNISIGHT_COORDINATOR_DECISION_LOG_DIR` | `<config_dir>/decision-log` | Append-only decision log dir (one `YYYY-MM-DD.jsonl` per UTC day). Shared with the bridge so both write one audit trail. |
docs/operations/coordinator-runbook.md:137:### Tier-2 LLM consultation — `pipeline_coordinator_llm_consultation.py`
docs/operations/coordinator-runbook.md:141:| `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD` | `5.0` | Per-UTC-day Tier-2 spend cap. **`<= 0` disables Tier-2 entirely** — the operator kill switch for LLM spend. On cap-hit the daemon degrades to Tier-1-only and alerts (reason `tier2_degraded_budget_cap`). |
docs/operations/coordinator-runbook.md:161:tick 1 s, JIRA poll 60 s, hourly sweep 3600 s, event-dedupe window
docs/operations/coordinator-runbook.md:181:`…/coordinator/decision-log/`. The log survives crashes and is what
docs/operations/coordinator-runbook.md:188:| `decision_tick` | every non-idle tick | `mode`, `reason`, `dry_run`, `actions[]` ({`kind`,`target`,`executed`,`shadow`}), optional `rule_name`, `tier`, `llm_consultation`, `mode_behavior`, `action_results` |
docs/operations/coordinator-runbook.md:189:| `startup_phase` | cold-start Phases 1–4 | `phase`, `ticket`, `classification`/`action` (Phase 2), `sweep`/`labels` (Phase 3) |
docs/operations/coordinator-runbook.md:191:| `shutdown_began` / `shutdown_complete` | L5 drain | `decision_id` (paired) |
docs/operations/coordinator-runbook.md:196:LOG=/home/user/.config/omnisight/coordinator/decision-log
docs/operations/coordinator-runbook.md:201:# Only Tier-2 LLM consultations (cost + confidence):
docs/operations/coordinator-runbook.md:204:# Decisions touching a specific ticket:
docs/operations/coordinator-runbook.md:205:jq -c --arg t OP-1234 'select((.actions[]?.target==$t) or (.ticket==$t))' "$LOG"/*.jsonl
docs/operations/coordinator-runbook.md:211:# Budget-degrade events (Tier-2 disabled by daily cap):
docs/operations/coordinator-runbook.md:215:jq -r 'select(.event=="shutdown_began" or .event=="shutdown_complete") | "\(.event) \(.decision_id)"' \
docs/operations/coordinator-runbook.md:227:### 5.1 Stop one ticket (no daemon change)
docs/operations/coordinator-runbook.md:229:Add the `coord-skip` label to that ticket. The coordinator drops it
docs/operations/coordinator-runbook.md:239:- **Stop Tier-2 LLM spend only:** set
docs/operations/coordinator-runbook.md:296:label, no recently-closed tickets, no assignee-less `進行中` tickets) —
docs/operations/coordinator-runbook.md:299:`needs-coordinator` to a test ticket.
docs/operations/coordinator-runbook.md:301:### 6.3 Tier-2 LLM consults stopped
docs/operations/coordinator-runbook.md:312:### 6.4 Coordinator did something unexpected to a ticket
docs/operations/coordinator-runbook.md:319:**Recover:** add `coord-skip` to stop further attention, then reverse
docs/operations/coordinator-runbook.md:329:labels, or a ticket stuck `進行中` with no live runner.
docs/operations/coordinator-runbook.md:350:| Stop touching one ticket | label `coord-skip` | ✅ Implemented — JQL exclusion + `operator-keep-out` rule. |
docs/operations/coordinator-runbook.md:351:| Force a mode for one decision | label `coord-mode:<mode>` | ✅ Implemented — `_OVERRIDE_SLUGS`, slug or canonical name. |
docs/operations/coordinator-runbook.md:352:| Defer a ticket | label `coord-resume-after:<date>` | ⚠ Action exists; **applied only in acting mode** (shadow logs intent). |
docs/operations/coordinator-runbook.md:353:| Keep a Published ticket open | label `coord-keep-open` | ✅ Implemented in the **bridge** auto-archive, not the coordinator. |
docs/operations/coordinator-runbook.md:354:| Stop touching all tickets | `OMNISIGHT_COORDINATOR_PAUSED=1` | ❌ Not implemented. Use `coord-skip` per-ticket, or stop the daemon (watchdog first, [§5.2](#52-pause-everything--the-watchdog-will-fight-you)). |
docs/operations/coordinator-runbook.md:359:| Cap / disable LLM spend | (budget cap, ADR §5.3) | ✅ `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD` (`<=0` disables Tier-2). |
docs/adr/ADR-0021-release-pipeline-coordinator.md:85:2. **Dual-scope routing**: ticket-level routing (Tier 1, immediate) + sprint-level capacity-aware re-planning (Tier 2, hourly). The sprint-level re-plan is **required** because subscription-tier vs API-tier runner capability + budget vary enormously.
docs/adr/ADR-0021-release-pipeline-coordinator.md:87:4. **Cold-start = full recovery orchestrator**: on boot, coordinator verifies infra, reconciles JIRA state, recovers interrupted tasks. **The coordinator is the canonical startup orchestrator** — if it is healthy, everything else is.
docs/adr/ADR-0021-release-pipeline-coordinator.md:89:6. **Bounded permission model**: coordinator can file/label/transition tickets and @-mention operator. It **cannot** cast Gerrit `+2`, force-revert other agents' Under-Review work, or override operator decisions.
docs/adr/ADR-0021-release-pipeline-coordinator.md:105:~/.config/omnisight/coordinator/decision-log/YYYY-MM-DD.jsonl  ← append-only audit
docs/adr/ADR-0021-release-pipeline-coordinator.md:107:~/.config/omnisight/coordinator/state/           ← optional in-flight LLM consultation persistence
docs/adr/ADR-0021-release-pipeline-coordinator.md:111:> pause, decision-log inspection, and troubleshooting for these
docs/adr/ADR-0021-release-pipeline-coordinator.md:118:maintains in-memory work-graph cache (refreshed every tick); writes decisions to
docs/adr/ADR-0021-release-pipeline-coordinator.md:119:append-only log. Stateless across restarts: every tick re-builds the world from
docs/adr/ADR-0021-release-pipeline-coordinator.md:158:**This is the coordinator's most important behavior**: it is *the* canonical startup orchestrator.
docs/adr/ADR-0021-release-pipeline-coordinator.md:174:  For each ticket T:
docs/adr/ADR-0021-release-pipeline-coordinator.md:190:  assignee=<bot> on To Do tickets with no claim:* label → clear
docs/adr/ADR-0021-release-pipeline-coordinator.md:213:- `coord-skip` — coordinator does NOT touch this ticket (operator handles)
docs/adr/ADR-0021-release-pipeline-coordinator.md:227:    if not has_label(ctx.ticket, "needs-coordinator"):
docs/adr/ADR-0021-release-pipeline-coordinator.md:229:    recent_comment = latest_comment(ctx.ticket)
docs/adr/ADR-0021-release-pipeline-coordinator.md:235:            blocking=ctx.ticket.key,
docs/adr/ADR-0021-release-pipeline-coordinator.md:244:1. **dependency-out-of-area** — `runner-discovered-dependency` mentions a known area outside ticket's area: file dep ticket + reroute
docs/adr/ADR-0021-release-pipeline-coordinator.md:247:4. **merger-repeat-fail** — merger fails > 3 attempts same ticket: escalate to operator with full conflict context
docs/adr/ADR-0021-release-pipeline-coordinator.md:248:5. **revert-loop-quarantine** — ticket reverted > 5 times in 24h: add `coord-quarantine` label + @-mention operator
docs/adr/ADR-0021-release-pipeline-coordinator.md:250:7. **stuck-in-progress** — status `進行中` > 2× CLI timeout, no live runner process: trigger Phase Startup-2-style reconciliation for that single ticket
docs/adr/ADR-0021-release-pipeline-coordinator.md:252:9. **wrong-class-routing** — ticket area matches `area:backend` strongly, currently `class:subscription-claude`, codex is idle: suggest reroute (action: comment only — don't relabel without LLM consult)
docs/adr/ADR-0021-release-pipeline-coordinator.md:253:10. **operator-keep-out** — `coord-skip` label present: no action, ever
docs/adr/ADR-0021-release-pipeline-coordinator.md:262:- Action would affect >3 tickets (cross-cutting)
docs/adr/ADR-0021-release-pipeline-coordinator.md:275:{focal_ticket and 5-hop blockedBy DAG}
docs/adr/ADR-0021-release-pipeline-coordinator.md:277:{recent_decisions on these tickets (last 7 days)}
docs/adr/ADR-0021-release-pipeline-coordinator.md:281:{currently_running_tickets}
docs/adr/ADR-0021-release-pipeline-coordinator.md:291:- file_ticket(spec)
docs/adr/ADR-0021-release-pipeline-coordinator.md:292:- relabel(ticket, add=[...], remove=[...])
docs/adr/ADR-0021-release-pipeline-coordinator.md:293:- transition(ticket, to_status)
docs/adr/ADR-0021-release-pipeline-coordinator.md:295:- mark_for_followup(ticket, when, why)
docs/adr/ADR-0021-release-pipeline-coordinator.md:302:  "actions": [{action, ticket, params}, ...],
docs/adr/ADR-0021-release-pipeline-coordinator.md:328:| `file_ticket(spec)` | JIRA POST /issue via `_create_issue` helper, with `coord-filed` label so it's traceable | Spec must include description, AC, areas, tier; no operator-only labels |
docs/adr/ADR-0021-release-pipeline-coordinator.md:329:| `relabel(ticket, add, remove)` | JIRA PUT labels | Cannot add/remove operator-only labels (e.g., `release:force-create`, `release:force-promote`, `class:operator`) |
docs/adr/ADR-0021-release-pipeline-coordinator.md:330:| `transition(ticket, to)` | JIRA POST /transitions | Allowed: To Do ↔ 進行中, Under Review → To Do (cancel work), 公開済み → Archived; Forbidden: Under Review → 公開済み (Gerrit owns; bridge handles), * → 公開済み (Gerrit owns), any → 承認済み directly |
docs/adr/ADR-0021-release-pipeline-coordinator.md:332:| `mark_for_followup(ticket, when, why)` | Add label `coord-resume-after:<date>`, no immediate action | unlimited |
docs/adr/ADR-0021-release-pipeline-coordinator.md:340:- **Push to Gerrit on a ticket** — runner authority (coordinator can `file` a ticket asking a runner to do it, but cannot push itself)
docs/adr/ADR-0021-release-pipeline-coordinator.md:341:- **Force-revert In-Progress / Under-Review work from another runner** — runners revert their own work; coordinator can re-route via `noop + comment + file replacement ticket`
docs/adr/ADR-0021-release-pipeline-coordinator.md:342:- **Touch operator-authority labels** — `coord-skip`, `release:force-*`, `class:operator`, `milestone:force-accept`
docs/adr/ADR-0021-release-pipeline-coordinator.md:343:- **Modify capability_matrix.yaml or any code file** — out of scope; if pattern surfaces, file a ticket
docs/adr/ADR-0021-release-pipeline-coordinator.md:344:- **Self-modify** — coordinator cannot relabel `coord-skip` off its own to-do list (operator escape hatch must remain intact)
docs/adr/ADR-0021-release-pipeline-coordinator.md:349:- Add `coord-skip` label → coordinator never touches that ticket
docs/adr/ADR-0021-release-pipeline-coordinator.md:353:- `git revert` any committed decision-log entry (it's append-only file in operator's home)
docs/adr/ADR-0021-release-pipeline-coordinator.md:367:| **risk** | low / medium / high | Blast radius of action (1 ticket vs full chain); reversibility of state changes; whether it touches `prod-gate` labelled tickets |
docs/adr/ADR-0021-release-pipeline-coordinator.md:369:| **reversibility** | low / medium / high | Can the action be undone in <5min? (relabel = yes; file-ticket = effectively no; transition = depends) |
docs/adr/ADR-0021-release-pipeline-coordinator.md:376:| **InvestigationMode** | low urgency + high novelty | Always Tier 2. LLM context wider (10-hop graph, 20 lessons). Files research / spike tickets. | Phase 6 initial |
docs/adr/ADR-0021-release-pipeline-coordinator.md:378:| **ArchitectureMode** | low urgency + low reversibility + high impact | Slow, ADR-driven. Files ADR proposal ticket. Multiple LLM consultations with different framings. | later add |
docs/adr/ADR-0021-release-pipeline-coordinator.md:379:| **RescueMode** | runner stuck > 24h OR > 5 reverts on one ticket | Full root-cause analysis. File `runner-blocked-pattern` ticket + write lesson. Aim: prevent recurrence, not just unstuck. | Phase 6 initial |
docs/adr/ADR-0021-release-pipeline-coordinator.md:385:`coord-mode:<mode>` label on a ticket forces that mode for any decision involving that ticket. Useful when operator knows the system is misprofiling (e.g., "this looks routine but I want investigation mode because we keep getting bitten").
docs/adr/ADR-0021-release-pipeline-coordinator.md:391:### 8.1 Why ticket-level alone fails
docs/adr/ADR-0021-release-pipeline-coordinator.md:393:Operator's concern (2026-05-13 conversation, paraphrased): **"subscription vs API runners have vastly different capabilities; pure ticket-level routing leads to wrong runner for the task, exhausted budgets, scope drift, duplicated work."**
docs/adr/ADR-0021-release-pipeline-coordinator.md:396:- 10 simple low-priority tickets queue
docs/adr/ADR-0021-release-pipeline-coordinator.md:397:- Each gets ticket-level-routed to `subscription-claude` because rule says "area:docs → claude"
docs/adr/ADR-0021-release-pipeline-coordinator.md:401:- One critical `area:backend` ticket arrives; both lanes can in principle handle it
docs/adr/ADR-0021-release-pipeline-coordinator.md:409:1. Enumerate next 10-20 pickable tickets across all sprints (priority DESC, age ASC)
docs/adr/ADR-0021-release-pipeline-coordinator.md:416:3. Estimate token cost per ticket (heuristic: LOC budget × 4 + test cost)
docs/adr/ADR-0021-release-pipeline-coordinator.md:417:4. LLM-consult (Tier 2, Investigation Mode): given (tickets, capacities, ticket-token-estimates), produce assignment plan
docs/adr/ADR-0021-release-pipeline-coordinator.md:418:5. Apply assignment: relabel tickets with class change (without forcing — operator can override)
docs/adr/ADR-0021-release-pipeline-coordinator.md:419:6. Identify scope-drift / duplication / merge candidates: file `coord-scope-review` tickets
docs/adr/ADR-0021-release-pipeline-coordinator.md:425:Each runner (claude-1/2, codex-1/2) emits per-tick `runner-quota-state.jsonl` line:
docs/adr/ADR-0021-release-pipeline-coordinator.md:435:  "tickets_completed_this_week": 12,
docs/adr/ADR-0021-release-pipeline-coordinator.md:462:Coordinator's own loop detection: same `(ticket, action_type)` decision triggered
docs/adr/ADR-0021-release-pipeline-coordinator.md:470:`~/.config/omnisight/coordinator/decision-log/YYYY-MM-DD.jsonl`. Append-only.
docs/adr/ADR-0021-release-pipeline-coordinator.md:518:- the tickets it touched
docs/adr/ADR-0021-release-pipeline-coordinator.md:595:6. **Action set extensibility**. The §6.1 allowed-action list is closed for safety. As patterns surface, operator adds new actions via reviewable patches (not Tier-2-LLM proposal). Hard rule.
docs/adr/ADR-0021-release-pipeline-coordinator.md:599:## §13. Implementation plan (drives AUDIT-29f ticket structure)
docs/adr/ADR-0021-release-pipeline-coordinator.md:601:The AUDIT-29f tickets to be filed once this ADR is `+2`'d:
docs/adr/ADR-0021-release-pipeline-coordinator.md:624:  AUDIT-29f-10  Chaos test (§9 L7) + decision-log validation                 (0.5d)
docs/adr/ADR-0021-release-pipeline-coordinator.md:626:  AUDIT-29f-12  Integration test: full pipeline scenario (runner stuck → coordinator handles → ticket flows through) (0.5d)
docs/adr/ADR-0021-release-pipeline-coordinator.md:652:| Action conflicts with operator | Operator overrides always win; `coord-skip` escape hatch; dry-run env |
docs/adr/ADR-0021-release-pipeline-coordinator.md:680:| Coordinator stops touching one ticket | Add label `coord-skip` |
docs/adr/ADR-0021-release-pipeline-coordinator.md:681:| Coordinator stops touching all tickets | `OMNISIGHT_COORDINATOR_PAUSED=1` env |
docs/adr/ADR-0021-release-pipeline-coordinator.md:694:  "tickets": ["OP-XXX", ...],
docs/adr/ADR-0021-release-pipeline-coordinator.md:706:    {"action": "relabel", "ticket": "OP-XXX", "params": {...}, "result": "success"}
backend/agents/idempotency.py:3:The runner retries whole ticks.  Without a local dedup store, retried
backend/agents/idempotency.py:27:    targeting different tickets) don't share a row and silently dedup
backend/agents/idempotency.py:103:def idempotent(key_arg: str = "idem_key", store: IdempotencyStore = DEFAULT_STORE) -> Callable[[F], F]:
backend/agents/idempotency.py:104:    """Decorator for functions that accept an ``idem_key`` keyword."""
backend/agents/pipeline_coordinator_llm_consultation.py:1:"""Pipeline-coordinator Tier-2 LLM consultation (AUDIT-29f-6 / OP-1004).
backend/agents/pipeline_coordinator_llm_consultation.py:6:an **LLM consultation** (Tier 2). This module owns that Tier-2 path — the
backend/agents/pipeline_coordinator_llm_consultation.py:19:    HybridDecisionEngine  — Tier-1 first; Tier-2 when rules abstain / conflict /
backend/agents/pipeline_coordinator_llm_consultation.py:67:    NoopAction,
backend/agents/pipeline_coordinator_llm_consultation.py:75:# Bumped when Tier-2 decision semantics change. 2.x = LLM-consult era.
backend/agents/pipeline_coordinator_llm_consultation.py:78:# decision log records "which engine produced this tick").
backend/agents/pipeline_coordinator_llm_consultation.py:82:# Tier-2 produced an LLM-consulted decision.
backend/agents/pipeline_coordinator_llm_consultation.py:90:# OP-1556: an idle / empty-work-graph tick (no focal ticket AND no work-graph
backend/agents/pipeline_coordinator_llm_consultation.py:91:# slice) has nothing to decide → Tier-1 NoopAction, never a Tier-2 consult.
backend/agents/pipeline_coordinator_llm_consultation.py:93:# (which still consults). Grep-able so the audit can confirm idle ticks no
backend/agents/pipeline_coordinator_llm_consultation.py:119:    "file_ticket": ACTION_FILE_TICKET,
backend/agents/pipeline_coordinator_llm_consultation.py:143:    """Model / timeout / budget knobs for Tier-2 consultation (ADR §5.3)."""
backend/agents/pipeline_coordinator_llm_consultation.py:177:    """Tracks Tier-2 LLM spend against a per-UTC-day USD cap (ADR §5.3).
backend/agents/pipeline_coordinator_llm_consultation.py:179:    ``daily_budget_usd <= 0`` disables Tier-2 entirely (the guard reports
backend/agents/pipeline_coordinator_llm_consultation.py:215:        Sums ``llm_consultation.cost_usd`` over every ``decision_tick`` whose
backend/agents/pipeline_coordinator_llm_consultation.py:254:    """Sum recorded Tier-2 consultation costs for ``day`` from the log file."""
backend/agents/pipeline_coordinator_llm_consultation.py:302:    """The assembled Tier-2 consultation prompt — 7 sections per ADR §5.2.
backend/agents/pipeline_coordinator_llm_consultation.py:308:    "Cognee lesson recall observable in Tier-2 context bundle (logged)").
backend/agents/pipeline_coordinator_llm_consultation.py:319:# The 7 canonical section titles (ADR §5.2). Order is load-bearing — the
backend/agents/pipeline_coordinator_llm_consultation.py:344:    ``## Work-graph slice`` walks the focal ticket's ``blocked_by`` DAG up to
backend/agents/pipeline_coordinator_llm_consultation.py:399:    focal = ctx.ticket
backend/agents/pipeline_coordinator_llm_consultation.py:401:        return "focal_ticket: (none — sprint-level / idle consult)"
backend/agents/pipeline_coordinator_llm_consultation.py:402:    lines = [f"focal_ticket: {_ticket_brief(focal)}"]
backend/agents/pipeline_coordinator_llm_consultation.py:403:    chain = _blocked_by_chain(focal, ctx.tickets, hops=hops)
backend/agents/pipeline_coordinator_llm_consultation.py:407:            blocker = ctx.tickets.get(key)
backend/agents/pipeline_coordinator_llm_consultation.py:408:            lines.append(f"  - {_ticket_brief(blocker) if blocker else key}")
backend/agents/pipeline_coordinator_llm_consultation.py:418:    tickets: Mapping[str, Ticket],
backend/agents/pipeline_coordinator_llm_consultation.py:434:            blocker = tickets.get(key)
backend/agents/pipeline_coordinator_llm_consultation.py:442:def _ticket_brief(ticket: Ticket) -> str:
backend/agents/pipeline_coordinator_llm_consultation.py:444:        f"{ticket.key} [status={ticket.status or '?'} type={ticket.issuetype or '?'} "
backend/agents/pipeline_coordinator_llm_consultation.py:445:        f"areas={','.join(ticket.areas) or '-'} labels={','.join(ticket.labels) or '-'}]"
backend/agents/pipeline_coordinator_llm_consultation.py:480:    "- file_ticket(blocking, target_area, description)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:481:    "- relabel(ticket, add=[...], remove=[...])\n"
backend/agents/pipeline_coordinator_llm_consultation.py:482:    "- transition(ticket, to_status)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:483:    "- mention_operator(ticket, message, urgency)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:484:    "- mark_for_followup(ticket, when, why)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:485:    "- escalate(ticket, reason)\n"
backend/agents/pipeline_coordinator_llm_consultation.py:493:    '  "actions": [{"action": "relabel", "ticket": "OP-XXX", "params": {...}}, ...],\n'
backend/agents/pipeline_coordinator_llm_consultation.py:621:    """A strict-parsed + action-validated Tier-2 decision (ADR §5.2 output)."""
backend/agents/pipeline_coordinator_llm_consultation.py:699:        ticket = str(raw.get("ticket", "") or "")
backend/agents/pipeline_coordinator_llm_consultation.py:704:            accepted.append(Action(kind=kind, target=ticket, params=dict(params)))
backend/agents/pipeline_coordinator_llm_consultation.py:710:# ── Tier-2 consultant ──────────────────────────────────────────────────
backend/agents/pipeline_coordinator_llm_consultation.py:718:    is the Appendix-C metadata block embedded in the decision-log line; and
backend/agents/pipeline_coordinator_llm_consultation.py:737:    Tier-2 output flows through the exact same action path as Tier-1
backend/agents/pipeline_coordinator_llm_consultation.py:775:        """Run one Tier-2 consultation for ``ctx`` (or degrade on budget)."""
backend/agents/pipeline_coordinator_llm_consultation.py:794:            "[coord-llm] Tier-2 consult trigger=%s mode=%s lessons=%d cited=[%s]",
backend/agents/pipeline_coordinator_llm_consultation.py:832:        # Tier-2 tick is never silently a no-op when the model wanted action.
backend/agents/pipeline_coordinator_llm_consultation.py:867:            f"[coord-budget] daily Tier-2 LLM budget "
backend/agents/pipeline_coordinator_llm_consultation.py:927:        except Exception as exc:  # noqa: BLE001 — recall must never break a tick
backend/agents/pipeline_coordinator_llm_consultation.py:933:    return ctx.ticket.key if ctx.ticket is not None else ""
backend/agents/pipeline_coordinator_llm_consultation.py:948:    focal = ctx.ticket
backend/agents/pipeline_coordinator_llm_consultation.py:954:    title = _ticket_brief(focal)
backend/agents/pipeline_coordinator_llm_consultation.py:958:        ticket_title=title,
backend/agents/pipeline_coordinator_llm_consultation.py:984:    """Tier-1 deterministic rules fronting Tier-2 LLM consultation.
backend/agents/pipeline_coordinator_llm_consultation.py:988:    consults Tier-2 when:
backend/agents/pipeline_coordinator_llm_consultation.py:995:    An ``operator-keep-out`` veto (priority-0 :class:`NoopAction`) short-circuits
backend/agents/pipeline_coordinator_llm_consultation.py:996:    *both* tiers — an escape-hatched ticket is never sent to the LLM. When the
backend/agents/pipeline_coordinator_llm_consultation.py:1029:        # OP-1556: idle / empty-work-graph tick short-circuit. With no focal
backend/agents/pipeline_coordinator_llm_consultation.py:1030:        # ticket AND no work-graph slice there is nothing to decide — every
backend/agents/pipeline_coordinator_llm_consultation.py:1031:        # Tier-1 rule abstains (each early-returns on ctx.ticket is None), but
backend/agents/pipeline_coordinator_llm_consultation.py:1033:        # couldn't make". Falling through to Tier-2 here invokes the LLM on
backend/agents/pipeline_coordinator_llm_consultation.py:1034:        # every idle tick (AUDIT-29f: 1254/1254 idle ticks were tier=2,
backend/agents/pipeline_coordinator_llm_consultation.py:1036:        # minutes. So return a Tier-1 NoopAction without consulting. The sole
backend/agents/pipeline_coordinator_llm_consultation.py:1039:        # allowed to proceed to Tier-2.
backend/agents/pipeline_coordinator_llm_consultation.py:1040:        if ctx.ticket is None and not ctx.tickets and not (always and ctx.situation is not None):
backend/agents/pipeline_coordinator_llm_consultation.py:1042:                decision_id=uuid.uuid4().hex,
backend/agents/pipeline_coordinator_llm_consultation.py:1043:                actions=(NoopAction(),),
backend/agents/pipeline_coordinator_llm_consultation.py:1054:        # Absolute operator veto (priority-0 NoopAction) short-circuits.
backend/agents/pipeline_coordinator_llm_consultation.py:1055:        if fired and isinstance(fired[0][1], NoopAction):
backend/agents/pipeline_coordinator_llm_consultation.py:1058:                decision_id=uuid.uuid4().hex,
backend/agents/pipeline_coordinator_llm_consultation.py:1059:                actions=(NoopAction(),),
backend/agents/pipeline_coordinator_llm_consultation.py:1074:                decision_id=uuid.uuid4().hex,
backend/agents/pipeline_coordinator_llm_consultation.py:1118:                degraded_actions = outcome.actions or (NoopAction(),)
backend/agents/pipeline_coordinator_llm_consultation.py:1121:                decision_id=uuid.uuid4().hex,
backend/agents/pipeline_coordinator_llm_consultation.py:1131:        actions = outcome.actions or (NoopAction(),)
backend/agents/pipeline_coordinator_llm_consultation.py:1133:            decision_id=uuid.uuid4().hex,
backend/agents/pipeline_coordinator_llm_consultation.py:1149:    conflict — they agree. A :class:`NoopAction` veto is handled before this
backend/agents/pipeline_coordinator_llm_consultation.py:1154:        if isinstance(action, NoopAction):
backend/agents/pipeline_coordinator_llm_consultation.py:1167:    """Production wiring: Tier-1 registry + Tier-2 consultant + day-budget guard.
backend/agents/jira_dispatch.py:1:"""JIRA dispatch — shared module for runner ticket pickup, transitions, comments.
backend/agents/jira_dispatch.py:3:Per ``docs/sop/jira-ticket-conventions.md`` §16. Replaces the TODO.md
backend/agents/jira_dispatch.py:8:- ``fetch_pickable_tickets(agent_class)`` — JQL search per §16, parsed
backend/agents/jira_dispatch.py:12:- ``transition_back_to_todo(key, reason)`` — In Progress → TODO (revert)
backend/agents/jira_dispatch.py:14:- ``parse_prerequisites(ticket)`` — extract YAML block from description
backend/agents/jira_dispatch.py:70:# ticket carrying a ``class:operator-window-*`` or ``class:operator-rehearsal``

 succeeded in 0ms:
# Coordinator §6 live action executor + acting-flip — DESIGN v4 (codex round-3 pinned)

**Status**: DRAFT v4 for codex round-4 confirmation (2026-05-22). Supersedes
`...-design-v3.md`. Lineage:
- v1 audit (13 findings) → v2 audit (needs-v3) → v3 audit (needs-v4):
  `coordinator-acting-executor-design{,-v2,-v3}-codex-audit-2026-05-22.txt`

v3 reached convergence: round-3 surfaced **no new architecture problems** and
codex supplied a concrete answer for every PARTIAL. v4 *pins* those answers —
this is a mechanical close-out, not a redesign. The §LiveActionExecutor dispatch
table, blast-cap location, env-gate wiring, and activation are carried from v3
unchanged unless listed below.

---

## v3 → v4 changelog (round-3 answers, now pinned)

| Round-3 item | round-3 status | v4 pin |
|---|---|---|
| **Q7 dry_run fork** | codex **ruled Option B** | **Option B chosen.** A wrapper between `engine.evaluate` and the executor (run_once:1699-1703) does `dataclasses.replace(action, dry_run=False)` for every non-noop action **when `acting=True`**. Rationale (codex): `run_once` logs `actions` + top-level `dry_run` from the action objects (:1358); Option A would mutate while the log still reads `dry_run:true` → false audit + false crash-recovery input. The frozen-dataclass cost (rules.py:80) is paid via `replace`, accepted for truthful logs. |
| **B3 / Q12 idem hash** | RESOLVED w/ condition | **content_hash = stable digest of the action's *canonical semantic params*, EXCLUDING volatile fields** (`decision_id`, timestamps, transient observations). For operator-visible text (comments/reasons), the hash includes the **exact emitted string** (or a deterministically derived one) so a reworded comment is a new action, not a silent dedupe. Key = `coord:{kind}:{target}:{content_hash}` + per-REST suffix (`:add:<l>`/`:remove:<l>`/`:clear-assignee`/`:transition`/`:comment`/`:label`). |
| **N13 label recheck** | behaviorally right, **wrong helper name** | **use `fetch_ticket_labels(client, key)`** (jira_dispatch.py:1826) — `get_issue_labels` does not exist. On exception → `executed:False,reason:"label_read_failed"` (fail-CLOSED, never mutate on unknown state). |
| **Destructive transition recheck** | PARTIAL — name the live helpers | before `transition → To Do`, re-verify at execution time with **`get_issue_status(client, key)`** (jira_dispatch.py:1586) still ∈ 進行中 AND **`_ticket_has_live_runner(key)`** (pipeline_coordinator.py:986) is False; else `executed:False,reason:"context_stale"`. |
| **B6 / Q14 cold-start caps** | PARTIAL — defaults? | **first-flip defaults `INFRA=1`, `RECONCILE=1`, `SWEEP=1`** (`OMNISIGHT_COORDINATOR_COLD_START_MAX_{INFRA,RECONCILE,SWEEP}`); raise only after canary evidence. Per-phase counters: Startup-1 `start_unit` (:653), Startup-2 transitions/labels (:735), Startup-3 sweep (:776). |
| **B4 / Q13 fairness cursor** | PARTIAL — persistence? | **cursor state is decision-log-derived, NOT a new file.** ADR-0021 §3.2 (ADR-0021:105) blesses only decision-log/state artifacts for stateless-across-restarts; the round-robin position is rebuilt from the recent decision-log tail (which targets were acted on last), same pattern as the budget guard rebuilding spend from the log. |
| **S10 / Q11 crash correlation** | PARTIAL — correlation undefined | **see §Crash recovery below** — explicit `tick_intent`↔outcome correlation key + why replay is idempotent. |

---

## §Crash recovery — correlation + idempotent replay (S10 / Q11 pinned)

The round-3 ask: define how a `tick_intent` matches its outcome, and how
completed subcalls avoid replay. v4:

1. **Correlation key** = the tick's `decision_id` (rules.py:266 — fresh per tick,
   so unique per intent). `run_once` writes:
   - `tick_intent` record `{decision_id, planned: [{kind, target, idem_keys:[...]}], ts}` **before** executing (closes the execute-before-append window at :1703-before-:1708);
   - the existing outcome `decision_tick` record `{decision_id, action_results, ...}` **after**, carrying the SAME `decision_id`.
2. **L6 replay rule** (`_resolve_interrupted_actions` :1514; L6 scans
   `decision_tick` + shutdown records today, :1493 — extend it to also scan
   `tick_intent`): a `tick_intent` whose matching `decision_tick` (same
   `decision_id`) is **absent** is a crash window → replay its planned actions.
3. **Why replay is safe (no double-apply)**: replay re-issues under the SAME
   stable idem keys (`coord:{kind}:{target}:{content_hash}:<suffix>`). The
   idempotency store (idempotency.py:90) dedupes any subcall that already
   completed before the crash — so a partially-applied composite action
   completes exactly the missing subcalls. This is the property the v3 B3 fix
   buys us: stable keys make crash-replay correct *by construction*, no
   per-subcall completion bookkeeping needed.
4. `action_results` is persisted in the `decision_tick` record (:1394) for
   observability/diff, but L6 correctness relies on the idem store, not on
   parsing partial `action_results`.

---

## §dry_run flip — Option B wiring (Q7 pinned)

```
result = self._engine.evaluate(ctx)                      # :1699 (unchanged)
actions = result.actions
if self._acting:                                          # new: only in acting mode
    actions = tuple(
        a if isinstance(a, NoopAction) else replace(a, dry_run=False)
        for a in actions
    )
# tick-record / dry_run logging (:1358) now sees the real dry_run=False ─ truthful
action_results = [...]  # cap + per-action try/except (B4/S7), over `actions`
```
- `self._acting` is set from the env-gate (`OMNISIGHT_COORDINATOR_ACTING`, S8) at
  construction, the single source of truth.
- Executor logic stays uniform: "mutate iff `dry_run=False`". A stray
  `dry_run=True` action in acting mode still can't mutate (it was a NoopAction or
  the replace didn't touch it) — default-deny preserved.
- Decision-log + `tick_intent` now record `dry_run:false` truthfully, which is
  exactly what L6 reads for crash recovery (the reason codex rejected Option A).

---

## §coord-skip / destructive recheck — pinned helper names

Immediately before any mutation, in the executor:
1. `labels = fetch_ticket_labels(client, action.target)` (jira_dispatch.py:1826);
   on exception → `executed:False,reason:"label_read_failed"` (fail-closed).
2. `coord-skip`/`coord-quarantine` ∈ labels → `executed:False,reason:"coord_skip"`.
3. **For `transition → To Do` only** (destructive — clears assignee,
   jira_dispatch.py:1991): additionally require
   `get_issue_status(client, target)` (jira_dispatch.py:1586) ∈ IN_PROGRESS names
   AND `not _ticket_has_live_runner(target)` (pipeline_coordinator.py:986); else
   `executed:False,reason:"context_stale"`.

---

## Carried unchanged from v3 (still in force)
- Dispatch on `action.kind` → low-level `jira_dispatch` via a client built from
  `config.jira_agent_class` (B1/B5/S9); escalate = label + comment (B2).
- Blast cap in `run_once`: `MAX_ACTIONS_PER_TICK` default 1 + per-action
  try/except + kill-switch `OMNISIGHT_COORDINATOR_ACTING_KILL=1` (B4/S7).
- env-gate: `from_env` parses `OMNISIGHT_COORDINATOR_ACTING`; `main` builds the
  executor + `acting=True` when set (S8). Activation via **systemd drop-in**
  (`systemctl --user edit`), NOT `env.conf` — inline `Environment=`
  (pipeline-coordinator.service:29-32), no EnvironmentFile (runbook:53).
- `file_ticket` not auto-executed → `mention_operator` + two result lines.
- Coordinator daemon is NOT capability-matrix gated (capability_matrix.yaml:27
  is runner pickup); the flip is daemon env authorization.

---

## Open questions for codex (round-4)
- Q16: is `decision_id` the right correlation key, or should `tick_intent` carry
  an explicit `intent_id` (e.g. if a tick can be re-evaluated)? 
- Q17: rebuilding the fairness cursor from the decision-log tail — how deep a
  tail is safe/cheap, and does it interact with log rotation (YYYY-MM-DD.jsonl)?
- Q18: anything v4 STILL misses for first live flip.

## Audit asks for codex (round-4 — confirmation)
1. Confirm Q7 Option B is wired correctly (replace at the seam, dry_run logging
   now truthful) — file:line.
2. Confirm each round-3 PARTIAL is now RESOLVED: idem hash (volatile exclusion),
   N13 helper (`fetch_ticket_labels`), destructive recheck (`get_issue_status` +
   `_ticket_has_live_runner`), cold-start defaults, fairness cursor (decision-log
   derived), crash correlation (`decision_id` + idempotent replay).
3. Final verdict: **file-ready** / file-ready-with-conditions / needs-v5.

---

## ✅ Round-4 verdict: file-ready-with-conditions

codex round-4 (`...-v4-codex-audit-2026-05-22.txt`) confirmed RESOLVED: idem hash,
N13 helper (`fetch_ticket_labels`), cold-start defaults (1/1/1), fairness cursor
(decision-log derived), dispatch, systemd drop-in, capability scope. Three items
are **implementation conditions** (not design changes) — carry these verbatim as
ticket acceptance criteria:

1. **Option B must rebuild `DecisionResult`, not just a local tuple.** After the
   acting dry-run flip, do `result = dataclasses.replace(result, actions=actions)`
   so `_build_tick_record()` (pipeline_coordinator.py:1394) AND the learning-loop
   writeback see `dry_run=false`. Rewriting only a local `actions` tuple leaves
   the logged tick reading `dry_run:true` (the exact failure that sank Option A).
   The rebuilt `DecisionResult` MUST preserve the same `decision_id`.
2. **Destructive `To Do` recheck must fail CLOSED on probe uncertainty.**
   `_ticket_has_live_runner()` treats probe errors as "not live" by design
   (pipeline_coordinator.py:987-990). The live executor needs a STRICT wrapper:
   an unknown/failed probe → abstain `executed:False,reason:"context_stale"`
   (do NOT proceed to clear assignee on an uncertain runner state).
3. **`tick_intent` stores FINAL stable idem keys + 24h replay window.** The
   intent record carries the resolved `coord:{kind}:{target}:{hash}:<suffix>`
   keys (not just action payloads); L6 replays a missing-outcome intent ONLY
   within the idempotency TTL window (idempotency.py:40, 24h) — beyond it the
   idem store has expired and replay could double-apply.

Round-4 Q-answers folded in:
- **Q16**: `decision_id` is the correct correlation key; add `intent_seq` (not a
  new identity) only if one evaluated decision ever emits multiple intent records.
- **Q17**: rebuild the fairness cursor from a bounded decision-log tail spanning
  **all date files in the 24h replay window** (not just today's `YYYY-MM-DD.jsonl`),
  with a record-count cap as a perf guard.

---

## §Tests (v4 — delta over v3)
- Option B: in acting mode every non-noop action reaches the executor with
  `dry_run=False` AND the logged tick record reads `dry_run:false`.
- idem hash: two ticks, same semantic action, different `decision_id` → one
  mutation; a reworded comment → a second mutation (not deduped).
- destructive recheck: now-live runner / status changed → `context_stale`.
- label read fail-closed: `fetch_ticket_labels` raises → no mutation.
- crash replay: `tick_intent` without matching `decision_tick` → replay; already
  applied subcalls deduped by idem store → no double-apply.
- Update existing: `test_l6_resolves_interrupted_actions_by_kind` (coldstart:473,
  now must handle `tick_intent`), `test_default_cold_start_gateway_shadow_vs_acting`
  (coldstart:954, assert agent_class), `test_pipeline_coordinator_systemd.py:68`.

codex
The ticket draft is fairly explicit, so the main risk is not missing whole sections but mismatch in conventions, dependencies, or line anchors. I’ve got the v4 conditions in view now and am checking the source anchors against the current repo.
exec
/bin/bash -lc "nl -ba backend/agents/jira_dispatch.py | sed -n '320,338p;1578,1592p;1818,1835p;1986,2002p'" in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator.py | sed -n '130,170p;640,665p;730,785p;970,995p;1138,1160p;1388,1405p;1488,1520p;1688,1712p;1878,1945p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   320	
   321	def _request(
   322	    client: DispatchClient,
   323	    method: str,
   324	    path: str,
   325	    body: dict | None = None,
   326	    idem_key: str | None = None,
   327	) -> dict:
   328	    return _request_raw(method, client.base_url + path, client.auth_header, body, idem_key)
   329	
   330	
   331	def _request_idempotent(
   332	    client: DispatchClient,
   333	    method: str,
   334	    path: str,
   335	    body: dict | None,
   336	    idem_key: str,
   337	) -> dict:
   338	    return DEFAULT_STORE.run(idem_key, lambda: _request(client, method, path, body, idem_key))
  1578	IN_PROGRESS_STATUS_NAMES = {"In Progress", "進行中"}
  1579	UNDER_REVIEW_STATUS_NAME = "Under Review"
  1580	UNDER_REVIEW_STATUS_NAMES = {UNDER_REVIEW_STATUS_NAME}
  1581	APPROVED_STATUS_NAMES = {"Approved", "承認済み"}
  1582	PUBLISHED_STATUS_NAMES = {"Published", "公開済み"}
  1583	ARCHIVED_STATUS_NAMES = {"Archived"}
  1584	
  1585	
  1586	def get_issue_status(client: "DispatchClient", key: str) -> str:
  1587	    """Return current status name for ``key`` (e.g. "In Progress", "Under Review").
  1588	
  1589	    Used by the runner's Phase 1.5 idempotency gate (OP-691): if the agent
  1590	    already transitioned the ticket itself, skip the runner's duplicate
  1591	    comment + transition.
  1592	    """
  1818	    """Return True if the ops-only sigil label is in ``labels``.
  1819	
  1820	    Defensive against ``LabelInjectionAttack`` per OP-956 error catalog:
  1821	    we match the exact sigil string and never interpret label values.
  1822	    """
  1823	    return OPS_ONLY_LABEL in set(labels)
  1824	
  1825	
  1826	def fetch_ticket_labels(client: "DispatchClient", key: str) -> tuple[str, ...]:
  1827	    """Return the *current* JIRA label set for ``key``.
  1828	
  1829	    OP-958 ``LabelsPropagationDrift``: the runner's ``TicketSnapshot``
  1830	    carries a point-in-time label copy taken at *selection* time (JQL
  1831	    ``fetch_pickable_tickets`` → :func:`to_snapshot`, or a single
  1832	    ``GET /issue`` for the target-override path). Operators routinely add
  1833	    ``runner:no-commits-expected`` *after* a ticket is already In
  1834	    Progress — the OP-925 R3 cascade on 2026-05-12 is the canonical
  1835	    case — so a stale snapshot makes :func:`has_ops_only_label` miss the
  1986	            )
  1987	    add_comment(
  1988	        client, key, f"Reverting to TODO. Reason:\n{reason}",
  1989	        idem_key=f"{base_key}-comment",
  1990	    )
  1991	    clear_assignee(client, key, idem_key=f"{base_key}-clear-assignee")
  1992	    _request_idempotent(
  1993	        client, "POST", f"/issue/{key}/transitions",
  1994	        {"transition": {"id": TRANSITION_IDS["back_to_todo"]}},
  1995	        f"{base_key}-transition",
  1996	    )
  1997	
  1998	    # OP-1140: stoploss circuit-breaker recording — best-effort.
  1999	    # A recorder fault (network blip, label-fetch 404 on a freshly-created
  2000	    # ticket, ...) must NEVER prevent the §11-revert from completing; the
  2001	    # operator still needs the ticket re-armed for triage even if the
  2002	    # counter slips one tick.

 succeeded in 0ms:
   130	    tick_interval_seconds: float = DEFAULT_TICK_INTERVAL_SECONDS
   131	    jira_poll_interval_seconds: float = DEFAULT_JIRA_POLL_INTERVAL_SECONDS
   132	    sweep_interval_seconds: float = DEFAULT_SWEEP_INTERVAL_SECONDS
   133	    event_dedupe_seconds: float = DEFAULT_EVENT_DEDUPE_SECONDS
   134	
   135	    def __post_init__(self) -> None:
   136	        if self.capacity_path is None:
   137	            object.__setattr__(
   138	                self,
   139	                "capacity_path",
   140	                capacity_path_from_env(self.config_dir),
   141	            )
   142	        if self.bridge_events_path is None:
   143	            object.__setattr__(
   144	                self,
   145	                "bridge_events_path",
   146	                self.config_dir / "bridge-events.jsonl",
   147	            )
   148	
   149	    @classmethod
   150	    def from_env(
   151	        cls,
   152	        env: dict[str, str] | None = None,
   153	        *,
   154	        config_dir: Path | None = None,
   155	    ) -> "CoordinatorConfig":
   156	        env = env if env is not None else dict(os.environ)
   157	        base = (
   158	            Path(config_dir)
   159	            if config_dir is not None
   160	            else Path(env.get(CONFIG_DIR_ENV, str(DEFAULT_CONFIG_DIR))).expanduser()
   161	        )
   162	        decision_log_dir = Path(
   163	            env.get(DECISION_LOG_DIR_ENV, str(base / "decision-log"))
   164	        ).expanduser()
   165	        return cls(
   166	            config_dir=base,
   167	            heartbeat_path=base / "heartbeat",
   168	            decision_log_dir=decision_log_dir,
   169	            capacity_path=capacity_path_from_env(base, env),
   170	            bridge_events_path=Path(
   640	                    "[pipeline_coordinator] cold-start JIRA client unavailable "
   641	                    "(reconcile/sweep degraded): %s",
   642	                    exc,
   643	                )
   644	                self._client = None
   645	        return self._client
   646	
   647	    # ── Startup-1 ──
   648	    def audit_infra(self) -> InfraAuditResult:
   649	        return run_deployment_audit(
   650	            repo_root=self._repo_root, timeout_seconds=self._cli_timeout
   651	        )
   652	
   653	    def start_unit(self, unit: str) -> bool:
   654	        try:
   655	            proc = subprocess.run(
   656	                ["systemctl", "--user", "start", unit],
   657	                capture_output=True,
   658	                text=True,
   659	                timeout=self._cli_timeout,
   660	            )
   661	        except (OSError, subprocess.SubprocessError) as exc:
   662	            logger.warning("[pipeline_coordinator] systemctl start %s failed: %s", unit, exc)
   663	            return False
   664	        if proc.returncode != 0:
   665	            logger.warning(
   730	            return False
   731	
   732	    def branch_has_commits(self, key: str) -> bool:
   733	        return _feature_branch_has_commits(key, repo_root=self._repo_root)
   734	
   735	    def mark_resumable(self, key: str) -> None:
   736	        self._add_label(key, RESUME_FROM_FEATURE_LABEL)
   737	
   738	    def transition_under_review(self, key: str) -> None:
   739	        client = self._jira()
   740	        if client is None:
   741	            return
   742	        try:
   743	            from backend.agents import jira_dispatch
   744	
   745	            # Idempotent per OP-691: skips when already Under Review.
   746	            jira_dispatch.transition_to_under_review_if_needed(client, key)
   747	        except Exception as exc:  # noqa: BLE001
   748	            logger.warning("[pipeline_coordinator] transition_under_review(%s) failed: %s", key, exc)
   749	
   750	    def reset_to_todo(self, key: str) -> None:
   751	        client = self._jira()
   752	        if client is None:
   753	            return
   754	        try:
   755	            from backend.agents import jira_dispatch
   756	
   757	            jira_dispatch.transition_back_to_todo(
   758	                client, key,
   759	                reason="[cold-start Startup-2(c)] interrupted with no commits/activity; "
   760	                       "reverting to To Do for clean re-pickup.",
   761	            )
   762	        except Exception as exc:  # noqa: BLE001
   763	            logger.warning("[pipeline_coordinator] reset_to_todo(%s) failed: %s", key, exc)
   764	
   765	    # ── Startup-3 ──
   766	    def stale_sweep_plan(self) -> StaleSweepPlan:
   767	        client = self._jira()
   768	        if client is None:
   769	            return StaleSweepPlan()
   770	        try:
   771	            return _build_stale_sweep_plan(client, repo_root=self._repo_root)
   772	        except Exception as exc:  # noqa: BLE001
   773	            logger.warning("[pipeline_coordinator] stale-sweep plan failed: %s", exc)
   774	            return StaleSweepPlan()
   775	
   776	    def remove_label(self, key: str, label: str) -> None:
   777	        client = self._jira()
   778	        if client is None:
   779	            return
   780	        try:
   781	            from backend.agents import jira_dispatch
   782	
   783	            jira_dispatch.remove_label(client, key, label)
   784	        except Exception as exc:  # noqa: BLE001
   785	            logger.warning("[pipeline_coordinator] remove_label(%s, %s) failed: %s", key, label, exc)
   970	
   971	def _last_json_line(path: Path) -> dict[str, Any] | None:
   972	    try:
   973	        lines = path.read_text(encoding="utf-8").splitlines()
   974	    except OSError:
   975	        return None
   976	    for line in reversed(lines):
   977	        if line.strip():
   978	            try:
   979	                rec = json.loads(line)
   980	            except json.JSONDecodeError:
   981	                return None
   982	            return rec if isinstance(rec, dict) else None
   983	    return None
   984	
   985	
   986	def _ticket_has_live_runner(key: str, *, repo_root: Path | None = None) -> bool:
   987	    """ADR §3.3 Startup-2: a runner is live for ``key`` if a process cmdline
   988	    references it (auto-runner / its fresh feature branch) or an ephemeral
   989	    worktree sentinel for the ticket exists. Best-effort; any probe error
   990	    means "unknown" → treated as *not* live so reconciliation can proceed."""
   991	    branch = f"feature/{key}-runner-fresh"
   992	    proc_dir = Path("/proc")
   993	    if proc_dir.is_dir():
   994	        for entry in proc_dir.iterdir():
   995	            if not entry.name.isdigit():
  1138	    config: CoordinatorConfig, *, acting: bool = False
  1139	) -> ColdStartGateway:
  1140	    """Production gateway wiring (no I/O at construction — lazy + fail-open).
  1141	
  1142	    ``acting=False`` (the default — the 29f-14 7-day shadow canary, OP-1555)
  1143	    wraps the live gateway in :class:`ShadowColdStartGateway` so cold-start
  1144	    observes the world but performs zero JIRA/git/systemctl mutation;
  1145	    ``acting=True`` returns the live gateway so the 4-phase recovery mutates as
  1146	    designed. The same single ``acting`` switch gates the engine action layer
  1147	    and the sprint re-plan handler (see :func:`build_default_coordinator`).
  1148	    """
  1149	    live = JiraDispatchColdStartGateway()
  1150	    return live if acting else ShadowColdStartGateway(live)
  1151	
  1152	
  1153	# ── Daemon ────────────────────────────────────────────────────────────
  1154	
  1155	
  1156	class PipelineCoordinator:
  1157	    """Skeleton coordinator daemon: tick → evaluate → log no-op → heartbeat.
  1158	
  1159	    Collaborators are injected for deterministic tests; ``from_env`` /
  1160	    default construction wires the production defaults. The daemon holds
  1388	        # 29f-6: the Tier-2 LLM-consultation block (ADR Appendix C) — only on a
  1389	        # tick that actually consulted (or degraded) the LLM, so a Tier-1 / no-op
  1390	        # tick keeps its minimal record shape and ``budget_cap`` events stay
  1391	        # grep-able + replayable for the daily-spend rebuild.
  1392	        if result.llm_consultation is not None:
  1393	            record["llm_consultation"] = dict(result.llm_consultation)
  1394	        if action_results:
  1395	            record["action_results"] = action_results
  1396	        return record
  1397	
  1398	    # ── cold-start 4-phase recovery (ADR §3.3 + §9 L6) ──
  1399	
  1400	    def _log_cold_start(self, record: dict[str, Any]) -> None:
  1401	        """Append a cold-start decision-log record, stamping ts/pid/engine."""
  1402	        base: dict[str, Any] = {
  1403	            "ts": self._clock().isoformat(),
  1404	            "engine_version": self._engine.engine_version,
  1405	            "pid": os.getpid(),
  1488	        began: dict[str, dict[str, Any]] = {}
  1489	        completed: set[str] = set()
  1490	        last_real_action_tick: dict[str, Any] | None = None
  1491	        for rec in records:
  1492	            event = rec.get("event")
  1493	            if event == "decision_tick" and not rec.get("dry_run", True) and rec.get("actions"):
  1494	                last_real_action_tick = rec
  1495	            elif event == "shutdown_began":
  1496	                began[str(rec.get("decision_id", ""))] = rec
  1497	                # Snapshot which real-action tick preceded this drain.
  1498	                rec["_preceding_real_tick"] = last_real_action_tick
  1499	            elif event == "shutdown_complete":
  1500	                completed.add(str(rec.get("decision_id", "")))
  1501	        for drain_id, drain_rec in began.items():
  1502	            if drain_id in completed:
  1503	                continue  # clean shutdown — nothing to recover
  1504	            tick = drain_rec.get("_preceding_real_tick")
  1505	            resolutions = self._resolve_interrupted_actions(tick)
  1506	            entry = {
  1507	                "event": CRASH_RECOVERY_EVENT,
  1508	                "crashed_drain_id": drain_id,
  1509	                "resolutions": resolutions,
  1510	            }
  1511	            report.crash_recoveries.append(entry)
  1512	            self._log_cold_start(entry)
  1513	
  1514	    def _resolve_interrupted_actions(self, tick: dict[str, Any] | None) -> list[dict[str, Any]]:
  1515	        """Map each interrupted action of a crashed tick to a recovery step."""
  1516	        if not tick:
  1517	            return [{"resolution": "noop", "reason": "no in-flight real action before crash"}]
  1518	        gw = self._cold_start_gateway
  1519	        out: list[dict[str, Any]] = []
  1520	        for action in tick.get("actions", []):
  1688	        """One tick: heartbeat → refresh world → evaluate → log decision.
  1689	
  1690	        Returns the engine's :class:`DecisionResult` (a no-op in the
  1691	        skeleton) so callers/tests can assert on it directly.
  1692	        """
  1693	        self._heartbeat.touch()
  1694	        self._refresh_work_graph()
  1695	        self._append_source_event_records()
  1696	        ctx = self.build_context()
  1697	        if self._config.capacity_path is not None:
  1698	            write_capacity_snapshot(ctx.capacity, self._config.capacity_path)
  1699	        result = self._engine.evaluate(ctx)
  1700	        # Hand each real (non-noop) action to the action layer. In shadow
  1701	        # mode it only records the would-be execution; the outcomes go into
  1702	        # the decision-log line so a fired rule is observable end-to-end.
  1703	        action_results = [
  1704	            self._action_executor(a, ctx)
  1705	            for a in result.actions
  1706	            if not isinstance(a, NoopAction)
  1707	        ]
  1708	        record = self._build_tick_record(result, action_results)
  1709	        self._decision_log.append(record)
  1710	        self._run_learning_loop(record, result)
  1711	        return result
  1712	
  1878	    action executor, the hourly sprint re-plan handler, AND the 4-phase
  1879	    cold-start recovery to mutate; ``acting=False`` wires every one of them
  1880	    observe-only (cold-start via :class:`ShadowColdStartGateway`, OP-1555).
  1881	    """
  1882	    config = config or CoordinatorConfig.from_env()
  1883	    if acting and action_executor is None:
  1884	        raise ValueError("acting=True requires an explicit action_executor")
  1885	    if not acting and action_executor is not None:
  1886	        raise ValueError("action_executor requires acting=True")
  1887	    decision_log = DecisionLog(config.decision_log_dir)
  1888	    # 29f-11: the learning loop reads + appends to the same decision log; build
  1889	    # it from the log so its daily/weekly schedule survives a restart (§3.2).
  1890	    from backend.agents.learning_loop import build_default_learning_loop
  1891	
  1892	    return PipelineCoordinator(
  1893	        config,
  1894	        engine=build_hybrid_engine(decision_log_dir=config.decision_log_dir),
  1895	        action_executor=action_executor,
  1896	        cold_start_gateway=_default_cold_start_gateway(config, acting=acting),
  1897	        sprint_replan_handler=build_default_sprint_replan_handler(
  1898	            config_dir=config.config_dir,
  1899	            decision_log_dir=config.decision_log_dir,
  1900	            capacity_path=config.capacity_path,
  1901	            agent_class=config.jira_agent_class,
  1902	            shadow=not acting,
  1903	        ),
  1904	        decision_log=decision_log,
  1905	        learning_loop=build_default_learning_loop(decision_log),
  1906	    )
  1907	
  1908	
  1909	def main(argv: list[str] | None = None) -> int:
  1910	    """CLI entrypoint. ``--once`` runs a single tick; default runs forever."""
  1911	    import argparse
  1912	
  1913	    parser = argparse.ArgumentParser(description=__doc__)
  1914	    parser.add_argument(
  1915	        "--once",
  1916	        action="store_true",
  1917	        help="run a single tick (heartbeat + one no-op decision) and exit "
  1918	             "(skips cold-start recovery — a diagnostic tick, not a boot)",
  1919	    )
  1920	    parser.add_argument(
  1921	        "--no-cold-start",
  1922	        action="store_true",
  1923	        help="enter the steady-state loop directly, skipping the 4-phase "
  1924	             "cold-start recovery (ADR §3.3) — for local debugging only",
  1925	    )
  1926	    parser.add_argument(
  1927	        "--config-dir",
  1928	        type=Path,
  1929	        default=None,
  1930	        help="override the coordinator config dir (default ~/.config/omnisight/coordinator)",
  1931	    )
  1932	    args = parser.parse_args(argv)
  1933	
  1934	    logging.basicConfig(level=logging.INFO)
  1935	    config = CoordinatorConfig.from_env(config_dir=args.config_dir)
  1936	    coordinator = build_default_coordinator(config)
  1937	    if args.once:
  1938	        coordinator.run_once()
  1939	        return 0
  1940	    if args.no_cold_start:
  1941	        coordinator.run_forever()
  1942	        return 0
  1943	    # Production boot: 4-phase cold-start recovery, then steady-state loop.
  1944	    coordinator.run()
  1945	    return 0

exec
/bin/bash -lc "nl -ba backend/agents/idempotency.py | sed -n '34,45p;84,96p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
    34	    if override:
    35	        return Path(override).expanduser()
    36	    return Path("~/.config/omnisight/idem-keys.db").expanduser()
    37	
    38	
    39	IDEMPOTENCY_DB = _resolve_idempotency_db_path()
    40	TTL_SECONDS = 24 * 60 * 60
    41	
    42	F = TypeVar("F", bound=Callable[..., Any])
    43	
    44	
    45	class IdempotencyStore:
    84	            conn.execute(
    85	                "INSERT OR REPLACE INTO idempotency_keys (key, response, ts) "
    86	                "VALUES (?, ?, ?)",
    87	                (key, json.dumps(response, sort_keys=True, default=str), current),
    88	            )
    89	
    90	    def run(self, key: str, fn: Callable[[], Any]) -> Any:
    91	        self.prune()
    92	        cached = self.get(key)
    93	        if cached is not None:
    94	            return cached
    95	        response = fn()
    96	        self.put(key, response if response is not None else {})

exec
/bin/bash -lc "nl -ba backend/agents/sprint_replan.py | sed -n '136,150p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   136	
   137	    def __init__(self, gateway: SprintReplanGateway, *, shadow: bool = True) -> None:
   138	        self._gateway = gateway
   139	        self._shadow = shadow
   140	
   141	    def execute(self, action: Action) -> dict[str, Any]:
   142	        if action.kind == ACTION_RELABEL:
   143	            return self._execute_relabel(action)
   144	        if action.kind == ACTION_FILE_TICKET:
   145	            return self._execute_file_ticket(action)
   146	        if action.kind == ACTION_MENTION_OPERATOR:
   147	            return self._execute_mention(action)
   148	        return {
   149	            "kind": action.kind,
   150	            "target": action.target,

exec
/bin/bash -lc 'tail -n 120 docs/audit/codex-reviews/coordinator-acting-executor-design-v4-codex-audit-2026-05-22.txt' in /home/user/work/sora/OmniSight-Productizer
exec
/bin/bash -lc "nl -ba deploy/systemd/pipeline-coordinator.service | sed -n '24,38p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
    24	StartLimitBurst=3
    25	
    26	[Service]
    27	Type=simple
    28	WorkingDirectory=/home/user/sora-bridge
    29	Environment=PYTHONPATH=/home/user/sora-bridge
    30	Environment=PYTHONUNBUFFERED=1
    31	Environment=OMNISIGHT_COORDINATOR_CONFIG_DIR=/home/user/.config/omnisight/coordinator
    32	Environment=OMNISIGHT_COORDINATOR_DECISION_LOG_DIR=/home/user/.config/omnisight/coordinator/decision-log
    33	; OP-1005 / 29f-7: the no-arg entrypoint runs the 4-phase cold-start
    34	; recovery (ADR-0021 §3.3 + §9 L6 crash-replay) BEFORE entering the
    35	; steady-state loop — so every real boot reconciles infra + interrupted
    36	; JIRA work. The decision-log dir (OMNISIGHT_COORDINATOR_DECISION_LOG_DIR,
    37	; below) is auto-created 0700 on first write and is what L6 replays.
    38	ExecStart=/usr/bin/python3 -m backend.agents.pipeline_coordinator

 succeeded in 0ms:
  1226	        # 29f-11: learning loop (ADR §10). Default ``None`` keeps the skeleton /
  1227	        # test daemon inert; build_default_coordinator wires the Cognee
  1228	        # write-back + 24h outcome-check + weekly graduation handlers. When
  1229	        # present, every Tier-2 tick is written back within the tick ("within
  1230	        # 60s", Integration AC) and the daily/weekly schedules are gated here.
  1231	        self._learning_loop = learning_loop
  1232	        self._learning_seeded = False
  1233	        self._stop = threading.Event()
  1234	        # Interruptible sleep: default waits on the stop event so SIGTERM
  1235	        # breaks the loop within one poll instead of one tick.

exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator_llm_consultation.py | sed -n '1120,1155p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
  1120	            return DecisionResult(
  1121	                decision_id=uuid.uuid4().hex,
  1122	                actions=degraded_actions,
  1123	                reason=outcome.reason,
  1124	                mode=mode,
  1125	                engine_version=self.engine_version,
  1126	                mode_behavior=behavior,
  1127	                rule_name=rule_name,
  1128	                tier=1,
  1129	                llm_consultation=outcome.llm_consultation,
  1130	            )
  1131	        actions = outcome.actions or (NoopAction(),)
  1132	        return DecisionResult(
  1133	            decision_id=uuid.uuid4().hex,
  1134	            actions=actions,
  1135	            reason=outcome.reason,
  1136	            mode=mode,
  1137	            engine_version=self.engine_version,
  1138	            mode_behavior=behavior,
  1139	            rule_name=None,
  1140	            tier=2,
  1141	            llm_consultation=outcome.llm_consultation,
  1142	        )
  1143	
  1144	
  1145	def _is_conflict(fired: Sequence[tuple[RegisteredRule, Action]]) -> bool:
  1146	    """True when ≥2 rules fire mutually-distinct (conflicting) actions.
  1147	
  1148	    Identical actions (same kind/target/params) from two rules are not a
  1149	    conflict — they agree. A :class:`NoopAction` veto is handled before this
  1150	    is called, so any Noop here is incidental and ignored.
  1151	    """
  1152	    distinct: list[tuple[str, str, str]] = []
  1153	    for _rule, action in fired:
  1154	        if isinstance(action, NoopAction):
  1155	            continue

codex
Audit only; no files modified, no tests run.

1. **Q7 Option B wiring: PARTIAL**  
   The seam is right because `run_once` evaluates before execution at `backend/agents/pipeline_coordinator.py:1699`, executes actions before building the tick record at `:1703`, and `_build_tick_record()` derives both `actions` and top-level `dry_run` from `result.actions` at `:1358-1368`. `Action` is frozen and has `dry_run=True` by default at `backend/agents/pipeline_coordinator_rules.py:80-100`; Tier-1 and Tier-2 pass actions through at `pipeline_coordinator_rules.py:904-912` and `pipeline_coordinator_llm_consultation.py:1071-1082`.  
   Condition: v4’s pseudocode only rewrites a local `actions` tuple. Implementation must also rebuild the `DecisionResult` used for `_build_tick_record()` and learning-loop writeback, e.g. `result = dataclasses.replace(result, actions=actions)`. Otherwise logging remains `dry_run:true`.

2. **Idem hash: RESOLVED**  
   Stable semantic keys are required because `decision_id` is fresh per tick at `pipeline_coordinator_rules.py:266` and idempotency dedupes exact keys only at `backend/agents/idempotency.py:90-96`. Low-level JIRA calls accept `idem_key` through `_request_idempotent()` at `backend/agents/jira_dispatch.py:331-338`, transition/comment helpers at `:1649`, `:1934`, `:2014`, and volatile-field exclusion plus exact comment text inclusion is correct.

3. **N13 label helper: RESOLVED**  
   `fetch_ticket_labels(client, key)` is the real helper and rereads live labels at `backend/agents/jira_dispatch.py:1826-1841`. Fail-closed on exception is the right executor behavior because Tier-1 `coord-skip` only protects the context snapshot via `operator_keep_out()` at `backend/agents/pipeline_coordinator_rules.py:600-613`.

4. **Destructive recheck: PARTIAL**  
   `get_issue_status()` is the correct live status helper at `backend/agents/jira_dispatch.py:1586-1594`, and `transition_back_to_todo()` is destructive because it clears assignee at `:1934-1995`. `_ticket_has_live_runner()` exists at `backend/agents/pipeline_coordinator.py:986-1016`.  
   Condition: current `_ticket_has_live_runner()` treats probe errors as “not live” by design at `:987-990`. For the live executor’s destructive `To Do` transition, unknown/probe failure should abstain as `context_stale`, either by adding a strict wrapper or documenting/implementing a fail-closed variant.

5. **Cold-start defaults INFRA/RECONCILE/SWEEP=1: RESOLVED**  
   The mutation surfaces are correctly separated: Startup-1 `start_unit()` at `backend/agents/pipeline_coordinator.py:653-669`, Startup-2 JIRA mutations at `:735-763` and loop at `:1579-1607`, Startup-3 sweep mutations at `:1626-1641`. Defaults of `1/1/1` are reasonable for first flip.

6. **Fairness cursor persistence: RESOLVED**  
   Decision-log-derived is right. ADR blesses decision-log, heartbeat, and optional `state/` only for in-flight LLM persistence at `docs/adr/ADR-0021-release-pipeline-coordinator.md:105-107`, and says the daemon is stateless across restarts, rebuilding from JIRA, bridge logs, and decision log at `:117-120`. Do not use `state/` for this cursor unless ADR changes.

7. **Crash correlation: PARTIAL**  
   `decision_id` is acceptable as the `tick_intent` ↔ `decision_tick` key because each evaluated result gets a fresh UUID at `pipeline_coordinator_rules.py:266` and current `run_once()` has one evaluation per tick at `backend/agents/pipeline_coordinator.py:1699`. Existing L6 currently scans only `decision_tick` and shutdown records at `:1491-1505`; v4 correctly requires extending it to `tick_intent`. Existing log rotation is not a blocker because `_read_recent_decision_log()` already scans all day files spanning the 24h window at `:1544-1577`.  
   Conditions: `tick_intent` must carry the final stable idem keys, not just action payloads; replay must be limited to the same 24h window as the idempotency TTL at `backend/agents/idempotency.py:40`; and the Option B rewrite must preserve the same `decision_id` in the rebuilt `DecisionResult`.

**Q16:** Use `decision_id`; no separate `intent_id` is needed unless implementation introduces multiple intent records for one evaluated decision. If that happens, add `intent_seq`, not a whole new identity.

**Q17:** Rebuild from bounded decision-log tail across all date files in the replay window, not only today’s file. A 24h tail is consistent with current L6 and idempotency TTL; add a record-count cap only as a performance guard.

**Q18:** v4 still needs the two implementation conditions above: rebuild `DecisionResult` after Option B action replacement, and make destructive live-runner recheck fail closed on unknown.

FINAL VERDICT: file-ready-with-conditions — implement `result = replace(result, actions=actions)` after the acting dry-run flip; make destructive `To Do` runner recheck fail closed on probe uncertainty; ensure `tick_intent` stores final stable idem keys and is replayed only within the 24h idem window.
tokens used
78,385
Audit only; no files modified, no tests run.

1. **Q7 Option B wiring: PARTIAL**  
   The seam is right because `run_once` evaluates before execution at `backend/agents/pipeline_coordinator.py:1699`, executes actions before building the tick record at `:1703`, and `_build_tick_record()` derives both `actions` and top-level `dry_run` from `result.actions` at `:1358-1368`. `Action` is frozen and has `dry_run=True` by default at `backend/agents/pipeline_coordinator_rules.py:80-100`; Tier-1 and Tier-2 pass actions through at `pipeline_coordinator_rules.py:904-912` and `pipeline_coordinator_llm_consultation.py:1071-1082`.  
   Condition: v4’s pseudocode only rewrites a local `actions` tuple. Implementation must also rebuild the `DecisionResult` used for `_build_tick_record()` and learning-loop writeback, e.g. `result = dataclasses.replace(result, actions=actions)`. Otherwise logging remains `dry_run:true`.

2. **Idem hash: RESOLVED**  
   Stable semantic keys are required because `decision_id` is fresh per tick at `pipeline_coordinator_rules.py:266` and idempotency dedupes exact keys only at `backend/agents/idempotency.py:90-96`. Low-level JIRA calls accept `idem_key` through `_request_idempotent()` at `backend/agents/jira_dispatch.py:331-338`, transition/comment helpers at `:1649`, `:1934`, `:2014`, and volatile-field exclusion plus exact comment text inclusion is correct.

3. **N13 label helper: RESOLVED**  
   `fetch_ticket_labels(client, key)` is the real helper and rereads live labels at `backend/agents/jira_dispatch.py:1826-1841`. Fail-closed on exception is the right executor behavior because Tier-1 `coord-skip` only protects the context snapshot via `operator_keep_out()` at `backend/agents/pipeline_coordinator_rules.py:600-613`.

4. **Destructive recheck: PARTIAL**  
   `get_issue_status()` is the correct live status helper at `backend/agents/jira_dispatch.py:1586-1594`, and `transition_back_to_todo()` is destructive because it clears assignee at `:1934-1995`. `_ticket_has_live_runner()` exists at `backend/agents/pipeline_coordinator.py:986-1016`.  
   Condition: current `_ticket_has_live_runner()` treats probe errors as “not live” by design at `:987-990`. For the live executor’s destructive `To Do` transition, unknown/probe failure should abstain as `context_stale`, either by adding a strict wrapper or documenting/implementing a fail-closed variant.

5. **Cold-start defaults INFRA/RECONCILE/SWEEP=1: RESOLVED**  
   The mutation surfaces are correctly separated: Startup-1 `start_unit()` at `backend/agents/pipeline_coordinator.py:653-669`, Startup-2 JIRA mutations at `:735-763` and loop at `:1579-1607`, Startup-3 sweep mutations at `:1626-1641`. Defaults of `1/1/1` are reasonable for first flip.

6. **Fairness cursor persistence: RESOLVED**  
   Decision-log-derived is right. ADR blesses decision-log, heartbeat, and optional `state/` only for in-flight LLM persistence at `docs/adr/ADR-0021-release-pipeline-coordinator.md:105-107`, and says the daemon is stateless across restarts, rebuilding from JIRA, bridge logs, and decision log at `:117-120`. Do not use `state/` for this cursor unless ADR changes.

7. **Crash correlation: PARTIAL**  
   `decision_id` is acceptable as the `tick_intent` ↔ `decision_tick` key because each evaluated result gets a fresh UUID at `pipeline_coordinator_rules.py:266` and current `run_once()` has one evaluation per tick at `backend/agents/pipeline_coordinator.py:1699`. Existing L6 currently scans only `decision_tick` and shutdown records at `:1491-1505`; v4 correctly requires extending it to `tick_intent`. Existing log rotation is not a blocker because `_read_recent_decision_log()` already scans all day files spanning the 24h window at `:1544-1577`.  
   Conditions: `tick_intent` must carry the final stable idem keys, not just action payloads; replay must be limited to the same 24h window as the idempotency TTL at `backend/agents/idempotency.py:40`; and the Option B rewrite must preserve the same `decision_id` in the rebuilt `DecisionResult`.

**Q16:** Use `decision_id`; no separate `intent_id` is needed unless implementation introduces multiple intent records for one evaluated decision. If that happens, add `intent_seq`, not a whole new identity.

**Q17:** Rebuild from bounded decision-log tail across all date files in the replay window, not only today’s file. A 24h tail is consistent with current L6 and idempotency TTL; add a record-count cap only as a performance guard.

**Q18:** v4 still needs the two implementation conditions above: rebuild `DecisionResult` after Option B action replacement, and make destructive live-runner recheck fail closed on unknown.

FINAL VERDICT: file-ready-with-conditions — implement `result = replace(result, actions=actions)` after the acting dry-run flip; make destructive `To Do` runner recheck fail closed on probe uncertainty; ensure `tick_intent` stores final stable idem keys and is replayed only within the 24h idem window.

exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator_rules.py | sed -n '60,110p;250,272p;600,618p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
    60	ACTION_FILE_TICKET = "file_ticket"
    61	ACTION_RELABEL = "relabel"
    62	ACTION_TRANSITION = "transition"
    63	ACTION_MENTION_OPERATOR = "mention_operator"
    64	ACTION_MARK_FOR_FOLLOWUP = "mark_for_followup"
    65	ACTION_ESCALATE = "escalate"
    66	
    67	ALLOWED_ACTION_KINDS: frozenset[str] = frozenset(
    68	    {
    69	        ACTION_NOOP,
    70	        ACTION_FILE_TICKET,
    71	        ACTION_RELABEL,
    72	        ACTION_TRANSITION,
    73	        ACTION_MENTION_OPERATOR,
    74	        ACTION_MARK_FOR_FOLLOWUP,
    75	        ACTION_ESCALATE,
    76	    }
    77	)
    78	
    79	
    80	@dataclass(frozen=True)
    81	class Action:
    82	    """A single thing the coordinator decides to do.
    83	
    84	    The skeleton only ever emits :class:`NoopAction`; the Tier-1 rule set
    85	    (29f-3) emits the §6.1 action kinds via the factory classmethods below.
    86	    The envelope is unchanged: ``kind`` is one of :data:`ALLOWED_ACTION_KINDS`,
    87	    ``target`` is the JIRA key the action applies to, ``params`` carries the
    88	    kind-specific payload, and ``dry_run`` defaults to True so a half-wired
    89	    (or shadow-mode, 29f-14) daemon can never mutate JIRA/Gerrit.
    90	
    91	    "Union type" (Code AC): :class:`Action` + :class:`NoopAction` over the
    92	    constrained :data:`ALLOWED_ACTION_KINDS` *is* the action union — a closed
    93	    set discriminated by ``kind`` rather than a ``typing.Union`` of N classes,
    94	    so the decision-log serialisation (:meth:`to_record`) stays uniform.
    95	    """
    96	
    97	    kind: str
    98	    target: str = ""
    99	    params: Mapping[str, Any] = field(default_factory=dict)
   100	    dry_run: bool = True
   101	
   102	    def __post_init__(self) -> None:
   103	        if not isinstance(self.kind, str) or not self.kind.strip():
   104	            raise ValueError("Action.kind must be a non-empty string")
   105	        if self.kind not in ALLOWED_ACTION_KINDS:
   106	            raise ValueError(
   107	                f"Action.kind {self.kind!r} is not in the §6.1 allowed set "
   108	                f"{sorted(ALLOWED_ACTION_KINDS)}"
   109	            )
   110	        object.__setattr__(self, "params", dict(self.params))
   250	    # The wider work-graph slice the cross-ticket rules traverse
   251	    # (runner-blocked-marker-stale resolves the blocker's status;
   252	    # chain-deadlock walks the blockedBy DAG). Keyed by JIRA key.
   253	    tickets: Mapping[str, "Ticket"] = field(default_factory=dict)
   254	
   255	    def __post_init__(self) -> None:
   256	        if self.now.tzinfo is None:
   257	            raise ValueError("DecisionContext.now must be timezone-aware")
   258	        object.__setattr__(self, "work_graph", dict(self.work_graph))
   259	        object.__setattr__(self, "tickets", dict(self.tickets))
   260	
   261	
   262	@dataclass(frozen=True)
   263	class DecisionResult:
   264	    """Outcome of one ``evaluate`` call.
   265	
   266	    ``decision_id`` is a fresh UUID per tick so a decision-log line can be
   267	    correlated with downstream action records (and L6 crash-recovery can
   268	    pair ``shutdown_began`` / ``shutdown_complete`` with the right tick).
   269	    """
   270	
   271	    decision_id: str
   272	    actions: tuple[Action, ...]
   600	@rule(name="operator-keep-out", priority=0)
   601	def operator_keep_out(ctx: DecisionContext) -> "Action | None":
   602	    """§5.1 #10 — ``coord-skip`` present: take no action, ever.
   603	
   604	    Returns a :class:`NoopAction` (not ``None``) so, sitting at priority 0,
   605	    it short-circuits the whole registry: an operator escape-hatched ticket
   606	    can never be touched by a lower-priority rule (ADR §6.2 self-modify ban,
   607	    §6.3 operator override). Abstains (``None``) when there's no focal ticket
   608	    or the label is absent, so other rules still run.
   609	    """
   610	    t = ctx.ticket
   611	    if t is None or not has_label(t, COORD_SKIP_LABEL):
   612	        return None
   613	    return NoopAction()
   614	
   615	
   616	@rule(name="chain-deadlock", priority=5)
   617	def chain_deadlock(ctx: DecisionContext) -> "Action | None":
   618	    """§5.1 #8 — A blocks B blocks A (or longer cycle): diagnose + escalate.

exec
/bin/bash -lc "rg --files backend/tests | rg 'pipeline_coordinator|coldstart|systemd'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
backend/tests/test_pipeline_coordinator_skeleton.py
backend/tests/test_pipeline_coordinator_rules.py
backend/tests/test_pipeline_coordinator_coldstart.py
backend/tests/test_pipeline_coordinator_integration.py
backend/tests/test_pipeline_coordinator_events.py
backend/tests/test_pipeline_coordinator_learning.py
backend/tests/test_pipeline_coordinator_llm_consultation.py
backend/tests/test_pipeline_coordinator_watchdog.py
backend/tests/test_pipeline_coordinator_modes.py
backend/tests/test_pipeline_coordinator_chaos.py

exec
/bin/bash -lc "nl -ba backend/tests/test_pipeline_coordinator_coldstart.py | sed -n '466,482p;946,962p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   466	    report = ColdStartReport()
   467	
   468	    coord._l6_crash_recovery(report)
   469	
   470	    assert report.crash_recoveries == []
   471	
   472	
   473	def test_l6_resolves_interrupted_actions_by_kind(tmp_path: Path) -> None:
   474	    """The real-action tick preceding an unmatched drain is resolved per kind:
   475	    idempotent relabel/transition → completed (Under-Review transition is
   476	    re-issued); file_ticket → escalated via @-operator; anything else → noop."""
   477	    clock = FakeClock()
   478	    gw = FakeColdStartGateway()
   479	    coord = _coordinator(tmp_path, gw, clock=clock)
   480	    tick_ts = (clock() - timedelta(hours=2)).isoformat()
   481	    drain_ts = (clock() - timedelta(hours=1)).isoformat()
   482	    _seed_log(
   946	    assert fake.marked_resumable == ["OP-BRANCH"]
   947	    assert fake.reset_todo == ["OP-STALE"]
   948	    assert [m[0] for m in fake.operator_mentions] == ["OP-AMBIG"]
   949	    assert ("OP-A", "claim:default:OP-A") in fake.removed_labels
   950	    assert ("OP-C", "runner-blocked:waiting-OP-50") in fake.removed_labels
   951	    assert fake.cleared_assignees == ["OP-A", "OP-B"]
   952	
   953	
   954	def test_default_cold_start_gateway_shadow_vs_acting(tmp_path: Path) -> None:
   955	    """_default_cold_start_gateway returns the observe-only ShadowColdStartGateway
   956	    by default (acting=False) and the live JiraDispatchColdStartGateway only
   957	    when acting=True."""
   958	    cfg = _config(tmp_path)
   959	    shadow = _default_cold_start_gateway(cfg)
   960	    assert isinstance(shadow, ShadowColdStartGateway)
   961	    assert isinstance(shadow._delegate, JiraDispatchColdStartGateway)
   962	

exec
/bin/bash -lc "rg -n \"test_l6_resolves_interrupted_actions_by_kind|test_default_cold_start_gateway_shadow_vs_acting|pipeline_coordinator_systemd|OMNISIGHT_COORDINATOR|systemd\" backend/tests deploy docs -g '*.py' -g '*.md' -g '*.service'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
deploy/systemd/auto-promote-develop.service:10:; backend.audit, which connects via OMNISIGHT_DATABASE_URL. A systemd
deploy/systemd/auto-promote-develop.service:30:;   mkdir -p ~/.config/systemd/user
deploy/systemd/auto-promote-develop.service:31:;   cp deploy/systemd/auto-promote-develop.{service,timer} ~/.config/systemd/user/
deploy/systemd/auto-promote-develop.service:54:Environment=OP877_EVENT_LOG=/home/user/work/sora/logs/release-milestone/systemd.log
deploy/systemd/pipeline-coordinator.service:9:;   mkdir -p ~/.config/systemd/user
deploy/systemd/pipeline-coordinator.service:10:;   cp deploy/systemd/pipeline-coordinator.service ~/.config/systemd/user/
deploy/systemd/pipeline-coordinator.service:11:;   cp deploy/systemd/pipeline-coordinator-watchdog.service ~/.config/systemd/user/
deploy/systemd/pipeline-coordinator.service:31:Environment=OMNISIGHT_COORDINATOR_CONFIG_DIR=/home/user/.config/omnisight/coordinator
deploy/systemd/pipeline-coordinator.service:32:Environment=OMNISIGHT_COORDINATOR_DECISION_LOG_DIR=/home/user/.config/omnisight/coordinator/decision-log
deploy/systemd/pipeline-coordinator.service:36:; JIRA work. The decision-log dir (OMNISIGHT_COORDINATOR_DECISION_LOG_DIR,
deploy/systemd/pipeline-coordinator.service:44:StandardOutput=append:/home/user/work/sora/logs/coordinator/systemd.log
deploy/systemd/pipeline-coordinator.service:45:StandardError=append:/home/user/work/sora/logs/coordinator/systemd.log
docs/adr/ADR-0040-single-trunk-release-train.md:75:**Atomic change groups**: {deploy-prod.sh + check_deploy_ref.sh + allowlist + runbook} · {staging-sync + GitLab publication + staging compose registry/tag defaults} · {Gerrit project.config + auto-promote/release-cut systemd disablement} · {FE effective-flags endpoint + lib/api.ts client + provider/hook + enum alignment} · {ADR supersession + runbook + rollback policy}.
deploy/systemd/omnisight-frontend.service:1:; systemd unit for the OmniSight Next.js frontend.
deploy/systemd/backup-restore-test.service:1:; [OP-887] systemd unit -- weekly isolated Postgres restore test.
docs/adr/ADR-0010-deployment-automation.md:26:Use JIRA `fixVersion` as the canonical milestone marker. A systemd timer
docs/operations/docs-site-hosting-cf-tunnel.md:92:   ln -sf "$PWD/deploy/systemd/omnisight-docs-build.service" ~/.config/systemd/user/
docs/operations/docs-site-hosting-cf-tunnel.md:93:   ln -sf "$PWD/deploy/systemd/omnisight-docs-build.timer"   ~/.config/systemd/user/
docs/operations/docs-site-hosting-cf-tunnel.md:136:- Build pipeline: `scripts/build_docs_site.sh` + `deploy/systemd/omnisight-docs-build.{service,timer}`
docs/operations/bridge-daemon-deploy.md:17:The bridge runs under three user-scope systemd units:
docs/operations/bridge-daemon-deploy.md:20:   Owned by `deploy/systemd/gerrit-jira-bridge.service`. See
docs/operations/bridge-daemon-deploy.md:39:mkdir -p ~/.config/systemd/user
docs/operations/bridge-daemon-deploy.md:40:cp deploy/systemd/sora-bridge-sync.service ~/.config/systemd/user/
docs/operations/bridge-daemon-deploy.md:41:cp deploy/systemd/sora-bridge-sync.timer   ~/.config/systemd/user/
docs/adr/ADR-0023-foundation-rebuild.md:73:- Installing new systemd units that do NOT touch prod compose containers
docs/adr/ADR-0023-foundation-rebuild.md:76:- Modifying / installing files in `/home/user/sora-bridge/deploy/systemd/*.service` source dirs (does not affect running unit until enable)
docs/adr/ADR-0023-foundation-rebuild.md:86:The hard rule: **non-operator-window changes never touch prod compose container lifecycle**. Phase 31.G observability and 31.H systemd discipline must be designed to satisfy "permitted" semantics. Phase 31.I and 31.J explicitly require operator-window because they cross this boundary.
docs/adr/ADR-0023-foundation-rebuild.md:293:                │   tails systemd journal            │
docs/adr/ADR-0023-foundation-rebuild.md:325:Sub-phases that ARE runner-throughput bound (31.D replication, 31.F image dist, 31.G observability, 31.H systemd install) scale ~linearly with runners up to C2; beyond C2 the parallelism return is small because the sub-tickets are individually <0.5d.
docs/adr/ADR-0023-foundation-rebuild.md:377:- 25 of 84 systemd user units (dev subset)
docs/adr/ADR-0023-foundation-rebuild.md:475:**Goal**: 84/84 sora-bridge systemd units installed (or explicitly retired with rationale). Critical daemons have `WatchdogSec=`. `omnisight-compose-prod.service` installed so prod auto-recovers on host reboot.
docs/adr/ADR-0023-foundation-rebuild.md:535:                                       31.H systemd 59-install (1-2 wk; can start anytime)                                                           │
docs/adr/ADR-0023-foundation-rebuild.md:555:- **Always parallel-safe**: 31.G observability, 31.H systemd 59-install, 31.K doc drafting
docs/adr/ADR-0023-foundation-rebuild.md:591:| 31.H | restart unit changes break running service | each unit restart tested in dev first | systemd revert to last good unit |
docs/adr/ADR-0023-foundation-rebuild.md:772:- Code edits in `backend/`, `frontend/`, `scripts/`, `docs/`, `deploy/systemd/*` (writing new unit files for review, not enabling them)
docs/adr/ADR-0023-foundation-rebuild.md:806:| systemd units | 84 units' install state (installed yes/no on each WSL distro) | `systemctl --user list-units` per distro |
docs/adr/ADR-0023-foundation-rebuild.md:811:| Timers | All systemd timers + last fire + result | `systemctl --user list-timers --all` |
docs/adr/ADR-0023-foundation-rebuild.md:899:10. sora-bridge 84 systemd units only 25 installed → "shipped but not deployed" pattern
docs/adr/ADR-0023-foundation-rebuild.md:915:  31.H (systemd 59-install — install scripts only)
deploy/systemd/omnisight-staging-compose.service:1:; [OP-971] AUDIT-19a — systemd wrapper that activates the OP-767/878
deploy/systemd/omnisight-staging-compose.service:39:;   mkdir -p ~/.config/systemd/user
deploy/systemd/omnisight-staging-compose.service:40:;   cp deploy/systemd/omnisight-staging-compose.service ~/.config/systemd/user/
deploy/systemd/omnisight-staging-compose.service:49:;   systemd-cgls /user.slice/user-1000.slice/user@1000.service/app.slice/omnisight-staging-compose.service
docs/operations/memory-tool-monitoring-runbook.md:17:sudo cp deploy/systemd/memory-tool-monitor.{service,timer} /etc/systemd/system/
docs/operations/release-conductor-runbook.md:266:`OMNISIGHT_DATABASE_URL`. A systemd unit does **not** inherit the login
docs/operations/release-conductor-runbook.md:269:(`EnvironmentFile=-` in `deploy/systemd/auto-promote-develop.service`).
docs/operations/memory-tool.md:87:| `L` | 🚧 Only when `OMNISIGHT_MEMORY_TIER_L_OPTIN=1` | Set the env var in the runner systemd unit |
docs/adr/ADR-0022-docs-site-hosting-cf-tunnel.md:55:`squidfunk/mkdocs-material` Docker image and refreshed by a systemd timer.**
docs/adr/ADR-0022-docs-site-hosting-cf-tunnel.md:70:  `deploy/systemd/omnisight-docs-build.{service,timer}` rebuild daily.
docs/adr/ADR-0022-docs-site-hosting-cf-tunnel.md:119:  (not GitHub's edge); one more systemd timer to own; the build runs from the
docs/operations/runner-operations-sop.md:13:| Instances | `runner-claude@1`, `runner-claude@2`, `runner-codex@1`, `runner-codex@2` (Family-F user-systemd; `Linger=yes` → survive reboot) |
docs/operations/cloudflare_tunnel_wizard.md:52:The wizard uses **token mode** (`cloudflared tunnel run --token <T>`) instead of credentials files. This avoids file management complexity and works in both systemd and container environments.
docs/operations/cloudflare_tunnel_wizard.md:54:### systemd Mode
docs/operations/cloudflare_tunnel_wizard.md:55:If `cloudflared` is installed and a systemd unit exists, the service is managed via `systemctl`. A sudoers rule is required:
docs/operations/cloudflare_tunnel_wizard.md:63:If `OMNISIGHT_CF_MODE=container` is set, or no systemd is available, `cloudflared` is spawned directly as a child process.
docs/operations/gitlab-cr-cutover-checklist.md:176:      `/etc/omnisight/prod.env` and the systemd unit's
docs/operations/cognee-drift-runbook.md:38:| systemd service | `deploy/systemd/cognee-drift-detect.service` |
docs/operations/cognee-drift-runbook.md:39:| systemd timer | `deploy/systemd/cognee-drift-detect.timer` (04:00 UTC daily) |
docs/operations/cognee-drift-runbook.md:130:re-provisioned), the systemd timer can be stopped:
docs/operations/multi-instance-runner-runbook.md:21:   tmux/systemd  │   runner-codex-bot           │  pickup     │
docs/operations/multi-instance-runner-runbook.md:59:# systemd-based (persistent, supervised):
docs/operations/multi-instance-runner-runbook.md:253:            (codex-bot OR claude-bot tmux/systemd healthy).
docs/operations/multi-instance-runner-runbook.md:262:[ ] Day 7: convert tmux to systemd for persistence.
docs/operations/deployment.md:17:   WSL2 Ubuntu (systemd-managed)
docs/operations/deployment.md:32:- **systemd keeps it up across WSL restarts.** Three small units.
docs/operations/deployment.md:36:1. WSL2 Ubuntu 22.04+ with systemd enabled
docs/operations/deployment.md:37:   (`/etc/wsl.conf`: `[boot]\nsystemd=true`, then `wsl --shutdown`).
docs/operations/deployment.md:87:## 3. Install systemd units
docs/operations/deployment.md:90:`deploy/systemd/`; they carry `USERNAME` / `USER_HOME` placeholders
docs/operations/deployment.md:96:for f in deploy/systemd/*.service; do
docs/operations/deployment.md:98:    | sudo tee "/etc/systemd/system/$(basename $f)" >/dev/null
docs/operations/deployment.md:119:- **Recommended (Windows 11):** enable "Auto-proxy" + WSL systemd;
docs/operations/deployment.md:120:  systemd brings the services up on its own when WSL boots.
docs/operations/deployment.md:178:So the worst-case is the systemd unit fails to come up, not a
docs/operations/deployment.md:222:- **WSL restart kills the tunnel** — systemd should bring it back.
docs/runbook/post-deploy-recovery.md:160:Operator runs `scripts/deploy-prod.sh` either via cron, systemd, or
docs/runbook/post-deploy-recovery.md:189:Cron, systemd timers, ssh-with-command, and `bash -c` all run
docs/runbook/post-deploy-recovery.md:214:# so cron / systemd / non-interactive shells also see it.
docs/runbook/post-deploy-recovery.md:264:# 5-second preflight: simulate the cron / systemd shell environment
docs/operations/gitlab-runner-install.md:148:### 3. Replace the deb-installed systemd unit with the project-managed template
docs/operations/gitlab-runner-install.md:150:The upstream `.deb` ships a generic systemd unit that runs the daemon
docs/operations/gitlab-runner-install.md:153:`deploy/systemd/gitlab-runner.service` (companion to this runbook)
docs/operations/gitlab-runner-install.md:166:sudo cp /home/user/work/sora/OmniSight-Productizer/deploy/systemd/gitlab-runner.service \
docs/operations/gitlab-runner-install.md:167:        /etc/systemd/system/gitlab-runner.service
docs/operations/gitlab-runner-install.md:172:**If `deploy/systemd/gitlab-runner.service` is not yet present in the
docs/operations/gitlab-runner-install.md:174:`/lib/systemd/system/gitlab-runner.service` is acceptable for the
docs/operations/gitlab-runner-install.md:188:# 4a: systemd unit is up
backend/tests/test_cgroup_verify.py:7:systemd-managed docker writes.
docs/operations/runner-sandbox.md:54:Then enable enforcement in the systemd unit:
docs/operations/runner-sandbox.md:57:# ~/.config/systemd/user/runner-claude@.service
docs/operations/runner-sandbox.md:109:systemd unit — that turns the default into "always-on network" and
docs/operations/runner-sandbox.md:185:* Set `OMNISIGHT_RUNNER_SANDBOX_ENFORCE=1` in the active runner systemd
deploy/systemd/sdk-failure-audit-weekly.service:1:; [OP-822] systemd unit -- weekly SDK runner failure-pattern audit.
deploy/systemd/sdk-failure-audit-weekly.service:12:;   mkdir -p ~/.config/systemd/user
deploy/systemd/sdk-failure-audit-weekly.service:13:;   cp deploy/systemd/sdk-failure-audit-weekly.service ~/.config/systemd/user/
deploy/systemd/sdk-failure-audit-weekly.service:14:;   cp deploy/systemd/sdk-failure-audit-weekly.timer   ~/.config/systemd/user/
docs/codex-review-prompts/2026-05-21-retire-main-release-train-audit-prompt.md:39:A. **Additional `main` / release-cut / promote couplings** not in the list above — grep exhaustively (backend, scripts, deploy, .gerrit, .gitlab-ci.yml, frontend, systemd units, alembic, prometheus rules, Caddyfile, compose files, JIRA/conductor code). Include indirect couplings (env vars, hashtags, topics, JQL, label names, audit sinks).
docs/codex-review-prompts/2026-05-21-retire-main-release-train-audit-prompt.md:56:- Read at minimum: `docs/adr/ADR-0001-five-branch-gitflow.md`, `ADR-0020-*`, `ADR-0021-*`, `.gerrit/project.config`, `.gitlab-ci.yml`, `scripts/deploy-prod.sh`, `scripts/staging_deploy.sh`, `backend/agents/auto_tag_release.py`, `backend/agents/auto_promote_main.py`, `backend/routers/webhooks.py`, `backend/feature_flag_sdk.py`, `backend/agents/feature_flags.py`, `backend/routers/health.py`, `backend/frontend_compat.py`, and the `deploy/systemd/` release units. Then range wider as your findings dictate.
docs/operations/runner-usage-guide.md:72:(`OMNISIGHT_RUNNER_CLASS`, set per host via systemd drop-in). The two
docs/adr/ADR-0034-override-review-and-separation-of-duties.md:59:- `destructive_op_classes: git-ref-rewrite | systemd-control | env-edit` AND `destructive_op_scope: system`
docs/operations/replay-deprecation.md:37:1. **Stop calling `scripts/replay*.py` from cron / systemd / operator
docs/operations/replay-deprecation.md:87:| T+30 | `scripts/replay.py` deleted. Cron / systemd should be migrated by then. |
docs/operations/agent-rpg-system.md:45:| W3.3   | `backend/agents/style_fingerprint_cron.py` — daily recompute sweep + drift logging | **Live** as of 2026-05-16 (OP-130) — helper + drift threshold live; systemd timer wiring still owned by devops |
docs/operations/agent-rpg-system.md:753:Driven by `deploy/systemd/rpg-skill-decay.{service,timer}` (Mondays
docs/adr/ADR-0021-release-pipeline-coordinator.md:103:deploy/systemd/pipeline-coordinator.service      ← user systemd unit
docs/adr/ADR-0021-release-pipeline-coordinator.md:104:deploy/systemd/pipeline-coordinator-watchdog.service  ← separate watchdog (L2)
docs/adr/ADR-0021-release-pipeline-coordinator.md:208:| **Startup boot** | systemd `ExecStartPost`-fires-after-startup-complete event | once | 4-phase recovery (§3.3) |
docs/adr/ADR-0021-release-pipeline-coordinator.md:350:- Set env `OMNISIGHT_COORDINATOR_PAUSED=1` → coordinator daemon stays alive but takes zero actions until env unset
docs/adr/ADR-0021-release-pipeline-coordinator.md:351:- Set env `OMNISIGHT_COORDINATOR_DRY_RUN=1` → coordinator logs decisions but doesn't execute
docs/adr/ADR-0021-release-pipeline-coordinator.md:352:- `kill -SIGUSR1 <coord-pid>` → graceful drain + write final decision log + exit (systemd will restart unless `OMNISIGHT_COORDINATOR_DISABLE_RESTART=1` is also set)
docs/adr/ADR-0021-release-pipeline-coordinator.md:454:`pipeline-coordinator-watchdog.service` — a separate systemd unit, polls heartbeat
docs/adr/ADR-0021-release-pipeline-coordinator.md:608:                   - systemd user unit + watchdog unit
docs/adr/ADR-0021-release-pipeline-coordinator.md:642:- AUDIT-29c (runner systemd-ify) — required for coordinator to know runners are systemd-managed (so it can restart them on Phase Startup-1)
docs/adr/ADR-0021-release-pipeline-coordinator.md:681:| Coordinator stops touching all tickets | `OMNISIGHT_COORDINATOR_PAUSED=1` env |
docs/adr/ADR-0021-release-pipeline-coordinator.md:682:| Coordinator logs but doesn't act | `OMNISIGHT_COORDINATOR_DRY_RUN=1` env |
docs/adr/ADR-0021-release-pipeline-coordinator.md:684:| Coordinator runs but won't restart on crash | `OMNISIGHT_COORDINATOR_DISABLE_RESTART=1` + drain |
docs/operations/slo-monitor-rollback.md:10:`deploy/systemd/omnisight-slo-monitor.service` runs `python -m backend.slo_monitor`
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:24:| **B6 / Q14 cold-start caps** | PARTIAL — defaults? | **first-flip defaults `INFRA=1`, `RECONCILE=1`, `SWEEP=1`** (`OMNISIGHT_COORDINATOR_COLD_START_MAX_{INFRA,RECONCILE,SWEEP}`); raise only after canary evidence. Per-phase counters: Startup-1 `start_unit` (:653), Startup-2 transitions/labels (:735), Startup-3 sweep (:776). |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:69:- `self._acting` is set from the env-gate (`OMNISIGHT_COORDINATOR_ACTING`, S8) at
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:97:  try/except + kill-switch `OMNISIGHT_COORDINATOR_ACTING_KILL=1` (B4/S7).
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:98:- env-gate: `from_env` parses `OMNISIGHT_COORDINATOR_ACTING`; `main` builds the
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:99:  executor + `acting=True` when set (S8). Activation via **systemd drop-in**
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:130:(decision-log derived), dispatch, systemd drop-in, capability scope. Three items
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:169:- Update existing: `test_l6_resolves_interrupted_actions_by_kind` (coldstart:473,
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:170:  now must handle `tick_intent`), `test_default_cold_start_gateway_shadow_vs_acting`
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v4.md:171:  (coldstart:954, assert agent_class), `test_pipeline_coordinator_systemd.py:68`.
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:33:- **D4 — env-gate, default-safe.** CLI `main()` reads `OMNISIGHT_COORDINATOR_ACTING` (truthy → `acting=True` + `LiveActionExecutor`; default/unset → `acting=False` shadow). Code default stays acting=False. The flip is then a reversible env change + a coordinator restart — NOT a redeploy.
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:34:- **D5 — budget coupling.** Removing `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` happens AT the flip (restoring it while shadow pays for un-executed LLM calls). The env-gate doc must state both env changes go together.
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:50:- env-gate: `OMNISIGHT_COORDINATOR_ACTING` unset → shadow (acting=False, executor=Shadow); set → acting=True + LiveActionExecutor; `acting=True` without executor still raises.
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design.md:53:1. Set `OMNISIGHT_COORDINATOR_ACTING=1` + remove `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` in the coordinator `env.conf` drop-in.
docs/architecture/ADR-0036-forward-only-deploy-invariant.md:37:On 2026-05-14, the prod backend image was 7 days old; its bundled alembic migrations topped out at `0200_provider_usage_event.py`. The DB had been advanced to `0202` between 2026-05-07 and 2026-05-13 by operator-driven migrations against a freshly-merged develop branch. A Windows-host forced reboot stopped the container; on restart, the 7-day-old image attempted to serve traffic but its `/readyz` migration probe (correctly) returned 503 with `migration_pending: current=0202 latest_file=0200_provider_usage_event.py`. systemd marked the service `failed`, the `depends_on: service_healthy` chain stopped the rest of the compose stack, and the public URL was down from 05:29 (reboot) until 10:50 (manual recovery). The same drift had been silently failing readyz 118 consecutive times in the pre-reboot live container without alerting anyone. Full incident analysis: `docs/retrospectives/2026-05-14-host-reboot-image-db-drift.md`.
docs/architecture/ADR-0036-forward-only-deploy-invariant.md:88:8. **Exit 78 is the BEHIND exit code.** Not 1, not 70, not "fail-silent and serve partial traffic". `EX_CONFIG` (78) communicates to systemd / docker-compose / kubelet that this is a deploy-time misconfiguration — not a transient failure (so don't auto-restart) and not a program crash (so don't page on `up == 0` generically; page on `OmniSightAlembicDrift{drift_value=2}` instead).
docs/architecture/ADR-0036-forward-only-deploy-invariant.md:130:1. **Some restarts are not deploy-driven.** systemd, kubelet, host-reboot all start the container without the deploy script's involvement. If the container assumes EQUAL, those non-deploy restarts walk into AHEAD or BEHIND blind.
docs/architecture/ADR-0036-forward-only-deploy-invariant.md:150:- **Healthcheck wiring must stay on `/readyz` (not `/livez`) for the systemd dependency chain to work as designed.** The 2026-05-14 recovery's swap-to-/livez restored the URL but disabled the drift gate; reverting that swap is part of the rc2 closure checklist. Family ⑥ §10.3 + Family ⑦ ContractTest enforce this going forward.
docs/operations/backup-dr-runbook.md:40:sudo cp deploy/systemd/backup-daily.service /etc/systemd/system/
docs/operations/backup-dr-runbook.md:41:sudo cp deploy/systemd/backup-daily.timer /etc/systemd/system/
docs/operations/backup-dr-runbook.md:42:sudo cp deploy/systemd/backup-hourly.service /etc/systemd/system/
docs/operations/backup-dr-runbook.md:43:sudo cp deploy/systemd/backup-hourly.timer /etc/systemd/system/
docs/operations/backup-dr-runbook.md:44:sudo cp deploy/systemd/backup-restore-test.service /etc/systemd/system/
docs/operations/backup-dr-runbook.md:45:sudo cp deploy/systemd/backup-restore-test.timer /etc/systemd/system/
docs/operations/backup-dr-runbook.md:133:log. A failed service also leaves a non-zero systemd state and writes
docs/operations/backup-dr-runbook.md:162:sudo rm -f /etc/systemd/system/backup-daily.{service,timer}
docs/operations/backup-dr-runbook.md:163:sudo rm -f /etc/systemd/system/backup-hourly.{service,timer}
docs/operations/backup-dr-runbook.md:164:sudo rm -f /etc/systemd/system/backup-restore-test.{service,timer}
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:30:| S8 | D4 env-gate reads `OMNISIGHT_COORDINATOR_ACTING` | **SHOULD-FIX** — not implemented; `main` always `acting=False`, `from_env` reads no such var | **actually wire it**: `from_env` parses the flag; `main` builds the executor + `acting=True` when set. |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:90:1. cap at `OMNISIGHT_COORDINATOR_MAX_ACTIONS_PER_TICK` (**default 1** for first
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:96:3. a kill-switch env `OMNISIGHT_COORDINATOR_ACTING_KILL=1` forces the executor
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:107:- `OMNISIGHT_COORDINATOR_COLD_START_MAX_MUTATIONS` (default small, e.g. 5) — a
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:130:1. `CoordinatorConfig.from_env` (:149) parses `OMNISIGHT_COORDINATOR_ACTING`
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:145:Removing `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` happens AT the flip (shadow
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:180:   `dry_run=False` (Q7), capability-matrix / systemd EnvironmentFile gaps for
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:194:- env-gate: `OMNISIGHT_COORDINATOR_ACTING` unset → shadow; set → acting + Live
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:202:2. Set `OMNISIGHT_COORDINATOR_ACTING=1` + remove
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:203:   `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` in the coordinator `env.conf`
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v2.md:207:   intent. Rollback = `OMNISIGHT_COORDINATOR_ACTING_KILL=1` (no restart) OR unset
docs/operations/memory-tool-storage-runbook.md:60:`deploy/systemd/omnisight-backend.service`,
docs/operations/memory-tool-storage-runbook.md:61:`deploy/systemd/runner-claude@.service`, and
docs/operations/memory-tool-storage-runbook.md:62:`deploy/systemd/runner-codex@.service` set those defaults. The backend
docs/operations/memory-tool-storage-runbook.md:63:systemd unit also grants `ReadWritePaths=/var/omnisight/memory` because
docs/operations/memory-tool-storage-runbook.md:91:| `memory_dir_not_writable` | Backend cannot create or write the fleet dir at startup. | Re-run provisioning; check owner, mode, and systemd `ReadWritePaths`. |
docs/operations/memory-tool-storage-runbook.md:101:sudo cp deploy/systemd/memory-tool-storage-monitor.{service,timer} /etc/systemd/system/
docs/operations/cognee-runbook.md:157:| Nightly full rebuild | `0 3 * * *` | `deploy/systemd/cognee-nightly-rebuild.timer` → `python -m scripts.cognee_full_rebuild --repo-root /opt/omnisight` |
docs/operations/gitlab-cr-pull-credentials.md:414:  systemd unit). Confirm which user the pull runs as.
docs/operations/multi-instance-runner-provisioning.md:213:To run as a systemd service instead of tmux:
docs/operations/multi-instance-runner-provisioning.md:216:mkdir -p ~/.config/systemd/user
docs/operations/multi-instance-runner-provisioning.md:217:cp deploy/systemd/runner-codex@.service ~/.config/systemd/user/
docs/operations/multi-instance-runner-provisioning.md:223:The `@2` part of the unit name is the instance ID — systemd injects
docs/operations/staging-environment-runbook.md:6:**systemd-wrapped** staging stack on the 5a host. It does not replace the
docs/operations/staging-environment-runbook.md:14:What OP-971 adds on top of those: a **systemd user unit**
docs/operations/staging-environment-runbook.md:35:| systemd unit | `deploy/systemd/omnisight-staging-compose.service` | brings the compose up under `systemctl --user`, applies cgroup quota |
docs/operations/staging-environment-runbook.md:40:The systemd unit runs **user-level** (`systemctl --user`), matching the
docs/operations/staging-environment-runbook.md:54:#    Dry-run the contract check before wiring systemd:
docs/operations/staging-environment-runbook.md:60:mkdir -p ~/.config/systemd/user
docs/operations/staging-environment-runbook.md:61:cp deploy/systemd/omnisight-staging-compose.service ~/.config/systemd/user/
docs/operations/staging-environment-runbook.md:125:systemd-cgls /user.slice/user-1000.slice/user@1000.service/app.slice/omnisight-staging-compose.service
docs/operations/staging-environment-runbook.md:229:   On WSL: set `cgroupv2=1` (or `systemd.unified_cgroup_hierarchy=1`) for
docs/operations/staging-environment-runbook.md:236:   mkdir -p ~/.config/systemd/user/omnisight-staging-compose.service.d
docs/operations/staging-environment-runbook.md:237:   cat > ~/.config/systemd/user/omnisight-staging-compose.service.d/cgroup-v1.conf <<'EOF'
docs/operations/staging-environment-runbook.md:294:rm ~/.config/systemd/user/omnisight-staging-compose.service
docs/operations/staging-environment-runbook.md:304:Caddy ingress. This systemd unit instead owns the **default** compose
docs/operations/staging-environment-runbook.md:319:| 2026-05-12 | OP-971 | Initial runbook — systemd wrapper, env contract, cgroup quota, bring-up verification. |
deploy/systemd/omnisight-docs-build.service:8:; Install (user systemd):
deploy/systemd/omnisight-docs-build.service:9:;   ln -sf /home/user/work/sora/OmniSight-Productizer/deploy/systemd/omnisight-docs-build.service \
deploy/systemd/omnisight-docs-build.service:10:;          ~/.config/systemd/user/omnisight-docs-build.service
deploy/systemd/omnisight-docs-build.service:11:;   ln -sf /home/user/work/sora/OmniSight-Productizer/deploy/systemd/omnisight-docs-build.timer \
deploy/systemd/omnisight-docs-build.service:12:;          ~/.config/systemd/user/omnisight-docs-build.timer
deploy/systemd/gitlab-cr-monitor.service:26:; %h/.config/systemd/user/ per OP-877's existing sync mechanism):
docs/operations/release-image-pipeline.md:256:systemd `.timer` + `.service` pair installed on the prod monitoring
deploy/systemd/pipeline-coordinator-watchdog.service:25:Environment=OMNISIGHT_COORDINATOR_HEARTBEAT=/home/user/.config/omnisight/coordinator/heartbeat
deploy/systemd/pipeline-coordinator-watchdog.service:26:Environment=OMNISIGHT_COORDINATOR_WATCHDOG_POLL_SECONDS=30
deploy/systemd/pipeline-coordinator-watchdog.service:27:Environment=OMNISIGHT_COORDINATOR_WATCHDOG_STALE_SECONDS=90
deploy/systemd/pipeline-coordinator-watchdog.service:28:ExecStart=/usr/bin/bash -lc 'set -euo pipefail; heartbeat="$${OMNISIGHT_COORDINATOR_HEARTBEAT}"; poll="$${OMNISIGHT_COORDINATOR_WATCHDOG_POLL_SECONDS}"; stale="$${OMNISIGHT_COORDINATOR_WATCHDOG_STALE_SECONDS}"; while true; do now="$$(date +%%s)"; if [ -e "$$heartbeat" ]; then mtime="$$(stat -c %%Y "$$heartbeat" 2>/dev/null || echo 0)"; age="$$((now - mtime))"; else age="$$stale"; fi; if [ "$$age" -ge "$$stale" ]; then echo "coordinator heartbeat stale: path=$$heartbeat age=$$age stale=$$stale; restarting pipeline-coordinator.service"; systemctl --user restart pipeline-coordinator.service; fi; sleep "$$poll"; done'
docs/codex-review-prompts/2026-05-14-g-a-v2-spec-review-prompt.md:35:- Specifically scrutinise: `v2-A3` (11 validators?), `v2-⑥-RescueCLI` (CLI + override + audit), `v2-⑨-1bc` (probe + gauge + flag — split candidate?), `v2-⑧-2a` (systemd refactor — operator-window-tight?). List others you suspect are too coarse.
deploy/systemd/cloudflared.service:1:; systemd unit for cloudflared (Cloudflare Tunnel daemon).
deploy/systemd/runner-claude-bot@default.service:1:; [OP-1025] systemd user unit -- legacy claude-bot runner.
deploy/systemd/runner-claude-bot@default.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/runner-claude-bot@default.service:5:;   cp deploy/systemd/runner-claude-bot@default.service ~/.config/systemd/user/
backend/tests/test_deploy_sh_rolling.py:7:The legacy single-replica systemd flow must stay intact (operators
backend/tests/test_deploy_sh_rolling.py:14:These tests assert the *shape* of the script — no Docker / systemd
backend/tests/test_deploy_sh_rolling.py:79:        # Default must remain systemd so hosts that never adopted the
backend/tests/test_deploy_sh_rolling.py:82:            r'STRATEGY="?\$\{STRATEGY_ARG:-\$\{OMNISIGHT_DEPLOY_STRATEGY:-systemd\}\}"?',
backend/tests/test_deploy_sh_rolling.py:85:            "rolling mode must be opt-in (default strategy = systemd). "
backend/tests/test_deploy_sh_rolling.py:86:            "The resolution chain should be: positional arg > env var > systemd"
backend/tests/test_deploy_sh_rolling.py:91:            "the third positional arg must be `strategy` (rolling|systemd) "
backend/tests/test_deploy_sh_rolling.py:97:        # not silently fall through to systemd; strict validation catches
backend/tests/test_deploy_sh_rolling.py:100:            r'"\$STRATEGY"\s*!=\s*"rolling"\s*&&\s*"\$STRATEGY"\s*!=\s*"systemd"',
backend/tests/test_deploy_sh_rolling.py:104:            "'rolling' or 'systemd'"
backend/tests/test_deploy_sh_rolling.py:380:        # In systemd mode we only have one backend to smoke; in rolling
backend/tests/test_deploy_sh_rolling.py:397:# (8) Legacy-mode regression — systemd path must not be broken
backend/tests/test_deploy_sh_rolling.py:402:    def test_systemd_branch_still_calls_systemctl(self, deploy_sh_text: str) -> None:
backend/tests/test_deploy_sh_rolling.py:410:    def test_systemd_branch_still_uses_api_v1_health(self, deploy_sh_text: str) -> None:
backend/tests/test_deploy_sh_rolling.py:412:        # systemd flow predates /readyz and we keep it to avoid surprise
docs/operations/coordinator-runbook.md:20:The coordinator is a single long-running **user** systemd daemon
docs/operations/coordinator-runbook.md:50:| Daemon stdout/stderr | `/home/user/work/sora/logs/coordinator/systemd.log` |
docs/operations/coordinator-runbook.md:56:> shipped unit; set budget/model knobs with a systemd drop-in instead
docs/operations/coordinator-runbook.md:81:tail -f /home/user/work/sora/logs/coordinator/systemd.log
docs/operations/coordinator-runbook.md:131:| `OMNISIGHT_COORDINATOR_CONFIG_DIR` | `~/.config/omnisight/coordinator` | Base config dir (heartbeat, decision-log, capacity, bridge-events live under it). |
docs/operations/coordinator-runbook.md:132:| `OMNISIGHT_COORDINATOR_DECISION_LOG_DIR` | `<config_dir>/decision-log` | Append-only decision log dir (one `YYYY-MM-DD.jsonl` per UTC day). Shared with the bridge so both write one audit trail. |
docs/operations/coordinator-runbook.md:133:| `OMNISIGHT_COORDINATOR_BRIDGE_EVENTS_FILE` | `<config_dir>/bridge-events.jsonl` | Bridge event tap the coordinator tails (§4 source). |
docs/operations/coordinator-runbook.md:134:| `OMNISIGHT_COORDINATOR_JIRA_AGENT_CLASS` | `subscription-claude` | JIRA client class used for the 60 s poll + mutations. |
docs/operations/coordinator-runbook.md:141:| `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD` | `5.0` | Per-UTC-day Tier-2 spend cap. **`<= 0` disables Tier-2 entirely** — the operator kill switch for LLM spend. On cap-hit the daemon degrades to Tier-1-only and alerts (reason `tier2_degraded_budget_cap`). |
docs/operations/coordinator-runbook.md:142:| `OMNISIGHT_COORDINATOR_LLM_MODEL` | `claude-opus-4-7` | Model passed to the claude CLI. |
docs/operations/coordinator-runbook.md:143:| `OMNISIGHT_COORDINATOR_LLM_TIMEOUT_S` | `90.0` | Per-consult CLI timeout (seconds). |
docs/operations/coordinator-runbook.md:144:| `OMNISIGHT_COORDINATOR_CLAUDE_CLI` | `claude` | Path/name of the claude CLI binary. Must be on the daemon's `PATH`. |
docs/operations/coordinator-runbook.md:150:| `OMNISIGHT_COORDINATOR_HEARTBEAT` | `<config_dir>/heartbeat` | Heartbeat path read by the **bundled `.service` shell-loop** watchdog. |
docs/operations/coordinator-runbook.md:151:| `OMNISIGHT_COORDINATOR_HEARTBEAT_PATH` | `<config_dir>/heartbeat` | Heartbeat path read by the **Python** watchdog module (`pipeline_coordinator_watchdog.py:40`). Both name the same file; the two readers use different var names — set both if you relocate the heartbeat. |
docs/operations/coordinator-runbook.md:152:| `OMNISIGHT_COORDINATOR_WATCHDOG_SERVICE` | `pipeline-coordinator.service` | Unit the watchdog restarts on stale heartbeat. |
docs/operations/coordinator-runbook.md:153:| `OMNISIGHT_COORDINATOR_WATCHDOG_POLL_SECONDS` | `30` | Heartbeat poll interval. |
docs/operations/coordinator-runbook.md:154:| `OMNISIGHT_COORDINATOR_WATCHDOG_STALE_SECONDS` | `90` | Age beyond which the heartbeat is "stale" → restart. |
docs/operations/coordinator-runbook.md:155:| `OMNISIGHT_COORDINATOR_WATCHDOG_REDIE_SECONDS` | `300` | Re-die window: restarted-then-died-again inside this window escalates to operator (Python watchdog only). |
docs/operations/coordinator-runbook.md:167:There is no env file, so use a systemd user drop-in:
docs/operations/coordinator-runbook.md:173:#   Environment=OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=2.0
docs/operations/coordinator-runbook.md:222:> `OMNISIGHT_COORDINATOR_PAUSED`, `OMNISIGHT_COORDINATOR_DRY_RUN`,
docs/operations/coordinator-runbook.md:223:> `OMNISIGHT_COORDINATOR_DISABLE_RESTART`, and the `SIGUSR1`/`SIGUSR2`
docs/operations/coordinator-runbook.md:240:  `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` ([§3.1](#31-changing-an-env-knob))
docs/operations/coordinator-runbook.md:276:**Symptom:** `systemd.log` shows repeated boots; watchdog journal logs
docs/operations/coordinator-runbook.md:307:boundary), or `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD<=0`, or the claude
docs/operations/coordinator-runbook.md:309:knob and that `OMNISIGHT_COORDINATOR_CLAUDE_CLI` resolves on the
docs/operations/coordinator-runbook.md:354:| Stop touching all tickets | `OMNISIGHT_COORDINATOR_PAUSED=1` | ❌ Not implemented. Use `coord-skip` per-ticket, or stop the daemon (watchdog first, [§5.2](#52-pause-everything--the-watchdog-will-fight-you)). |
docs/operations/coordinator-runbook.md:355:| Log but don't act | `OMNISIGHT_COORDINATOR_DRY_RUN=1` | ❌ Not implemented as a knob — the daemon is **already** observe-only (shadow) by default. |
docs/operations/coordinator-runbook.md:357:| Run but don't restart on crash | `OMNISIGHT_COORDINATOR_DISABLE_RESTART=1` | ❌ Not implemented. `systemctl --user mask` the unit. |
docs/operations/coordinator-runbook.md:359:| Cap / disable LLM spend | (budget cap, ADR §5.3) | ✅ `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD` (`<=0` disables Tier-2). |
docs/operations/coordinator-runbook.md:370:- Units: `deploy/systemd/pipeline-coordinator.service`, `deploy/systemd/pipeline-coordinator-watchdog.service`
docs/research/merger-proactive-trigger-spike-2026-05.md:155:1. Operator confirms the bridge daemon process is up (systemd unit or
deploy/systemd/runner-instance-uniqueness-check.service:2:; Install: copy service+timer to /etc/systemd/system, daemon-reload,
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:32:| **NEW (Sec2): env wiring reality** | BLOCKER — unit uses inline `Environment=` (pipeline-coordinator.service:29-32), **no EnvironmentFile**; runbook:53 says so + use a drop-in | **activation rewritten** to a systemd drop-in (`systemctl --user edit`), NOT an `env.conf` file. See §Activation. |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:35:| **NEW (Sec2/3): test updates** | — | explicit test-update list (§Tests): `test_l6_resolves_interrupted_actions_by_kind` (coldstart:473), `test_default_cold_start_gateway_shadow_vs_acting` (coldstart:954), `test_pipeline_coordinator_systemd.py:68`. |
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:118:1. `OMNISIGHT_COORDINATOR_MAX_ACTIONS_PER_TICK` (default **1**). Excess →
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:124:4. Kill-switch `OMNISIGHT_COORDINATOR_ACTING_KILL=1` → observe-only without
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:135:Env: `OMNISIGHT_COORDINATOR_COLD_START_MAX_{INFRA,RECONCILE,SWEEP}`. First flip
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:168:(coordinator-runbook.md:53). So activation is a **systemd drop-in**, not an
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:173:   `Environment=OMNISIGHT_COORDINATOR_ACTING=1` and remove the
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:174:   `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` override (both together — D5).
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:178:   `OMNISIGHT_COORDINATOR_ACTING_KILL=1` (no restart) OR revert the drop-in +
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:196:- **Update existing**: `test_l6_resolves_interrupted_actions_by_kind`
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:197:  (coldstart:473), `test_default_cold_start_gateway_shadow_vs_acting`
docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md:198:  (coldstart:954, assert agent_class), `test_pipeline_coordinator_systemd.py:68`
deploy/systemd/runner-codex-bot@default.service:1:; [OP-1025] systemd user unit -- legacy codex-bot runner.
deploy/systemd/runner-codex-bot@default.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/runner-codex-bot@default.service:5:;   cp deploy/systemd/runner-codex-bot@default.service ~/.config/systemd/user/
deploy/systemd/omnisight-credential-expiry-check.service:1:; systemd oneshot unit for the daily credential expiry check (OP-727).
deploy/systemd/omnisight-credential-expiry-check.service:17:;   sudo cp deploy/systemd/omnisight-credential-expiry-check.{service,timer} \
deploy/systemd/omnisight-credential-expiry-check.service:18:;       /etc/systemd/system/
deploy/systemd/sora-bridge-sync.service:14:;   mkdir -p ~/.config/systemd/user
deploy/systemd/sora-bridge-sync.service:15:;   cp deploy/systemd/sora-bridge-sync.service ~/.config/systemd/user/
deploy/systemd/sora-bridge-sync.service:16:;   cp deploy/systemd/sora-bridge-sync.timer   ~/.config/systemd/user/
docs/retrospectives/2026-05-14-host-reboot-image-db-drift.md:19:Windows host force-installed an update at ~2026-05-14 05:29 CST while runners were mid-execution overnight. WSL2 began graceful shutdown but Docker / docker-compose subprocesses did not terminate within the 30 s `stop-sigterm` window, so systemd SIGKILL'd them and WSL emitted:
docs/retrospectives/2026-05-14-host-reboot-image-db-drift.md:25:Host stayed offline ~3 h 51 min (05:29 → 09:20). On reboot, `omnisight-compose-prod.service` started but failed at 09:22:04 because `backend-a` healthcheck never went healthy. systemd marked the unit `failed`, and the `depends_on: service_healthy` chain stopped frontend / caddy / cloudflared / installer from starting. The public URL was therefore down from 05:29 until manual recovery at 10:50.
docs/retrospectives/2026-05-14-host-reboot-image-db-drift.md:45:   503 returned. systemd unit went `failed`. Frontend chain never started.
docs/retrospectives/2026-05-14-host-reboot-image-db-drift.md:190:  - Bump `TimeoutStopSec` on the systemd unit to ~120 s.
deploy/systemd/gerrit-jira-bridge-watchdog.service:4:; configs/watchdog.yaml, checks each declared daemon's systemd unit
deploy/systemd/gerrit-jira-bridge-watchdog.service:12:;   mkdir -p ~/.config/systemd/user
deploy/systemd/gerrit-jira-bridge-watchdog.service:13:;   cp deploy/systemd/gerrit-jira-bridge-watchdog.service ~/.config/systemd/user/
deploy/systemd/gerrit-jira-bridge-watchdog.service:14:;   cp deploy/systemd/gerrit-jira-bridge-watchdog.timer ~/.config/systemd/user/
deploy/systemd/gerrit-jira-bridge-watchdog.service:20:; which is exactly what the T3 systemd-journal monitor catches as the
deploy/systemd/runner-codex-bot@worker2.service:1:; [OP-1025] systemd user unit -- codex-bot-2 runner.
deploy/systemd/runner-codex-bot@worker2.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/runner-codex-bot@worker2.service:5:;   cp deploy/systemd/runner-codex-bot@worker2.service ~/.config/systemd/user/
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:127:  - `run_once` caps executed actions at `OMNISIGHT_COORDINATOR_MAX_ACTIONS_PER_TICK`
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:132:  - **Kill-switch** `OMNISIGHT_COORDINATOR_ACTING_KILL=1` → observe-only without
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:166:  idempotent; 24h-window cutoff; **update `test_l6_resolves_interrupted_actions_by_kind`
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:174:  - `CoordinatorConfig.from_env` (:149) parses `OMNISIGHT_COORDINATOR_ACTING`
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:182:    `OMNISIGHT_COORDINATOR_COLD_START_MAX_{INFRA,RECONCILE,SWEEP}`, **defaults
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:193:  `test_default_cold_start_gateway_shadow_vs_acting` coldstart:954**); per-phase
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:194:  cap; **update `test_pipeline_coordinator_systemd.py:68` for new env**.
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:201:- **Code/Config AC**: systemd **drop-in** via `systemctl --user edit
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:202:  pipeline-coordinator` adding `Environment=OMNISIGHT_COORDINATOR_ACTING=1` (NOT an
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:205:  `OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD=0` override** (D5 — shadow pays for
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:210:  rollback verified = `OMNISIGHT_COORDINATOR_ACTING_KILL=1` (no restart) OR revert
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:240:| §Activation systemd drop-in + D5 + phased (operator) | T7 |
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:241:| update test_l6_resolves_interrupted_actions_by_kind | T5 |
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:242:| update test_default_cold_start_gateway_shadow_vs_acting | T6 |
docs/operations/2026-05-23-coordinator-acting-executor-ticket-draft.md:243:| update test_pipeline_coordinator_systemd.py:68 | T6 |
docs/operations/shutdown-restart.md:15:### 1.1 systemd topology (single-replica prod / WSL install)
docs/operations/shutdown-restart.md:28:| `backend-a`    | 8000 | `/readyz` | same as systemd backend (30 s in-flight drain) |
docs/operations/shutdown-restart.md:29:| `backend-b`    | 8001 | `/readyz` | same as systemd backend |
docs/operations/shutdown-restart.md:47:The script prefers systemd when `omnisight-backend.service` is
docs/operations/shutdown-restart.md:49:`--mode systemd|compose`.
docs/operations/shutdown-restart.md:100:| `systemd`    | single-replica host; config or code change | full (≤ 2 min) |
docs/operations/shutdown-restart.md:105:`auto` resolves to `systemd` if the backend unit is installed, else
docs/operations/shutdown-restart.md:183:1. `journalctl -u omnisight-backend -n 200` (systemd) or
deploy/systemd/backup-daily.service:1:; [OP-887] systemd unit -- daily Postgres dump to S3-compatible storage.
deploy/systemd/backup-daily.service:4:;   sudo cp deploy/systemd/backup-daily.service /etc/systemd/system/
deploy/systemd/backup-daily.service:5:;   sudo cp deploy/systemd/backup-daily.timer   /etc/systemd/system/
docs/operations/staging-environment.md:62:  --event-log /home/user/work/sora/logs/release-milestone/systemd.log \
docs/operations/staging-environment.md:87:  >> /home/user/work/sora/logs/release-milestone/systemd.log
docs/operations/gitlab-cr-monitor.md:66:The systemd unit files ship via this ticket but are NOT enabled. The
docs/operations/gitlab-cr-monitor.md:68:`deploy/systemd/gitlab-cr-monitor.{service,timer}` into
docs/operations/gitlab-cr-monitor.md:69:`%h/.config/systemd/user/` on the prod host. After that lands the
docs/operations/gitlab-cr-monitor.md:166:| 1    | Script + service + timer + docs files exist with the documented shape  | `scripts/verify_gitlab_cr.sh`, `deploy/systemd/gitlab-cr-monitor.{service,timer}`, this file |
docs/operations/gitlab-cr-monitor.md:173:- `deploy/systemd/gitlab-runner.service` — OP-1511 runner template
docs/operations/agent-drift-runbook.md:64:mkdir -p ~/.config/systemd/user
docs/operations/agent-drift-runbook.md:65:cp deploy/systemd/agent-drift-monthly.service ~/.config/systemd/user/
docs/operations/agent-drift-runbook.md:66:cp deploy/systemd/agent-drift-monthly.timer ~/.config/systemd/user/
deploy/systemd/rpg-skill-decay.service:1:; [OP-217] systemd unit -- weekly RPG.W12 skill XP decay sweep.
deploy/systemd/rpg-skill-decay.service:10:;   mkdir -p ~/.config/systemd/user
deploy/systemd/rpg-skill-decay.service:11:;   cp deploy/systemd/rpg-skill-decay.service ~/.config/systemd/user/
deploy/systemd/rpg-skill-decay.service:12:;   cp deploy/systemd/rpg-skill-decay.timer   ~/.config/systemd/user/
deploy/systemd/staging-pg-snapshot.service:16:; This unit pairs with deploy/systemd/omnisight-staging-compose.service
deploy/systemd/staging-pg-snapshot.service:22:; Exit codes -> systemd unit result:
deploy/systemd/staging-pg-snapshot.service:32:; use). A systemd unit does not inherit the login shell's environment, so the
deploy/systemd/staging-pg-snapshot.service:46:;   mkdir -p ~/.config/systemd/user
deploy/systemd/staging-pg-snapshot.service:47:;   cp deploy/systemd/staging-pg-snapshot.{service,timer} ~/.config/systemd/user/
docs/operations/2026-05-18-evening-operator-tasks.md:19:**Goal**: Create 5 Gerrit accounts (`codex-bot-codex-1/2/3`, `claude-bot-claude-1/2`) so Family F systemd runners can use per-instance authentication instead of the umbrella `codex-bot`/`claude-bot` fallback that today's tactical fix installed.
docs/operations/2026-05-18-evening-operator-tasks.md:99:### Step A.4 — Revert systemd to per-instance INSTANCE_ID
docs/operations/2026-05-18-evening-operator-tasks.md:105:    ~/.config/systemd/user/runner-${cls}@.service
docs/operations/2026-05-18-evening-operator-tasks.md:109:grep "INSTANCE_ID" ~/.config/systemd/user/runner-{claude,codex}@.service
docs/operations/2026-05-18-evening-operator-tasks.md:132:echo "Task A complete: 5 per-instance Gerrit accounts created + systemd reverted to per-instance INSTANCE_ID."
docs/operations/2026-05-18-evening-operator-tasks.md:250:**Goal**: Activate existing `scripts/backup_postgres_daily.sh` (already shipped; OP-887) with a systemd daily timer + S3 credentials. Replaces today's pg_dump-to-local-disk fallback.
docs/operations/2026-05-18-evening-operator-tasks.md:320:### Step C.5 — Wire systemd user timer for daily 03:00 UTC
docs/operations/2026-05-18-evening-operator-tasks.md:324:cat > ~/.config/systemd/user/omnisight-pgdump-s3-daily.service <<'EOF'
docs/operations/2026-05-18-evening-operator-tasks.md:339:cat > ~/.config/systemd/user/omnisight-pgdump-s3-daily.timer <<'EOF'
deploy/systemd/omnisight-backend.service:1:; systemd unit for the OmniSight FastAPI backend.
deploy/systemd/omnisight-backend.service:4:;   sudo cp deploy/systemd/omnisight-backend.service /etc/systemd/system/
deploy/systemd/omnisight-backend.service:17:; before systemd escalates to SIGKILL.
deploy/systemd/omnisight-backend.service:24:; ``image_head``, ``db_head``, ``drift_direction`` and exits 1 — systemd
docs/ops/autostart_wsl.md:15:│ Layer 2 — WSL systemd → docker.service (dockerd) starts     │  ← systemctl enable docker
docs/ops/autostart_wsl.md:19:│ Layer 4 — (safety net) systemd → `compose up -d` if any     │  ← systemd unit
docs/ops/autostart_wsl.md:78:  systemd (as PID 1) holds it alive.
docs/ops/autostart_wsl.md:109:## Layers 2–4 — WSL: dockerd + systemd unit
docs/ops/autostart_wsl.md:120:2. Installs `/etc/systemd/system/omnisight-compose-prod.service` — layer 4
docs/ops/autostart_wsl.md:166:| WSL systemd + dockerd ready | 10–20 s |
docs/operations/ci-worker-runbook.md:19:- `deploy/systemd/ci-worker.service` — systemd unit
docs/operations/ci-worker-runbook.md:26:mkdir -p ~/.config/systemd/user
docs/operations/ci-worker-runbook.md:27:cp deploy/systemd/ci-worker.service ~/.config/systemd/user/
deploy/systemd/sdk-cost-report.service:1:; [OP-821] systemd unit -- daily SDK CostGuard spend report.
deploy/systemd/sdk-cost-report.service:10:;   mkdir -p ~/.config/systemd/user
deploy/systemd/sdk-cost-report.service:11:;   cp deploy/systemd/sdk-cost-report.service ~/.config/systemd/user/
deploy/systemd/sdk-cost-report.service:12:;   cp deploy/systemd/sdk-cost-report.timer   ~/.config/systemd/user/
docs/operations/release-cut-runbook.md:138:orchestrator and its systemd units are installed and running on the
docs/operations/release-cut-runbook.md:147:# or, if the unit logs to a file: release_milestone_checker.systemd.log
docs/operations/disaster-recovery.md:34:Production host cron or systemd timers may wrap the same commands, but
deploy/systemd/omnisight-canary.service:1:; [OP-725] systemd unit — hourly Gerrit -> bridge daemon canary.
deploy/systemd/omnisight-canary.service:7:;   mkdir -p ~/.config/systemd/user
deploy/systemd/omnisight-canary.service:8:;   cp deploy/systemd/omnisight-canary.service ~/.config/systemd/user/
deploy/systemd/omnisight-canary.service:9:;   cp deploy/systemd/omnisight-canary.timer   ~/.config/systemd/user/
deploy/systemd/omnisight-canary.service:10:;   cp deploy/systemd/omnisight-canary-alert.service ~/.config/systemd/user/
deploy/systemd/omnisight-canary.service:36:ExecStart=/usr/bin/python3 %h/work/sora/OmniSight-claude-worktree/scripts/canary_pipeline.py --bridge-log %h/work/sora/logs/bridge/systemd.log
deploy/systemd/cognee-nightly-rebuild.service:4:;   sudo cp deploy/systemd/cognee-nightly-rebuild.service /etc/systemd/system/
deploy/systemd/cognee-nightly-rebuild.service:5:;   sudo cp deploy/systemd/cognee-nightly-rebuild.timer   /etc/systemd/system/
docs/design/2026-05-21-release-train-stories.md:31:Files: systemd user units (auto-promote-main/develop, auto-tag-release, release-cut-guard*); webhook trigger config/env; docs/operations rollback-boundary note.
docs/design/2026-05-21-release-train-stories.md:42:Out-of-scope: NO systemd/timer changes (=RT-01).
deploy/systemd/gerrit-jira-bridge.service:1:; systemd unit for the OmniSight Gerrit/JIRA bridge (OP-689 + OP-715 + OP-717).
deploy/systemd/gerrit-jira-bridge.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/gerrit-jira-bridge.service:5:;   cp deploy/systemd/gerrit-jira-bridge.service ~/.config/systemd/user/
deploy/systemd/gerrit-jira-bridge.service:21:; in the systemd journal in real time (without this Python buffers and
deploy/systemd/gerrit-jira-bridge.service:66:; not writable by user-level systemd; the resulting PermissionError
deploy/systemd/gerrit-jira-bridge.service:103:StandardOutput=append:/home/user/work/sora/logs/bridge/systemd.log
deploy/systemd/gerrit-jira-bridge.service:104:StandardError=append:/home/user/work/sora/logs/bridge/systemd.log
deploy/systemd/omnisight-hardware-bridge.service:4:;   sudo cp deploy/systemd/omnisight-hardware-bridge.service /etc/systemd/system/
docs/operations/credential_rotation_runbook.md:276:The systemd units in `deploy/systemd/` schedule the daily run:
docs/operations/credential_rotation_runbook.md:285:sudo cp deploy/systemd/omnisight-credential-expiry-check.{service,timer} \
docs/operations/credential_rotation_runbook.md:286:    /etc/systemd/system/
docs/operations/bubblewrap-profile.md:55:override is logged with the ticket key and must not be baked into systemd.
deploy/systemd/backup-hourly.service:1:; [OP-887] systemd unit -- hourly WAL archive marker for 1h RPO.
docs/operations/main-repo-sync.md:19:The runner is a bash loop in tmux, not a systemd unit. Each tick re-reads source from disk via `python3 auto-runner-jira.py`, so a successful pull is automatically picked up by the next tick (~90 s). The script just emits structured logs; no `systemctl restart` needed.
docs/operations/main-repo-sync.md:26:mkdir -p ~/.config/systemd/user
docs/operations/main-repo-sync.md:27:cp deploy/systemd/omnisight-main-sync.service ~/.config/systemd/user/
docs/operations/main-repo-sync.md:28:cp deploy/systemd/omnisight-main-sync.timer   ~/.config/systemd/user/
docs/operations/staging-migration-5a-to-5c-runbook.md:44:  5a systemd-wrapped stack (what we are migrating *from*).
docs/operations/staging-migration-5a-to-5c-runbook.md:59:| `deploy/systemd/omnisight-staging-compose.service` | `~/.config/systemd/user/*.{service,timer}` (the installed copies) |
docs/operations/staging-migration-5a-to-5c-runbook.md:60:| `deploy/systemd/staging-sync.{service,timer}` | `~/.config/omnisight/*.env` (release-audit DSN, per-host overrides, chmod 600) |
docs/operations/staging-migration-5a-to-5c-runbook.md:61:| `deploy/systemd/staging-pg-snapshot.{service,timer}` | `~/.config/omnisight/gerrit-claude-bot-ed25519` (bridge SSH key — only if the `bridge` profile is used) |
docs/operations/staging-migration-5a-to-5c-runbook.md:62:| `deploy/systemd/staging-gate-{canary,smoke}.{service,timer}`, `staging-gate-alert.service` | Docker named volumes (`staging-postgres`, `staging-artifacts`, `staging-sdks`, `staging-caddy-data`, `staging-caddy-config`) |
docs/operations/staging-migration-5a-to-5c-runbook.md:85:hand-editing anything except the host-prefix in the systemd units.
docs/operations/staging-migration-5a-to-5c-runbook.md:110:`deploy/systemd/*` units for the four anti-patterns below and assert none
docs/operations/staging-migration-5a-to-5c-runbook.md:133:| 4 | **Cgroup limits as fractions (auto-scale with host)** | The *aggregate* ceiling — the systemd `omnisight-staging-compose.service` `MemoryMax`/`CPUQuota` — must be a **percentage** (`NN%`), so it re-derives from the new box's RAM/CPU automatically. | ✅ `MemoryMax=30%`, `CPUQuota=30%`, `IOWeight=50` (a relative weight, not a byte count). |
docs/operations/staging-migration-5a-to-5c-runbook.md:140:* **Absolute paths in the systemd `[Service]` sections** —
docs/operations/staging-migration-5a-to-5c-runbook.md:143:  `StandardOutput=append:/home/user/work/sora/logs/...`. systemd
docs/operations/staging-migration-5a-to-5c-runbook.md:155:  systemd `MemoryMax=30%` (check #4), which is the one that matters.
docs/operations/staging-migration-5a-to-5c-runbook.md:170:systemd path prefix), fixing it is an `area:devops` change on the
docs/operations/staging-migration-5a-to-5c-runbook.md:179:path, reads tracked files only — no docker / systemd / network — and exits:
docs/operations/staging-migration-5a-to-5c-runbook.md:250:### 2.2 systemd in WSL2 — supported, but **off by default**
docs/operations/staging-migration-5a-to-5c-runbook.md:252:Recent WSL (≥ 0.67.6, shipping in Windows 11) supports running systemd as
docs/operations/staging-migration-5a-to-5c-runbook.md:254:(`omnisight-staging-compose.service`, the timers) is a **systemd user
docs/operations/staging-migration-5a-to-5c-runbook.md:260:systemd=true
docs/operations/staging-migration-5a-to-5c-runbook.md:306:* Make sure `systemd-timesyncd` is running inside the distro
docs/operations/staging-migration-5a-to-5c-runbook.md:307:  (`systemctl status systemd-timesyncd`) — with `systemd=true` it usually
docs/operations/staging-migration-5a-to-5c-runbook.md:308:  is; if not, `apt install systemd-timesyncd` and enable it.
docs/operations/staging-migration-5a-to-5c-runbook.md:365:* [ ] **[5c]** WSL2 Ubuntu-24.04 is installed, `systemd=true` is set
docs/operations/staging-migration-5a-to-5c-runbook.md:371:* [ ] **[5c]** `systemd-timesyncd` running (§2.4); `date -u` matches the
docs/operations/staging-migration-5a-to-5c-runbook.md:408:      into the systemd units in step 5. Also pick `$LOGDIR`
docs/operations/staging-migration-5a-to-5c-runbook.md:459:### Step 5 — Install + path-fix the systemd units on 5c
docs/operations/staging-migration-5a-to-5c-runbook.md:467:mkdir -p ~/.config/systemd/user
docs/operations/staging-migration-5a-to-5c-runbook.md:480:      "$REPO/deploy/systemd/$u" > "$HOME/.config/systemd/user/$u"
docs/operations/staging-migration-5a-to-5c-runbook.md:485:> Sanity-check the rewrite: `grep -rn '/home/user/' ~/.config/systemd/user/`
docs/operations/staging-migration-5a-to-5c-runbook.md:492:      dry-run it so a bad `.env` fails *before* systemd):
docs/operations/staging-migration-5a-to-5c-runbook.md:532:      .../logs/release-milestone/systemd.log` — the R3 gate verdict
docs/operations/staging-migration-5a-to-5c-runbook.md:554:* [ ] **[5a]** `rm ~/.config/systemd/user/{omnisight-staging-compose,staging-*}.{service,timer}`;
docs/operations/staging-migration-5a-to-5c-runbook.md:578:These are pure-Python/bash, no docker/systemd needed — they should be
docs/operations/staging-migration-5a-to-5c-runbook.md:615:by `deploy/systemd/staging-gate-{canary,smoke}.service`, which run
docs/operations/staging-migration-5a-to-5c-runbook.md:624:  `deploy/systemd/` set this runbook installs in step 5): the probe is
docs/operations/staging-migration-5a-to-5c-runbook.md:704:tail -n5 .../logs/release-milestone/systemd.log
docs/operations/staging-migration-5a-to-5c-runbook.md:768:  tail -n5 .../logs/release-milestone/systemd.log
deploy/systemd/omnisight-release-cut-guard.service:9:; Install: ln -sf <repo>/deploy/systemd/omnisight-release-cut-guard.{service,timer}
deploy/systemd/omnisight-release-cut-guard.service:10:;          ~/.config/systemd/user/  (or copy) ; daemon-reload ; enable --now timer.
deploy/systemd/omnisight-smoke-test.service:2:; Install: copy service+timer to /etc/systemd/system, daemon-reload,
deploy/systemd/auto-tag-release.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/auto-tag-release.service:5:;   cp deploy/systemd/auto-tag-release.service ~/.config/systemd/user/
deploy/systemd/runner-codex@.service:1:; systemd template unit — OmniSight codex runner instance N (OP-783).
deploy/systemd/runner-codex@.service:10:;   mkdir -p ~/.config/systemd/user
deploy/systemd/runner-codex@.service:11:;   cp deploy/systemd/runner-codex@.service ~/.config/systemd/user/
deploy/systemd/runner-codex@.service:27:;   * systemd persistent (this unit) replaces the tmux loop with a
deploy/systemd/omnisight-compose-prod.service:1:; systemd unit — OmniSight production compose stack auto-boot on WSL start.
deploy/systemd/omnisight-compose-prod.service:14:;     deploy/systemd/omnisight-compose-prod.service > /tmp/omnisight-compose-prod.service
deploy/systemd/omnisight-compose-prod.service:15:;   sudo install -m 644 /tmp/omnisight-compose-prod.service /etc/systemd/system/
deploy/systemd/omnisight-compose-prod.service:22:; so systemd actually runs it on WSL boot — without that, this unit waits
deploy/systemd/omnisight-compose-prod.service:47:; "containers actually flush" rather than "systemd gives up".
deploy/systemd/omnisight-compose-prod.service:81:; backend is force-killed; postgres-ha runs in its own systemd timeline,
docs/design/2026-05-21-release-train-workplan.md:17:- 🤖 = code child runner-pickable; 👤 = operator/ops-only (`runner:no-commits-expected`) for protected GitLab/Gerrit settings, prod env locks, prod rollback, systemd unit removal.
deploy/systemd/release-notes-generator.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/release-notes-generator.service:5:;   cp deploy/systemd/release-notes-generator.service ~/.config/systemd/user/
deploy/systemd/release-notes-generator.service:22:  --event-log /home/user/work/sora/logs/release-milestone/systemd.log \
docs/design/tiered-sandbox-architecture.md:99:* systemd 範本在 `deploy/systemd/omnisight-hardware-bridge.service`，預設
deploy/systemd/omnisight-graphiti-mcp.service:1:; systemd unit for the OmniSight Graphiti MCP service (OP-1021).
deploy/systemd/omnisight-graphiti-mcp.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/omnisight-graphiti-mcp.service:5:;   cp deploy/systemd/omnisight-graphiti-mcp.service ~/.config/systemd/user/
deploy/systemd/gitlab-runner.service:1:; systemd unit template for GitLab Runner on the OmniSight prod+dev host.
deploy/systemd/gitlab-runner.service:28:;   sudo cp deploy/systemd/gitlab-runner.service /etc/systemd/system/
deploy/systemd/gitlab-runner.service:37:; in the docker group with sudoless docker. System-level systemd is
deploy/systemd/gitlab-runner.service:45:; /etc/systemd/system/gitlab-runner.service automatically. This
deploy/systemd/tier-cooldown-daily.service:1:; [OP-807] systemd unit -- daily Tier-classification cooldown sweep.
deploy/systemd/tier-cooldown-daily.service:10:;   mkdir -p ~/.config/systemd/user
deploy/systemd/tier-cooldown-daily.service:11:;   cp deploy/systemd/tier-cooldown-daily.service ~/.config/systemd/user/
deploy/systemd/tier-cooldown-daily.service:12:;   cp deploy/systemd/tier-cooldown-daily.timer   ~/.config/systemd/user/
deploy/systemd/runner-claude@.service:1:; systemd template unit — OmniSight claude runner instance N (OP-783).
deploy/systemd/runner-claude@.service:7:;   cp deploy/systemd/runner-claude@.service ~/.config/systemd/user/
deploy/systemd/cognee-drift-detect.service:4:;   sudo cp deploy/systemd/cognee-drift-detect.service /etc/systemd/system/
deploy/systemd/cognee-drift-detect.service:5:;   sudo cp deploy/systemd/cognee-drift-detect.timer   /etc/systemd/system/
deploy/systemd/ci-worker.service:1:; systemd unit for the OmniSight CI worker (OP-742, parent META OP-739).
deploy/systemd/ci-worker.service:8:;   mkdir -p ~/.config/systemd/user
deploy/systemd/ci-worker.service:9:;   cp deploy/systemd/ci-worker.service ~/.config/systemd/user/
deploy/systemd/ci-worker.service:53:StandardOutput=append:/home/user/work/sora/logs/ci/systemd.log
deploy/systemd/ci-worker.service:54:StandardError=append:/home/user/work/sora/logs/ci/systemd.log
deploy/systemd/staging-gate-canary.service:15:; backend.audit.log, which connects via OMNISIGHT_DATABASE_URL. A systemd
deploy/systemd/staging-gate-canary.service:18:; auto-promote-develop.service uses; see deploy/systemd/auto-promote-develop.service).
deploy/systemd/staging-gate-canary.service:23:;   mkdir -p ~/.config/systemd/user
deploy/systemd/staging-gate-canary.service:24:;   cp deploy/systemd/staging-gate-canary.{service,timer} ~/.config/systemd/user/
deploy/systemd/staging-gate-canary.service:25:;   cp deploy/systemd/staging-gate-alert.service ~/.config/systemd/user/
deploy/systemd/runner-claude-bot@worker2.service:1:; [OP-1025] systemd user unit -- claude-bot-2 runner.
deploy/systemd/runner-claude-bot@worker2.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/runner-claude-bot@worker2.service:5:;   cp deploy/systemd/runner-claude-bot@worker2.service ~/.config/systemd/user/
deploy/systemd/staging-audit-verify-token.service:4:;   cp deploy/systemd/staging-audit-verify-token.service ~/.config/systemd/user/
deploy/systemd/submit-queue-worker.service:1:; systemd unit for the OmniSight submit-queue worker (OP-751).
deploy/systemd/submit-queue-worker.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/submit-queue-worker.service:5:;   cp deploy/systemd/submit-queue-worker.service ~/.config/systemd/user/
deploy/systemd/submit-queue-worker.service:47:StandardOutput=append:/home/user/work/sora/logs/submit-queue/systemd.log
deploy/systemd/submit-queue-worker.service:48:StandardError=append:/home/user/work/sora/logs/submit-queue/systemd.log
deploy/systemd/omnisight-worker@.service:1:; systemd unit template for the OmniSight stateless agent worker (O3).
deploy/systemd/omnisight-worker@.service:4:;   sudo cp deploy/systemd/omnisight-worker@.service /etc/systemd/system/
deploy/systemd/omnisight-worker@.service:23:; Graceful shutdown: systemd sends SIGTERM, the worker stops claiming
deploy/systemd/conflict-report.service:1:; [OP-746] systemd unit -- daily conflict-rate observability report.
deploy/systemd/conflict-report.service:10:;   mkdir -p ~/.config/systemd/user
deploy/systemd/conflict-report.service:11:;   cp deploy/systemd/conflict-report.service ~/.config/systemd/user/
deploy/systemd/conflict-report.service:12:;   cp deploy/systemd/conflict-report.timer   ~/.config/systemd/user/
deploy/systemd/conflict-report.service:35:Environment=OMNISIGHT_BRIDGE_LOG=%h/work/sora/logs/bridge/systemd.log
deploy/systemd/omnisight-main-sync.service:10:; tmux, not a systemd unit. Each tick re-reads source from disk, so a
deploy/systemd/omnisight-main-sync.service:17:;   mkdir -p ~/.config/systemd/user
deploy/systemd/omnisight-main-sync.service:18:;   cp deploy/systemd/omnisight-main-sync.service ~/.config/systemd/user/
deploy/systemd/omnisight-main-sync.service:19:;   cp deploy/systemd/omnisight-main-sync.timer   ~/.config/systemd/user/
deploy/systemd/staging-sync.service:20:; / staging-gate-*.service / staging-pg-snapshot.service use). A systemd unit
deploy/systemd/staging-sync.service:33:;   mkdir -p ~/.config/systemd/user
deploy/systemd/staging-sync.service:34:;   cp deploy/systemd/staging-sync.{service,timer} ~/.config/systemd/user/
deploy/systemd/staging-sync.service:35:;   cp deploy/systemd/staging-gate-alert.service   ~/.config/systemd/user/   # if not already there
deploy/systemd/staging-sync.service:41:;   cp deploy/systemd/staging-gate-{canary,smoke}.{service,timer} ~/.config/systemd/user/
deploy/systemd/staging-sync.service:44:; If systemd refuses (GateTimerEnableFailed — e.g. missing EnvironmentFile path,
deploy/systemd/staging-sync.service:55:;   tail -n5 /home/user/work/sora/logs/release-milestone/systemd.log   # milestone_ready from a non manual-override-* run_id
deploy/systemd/auto-promote-main.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/auto-promote-main.service:5:;   cp deploy/systemd/auto-promote-main.service ~/.config/systemd/user/
deploy/systemd/auto-promote-main.service:22:  --event-log /home/user/work/sora/logs/release-milestone/systemd.log \
deploy/systemd/neo4j.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/neo4j.service:5:;   cp deploy/systemd/neo4j.service ~/.config/systemd/user/
deploy/systemd/release-milestone-checker.service:1:; [OP-762] systemd unit for the release milestone readiness checker.
deploy/systemd/release-milestone-checker.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/release-milestone-checker.service:5:;   cp deploy/systemd/release-milestone-checker.{service,timer} ~/.config/systemd/user/
deploy/systemd/release-milestone-checker.service:28:StandardOutput=append:/home/user/work/sora/logs/release-milestone/systemd.log
deploy/systemd/release-milestone-checker.service:29:StandardError=append:/home/user/work/sora/logs/release-milestone/systemd.log
deploy/systemd/gerrit-jira-archive-sweep.service:4:; systemd scope so it reuses the bridge's JIRA/Gerrit bot credentials:
deploy/systemd/gerrit-jira-archive-sweep.service:6:;   mkdir -p ~/.config/systemd/user
deploy/systemd/gerrit-jira-archive-sweep.service:7:;   cp deploy/systemd/gerrit-jira-archive-sweep.service ~/.config/systemd/user/
deploy/systemd/gerrit-jira-archive-sweep.service:8:;   cp deploy/systemd/gerrit-jira-archive-sweep.timer ~/.config/systemd/user/
deploy/systemd/gerrit-jira-archive-sweep.service:24:Environment=OMNISIGHT_COORDINATOR_DECISION_LOG_DIR=/home/user/.config/omnisight/coordinator/decision-log
deploy/systemd/deployment-audit.service:9:;   mkdir -p ~/.config/systemd/user
deploy/systemd/deployment-audit.service:10:;   cp deploy/systemd/deployment-audit.{service,timer} ~/.config/systemd/user/
deploy/systemd/deployment-audit.service:11:;   cp deploy/systemd/deployment-audit-alert.service ~/.config/systemd/user/
deploy/systemd/omnisight-journal-error-forwarder.service:3:; Long-running daemon. Tails the systemd journal for ERROR / CRIT /
deploy/systemd/omnisight-journal-error-forwarder.service:14:;   sudo cp deploy/systemd/omnisight-journal-error-forwarder.service \
deploy/systemd/omnisight-journal-error-forwarder.service:15:;           /etc/systemd/system/
deploy/systemd/omnisight-journal-error-forwarder.service:19:; AC #4 — "Forwarder itself crashes → systemd restarts, no log loss
deploy/systemd/omnisight-journal-error-forwarder.service:31:;      permabug loop at 10 restarts in 10 min, after which systemd
deploy/systemd/omnisight-journal-error-forwarder.service:42:After=network-online.target systemd-journald.service
deploy/systemd/omnisight-journal-error-forwarder.service:44:Requires=systemd-journald.service
deploy/systemd/runner-orphan-reaper.service:1:; [OP-1059] systemd unit — C1 runner orphan reaper.
deploy/systemd/runner-orphan-reaper.service:10:;   mkdir -p ~/.config/systemd/user
deploy/systemd/runner-orphan-reaper.service:11:;   cp deploy/systemd/runner-orphan-reaper.service ~/.config/systemd/user/
deploy/systemd/runner-orphan-reaper.service:12:;   cp deploy/systemd/runner-orphan-reaper.timer   ~/.config/systemd/user/
deploy/systemd/staging-gate-smoke.service:17:; §6 audit trail + install instructions: see deploy/systemd/staging-gate-canary.service.
deploy/systemd/release-conductor-cron.service:1:; [OP-939] systemd unit for the release conductor daily cron skeleton.
deploy/systemd/release-conductor-cron.service:4:;   mkdir -p ~/.config/systemd/user
deploy/systemd/release-conductor-cron.service:5:;   cp deploy/systemd/release-conductor-cron.{service,timer} ~/.config/systemd/user/
deploy/systemd/release-conductor-cron.service:26:StandardOutput=append:/home/user/work/sora/logs/release-conductor/systemd.log
deploy/systemd/release-conductor-cron.service:27:StandardError=append:/home/user/work/sora/logs/release-conductor/systemd.log
docs/ops/runbook/daemon_silent.md:12:The systemd timer `gerrit-jira-bridge-watchdog.timer` fires
backend/tests/test_release_milestone_checker.py:18:SERVICE = REPO_ROOT / "deploy" / "systemd" / "release-milestone-checker.service"
backend/tests/test_release_milestone_checker.py:19:TIMER = REPO_ROOT / "deploy" / "systemd" / "release-milestone-checker.timer"
backend/tests/test_release_milestone_checker.py:189:def test_systemd_timer_runs_every_five_minutes() -> None:
docs/security/bs-installer-threat-model.md:118:Host bind-mount 必須 chown 給 `10001:10001`（compose `volumes:` block 加 `:Z` SELinux 標籤、`uid=10001,gid=10001` mount option for non-systemd hosts）。
deploy/systemd/jira-stale-audit.service:1:; [OP-873] systemd unit -- daily JIRA stale-ticket audit.
deploy/systemd/jira-stale-audit.service:8:;   mkdir -p ~/.config/systemd/user
deploy/systemd/jira-stale-audit.service:9:;   cp deploy/systemd/jira-stale-audit.service ~/.config/systemd/user/
deploy/systemd/jira-stale-audit.service:10:;   cp deploy/systemd/jira-stale-audit.timer   ~/.config/systemd/user/
docs/product/2026-05-20-what-is-omnisight.md:20:- 5 runner instances (claude × 2 + codex × 3) on Family F systemd, Linger=yes, OP-1137 ephemeral per-cycle clone — verified cold-start safe ([[reference_cold_start_safety_inventory]])
docs/ops/runbook/credential_expiry.md:13:`omnisight-credential-expiry-check.timer` systemd unit. It reads
docs/product/2026-05-14-what-is-omnisight.md:302:- WSL2 強制重啟時 systemd graceful shutdown 不夠時間 → Atlas v2-⑧ 修
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:48:| systemd unit shipped, not activated | OP-762 (`release-milestone-checker.timer`), OP-798 (`sora-bridge-sync.timer`), OP-877 (`auto-promote-develop.timer`) | unit not copied to `~/.config/systemd/user/`; not `enable --now`'d; `loginctl enable-linger` not set | timer never fires → no `milestone_ready` events → release chain stalls silently; bridge runs stale code |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:50:| container compose shipped, no operator started it | OP-767, OP-878 (`deploy/staging/docker-compose.yml`) | never `docker compose -f … up -d`; no systemd wrapper unit (`omnisight-compose-staging.service` does not exist) | `staging.sora.services` does not resolve → R3 `ci_canary` / `ci_smoke` gates have no producer → R3 cascade |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:60:deliverable is *infrastructure* — a systemd unit/timer, a container or compose
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:78:2. `deploy/systemd/*.{service,timer}` headers — every unit names its origin
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:89:> `deploy/systemd/`, will be missed. **Follow-up `AUDIT-23-a`**: run the JQL
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:108:| OP-762 | D | `release-milestone-checker.{service,timer}` + `scripts/release_milestone_checker.py` | **late** — unit shipped 2026-05-08; timer **not enabled until 2026-05-12** | no `milestone_ready`/`milestone_blocked` events → entire RELEASE META chain stalls with no signal | enabled 2026-05-12; **standing fix** = §6 DoD `deployed:` AC + the `deployment-audit.sh` cron | `systemd-timer` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:109:| OP-766 | D | `auto-promote-main.service` (`backend.agents.auto_promote_main` daemon, `Restart=always`) | **partial** — superseded-in-place: D5 now promotes via a Gerrit *review* change (`refs/for/main` + `milestone:R3-fastforward` hashtag, ADR-0016 / OP-960), not this daemon's direct push. The daemon may still be running, idle, or never installed — **host probe required** | if the daemon *is* still installed and pushing `develop:main` directly it races OP-960's Gerrit-review path → double-promote / non-FF abort loops | confirm on host whether `auto-promote-main.service` is active; if yes, decide keep-vs-retire vs OP-960 — tracked under **OP-960 follow-ups** (memory `project_d5_main_promote_mechanism.md`) | `systemd-unit` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:110:| OP-767 | D | `deploy/staging/docker-compose.yml` + `deploy/staging/caddy.json` (staging auto-deploy env) | **no** — compose file in repo; never `docker compose … up -d`; no `omnisight-compose-staging.service` wrapper exists in `deploy/systemd/` | `staging.sora.services` does not exist → D6 auto-deploy-staging is a no-op → R3 `ci_canary` + `ci_smoke` gates have **no producer** → **OP-925 R3 cascade** | **= OP-927 R5 / AUDIT-19** (stand up a develop-tracking staging env). Add the `omnisight-compose-staging.service` wrapper as part of that ticket | `container` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:111:| OP-798 | E | `sync_sora_bridge.sh` + `sora-bridge-sync.{service,timer}` (Phase A — automate sora-bridge git-sync) | **partial → flagged** — unit + timer shipped 2026-05-?; per the OP-925 cascade post-mortem the timer was **never enabled**; install recipe sits unrun in the unit header. Host probe needed to confirm current state | sora-bridge runs yesterday's code; staleness window (5 min) never closes; the OP-723 *liveness* watchdog stays green so nothing alerts | enable `sora-bridge-sync.timer` on the bridge host (`systemctl --user enable --now`); add it to the host's unit manifest checked by `deployment-audit.sh`. Tracked under **OP-798 deploy follow-up** (or co-opt the OP-925 cascade remediation ticket) | `systemd-timer` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:112:| OP-877 | D | `auto-promote-develop.{service,timer}` (daily 07:00 UTC `develop→refs/for/main`) | **partial** — unit shipped; host probe needed for `is-enabled`; **its OP-964 EnvironmentFile is the real gap** (next row) | if not enabled: D5 daily promote evaluation never runs → release chain depends on the 5-min checker only; if enabled but env-file missing: see OP-964 | confirm `is-enabled` on host; ship `release-audit.env` per OP-964 row | `systemd-timer` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:118:| OP-960 | D | D5 main-promote via Gerrit review (ADR-0016 + `scripts/auto_promote_develop_to_main.sh` pushing `refs/for/main`) | **yes (path)** — mechanism documented + script shipped + ADR-0016 accepted; the `develop→main` advance now goes through a Gerrit review change with `milestone:R3-fastforward` hashtag. Open items are ADR/script polish (memory `project_d5_main_promote_mechanism.md`), not the core path | n/a (core path live) | track residual polish under existing OP-960 follow-ups | `systemd-timer` (via `auto-promote-develop.timer`) |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:119:| OP-964 | F | AUDIT-16 — `EnvironmentFile=-/home/user/.config/omnisight/release-audit.env` in `auto-promote-develop.service` (+ in `staging-gate-{canary,smoke}.service`) + asyncpg audit-DB smoke test | **partial** — the *unit-file line* shipped (`deploy/systemd/auto-promote-develop.service:44`, `staging-gate-canary.service:49`, `staging-gate-smoke.service:37`); the `-` prefix means the file is **optional**, so if `~/.config/omnisight/release-audit.env` was never created on prod the sink silently writes to local SQLite — the exact OP-925 symptom | `release_audit` durable trail in pg-primary stays empty → "where is the audit row" → cannot prove a promotion happened | **create `~/.config/omnisight/release-audit.env` (mode 0600) on the prod host** with `OMNISIGHT_DATABASE_URL=postgresql+asyncpg://…@pg-primary:5432/omnisight`; verify with the runbook's "Audit DB connectivity smoke test". Also: the sink **still writes the generic `audit` table, not `release_audit` 0207** — that retarget is an open **backend** follow-up (memory `project_release_audit_sink_mismatch.md`), out of this ticket's area | `env-var` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:120:| OP-965 | F | AUDIT-17 — `staging-gate-{canary,smoke}.{service,timer}` + `staging-gate-alert.service` + `scripts/staging_gate.py` | **no (by design)** — units shipped + carry their install recipe; timers deliberately **left disabled** until a develop-tracking staging env exists (memory `project_staging_gate_gap.md`) | none *yet* — but the moment staging exists these MUST be enabled or R3 `ci_canary`/`ci_smoke` gates stay producerless | **enable the two timers as the final step of AUDIT-19 / OP-927 R5** (the unit headers say so); add to host unit manifest. ADR §2 prose deferred (docs out-of-area for OP-965, in-area here — see §6.4) | `systemd-timer` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:133:> The script's `systemd-timer` check therefore reports `last-trigger` /
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:146:| `systemd-unit` | `systemctl --user is-active <unit>` **and** `systemctl --user is-enabled <unit>` | `active` **and** `enabled` (not `linked`, not `static` masquerading as enabled) |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:147:| `systemd-timer` | `systemctl --user is-enabled <timer>` **and** `systemctl --user list-timers --all | grep <timer>` **and** `systemctl --user show <service> -p ActiveEnterTimestamp,Result` | timer `enabled` **and** appears in `list-timers` with a `LAST` within ~2× its cadence **and** the *service*'s last `Result=success` |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:200:so that **any ticket whose `Files touched` includes `deploy/`, a systemd unit,
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:223:Wrap `scripts/deployment-audit.sh` in `deploy/systemd/deployment-audit.{service,timer}`
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:236:> **Symptom**: Ticket merged + 公開済み, but the artefact (systemd unit, container,
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:270:| `FalsePositiveDeployed` — "enabled" ≠ "working" | §3 marks host-unverified rows `partial — host probe required`, not `yes`; §4's `systemd-timer` check reports the *service*'s `last Result=success` + `list-timers` LAST, not just `is-enabled`; the §3.1 caveat says so explicitly |
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:284:Schedule it (Tactic #3): copy `deploy/systemd/deployment-audit.{service,timer}`
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:285:(to be added in the §6.3 follow-up) to `~/.config/systemd/user/`, `systemctl
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:299:✓ OK    systemd-timer   release-milestone-checker.timer   enabled; service Result=success; LAST=~5min ago
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:300:✗ RED   systemd-timer   auto-promote-develop.timer        timer not installed
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:301:✗ RED   systemd-unit    auto-promote-main.service         unit not installed
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:303:✗ RED   systemd-timer   sora-bridge-sync.timer            timer not installed   (expected here — bridge host is separate)
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:305:✗ RED   systemd-timer   staging-gate-canary.timer         timer not installed   (gated by design)
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:306:✗ RED   systemd-timer   staging-gate-smoke.timer          timer not installed   (gated by design)
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:335:  closes that). `git log --all` on `develop`, `deploy/systemd/*` headers,
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:337:- Repo-side evidence cited inline: `deploy/systemd/auto-promote-develop.service:9-15,44`
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:340:  `deploy/blue-green/*`, `deploy/systemd/release-milestone-checker.service` header
docs/audit/2026-05-12-shipped-not-deployed-sprint-dEF.md:341:  (install recipe), `deploy/systemd/sora-bridge-sync.{service,timer}` headers.
deploy/gerrit/install-tier-hook.md:213:`GERRIT_HOOK_AUDIT_LOG=/dev/stderr` and piping stderr to systemd
backend/tests/test_bootstrap_reset.py:366:    """Pinning OMNISIGHT_DEPLOY_MODE=systemd → 403, no mutation."""
backend/tests/test_bootstrap_reset.py:368:    monkeypatch.setenv("OMNISIGHT_DEPLOY_MODE", "systemd")
backend/tests/test_bootstrap_reset.py:385:    assert body["deploy_mode"] == "systemd"
docs/audit/2026-05-12-audit-26-phase-0-verification.md:179:| 3 | `scripts/auto_promote_develop_to_main.sh` | Thin CLI wrapper around #1 (the D5 systemd timer entrypoint, `auto-promote-develop.timer`). | Inherits #1's break; fixed transitively by AUDIT-26d. No separate change. |
docs/audit/2026-05-12-audit-26-phase-0-verification.md:217:   `deployed:` AC so the systemd timer change is actually activated — see
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:106:| **Daemon startup contract** (Phase 1 runner systemd-ify) | `deploy/systemd/*.service` template exists for many services (gerrit-jira-bridge, release-milestone-checker etc.) | 4 runner tmux loops are the only daemons NOT systemd-managed. Need 4 new unit files + an SOP. |
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:132:| systemd user timers active | 8 | ✅ (linger=yes) |
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:133:| systemd user services running | 1 | ✅ (gerrit-jira-bridge) |
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:162:- systemd user units (linger=yes): auto-start, BUT:
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:197:  29b-i   Cognee container + Neo4j 5.24 systemd unit + on-host TLS  
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:204:Phase 3 — Runner systemd-ification (0.5d)
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:208:  29c-iv  Pre-commit hook: any new systemd service must have Restart= + StandardOutput= + ExecStart=
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:225:  29f-2   pipeline_coordinator skeleton + daemon + systemd + watchdog (0.5d)
docs/audit/2026-05-12-audit-29-phase-0-state-audit.md:294:| 29c Runner systemd-ify | 0.5d | 0.75d |
docs/audit/2026-05-13-bridge-watchdog-diagnosis.md:67:- `deploy/systemd/gerrit-jira-bridge-watchdog.timer` fires every 5 minutes.
docs/ops/runbook/unit_not_loaded.md:38:       ls /etc/systemd/system/gerrit-jira-bridge.service \
docs/ops/runbook/unit_not_loaded.md:39:          ~/.config/systemd/user/gerrit-jira-bridge.service 2>/dev/null
docs/ops/runbook/unit_not_loaded.md:47:       cp deploy/systemd/gerrit-jira-bridge.service \
docs/ops/runbook/unit_not_loaded.md:48:          ~/.config/systemd/user/gerrit-jira-bridge.service
docs/ops/runbook/unit_not_loaded.md:69:  `deploy/systemd/`. Drift here is also detected by T5
docs/audit/2026-05-13-linear-history-consumers.md:74:| 3 | `scripts/auto_promote_develop_to_main.sh` | Thin CLI/systemd-timer wrapper around #1. | No separate break (inherits #1). | **superseded-ish / inherits #1.** Still a wrapper; fixed transitively whenever #1's `evaluate_fast_forward` is fixed. No own change. | inherits #1 |
docs/audit/2026-05-13-deployment-audit-baseline.md:14:The daily systemd timer is the host equivalent of cron expression
docs/audit/2026-05-13-deployment-audit-baseline.md:34:| `OMNISIGHT_DATABASE_URL@auto-promote-develop.service` | The audit required a live PID and failed for the oneshot service. | The audit now verifies systemd `Environment` / `EnvironmentFile` configuration for oneshot services; `OMNISIGHT_DATABASE_URL` is configured via the host env file. |
docs/audit/2026-05-13-deployment-audit-baseline.md:35:| `alembic-head auto` | The audit ran Alembic from repo root and could not read the backend config. | The audit now runs Alembic from `backend/` and resolves the systemd-visible Alembic executable; prod DB reports `current=0245 == repo head`. |
docs/ops/runbook/canary_pipeline_check_failed.md:12:The systemd timer `omnisight-canary.timer` runs once an hour. Each
docs/ops/runbook/bridge_drift.md:13:checkout on the host (the directory the systemd unit's
docs/ops/production_deploy.md:16:* **Host reboot auto-recovery** (Windows Task Scheduler + systemd) → [`docs/ops/autostart_wsl.md`](./autostart_wsl.md)
docs/ops/production_deploy.md:131:#   - systemd unit for cloudflared
docs/ops/production_deploy.md:176:> strategies (`systemd` / `rolling` / `blue-green`) all assume the
docs/ops/production_deploy.md:213:### 4.1 First-time — Path A: systemd (single-host, recommended)
docs/ops/production_deploy.md:236:# ─── (c) Install systemd unit files (one-time) ────────────────
docs/ops/production_deploy.md:241:sudo cp deploy/systemd/omnisight-backend.service  /etc/systemd/system/
docs/ops/production_deploy.md:242:sudo cp deploy/systemd/omnisight-worker@.service  /etc/systemd/system/
docs/ops/production_deploy.md:243:sudo cp deploy/systemd/omnisight-frontend.service /etc/systemd/system/
docs/ops/production_deploy.md:244:sudo cp deploy/systemd/cloudflared.service        /etc/systemd/system/
docs/ops/production_deploy.md:329:| `systemd` (default) | Path A bootstrap (single-host) | `scripts/deploy.sh prod <tag>` |
docs/ops/production_deploy.md:337:  was labelled `requires-blue-green` but `systemd` strategy is
docs/ops/production_deploy.md:353:For a systemd rollback (Path A), checkout the previous tag and
docs/ops/production_deploy.md:440:# systemd path
docs/ops/production_deploy.md:477:# systemd
backend/tests/test_deploy_sh_rollback.py:303:            "rollback runs in the compose/Caddy topology, not systemd; "
backend/tests/test_deploy_sh_rollback.py:505:            "host systemd / external LB) so we delegate"
docs/ops/deploy_postmortem_2026-04-19.md:42:We initially followed `docs/ops/production_deploy.md §4.1 Path A` (systemd
docs/ops/deploy_postmortem_2026-04-19.md:45:Path B matches the documented topology. The systemd attempt was reverted via
docs/ops/deploy_postmortem_2026-04-19.md:218:  the systemd-path artifacts; safe to delete once this postmortem is reviewed.
backend/tests/test_bootstrap_start_services.py:6:  * ``systemd`` mode builds ``systemctl start omnisight-backend omnisight-frontend``
backend/tests/test_bootstrap_start_services.py:116:# ── systemd mode ──────────────────────────────────────────────────────
backend/tests/test_bootstrap_start_services.py:120:async def test_start_services_systemd_success(client, monkeypatch):
backend/tests/test_bootstrap_start_services.py:121:    """``systemd`` mode exec's ``sudo -n systemctl start`` for both units."""
backend/tests/test_bootstrap_start_services.py:129:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:135:    assert body["mode"] == "systemd"
backend/tests/test_bootstrap_start_services.py:151:async def test_start_services_systemd_nonzero_returns_502(client, monkeypatch):
backend/tests/test_bootstrap_start_services.py:163:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:168:    assert body["mode"] == "systemd"
backend/tests/test_bootstrap_start_services.py:183:    """systemd stderr 'sudo: a password is required' → kind=sudoers_missing."""
backend/tests/test_bootstrap_start_services.py:193:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:199:    assert body.get("mode") == "systemd"
backend/tests/test_bootstrap_start_services.py:204:    """systemd stderr 'Unit not found' → kind=unit_missing."""
backend/tests/test_bootstrap_start_services.py:214:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:223:    """systemd stderr w/o sudoers/unit signals → kind=unit_failed catch-all."""
backend/tests/test_bootstrap_start_services.py:233:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:246:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:263:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:284:    """docker-compose rc!=0 routes to unit_failed (sudoers/unit branches are systemd-only)."""
backend/tests/test_bootstrap_start_services.py:364:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:369:    assert body["mode"] == "systemd"
backend/tests/test_bootstrap_start_services.py:385:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:390:    assert body["mode"] == "systemd"
backend/tests/test_bootstrap_start_services.py:406:        json={"mode": "systemd"},
backend/tests/test_bootstrap_start_services.py:415:    assert after.get("mode") == "systemd"
backend/tests/test_bootstrap_start_services.py:452:    monkeypatch.setattr(_dm, "_SYSTEMD_RUN_DIR", tmp_path / "nope-systemd")
backend/tests/test_bootstrap_start_services.py:460:def test_start_command_systemd_is_sudo_wrapped():
backend/tests/test_bootstrap_start_services.py:461:    """systemd branch prefixes ``sudo -n`` so the K1 sudoers NOPASSWD grant
backend/tests/test_bootstrap_start_services.py:466:    cmd = _br._start_command("systemd", "")
docs/ops/observability_runbook.md:98:│     or crashed. Evict the pod / restart the systemd unit and
backend/tests/test_lifecycle.py:61:    """HA-01 spec: drain budget is 30 s; systemd's TimeoutStopSec=40
backend/tests/test_lifecycle.py:308:    """Liveness must NOT 503 — otherwise k8s/systemd will flap the
docs/ops/dr_manual_failover.md:185:# In the env file the app reads (docker-compose.prod.yml or systemd
backend/tests/test_staging_gate.py:33:SYSTEMD = REPO_ROOT / "deploy" / "systemd"
backend/tests/test_staging_gate.py:497:# ───────────────────────── systemd contract ─────────────────────────────
backend/tests/test_bootstrap_service_tick.py:8:  * ``systemd`` mode builds ``journalctl --follow -u …`` for both unit
backend/tests/test_bootstrap_service_tick.py:197:# ── systemd mode ──────────────────────────────────────────────────────
backend/tests/test_bootstrap_service_tick.py:201:async def test_service_tick_systemd_streams_stdout_lines(client, monkeypatch):
backend/tests/test_bootstrap_service_tick.py:213:        client, "/api/v1/bootstrap/service-tick?mode=systemd&tail=20",
backend/tests/test_bootstrap_service_tick.py:240:async def test_service_tick_systemd_interleaves_stderr(client, monkeypatch):
backend/tests/test_bootstrap_service_tick.py:249:        client, "/api/v1/bootstrap/service-tick?mode=systemd",
backend/tests/test_bootstrap_service_tick.py:320:        client, "/api/v1/bootstrap/service-tick?mode=systemd",
backend/tests/test_bootstrap_service_tick.py:345:        "/api/v1/bootstrap/service-tick?mode=systemd&max_seconds=1",
backend/tests/test_bootstrap_service_tick.py:364:    cmd = _br._tick_command("systemd", "", -10)
backend/tests/test_k8s_probes_g5_4.py:188:        # / systemd callers, but this manifest must use /livez.
backend/tests/test_pipeline_coordinator_coldstart.py:24:         test_l6_resolves_interrupted_actions_by_kind,
backend/tests/test_pipeline_coordinator_coldstart.py:56:      → test_default_cold_start_gateway_shadow_vs_acting,
backend/tests/test_pipeline_coordinator_coldstart.py:473:def test_l6_resolves_interrupted_actions_by_kind(tmp_path: Path) -> None:
backend/tests/test_pipeline_coordinator_coldstart.py:707:    """run_deployment_audit runs the audit script and maps systemd-unit /
backend/tests/test_pipeline_coordinator_coldstart.py:708:    systemd-timer JSONL rows to InfraUnits (live = status == 'OK'); non-unit
backend/tests/test_pipeline_coordinator_coldstart.py:715:            {"kind": "systemd-unit", "name": "coordinator.service", "status": "OK"},
backend/tests/test_pipeline_coordinator_coldstart.py:716:            {"kind": "systemd-unit", "name": "bridge.service", "status": "FAIL"},
backend/tests/test_pipeline_coordinator_coldstart.py:717:            {"kind": "systemd-timer", "name": "audit.timer", "status": "OK"},
backend/tests/test_pipeline_coordinator_coldstart.py:954:def test_default_cold_start_gateway_shadow_vs_acting(tmp_path: Path) -> None:
docs/ops/bootstrap_modes.md:17:| `systemd`        | `/run/systemd/system` present **or** `systemctl` on PATH | `sudo -n systemctl start omnisight-backend.service omnisight-frontend.service` | `journalctl -u <unit>… --follow …`                  | K1 sudoers NOPASSWD (see below)      |
docs/ops/bootstrap_modes.md:19:| `dev`            | Fallback — no systemd, no docker daemon                  | *(no-op — `uvicorn` / `next dev` are already running)*    | *(informational tick only)*                           | n/a                                   |
docs/ops/bootstrap_modes.md:32:- `mode` — the chosen `systemd` / `docker-compose` / `dev`
docs/ops/bootstrap_modes.md:33:- `in_docker`, `has_systemd`, `has_docker_socket`, `has_docker_binary`,
docs/ops/bootstrap_modes.md:38:  `"systemctl on PATH (no /run/systemd/system)"`)
docs/ops/bootstrap_modes.md:49:| 3 | `has_systemd`                                       | `systemd`        |
docs/ops/bootstrap_modes.md:53:First match wins. Row 3 (systemd) beats row 4 (docker) intentionally:
docs/ops/bootstrap_modes.md:54:if both are available on a bare-metal host, systemd units are the
docs/ops/bootstrap_modes.md:60:`systemd`, `docker-compose`, or `dev` to pin a mode. Unknown values are
docs/ops/bootstrap_modes.md:76:## Mode: `systemd`
docs/ops/bootstrap_modes.md:94:   `/etc/systemd/system/`:
docs/ops/bootstrap_modes.md:130:3. **Optional logging group** — add `omnisight` to `systemd-journal`
docs/ops/bootstrap_modes.md:139:| `Unit omnisight-backend.service not found`   | Unit files not installed or daemon-reload not run | Drop units into `/etc/systemd/system/` + `daemon-reload` |
docs/ops/bootstrap_modes.md:174:`systemd` — running `systemd` inside a plain container rarely works,
docs/ops/bootstrap_modes.md:200:  `npm run dev` by hand; no systemd, no docker on the box.
docs/ops/bootstrap_modes.md:210:the transition from `dev` to `systemd` / `docker-compose`
docs/ops/bootstrap_modes.md:233:  WSL + Cloudflare Tunnel (uses the `systemd` mode).
backend/tests/test_staging_snapshot_restore.py:22:  * deploy/systemd/staging-pg-snapshot.{service,timer} — daily 02:00,
backend/tests/test_staging_snapshot_restore.py:27:dep) only. No docker / systemd / Postgres required.
backend/tests/test_staging_snapshot_restore.py:43:SYSTEMD = REPO_ROOT / "deploy" / "systemd"
backend/tests/test_staging_snapshot_restore.py:422:# 5. systemd units
backend/tests/test_auto_promote_main.py:345:    event_log = tmp_path / "systemd.log"
backend/tests/test_slo_monitor.py:22:SYSTEMD_SERVICE = PROJECT_ROOT / "deploy" / "systemd" / "omnisight-slo-monitor.service"
backend/tests/test_slo_monitor.py:62:def test_monitor_systemd_worker_runs_persistent_30s_loop() -> None:
docs/ops/failure_modes.md:97:  systemd (`sudo systemctl daemon-reload && sudo systemctl restart
docs/ops/failure_modes.md:259:systemd-cgtop --iterations=1 --order=cpu | head -20
backend/tests/test_force_promote_integration.py:147:    #    auto-promote consumer tails — with a systemd-journal-shaped prefix in
backend/tests/test_force_promote_integration.py:152:    event_log = tmp_path / "release-milestone-systemd.log"
backend/tests/test_force_promote_integration.py:239:    event_log = tmp_path / "release-milestone-systemd.log"
backend/tests/test_deploy_mode.py:8:pytest happens to have systemd / docker / a docker socket.
backend/tests/test_deploy_mode.py:30:    systemd_run_dir: bool = False,
backend/tests/test_deploy_mode.py:48:    systemd = tmp_path / "run-systemd-system"
backend/tests/test_deploy_mode.py:57:    if systemd_run_dir:
backend/tests/test_deploy_mode.py:58:        systemd.mkdir()
backend/tests/test_deploy_mode.py:68:    monkeypatch.setattr(_dm, "_SYSTEMD_RUN_DIR", systemd)
backend/tests/test_deploy_mode.py:113:def test_has_systemd_detects_run_dir_plus_binary(monkeypatch, tmp_path):
backend/tests/test_deploy_mode.py:117:        systemd_run_dir=True,
backend/tests/test_deploy_mode.py:120:    detected, evidence = _dm._has_systemd()
backend/tests/test_deploy_mode.py:125:def test_has_systemd_detects_run_dir_only(monkeypatch, tmp_path):
backend/tests/test_deploy_mode.py:126:    _stub_host(monkeypatch, tmp_path, systemd_run_dir=True)
backend/tests/test_deploy_mode.py:127:    detected, evidence = _dm._has_systemd()
backend/tests/test_deploy_mode.py:132:def test_has_systemd_detects_binary_only(monkeypatch, tmp_path):
backend/tests/test_deploy_mode.py:137:    detected, evidence = _dm._has_systemd()
backend/tests/test_deploy_mode.py:142:def test_has_systemd_false_on_clean_host(monkeypatch, tmp_path):
backend/tests/test_deploy_mode.py:144:    detected, evidence = _dm._has_systemd()
backend/tests/test_deploy_mode.py:146:    assert "no systemd" in evidence
backend/tests/test_deploy_mode.py:220:    _stub_host(monkeypatch, tmp_path, systemd_run_dir=True,
backend/tests/test_deploy_mode.py:230:    _stub_host(monkeypatch, tmp_path, systemd_run_dir=True,
backend/tests/test_deploy_mode.py:240:        systemd_run_dir=True,
backend/tests/test_deploy_mode.py:247:    assert result.mode == "systemd"
backend/tests/test_deploy_mode.py:300:def test_systemd_host_picks_systemd(monkeypatch, tmp_path):
backend/tests/test_deploy_mode.py:303:        systemd_run_dir=True,
backend/tests/test_deploy_mode.py:308:    assert result.mode == "systemd"
backend/tests/test_deploy_mode.py:309:    assert result.has_systemd is True
backend/tests/test_deploy_mode.py:344:    assert result.has_systemd is False
backend/tests/test_deploy_mode.py:352:        systemd_run_dir=True,
backend/tests/test_deploy_mode.py:358:        "mode", "in_docker", "has_systemd", "has_docker_socket",
backend/tests/test_deploy_mode.py:362:    assert payload["signals"].get("systemd")
backend/tests/test_deploy_mode.py:366:def test_systemd_preferred_over_docker_when_both_present(monkeypatch, tmp_path):
backend/tests/test_deploy_mode.py:367:    """Regression: a bare-metal host with both signals must pick systemd
backend/tests/test_deploy_mode.py:372:        systemd_run_dir=True, docker_socket=True,
backend/tests/test_deploy_mode.py:379:    assert _dm.detect_deploy_mode().mode == "systemd"
backend/tests/test_conflict_report.py:561:def test_parse_bridge_log_handles_systemd_prefix(tmp_path) -> None:
backend/tests/test_auto_promote.py:41:* ``test_systemd_*`` — unchanged systemd unit contract.
backend/tests/test_auto_promote.py:60:SERVICE = REPO_ROOT / "deploy" / "systemd" / "auto-promote-develop.service"
backend/tests/test_auto_promote.py:61:TIMER = REPO_ROOT / "deploy" / "systemd" / "auto-promote-develop.timer"
backend/tests/test_auto_promote.py:560:# systemd contract: daily 07:00 UTC trigger; service runs the cron once
backend/tests/test_auto_promote.py:564:def test_systemd_timer_fires_daily_at_07_utc() -> None:
backend/tests/test_auto_promote.py:571:def test_systemd_service_invokes_the_promote_script() -> None:
backend/tests/test_auto_promote.py:584:def test_systemd_service_loads_audit_db_env() -> None:
backend/tests/test_release_notes_generator.py:13:SERVICE = REPO_ROOT / "deploy" / "systemd" / "release-notes-generator.service"
backend/tests/test_release_notes_generator.py:184:    event_log = tmp_path / "systemd.log"
backend/tests/test_release_notes_generator.py:187:        "systemd prefix "
backend/tests/test_release_notes_generator.py:247:def test_systemd_service_tails_release_log_with_sub_five_minute_poll() -> None:
backend/tests/test_release_notes_generator.py:251:    assert "--event-log /home/user/work/sora/logs/release-milestone/systemd.log" in text
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:12:  - scripts/deployment-audit.sh (today's systemd-unit / timer / container audit; this contract EXTENDS — does not replace — that file's responsibility into image-version auditing)
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:33:This spec is the **contract** — not the implementation. Code lands in `v2-⑤-1bc` (the `/version` endpoint + tests), `v2-⑤-Dockerfile-Manifest` (the build-time bake step), `v2-⑤-2bc` (the audit script), `v2-⑤-2bc-Timer` (systemd timer + evidence file + Discord post), `v2-⑤-AlertRule` (the Prometheus rule consuming the v2-AlertBridge contract), and `v2-⑤-AutoRedeploy` (the closing-the-loop cron / sidecar pull). Anchors in §2 through §9 are stable and will be referenced from each downstream ticket's AC.
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:43:Concrete instance from 2026-05-14: a routine operator check during the morning of the host-reboot incident surfaced that the prod backend was serving requests from an image tagged `:latest` whose GHCR digest had last changed **7 days earlier**. The repo had merged 11 commits in those 7 days. None of those merges were detectable from inside the running backend (no `/version` endpoint), from the host (`docker compose ps` showed the container as `Up 7d` but with no link from `Up` to the digest), or from any periodic audit (the audit script existed for systemd-units but not for image-vs-GHCR comparison).
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:627:**Option (a) — cron-based, default.** A systemd timer `omnisight-auto-redeploy.timer` fires daily at a low-traffic hour (default 03:30 UTC) and runs `scripts/auto-redeploy.sh`:
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:674:The timer/service pair lives at `deploy/systemd/omnisight-auto-redeploy.{service,timer}`:
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:704:`SuccessExitStatus=0 78` means a backward-fail-fast exit (78) is NOT a systemd-level failure — the script ran correctly and refused for correct reasons. Other non-zero exits (script bug, docker daemon unreachable) ARE systemd failures and trigger journald-level alerting per the Family ⑧ shutdown contract integration.
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:714:1. `v2-⑤-AutoRedeploy` lands `scripts/auto-redeploy.sh` + the systemd unit pair.
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:715:2. Operator manually copies the unit pair into `~/.config/systemd/user/` (or system-wide as appropriate), runs `systemctl --user daemon-reload`, then `systemctl --user enable --now omnisight-auto-redeploy.timer`.
docs/sprint-s12/2026-05-16-v2-family5-image-surfacing-contract.md:748:                │                └─→ v2-⑤-2bc-Timer (systemd timer + evidence + Discord)
backend/tests/test_bluegreen_precut_ceremony.py:253:        # (compose / host systemd / external LB) so we don't hardcode
backend/tests/test_gerrit_jira_bridge.py:433:# OP-831: ``CURSOR_FILE`` is now env-driven so user-level systemd installs
backend/tests/test_gerrit_jira_bridge.py:465:    User-level systemd installs set this to ``~/.local/state/omnisight-
backend/tests/test_ci_test_impact.py:167:            ["deploy/systemd/foo.service"], repo,
backend/tests/test_sigterm_integration.py:221:    This is the shape the systemd+docker-compose orchestrators rely
backend/tests/test_deploy_sh_blue_green_flag.py:15:    * `blue-green` joins `rolling` / `systemd` as an accepted strategy.
backend/tests/test_deploy_sh_blue_green_flag.py:21:      newline would silently pick `systemd` from the default resolution
backend/tests/test_deploy_sh_blue_green_flag.py:29:parse path. No Docker / systemd is touched; the blue-green branch is
backend/tests/test_deploy_sh_blue_green_flag.py:136:            r'STRATEGY="?\$\{STRATEGY_ARG:-\$\{OMNISIGHT_DEPLOY_STRATEGY:-systemd\}\}"?',
backend/tests/test_deploy_sh_blue_green_flag.py:140:            "systemd) must survive the --strategy flag addition"
backend/tests/test_deploy_sh_blue_green_flag.py:161:            r'"\$STRATEGY"\s*!=\s*"rolling"\s*&&\s*"\$STRATEGY"\s*!=\s*"systemd"\s*&&\s*"\$STRATEGY"\s*!=\s*"blue-green"',
backend/tests/test_deploy_sh_blue_green_flag.py:164:            "strategy validator must accept 'rolling', 'systemd', AND 'blue-green' "
backend/tests/test_deploy_sh_blue_green_flag.py:183:        for choice in ("rolling", "systemd", "blue-green"):
backend/tests/test_deploy_sh_blue_green_flag.py:198:        # block silently falls through to systemd — the exact bug the
backend/tests/test_deploy_sh_blue_green_flag.py:357:        # fall through to the default `systemd`.
backend/tests/test_deploy_sh_blue_green_flag.py:440:    def test_systemd_default_when_no_flag_no_positional(
backend/tests/test_deploy_sh_blue_green_flag.py:444:        # to `systemd` (the legacy single-replica default). A future
backend/tests/test_deploy_sh_blue_green_flag.py:448:            r'STRATEGY=.*:-systemd\}', deploy_sh_text
backend/tests/test_deploy_sh_blue_green_flag.py:449:        ), "default strategy must remain `systemd`, not `rolling` or `blue-green`"
backend/tests/test_g2_delivery_bundle.py:37:Docker, or systemd; we verify the checked-in text agrees with itself.
backend/tests/test_memory_tool_storage_op900.py:129:def test_systemd_hourly_du_report_contract() -> None:
backend/tests/test_memory_tool_storage_op900.py:132:        REPO_ROOT / "deploy/systemd/memory-tool-storage-monitor.service"
backend/tests/test_memory_tool_storage_op900.py:135:        REPO_ROOT / "deploy/systemd/memory-tool-storage-monitor.timer"
backend/tests/test_memory_tool_storage_op900.py:138:        REPO_ROOT / "deploy/systemd/omnisight-backend.service"
backend/tests/test_memory_tool_storage_op900.py:141:        REPO_ROOT / "deploy/systemd/runner-codex@.service"
backend/tests/test_memory_tool_storage_op900.py:144:        REPO_ROOT / "deploy/systemd/runner-claude@.service"
docs/sprint-s12/phase-31b-ticket-spec.md:33:- **`destructive_op_classes:` boundary field** — list enum (state-append / tmp-delete / env-edit / systemd-control / git-ref-rewrite). Replaces the binary `destructive_ops_allowed`.
docs/sprint-s12/phase-31b-ticket-spec.md:127:| `systemd-control` | systemctl stop/start unit | one runner process | yes |
docs/sprint-s12/phase-31b-ticket-spec.md:1024:  consumed_by: "31.B-8c (tests), future systemd timer"
docs/sprint-s12/phase-31b-ticket-spec.md:1026:  outputs_guaranteed: "List stale /tmp/runner-pickup/ >24h; --apply deletes; suitable as systemd timer"
docs/sprint-s12/phase-31b-ticket-spec.md:1028:    - "DO NOT add systemd unit"
docs/sprint-s12/phase-31b-ticket-spec.md:1224:  destructive_op_classes: [systemd-control, env-edit]
docs/sprint-s12/phase-31b-ticket-spec.md:1262:  destructive_op_classes: [systemd-control, env-edit]
docs/sprint-s12/phase-31b-ticket-spec.md:1303:  destructive_op_classes: [systemd-control, env-edit, state-append]
docs/sprint-s12/phase-31b-ticket-spec.md:1469:| `systemd-control` — user units `runner-*` | `project` |
docs/sprint-s12/phase-31b-ticket-spec.md:1470:| `systemd-control` — system units OR user units outside `runner-*` | `system` |
docs/sprint-s12/phase-31b-ticket-spec.md:1481:- 31.B-10a/10b (cutover/rollback scripts) — `destructive_op_classes: [systemd-control, env-edit]` + `destructive_op_scope: project` (only `runner-*` user units; env files in project paths) → no operator-class required ✓
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:33:| ⑧ | WSL2 graceful shutdown / docker 30 s timeout | `omnisight-compose-prod.service:TimeoutStopSec=60` for a 7-container stop (Docker default 10 s SIGTERM → SIGKILL × 7 ≥ 70 s minimum). No `stop_grace_period` on any prod service. `scripts/shutdown.sh` EXISTS with proper graduated timeouts (cloudflared → frontend 15 s → backend 40 s → workers 60 s) but is **NOT wired to systemd ExecStop**. WSL2 has a 10 s hard limit before `RB_POWER_OFF`. | `/etc/systemd/system/omnisight-compose-prod.service:46`; `docker-compose.prod.yml` (no `stop_grace_period` at lines 58 / 212 / 297 / 341 / 421 / 530 / 653 / 726 / 825); `scripts/shutdown.sh` exists but not invoked |
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:277:# v2-⑧-2a (systemd unit refactor — TimeoutStopSec + ExecStop wiring)
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:280:  destructive_op_classes: [service-stop, systemd-unit-change]
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:281:  destructive_op_scope: system  # touches /etc/systemd outside repo
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:282:  scope_components: [deploy/systemd, scripts]
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:285:  evidence_class: operator-rehearsal  # systemd-analyze verify + dry-run
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:288:    - deploy/systemd/omnisight-compose-prod.service
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:295:# v2-⑤-2bc-Timer (systemd timer + JSON evidence + Discord post)
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:300:  scope_components: [scripts, docs/audit, deploy/systemd, integration:discord]
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:308:    - deploy/systemd/omnisight-deployment-audit.{service,timer}
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:395:  scope_components: [docker-compose, deploy/systemd, scripts]
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:403:    - deploy/systemd/omnisight-auto-redeploy.{service,timer}
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:499:| 5 | v2-⑤-2bc-Timer | systemd timer `omnisight-deployment-audit.timer` — daily run + JSON evidence to `docs/audit/AUDIT-deployment/YYYY-MM-DD.json` + posts to Discord on drift | S | claude + operator-prepare-only | D1+D2 | v2-⑤-2bc, 31.C-Integration-external |
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:550:| 3 | v2-⑧-2a | systemd unit refactor: `omnisight-compose-prod.service` `ExecStop` calls `scripts/shutdown.sh` (not raw `docker compose down`); `TimeoutStopSec` raised to 180; verification via `systemd-analyze verify` | S | claude + operator-prepare-only | D3 | v2-⑧-1bc |
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:553:| 6 | v2-⑧-4a-Doc | `docs/sop/wsl2-shutdown-contract.md` — operator runbook: how Windows shutdown interacts with WSL2 + the 10 s RB_POWER_OFF hard limit; recommended Windows side hooks (manual). **v1.4 mandatory section (codex P1-4 caveat)**: explicit "Productizer cannot detect last forced shutdown" limitation paragraph — Windows-side BugCheck / Update / Schedule data is not readable from inside WSL2 systemd; operator-visible runbook must call this out so post-reboot the operator knows to manually check whether prior shutdown was forced (and accordingly decide whether to manually run pg integrity probe / image-vs-DB drift check) | S | claude | D3 (operator-side) | v2-⑧-2a |
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:554:| 7 | v2-⑧-DRDrill | Chaos test: simulate forced shutdown (kill -9 systemd) → verify post-reboot PG integrity (pg_resetwal NOT needed) + backend healthcheck green within 60 s | S | claude + operator-rehearsal | D3+D4 | v2-⑧-2a, v2-⑧-2b, v2-⑧-3a |
docs/sprint-s12/sprint-s12g-A-v2-runtime-defense-contract-spec.md:668:| ⑧ Graceful shutdown (v2-⑧-*) | 8 | 1 (Integration) | 0 | 2 (systemd refactor, DRDrill) |
backend/tests/test_runner_multi_instance.py:29:_SYSTEMD_DIR = REPO_ROOT / "deploy" / "systemd"
backend/tests/test_runner_multi_instance.py:385:# ── systemd template units ──────────────────────────────────────────
backend/tests/test_runner_multi_instance.py:388:def test_systemd_codex_template_unit_present() -> None:
backend/tests/test_runner_multi_instance.py:392:def test_systemd_claude_template_unit_present() -> None:
backend/tests/test_runner_multi_instance.py:403:def test_systemd_unit_propagates_instance_id_via_template_specifier(
backend/tests/test_runner_multi_instance.py:413:    # Standard systemd discipline: SIGTERM, restart-on-failure, log to file.
backend/tests/test_runner_multi_instance.py:425:def test_systemd_unit_sets_per_instance_idempotency_db(
docs/sprint-s12/phase-31e-ticket-spec.md:335:  produces: "backend/ci/queue_monitor.py + deploy/systemd-user/omnisight-ci-queue-monitor.{service,timer} + backend/ci/tests/test_queue_monitor.py"
docs/sprint-s12/phase-31e-ticket-spec.md:339:    Module polls daily (via systemd USER timer 08:00 local):
docs/sprint-s12/phase-31e-ticket-spec.md:358:  required_paths: [backend/ci/queue_monitor.py, deploy/systemd-user/omnisight-ci-queue-monitor.service, deploy/systemd-user/omnisight-ci-queue-monitor.timer, backend/ci/tests/test_queue_monitor.py]
docs/sprint-s12/phase-31e-ticket-spec.md:361:  destructive_op_classes: [state-append, systemd-control]
docs/sprint-s12/phase-31e-ticket-spec.md:375:  - {desc: "timer files exist + USER systemd", verify: {command: "test -f deploy/systemd-user/omnisight-ci-queue-monitor.timer && grep -cE 'WantedBy=default.target' deploy/systemd-user/omnisight-ci-queue-monitor.timer", expect_stdout_match: "[1-9]"}}
docs/sprint-s12/phase-31e-ticket-spec.md:376:  - {desc: "NO multi-user.target", verify: {command: "grep -cE 'WantedBy=multi-user.target' deploy/systemd-user/omnisight-ci-queue-monitor.timer", expect_stdout_match: "^0$"}}
docs/sprint-s12/phase-31d-ticket-spec.md:130:| L10 | 24 | 31.D-9-Doc | Operator runbook + **daily-parity systemd timer setup** | S | claude | claude | ReplicationLiveSmoke |
docs/sprint-s12/phase-31d-ticket-spec.md:380:  produces: "docs/audit/AUDIT-31-phase-D-evidence/parity-check.json (initial post-backfill run; daily snapshots come later via 9-Doc systemd timer)"
docs/sprint-s12/phase-31d-ticket-spec.md:387:    - "Daily snapshot setup is 9-Doc's job (systemd timer wires the daily run)"
docs/sprint-s12/phase-31d-ticket-spec.md:469:### 31.D-9-Doc — Operator runbook + daily-parity systemd timer (P2 fix per codex Q5)
docs/sprint-s12/phase-31d-ticket-spec.md:473:  - context_hint.outputs_guaranteed: ADD daily-parity systemd user timer
docs/sprint-s12/phase-31d-ticket-spec.md:474:  - new section in runbook: "Daily Parity Snapshot Setup" — installs systemd user timer that runs parity-check.py daily 09:00 local; writes to docs/audit/AUDIT-31-phase-D-evidence/daily-parity-YYYYMMDD.json
docs/sprint-s12/phase-31d-ticket-spec.md:475:  - timer file: deploy/systemd-user/omnisight-replication-daily-parity.{service,timer}
docs/sprint-s12/phase-31d-ticket-spec.md:478:  - {desc: "systemd timer files exist", verify: {command: "test -f deploy/systemd-user/omnisight-replication-daily-parity.service && test -f deploy/systemd-user/omnisight-replication-daily-parity.timer", expect_exit_code: 0}}
docs/sprint-s12/phase-31d-ticket-spec.md:479:  - {desc: "timer scheduled daily at 09:00 local", verify: {command: "grep -cE 'OnCalendar=\\*-\\*-\\* 09:00|OnCalendar=daily.*09:00' deploy/systemd-user/omnisight-replication-daily-parity.timer", expect_stdout_match: "[1-9]"}}
docs/sprint-s12/phase-31d-ticket-spec.md:480:  - {desc: "timer is USER-level (NOT system)", verify: {command: "grep -cE 'WantedBy=multi-user.target' deploy/systemd-user/omnisight-replication-daily-parity.timer", expect_stdout_match: "^0$"}}
docs/sprint-s12/phase-31d-ticket-spec.md:481:  - {desc: "timer requires default.target", verify: {command: "grep -cE 'WantedBy=default.target' deploy/systemd-user/omnisight-replication-daily-parity.timer", expect_stdout_match: "[1-9]"}}
docs/sprint-s12/phase-31d-ticket-spec.md:501:  - runbook step 5 rewritten: operator initiates soak; daily snapshots are auto-captured by 9-Doc systemd timer; Integration ticket closes when ≥14 snapshots present + all parity_pct==100
docs/sprint-s12/phase-31d-ticket-spec.md:571:  inputs_expected: "ReplicationLiveSmoke PASS; daily-parity systemd timer running (from 9-Doc) → ≥1 daily snapshot file exists"
docs/sprint-s12/phase-31c-ticket-spec.md:25:| 8b verify BUG | grep accepted `multi-user.target` OR `default.target` — equivalent to allowing forbidden system-level systemd | v2: rejects `multi-user.target`; requires `default.target` |
docs/sprint-s12/phase-31c-ticket-spec.md:26:| Schema gap | `destructive_op_scope` only models filesystem/systemd blast radius — does NOT model "data leaves the machine" (Discord POST, SMTP send) | v2: adds `external_side_effect:` + `external_payload_class:` fields (orthogonal dimension to destructive_op_scope) |
docs/sprint-s12/phase-31c-ticket-spec.md:82:`destructive_op_scope` only models filesystem/systemd blast radius. It does NOT model "data leaves the machine" — sending to Discord, SMTP, external HTTP. That is a DIFFERENT safety dimension. Phase 31.C adds:
docs/sprint-s12/phase-31c-ticket-spec.md:158:| L9 | 21 | 31.C-8b | Digest user systemd timer (**FIX verify: reject multi-user.target**) | S | codex | 8a |
docs/sprint-s12/phase-31c-ticket-spec.md:450:### 31.C-8b — Digest user systemd timer (**P0 verify BUG FIXED**)
docs/sprint-s12/phase-31c-ticket-spec.md:456:  - FIX: AC verify "USER systemd" was bugged in v1 (accepted multi-user.target OR default.target — equivalent to allowing forbidden system level)
docs/sprint-s12/phase-31c-ticket-spec.md:459:  - {desc: "REQUIRES default.target", verify: {command: "grep -cE 'WantedBy=default.target' deploy/systemd-user/omnisight-email-digest.timer", expect_stdout_match: "[1-9]"}}
docs/sprint-s12/phase-31c-ticket-spec.md:460:  - {desc: "FORBIDS multi-user.target (system-level)", verify: {command: "grep -cE 'WantedBy=multi-user.target' deploy/systemd-user/omnisight-email-digest.timer", expect_stdout_match: "^0$"}}
backend/tests/test_auto_tag_release.py:15:SERVICE = REPO_ROOT / "deploy" / "systemd" / "auto-tag-release.service"
backend/tests/test_auto_tag_release.py:265:def test_systemd_service_tails_staging_log_and_pushes_both_remotes() -> None:
docs/sprint-s12/2026-05-16-v2-family10-runner-defense-contract.md:434:3. Returns the count of expired rows so the caller (cron / systemd /
docs/sprint-s12/2026-05-16-v2-family10-runner-defense-contract.md:865:| §4.5 `expire_stale_active_claims` | ✅ FULLY shipped | v2-⑩-2d wires the cron/systemd timer caller |
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:5:scope: Contract spec for the productizer's graceful-shutdown obligations on a WSL2 host — per-service-class timeout requirements, `scripts/shutdown.sh` graduated SIGTERM→SIGKILL contract, `omnisight-compose-prod.service` systemd wiring, `docker-compose.prod.yml` `stop_grace_period` invariants, Postgres WAL-safety verification, post-restart probes, the Windows-side `RB_POWER_OFF` limitation, cross-family delegation to Family ⑤ / ⑥, chaos-test contract, and the 14-day soak invariants. Doc-only ticket (v2-⑧-1a); no runtime, db, devops, embedded, frontend, security, tests, or tooling code is touched by OP-1158.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:12:  - scripts/shutdown.sh (existing 390-line graduated-shutdown script; today NOT wired to systemd `ExecStop`)
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:13:  - /etc/systemd/system/omnisight-compose-prod.service (today `TimeoutStopSec=60`; mathematically insufficient — see §3)
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:26:4. §4 — the **systemd wiring contract**: `omnisight-compose-prod.service` `ExecStop` MUST call `scripts/shutdown.sh`; `TimeoutStopSec=180` justified mathematically (sum-of-per-service + buffer).
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:27:5. §5 — the **`docker-compose.prod.yml` contract**: `stop_grace_period` per service exactly matching the script's per-class budget; required for the SIGKILL fallback to land inside the systemd `TimeoutStopSec` envelope.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:37:This spec is the **contract** — not the implementation. Code lands in `v2-⑧-1bc` (the extended `scripts/shutdown.sh`), `v2-⑧-2a` (the systemd unit refactor with operator-window cooperation), `v2-⑧-2b` (the `docker-compose.prod.yml` `stop_grace_period` additions), `v2-⑧-3a` (the PG WAL safety probes), `v2-⑧-4a-Doc` (the operator runbook for WSL2-specific limits), `v2-⑧-DRDrill` (the chaos test), and `v2-⑧-Integration` (the 14-day soak). Anchors in §2 through §11 are stable and will be referenced from each downstream ticket's AC.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:47:Concrete instance from 2026-05-14: a routine Windows update triggered a host reboot at 02:00. WSL2 issued its 10-second pre-`RB_POWER_OFF` warning to systemd. `omnisight-compose-prod.service` ran `ExecStop=docker compose down` with `TimeoutStopSec=60`. For a 7-container stack with the docker daemon's default per-container 10 s SIGTERM-to-SIGKILL window, the floor budget is **7 × 10 s = 70 s** — already over the unit's 60 s envelope before any drain logic runs. Postgres was SIGKILLed mid-checkpoint. On reboot, Postgres replayed WAL, came up clean, and `alembic_version` was internally consistent — but ONLY because Postgres' own crash-safety guarantees held; the productizer's defense contract contributed nothing to that recovery.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:59:The bug class is **no party owns the shutdown contract**. The shell script knows the right budgets but is not in the call chain. The systemd unit is in the call chain but has the wrong envelope. The compose file declares no per-service grace, so the docker daemon falls back to the 10 s default for every container including the one that absolutely must finish its checkpoint. WSL2 caps the whole exercise at 10 s with no observability.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:78:1. **WSL2 caps the host budget at 10 s.** A `TimeoutStopSec=300` on the systemd unit cannot survive a Windows-side `RB_POWER_OFF` that fires 10 s after the WSL2 init starts shutting down. The unit envelope only matters for *cooperative* shutdowns (`systemctl stop omnisight-compose-prod`, `wsl --shutdown` with no prior Windows reboot). The contract must distinguish the two cases — see §8.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:80:3. **A timeout without verification is silent.** Today's `TimeoutStopSec=60` already silently exceeds budget; the failure mode is "SIGKILL fired at second 60, services may or may not have finished, nobody knows." The contract requires a verification loop (§3.4) that proves every service reached `inactive` (systemd) or `exited` (compose) within budget. Without verification, a 180 s envelope is just a 60 s envelope with more room to hide.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:107:| **Backend (in-flight request drain)** | **40 s** | `backend-a`, `backend-b` | FastAPI `lifespan` close hook runs `lifecycle.py` drain: 30 s for in-flight requests to complete + 10 s buffer for the asyncio event loop teardown. Matches `omnisight-backend.service:TimeoutStopSec=40` from the systemd unit. | In-flight HTTP requests return 502 to clients; clients on idempotent endpoints retry cleanly; clients on non-idempotent endpoints may double-post or skip. |
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:140:### §2.4 Sum-of-budgets — the systemd envelope floor
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:142:The systemd `TimeoutStopSec` envelope (§4) must be at least the sum of per-class budgets across the longest realistic stop sequence. For the worst case (two backends draining in series, one PG checkpointing, two stateless drains, four other-class flushes):
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:150:Adding a 45 s buffer for systemd's own teardown sequencing, pre-stop verification (§6), and post-stop verification loop (§3.4): **180 s**. This is the value §4 fixes for `TimeoutStopSec`.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:166:Invocation form when called from systemd `ExecStop` (per §4):
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:176:| `--mode <systemd\|compose\|auto>` | Which orchestration layer to drive. `auto` (default) prefers systemd if `omnisight-backend.service` is installed, else falls back to compose. Today's prod is compose; systemd mode is supported for installer-managed prod variants. | `auto` |
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:178:| `--timeout <seconds>` | Override the longest per-service grace period. **MUST be ≥ 40** (backend drain minimum). | `90` (script's own default; the systemd invocation passes `--timeout=180` to match `TimeoutStopSec`) |
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:196:The script MUST pass the §2 grace periods to the underlying orchestrator (systemd or compose). The contract pins the per-service `-t` argument to `docker compose stop` exactly as §2.3 declares:
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:215:The script's existing systemd-mode flow (lines 196–235 of today's `scripts/shutdown.sh`) follows the same order but talks to systemd units instead of compose services. The order is identical: ingress → frontend → backend → PG → workers/other.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:222:- A second invocation when a previous invocation hard-failed (a service hung past grace, was SIGKILLed) cleans up the residue: removes dangling containers (`docker compose rm -f -s` for any service in `Exited` state), verifies systemd units are `inactive`, exits 0.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:247:For systemd mode the verification calls `systemctl is-active --quiet <unit>` for every unit in the stop sequence; any unit still `active` after the loop budget yields exit 1.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:252:- The wall-clock time elapsed per service (from the script's own timer; not from docker / systemd) — this is what feeds the §11 RTO p99 invariant.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:263:- It does NOT restart services. It only stops them. Re-up is the systemd unit's `ExecStart` job.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:271:The systemd unit `omnisight-compose-prod.service` MUST be refactored per the following contract. Today's unit (audited 2026-05-14) has `ExecStop=docker compose down` + `TimeoutStopSec=60`; both are wrong. The contract fixes both.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:289:3. **`ExecStop` MUST pass `--mode=compose --timeout=180` explicitly.** The script's auto-detection works but the systemd-side caller is the source of truth for the envelope; making the argument explicit prevents the script's default (90 s) from silently undercutting the unit envelope on a host where systemd-unit detection mis-fires.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:308:Add buffer for systemd teardown + script verification loop (§3.4) + log flush:
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:319:    - Well under the systemd default `TimeoutStopSec=infinity` so a buggy script cannot hang the host indefinitely
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:327:- `Type=oneshot` + `RemainAfterExit=yes` — the unit is a "managed compose stack" wrapper, not a long-running daemon. `ExecStart=docker compose up -d` returns when the stack is up; `RemainAfterExit=yes` keeps systemd's view of the unit as `active` so `ExecStop` fires on system shutdown.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:332:### §4.4 Verification via `systemd-analyze verify`
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:334:Per the parent spec `v2-⑧-2a` row, the refactored unit MUST pass `systemd-analyze verify omnisight-compose-prod.service` without warnings. This is a static check (no actual stop required); CI in `v2-⑧-2a`'s scope runs it as a unit test.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:418:Before issuing `docker compose stop db_ha` (or the systemd equivalent), the script MUST verify that Postgres is in a state where a `stop_grace_period=30s` SIGTERM-then-SIGKILL window will not leave WAL in a corrupt state.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:468:After a restart (whether graceful or forced), a post-restart probe MUST verify the stack reached a known-good state within the §11 RTO budget. The probe is a separate process from the shutdown script; it runs from the systemd `ExecStartPost=` hook (or a sidecar `omnisight-postrestart-probe.service` oneshot, per `v2-⑧-3a`'s impl choice).
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:511:The probe's overall deadline is 60 s from when systemd marks `omnisight-compose-prod.service` as `active`. This is the same RTO Family ⑧'s 14-day soak asserts (§11).
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:513:If the probe fails inside 60 s, the failure is logged and the systemd unit's `OnFailure=` hook fires (`v2-⑧-3a` decides whether `OnFailure=` triggers an operator alert or simply marks the unit `failed` for the operator's next `systemctl status` check).
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:531:When Windows initiates a host reboot (Windows Update, scheduled restart, user-triggered restart), WSL2 receives a shutdown signal and propagates a SIGTERM-like notification to the guest's PID 1 (systemd). The guest has approximately **10 seconds** before the Linux kernel inside WSL2 receives `RB_POWER_OFF` and the entire VM is hard-stopped.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:533:10 seconds is **less than the 180 s `TimeoutStopSec` envelope** declared in §4. Under a Windows-side reboot, the guest cannot complete the §3 shutdown script — the VM goes away in 10 s regardless of what systemd is doing.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:550:4. **§7 post-restart probe** — surfaces the symptoms above into the systemd unit's status; the operator sees `systemctl status omnisight-compose-prod` go `failed` if the probe doesn't pass.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:589:Covered by §3 (the script) + §4 (the systemd unit) + §5 (the compose file). The script's per-service-class graduated stop sequence IS the D3 contract for the prod stack.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:641:6. **Repeated invocations of `scripts/shutdown.sh`** — invoke the script three times in sequence; verify (a) first invocation does the work, (b) second invocation exits 0 with "already inactive" logs, (c) third invocation also exits 0; verify no residue (dangling containers, stuck systemd units).
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:642:7. **`systemd-analyze verify omnisight-compose-prod.service`** — static check; verify the unit file passes without warnings.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:736:3. **Q12.1.3 — Does the §7 post-restart probe run on EVERY restart (including operator `docker compose restart backend-a` for a single service)?** *Resolution at `v2-⑧-3a`:* the probe runs from `omnisight-compose-prod.service`'s `ExecStartPost=`; a single-service restart via `docker compose restart` does NOT re-trigger the systemd unit, so the probe does NOT re-run. This is by design — single-service restart is the operator's chosen scope; the probe is a whole-stack check.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:739:6. **Q12.1.6 — What happens if `scripts/shutdown.sh` itself is missing on the host at the moment systemd fires `ExecStop`?** *Resolution at `v2-⑧-2a`:* the systemd unit's `ExecStop=` path is absolute (`/opt/omnisight/scripts/shutdown.sh`); if missing, systemd logs "exec format error" and falls back to its default cgroup-kill. The fallback is graceless but Postgres' own crash-safety covers WAL. The operator runbook warns against `rm`ing the script. A future `v2-⑧-2a` enhancement could add `Condition` directives to refuse to start the unit if the script is missing; not in scope today.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:773:- **`omnisight-compose-prod.service`** — existing systemd unit; the §4 contract pins what `v2-⑧-2a` refactors.
docs/sprint-s12/2026-05-16-v2-family8-graceful-shutdown-contract.md:796:**Exemption from the area-block list (out-of-area: backend / db / devops / embedded / frontend / security / tests / tooling):** the area-block list in the ticket envelope excludes work in those areas because this is a docs-only ticket. The work is `area:docs` only; the spec describes future devops / systemd / compose / backend / tests contract surfaces but does not modify any backend / db / devops / embedded / frontend / security / tests / tooling file. The downstream `v2-⑧-1bc` (extends `scripts/shutdown.sh` — area:devops + area:tests), `v2-⑧-2a` (modifies `omnisight-compose-prod.service` — area:devops), `v2-⑧-2b` (modifies `docker-compose.prod.yml` — area:devops), `v2-⑧-3a` (adds PG probes — area:devops + area:db + area:tests), `v2-⑧-4a-Doc` (adds operator runbook — area:docs), `v2-⑧-DRDrill` (chaos test — area:tests + area:devops), and `v2-⑧-Integration` (14-day soak — area:devops + area:tests + area:docs) will be re-evaluated under the full area-block set at filing time.
docs/sprint-s12/phase-31f-ticket-spec.md:108:- `systemd-user` — user-level systemctl (project)
docs/sprint-s12/phase-31f-ticket-spec.md:109:- `systemd-system` — system-level systemctl (system)
docs/sprint-s12/phase-31f-ticket-spec.md:454:    1. ≥1 daily-cosign snapshot from systemd timer (set up in 10-Doc) with sign_success_rate ≥99%
docs/sprint-s12/phase-31f-ticket-spec.md:504:  inputs_expected: "StabilityCheckpoint passed; daily systemd timer producing daily-cosign-YYYYMMDD.json snapshots"
docs/sprint-s12/phase-31j-ticket-spec.md:24:| **Cutover steps homogeneous boundary** | all 5 collapsed to `system+network-production`; hides blast differences | v2: each step has **explicit `scope_components`** per codex spec (6a: [host-runner-env, systemd-user, runner-prod-state]; 6e: [git-refs-shared, release-tag, wave2-build, cosign-sign]; etc.) |
docs/sprint-s12/phase-31j-ticket-spec.md:240:    boundaries.scope_components: [host-runner-env, systemd-user, runner-prod-state]
docs/sprint-s12/phase-31j-ticket-spec.md:241:    boundaries.destructive_op_classes: [env-edit, systemd-control]
docs/sprint-s12/phase-31j-ticket-spec.md:337:  L4: 6a (scope_components: host-runner-env / systemd-user / runner-prod-state)
backend/tests/test_memory_tool_monitor_op908.py:185:def test_systemd_timer_is_hourly_and_uses_monitor_script() -> None:
backend/tests/test_memory_tool_monitor_op908.py:187:    service = (REPO_ROOT / "deploy/systemd/memory-tool-monitor.service").read_text(encoding="utf-8")
backend/tests/test_memory_tool_monitor_op908.py:188:    timer = (REPO_ROOT / "deploy/systemd/memory-tool-monitor.timer").read_text(encoding="utf-8")
backend/tests/test_staging_sync.py:31:SYSTEMD = REPO_ROOT / "deploy" / "systemd"
backend/tests/test_staging_sync.py:300:# ─────────────────────────── systemd contract ──────────────────────────────
docs/sop/cross-host-portability-staging.md:4:`deploy/staging/`, `infra/staging/`, or `deploy/systemd/staging-*`, or
docs/sop/cross-host-portability-staging.md:22:  — the systemd-wrapped 5a stack (AUDIT-19a / OP-971).
docs/sop/cross-host-portability-staging.md:63:pass on the systemd unit paths + open one firewall port — nothing else.*
docs/sop/cross-host-portability-staging.md:148:| 5 | **Addressed by port, not by a host-specific name** | No tracked staging artifact (`docker-compose.yml`, `caddy.json`, the systemd units, `infra/staging/*`, the frontend build args, the `OMNISIGHT_STAGING_URL` / `STAGING_HOSTNAME` defaults) may contain a host-specific DNS name as a literal or as an env-var *default*. The default is a port-based `localhost` URL; a host name is an `.env.local` / `~/.config/omnisight/*.env` override. Caddy has no `host:` matcher. | audit check #5 (added by AUDIT-29d-1, OP-1029) |
docs/sop/cross-host-portability-staging.md:165:an absolute bind mount, a non-`%` cgroup limit, a divergent systemd path
docs/sop/cross-host-portability-staging.md:177:* **Absolute paths in the systemd `[Service]` sections** —
docs/sop/cross-host-portability-staging.md:179:  `StandardOutput=append:`. systemd *requires* `ExecStart` to be an
docs/sop/cross-host-portability-staging.md:189:  *aggregate* auto-scaling lever is the systemd `MemoryMax=NN%` (check #4).
docs/sop/cross-host-portability-staging.md:228:  the systemd unit paths, set `OMNISIGHT_STAGING_URL=http://<new-host>:18080`
docs/sop/cross-host-portability-staging.md:237:`deploy/systemd/staging-*`:
docs/sop/cross-host-portability-staging.md:247:   systemd unit, not a byte count.
backend/tests/test_pipeline_coordinator_llm_consultation.py:318:    monkeypatch.setenv("OMNISIGHT_COORDINATOR_DAILY_BUDGET_USD", "1.50")
docs/sprint-s12/phase-31a-ticket-spec.md:205:| L5 | 24 | 31.A-8b | systemd installer (symlink + daemon-reload) | S | codex | 8a, 3c |
docs/sprint-s12/phase-31a-ticket-spec.md:593:  consumed_by: "31.A-3c, 31.A-8b (systemd unit symlink source path is sora-bridge)"
docs/sprint-s12/phase-31a-ticket-spec.md:610:    outputs_for_downstream: ["~/sora-bridge/.git", "deploy/systemd/*.service exists"]
docs/sprint-s12/phase-31a-ticket-spec.md:1039:  produces: "scripts/setup-dev-env-full/08-systemd-units.dev-subset.txt (canonical 20-30 unit filenames)"
docs/sprint-s12/phase-31a-ticket-spec.md:1041:  inputs_expected: "operator + claude review 84 units in ~/sora-bridge/deploy/systemd/ and decide dev relevance"
docs/sprint-s12/phase-31a-ticket-spec.md:1050:  required_paths: [scripts/setup-dev-env-full/08-systemd-units.dev-subset.txt]
docs/sprint-s12/phase-31a-ticket-spec.md:1051:  forbidden_paths: [scripts/setup-dev-env-full/08-systemd-install.sh, scripts/setup-dev-env-full/08-systemd-enable.sh]
docs/sprint-s12/phase-31a-ticket-spec.md:1061:    verify: {command: "grep -vE '^#|^$' scripts/setup-dev-env-full/08-systemd-units.dev-subset.txt | wc -l", expect_stdout_match: "^(2[0-9]|30)$"}
docs/sprint-s12/phase-31a-ticket-spec.md:1063:    verify: {command: "grep -cE 'omnisight-compose-prod|backup-daily|backup-hourly|canary-' scripts/setup-dev-env-full/08-systemd-units.dev-subset.txt", expect_stdout_match: "^0$"}
docs/sprint-s12/phase-31a-ticket-spec.md:1065:    verify: {command: "grep -cE '\\.(service|timer)$' scripts/setup-dev-env-full/08-systemd-units.dev-subset.txt", expect_stdout_match: "^(2[0-9]|30)$"}
docs/sprint-s12/phase-31a-ticket-spec.md:1069:### 31.A-8b — systemd installer (symlink + daemon-reload)
docs/sprint-s12/phase-31a-ticket-spec.md:1076:  produces: "scripts/setup-dev-env-full/08-systemd-install.sh"
docs/sprint-s12/phase-31a-ticket-spec.md:1079:  outputs_guaranteed: "All units in dev-subset.txt symlinked to ~/.config/systemd/user/; daemon-reload run; NO units started"
docs/sprint-s12/phase-31a-ticket-spec.md:1087:  required_paths: [scripts/setup-dev-env-full/08-systemd-install.sh]
docs/sprint-s12/phase-31a-ticket-spec.md:1088:  forbidden_paths: [scripts/setup-dev-env-full/08-systemd-units.dev-subset.txt, scripts/setup-dev-env-full/08-systemd-enable.sh]
docs/sprint-s12/phase-31a-ticket-spec.md:1092:  dependency_artifacts: [dev-subset.txt, ~/sora-bridge/deploy/systemd/]
docs/sprint-s12/phase-31a-ticket-spec.md:1095:    outputs_for_downstream: [~/.config/systemd/user/<unit> symlinks]
docs/sprint-s12/phase-31a-ticket-spec.md:1098:    verify: {command: "grep -cE 'ln -sf.*sora-bridge/deploy/systemd' scripts/setup-dev-env-full/08-systemd-install.sh", expect_stdout_match: "[1-9]"}
docs/sprint-s12/phase-31a-ticket-spec.md:1100:    verify: {command: "grep -cE '^\\s*cp .*\\.service' scripts/setup-dev-env-full/08-systemd-install.sh", expect_stdout_match: "^0$"}
docs/sprint-s12/phase-31a-ticket-spec.md:1102:    verify: {command: "grep -c 'systemctl --user daemon-reload' scripts/setup-dev-env-full/08-systemd-install.sh", expect_stdout_match: "[1-9]"}
docs/sprint-s12/phase-31a-ticket-spec.md:1113:  produces: "scripts/setup-dev-env-full/08-systemd-enable.sh"
docs/sprint-s12/phase-31a-ticket-spec.md:1124:  required_paths: [scripts/setup-dev-env-full/08-systemd-enable.sh]
docs/sprint-s12/phase-31a-ticket-spec.md:1125:  forbidden_paths: [scripts/setup-dev-env-full/08-systemd-install.sh, scripts/setup-dev-env-full/08-systemd-units.dev-subset.txt]
docs/sprint-s12/phase-31a-ticket-spec.md:1131:    inputs_from_deps: [units installed in ~/.config/systemd/user/]
docs/sprint-s12/phase-31a-ticket-spec.md:1135:    verify: {command: "grep -c 'systemctl --user enable' scripts/setup-dev-env-full/08-systemd-enable.sh", expect_stdout_match: "[1-9]"}
docs/sprint-s12/phase-31a-ticket-spec.md:1137:    verify: {command: "grep -cE 'systemctl.*--now' scripts/setup-dev-env-full/08-systemd-enable.sh", expect_stdout_match: "^0$"}
docs/sprint-s12/phase-31a-ticket-spec.md:1139:    verify: {command: "grep -cE 'systemctl --user start' scripts/setup-dev-env-full/08-systemd-enable.sh", expect_stdout_match: "^0$"}
docs/sprint-s12/phase-31a-ticket-spec.md:1152:  inputs_expected: "Launcher roots populated (4b); systemd units enabled but not started (8c)"
docs/sprint-s12/phase-31a-ticket-spec.md:1157:    - "DO NOT enable systemd units — 8c owns"
docs/sprint-s12/phase-31a-ticket-spec.md:1162:  forbidden_paths: [scripts/setup-dev-env-full/04-launcher-roots-mkdir.sh, scripts/setup-dev-env-full/04-launcher-roots-content.sh, scripts/setup-dev-env-full/08-systemd-enable.sh]
docs/sprint-s12/phase-31a-ticket-spec.md:1166:  dependency_artifacts: [launcher.sh in each runner root, systemd units enabled]
docs/sprint-s12/phase-31a-ticket-spec.md:1699:| 4 | systemd dev-subset exact list | **Deferred to 31.A-8a curation pass** |
docs/sprint-s12/phase-31g-ticket-spec.md:340:  - boundaries.scope_components: [host-fs-project, systemd-user, external-network-prod]
docs/sop/daemon-startup-contract.md:1:# Daemon startup contract — required systemd unit directives (AUDIT-29c)
docs/sop/daemon-startup-contract.md:5:**Scope**: every `*.service` unit committed under `deploy/systemd/`
docs/sop/daemon-startup-contract.md:6:**Enforced by**: `scripts/check_systemd_unit_contract.py` pre-commit hook (OP-1028 / AUDIT-29c-4)
docs/sop/daemon-startup-contract.md:10:OmniSight runs ~20 user-level systemd units (the runner instances, the
docs/sop/daemon-startup-contract.md:21:  path. Because the unit had no `Restart=`, systemd marked it `failed` and
docs/sop/daemon-startup-contract.md:25:- **AUDIT-23 / OP-976** — several units shipped to `deploy/systemd/` were
docs/sop/daemon-startup-contract.md:37:Every `[Service]` section in `deploy/systemd/*.service` MUST set all three
docs/sop/daemon-startup-contract.md:90:- **Absolute interpreter path.** systemd does not run `ExecStart=` through a
docs/sop/daemon-startup-contract.md:106:the user's systemd manager (and therefore all `systemctl --user` units)
docs/sop/daemon-startup-contract.md:118:timers — i.e. anything in `deploy/systemd/` that is meant to be "always on".
docs/sop/daemon-startup-contract.md:175:(See `deploy/systemd/runner-codex@.service` for the fully commented version.)
docs/sop/daemon-startup-contract.md:199:(See `deploy/systemd/omnisight-canary.service`.)
docs/sop/daemon-startup-contract.md:203:For each `deploy/systemd/*.service` in the commit:
docs/sop/daemon-startup-contract.md:220:- `deploy/systemd/gerrit-jira-bridge.service` — reference long-lived daemon.
docs/sop/daemon-startup-contract.md:221:- `deploy/systemd/runner-codex@.service`, `runner-claude@.service` —
docs/sop/daemon-startup-contract.md:223:- `deploy/systemd/omnisight-canary.service` — reference oneshot + timer +
docs/sop/daemon-startup-contract.md:231:  tickets that turn out to need an out-of-area systemd change.
docs/sop/runner-ticket-filing-checklist.md:151:- [ ] EDIT: container up / systemd unit active / dependency installed.
docs/sop/release-lifecycle-states.md:47:- `deploy/systemd/gerrit-jira-archive-sweep.service`
docs/sop/release-lifecycle-states.md:48:- `deploy/systemd/gerrit-jira-archive-sweep.timer`
docs/sprint-s12/phase-31h-ticket-spec.md:36:| 5c evidence weak | only installed_count + units_started=0 | v2: + `systemd-analyze verify` + dependency closure + path existence + ownership/mode + uninstall manifest ACs |
docs/sprint-s12/phase-31h-ticket-spec.md:74:| L1 | 5 | 31.H-1bc | prod-systemd-units.txt + tests | S | claude | claude | 1a | — |
docs/sprint-s12/phase-31h-ticket-spec.md:80:| L4 | 11 | 31.H-5c | **OPERATOR**: install 50+ systemd units + systemd-analyze verify | S | claude | claude + operator-window | 5b, 1bc | **prod-host-install** |
docs/sprint-s12/phase-31h-ticket-spec.md:237:### 31.H-5c — + systemd-analyze + dependency closure + uninstall manifest (codex P1)
docs/sprint-s12/phase-31h-ticket-spec.md:243:  - {desc: "systemd-analyze verify passes on installed units", verify: {command: "jq -r .systemd_analyze_verify_clean docs/audit/AUDIT-31-phase-H-evidence/5c-deploy.json", expect_stdout_match: "^true$"}, run_as: operator}
docs/sop/gerrit-jira-bridge.md:18:     deploy/systemd/gerrit-jira-bridge.service \
docs/sop/gerrit-jira-bridge.md:19:     | sudo tee /etc/systemd/system/gerrit-jira-bridge.service >/dev/null
docs/sop/gerrit-jira-bridge.md:64:  not readable. The daemon exits 2 and systemd restarts it every 5s.
docs/sop/gerrit-jira-bridge.md:105:Install the daily sweep in the same user-level systemd scope as the bridge:
docs/sop/gerrit-jira-bridge.md:108:mkdir -p ~/.config/systemd/user
docs/sop/gerrit-jira-bridge.md:109:cp deploy/systemd/gerrit-jira-archive-sweep.service ~/.config/systemd/user/
docs/sop/gerrit-jira-bridge.md:110:cp deploy/systemd/gerrit-jira-archive-sweep.timer ~/.config/systemd/user/
docs/sop/error-pager-config.md:5:tails the systemd journal for ERROR / CRIT / ALERT records emitted by
docs/sop/error-pager-config.md:40:| #4 — forwarder crash → systemd restart, ≤ 10s log loss | `Restart=always` + `RestartSec=5` + `journalctl --cursor-file` resume | `test_ac4_*` (unit-file contract + cursor primitives) |
docs/sop/error-pager-config.md:46:The systemd units to follow. Each entry has:
docs/sop/error-pager-config.md:48:* `name` — the systemd unit name (e.g. `omnisight-backend.service`).
docs/sop/error-pager-config.md:154:sudo cp deploy/systemd/omnisight-journal-error-forwarder.service \
docs/sop/error-pager-config.md:155:        /etc/systemd/system/
docs/sop/canary-pipeline.md:5:**Cadence**: hourly (systemd timer)
docs/sop/canary-pipeline.md:25:   (`~/work/sora/logs/bridge/systemd.log`).
docs/sop/canary-pipeline.md:47:| 2  | DEGRADED — push succeeded but daemon log silent OR push failed | systemd `OnFailure=` chains to `omnisight-canary-alert.service` |
docs/sop/canary-pipeline.md:61:User-level systemd, same identity as `gerrit-jira-bridge.service`:
docs/sop/canary-pipeline.md:64:mkdir -p ~/.config/systemd/user
docs/sop/canary-pipeline.md:65:cp deploy/systemd/omnisight-canary.service       ~/.config/systemd/user/
docs/sop/canary-pipeline.md:66:cp deploy/systemd/omnisight-canary.timer         ~/.config/systemd/user/
docs/sop/canary-pipeline.md:67:cp deploy/systemd/omnisight-canary-alert.service ~/.config/systemd/user/
docs/sop/canary-pipeline.md:83:  --bridge-log ~/work/sora/logs/bridge/systemd.log \
backend/tests/fixtures/merger_replay/change_698/before/docs/operations/agent-rpg-system.md:44:| W3.3   | `backend/agents/style_fingerprint_cron.py` — daily recompute sweep + drift logging | **Live** (OP-130) — helper + drift threshold live; systemd timer wiring still owned by devops |
backend/tests/fixtures/merger_replay/change_698/before/docs/operations/agent-rpg-system.md:731:Driven by `deploy/systemd/rpg-skill-decay.{service,timer}` (Mondays
docs/sop/architecture-anti-patterns.md:48:| 13 | [Shipped-but-not-deployed](#13-shipped-but-not-deployed) | Code merged + ticket closed, but the systemd unit / container / env-wire / migration never reached prod — found only when a downstream consumer fails |
docs/sop/architecture-anti-patterns.md:190:**Root cause**: Daemon's connection-recovery logic (systemd-restart, SSH
docs/sop/architecture-anti-patterns.md:394:Published, and everyone moves on — but the artefact it produced (a systemd
docs/sop/architecture-anti-patterns.md:414:   whose `Files touched` includes `deploy/`, a systemd unit, a compose file, a
docs/sop/architecture-anti-patterns.md:423:   `scripts/deployment-audit.sh` in `deploy/systemd/deployment-audit.{service,timer}`
docs/sprint-s12/2026-05-16-v2-family6-image-db-drift-contract.md:55:— and so systemd marked `omnisight-compose-prod.service` `failed`, and the `depends_on: service_healthy` chain stopped frontend / caddy / cloudflared / installer from starting. The public URL was therefore down from 05:29 (reboot) until manual recovery at 10:50, despite the actual breakage being a 7-day-old image / DB-ahead-of-image misalignment (= image **BEHIND** in the vocabulary of this spec) that had been silently failing readyz 118 consecutive times in the live container without alerting anyone.
docs/sprint-s12/2026-05-16-v2-family6-image-db-drift-contract.md:121:3. **Refusing-to-start makes the problem loud, not soft-recoverable.** A backend that crashloops surfaces to systemd (`failed` state), to Prometheus (`up == 0`), to the operator (the public URL is down). Soft recovery — "serve traffic for endpoints that don't touch the missing columns" — would hide the problem until the next code path tickled it, by which time the operator has moved on. Loudness on startup is the correct severity for a drift this dangerous.
docs/sprint-s12/2026-05-16-v2-family6-image-db-drift-contract.md:122:4. **Exit 78 is semantically right.** `EX_CONFIG` (78) is the BSD `sysexits.h` code for "configuration error". Image-BEHIND-DB is a deploy-time misconfiguration: the operator selected an image incompatible with the DB. Other exit codes (1, 2, 70) would either be too generic or wrong; 78 communicates to systemd / docker-compose / kubelet that this is not transient (so don't restart) and not a program bug (so don't page on `up == 0` per `OmniSightBackendCrashloop`).
docs/sprint-s12/2026-05-16-v2-family6-image-db-drift-contract.md:202:Today (2026-05-16) `/readyz` includes a migration probe: each `/readyz` GET runs an inline check that `db_head == latest_alembic_head_in_filesystem` and returns 503 if not. This was sufficient for the *symptom* in 2026-05-14 (the systemd healthcheck stopped the dependency chain) but is **insufficient as a detection primitive**:
docs/sprint-s12/2026-05-16-v2-family6-image-db-drift-contract.md:281:- **Does not gate `/readyz`.** The `/readyz` 503-with-`remediation` of §5 is the per-poll synchronous detector for systemd / docker-compose / kubelet's healthcheck. The gauge is the periodic observational detector for Prometheus / AlertBridge. They are independent.
docs/sprint-s12/2026-05-16-v2-family6-image-db-drift-contract.md:691:- Family ⑤'s audit script is the out-of-band detector (daily systemd timer, runs on the host outside the backend container).
docs/sop/notifier-config.md:26:All read from the process environment on first call. The systemd unit
docs/sop/notifier-config.md:27:`deploy/systemd/gerrit-jira-bridge.service` shows the canonical layout.
docs/sop/notifier-config.md:230:`{channel_name: "ok" | error_str}`. The systemd unit logs this on
docs/sop/notifier-config.md:274:- Systemd: `deploy/systemd/gerrit-jira-bridge.service`
docs/sprint-s12/phase-31i-ticket-spec.md:38:| 8b title mismatch | overview "Verification tests" / detailed "Daily health-verify systemd timer" | v2: standardized — **"Daily health-verify systemd timer + alert wiring"** |
docs/sprint-s12/phase-31i-ticket-spec.md:95:| L9 | 18 | 31.I-8b | **renamed**: Daily health-verify systemd timer + alert wiring | S | codex | codex + operator-prepare-only | 8a |
docs/sprint-s12/phase-31i-ticket-spec.md:254:  - title: "AUDIT-31.I-8b: Daily health-verify systemd timer + alert wiring"   (was inconsistent)
docs/sop/sprint-s12-ticket-decomposition-rules.md:56:| `deploy/systemd/`, `deploy/k8s/`, `deploy/helm/`, `*.service`, `*.timer`, `Dockerfile*`, `docker-compose*.yml` | `area:devops` |
docs/sop/sprint-s12-ticket-decomposition-rules.md:350:| 84 systemd unit files in `deploy/systemd/` | Only 25 installed on host (`systemctl --user list-units`) | "shipped but not deployed" anti-pattern (AUDIT-23) |
docs/sop/sprint-s12-ticket-decomposition-rules.md:358:> Every functional ticket whose deliverable is a deployable artefact (systemd unit / container / cron / alert rule / script that must run / config that must be loaded) MUST have a paired **activation ticket**:
docs/sop/sprint-s12-ticket-decomposition-rules.md:428:- `area:devops` + creates new file in `deploy/systemd/` or `deploy/observability/`
docs/sop/sprint-s12-ticket-decomposition-rules.md:571:**Origin**: AUDIT-29 ticket spec — "Cognee container + Neo4j systemd unit + on-host TLS config" as one bullet. Runner can't split; either does all or none.
docs/sop/lessons/L-OP-783-horizontal-scaling-runner-instances.md:35:short-circuit on `tmux has-session`); the systemd templates
docs/sop/lessons/L-OP-783-horizontal-scaling-runner-instances.md:46:systemd unit templates wiring `%i → INSTANCE_ID`. Existing
docs/sop/runbooks/runner-pickup-block-rate.md:50:#        contender via systemd and re-check its config.
docs/sop/runbooks/runner-instance-collision.md:4:values across systemd runner services. It scans `systemctl list-units
docs/sop/runbooks/runner-instance-collision.md:11:sudo cp deploy/systemd/runner-instance-uniqueness-check.service /etc/systemd/system/
docs/sop/runbooks/runner-instance-collision.md:12:sudo cp deploy/systemd/runner-instance-uniqueness-check.timer   /etc/systemd/system/
docs/sop/runbooks/runner-instance-collision.md:18:or correct its ID, reload systemd if unit files changed, restart only the
docs/sop/runbooks/orphan-reaper.md:10:The reaper is a single-shot systemd job. The timer fires every ten
docs/sop/runbooks/orphan-reaper.md:51:mkdir -p ~/.config/systemd/user
docs/sop/runbooks/orphan-reaper.md:52:cp deploy/systemd/runner-orphan-reaper.service ~/.config/systemd/user/
docs/sop/runbooks/orphan-reaper.md:53:cp deploy/systemd/runner-orphan-reaper.timer   ~/.config/systemd/user/
docs/sop/runbooks/orphan-reaper.md:65:`~/.config/omnisight/runner-orphan-reaper.env` (the systemd unit pulls
backend/tests/fixtures/merger_replay/change_698/after/docs/operations/agent-rpg-system.md:43:| W3.3   | `backend/agents/style_fingerprint_cron.py` — daily recompute sweep + drift logging | **Live** (OP-130) — helper + drift threshold live; systemd timer wiring still owned by devops |
backend/tests/fixtures/merger_replay/change_698/after/docs/operations/agent-rpg-system.md:727:Driven by `deploy/systemd/rpg-skill-decay.{service,timer}` (Mondays
backend/tests/fixtures/merger_replay/change_698/ancestor/docs/operations/agent-rpg-system.md:43:| W3.3   | `backend/agents/style_fingerprint_cron.py` — daily recompute sweep + drift logging | **Live** (OP-130) — helper + drift threshold live; systemd timer wiring still owned by devops |
backend/tests/fixtures/merger_replay/change_698/ancestor/docs/operations/agent-rpg-system.md:726:Driven by `deploy/systemd/rpg-skill-decay.{service,timer}` (Mondays
docs/sop/runbooks/bridge-health-degraded.md:87:### B. Heartbeat path unwritable (post-OP-831 user-systemd path drift)
docs/sop/jira-ticket-creation-checklist.md:32:| systemd timer / cron service          | `devops, docs, tests`                |
docs/sop/lessons/L-OP-798-sora-bridge-staleness.md:35:→ sync_skipped_dirty). The systemd units pass `systemd-analyze verify`
docs/sop/lessons/L-OP-762-milestones-need-machine-checkable-gates.md:25:and the 5-minute systemd timer contract.
docs/sop/lessons/L-OP-827-twin-defects-runner-rebase-and-bridge-cursor.md:18:* **Defect B (bridge)**: while diagnosing A, found the bridge daemon was in a hidden crash loop. `CURSOR_FILE = Path("/var/lib/omnisight-bridge/event-cursor.json")` was hard-coded; user-level systemd lacks permission to `mkdir` under root-owned `/var/lib/`. Each restart cycle: catchup → enter `stream_forever()` → first event arrives → `save_event_cursor()` → `mkdir` raises `PermissionError` → process dies → `Restart=always` respawns. ~7 seconds per cycle. Live `change-merged` events streamed past unprocessed for ~16 minutes (01:08–01:24); JIRA tickets stayed at "Under Review" even though their Gerrit changes had merged. The catchup pass at startup was stateless (no cursor write needed) and so succeeded every cycle, masking the inner failure behind a log of repeated `catchup_done` entries.
docs/sop/lessons/L-OP-827-twin-defects-runner-rebase-and-bridge-cursor.md:30:* **OP-831** — `OMNISIGHT_BRIDGE_CURSOR_FILE` env override on `CURSOR_FILE` in `backend/agents/gerrit_jira_bridge.py`. The legacy `/var/lib/` path remains the fallback default so root-installed deployments stay backward compatible. `deploy/systemd/gerrit-jira-bridge.service` ships with the override pre-set to `~/.local/state/omnisight-bridge/event-cursor.json` (XDG-compliant, user-writable for user-level systemd). Live operator-side mirror applied first to `/home/user/sora-bridge/...` so the production bridge stopped crash-looping immediately, then committed canonically to the main repo for future deploys.
docs/sop/lessons/L-OP-827-twin-defects-runner-rebase-and-bridge-cursor.md:40:1. **Stateful daemons whose persistence file is unwritable are silent killers.** `Restart=always` masks the crash as continuous uptime; a stateless catchup pass succeeds every cycle and prints reassuring `catchup_done` logs. Mitigation: every daemon's startup should write a smoke token to its persistence path *before* entering its main loop, and abort loudly (non-restart-able exit code) if the write fails. systemd should not be the only thing that knows the daemon is broken.
docs/sop/lessons/L-OP-976-shipped-not-deployed.md:13:Sprint D/E/F tickets whose deliverable was *infrastructure* — a systemd
docs/sop/lessons/L-OP-976-shipped-not-deployed.md:39:   touched` includes `deploy/`, a systemd unit, a compose file, a cron, or a
docs/sop/lessons/L-OP-976-shipped-not-deployed.md:47:   `deploy/systemd/deployment-audit.{service,timer}` (daily, before
docs/sop/lessons/L-OP-777-release-notes-should-be-reviewable-patchsets.md:21:links, event-log cursor consumption, and the systemd polling contract.
docs/sop/lessons/L-OP-689-gerrit-webhooks-plugin-v3-13-5-has-zero-auth-surface-op-689.md:14:While evaluating OP-715 (the migration plan to stream-events), a second silent failure surfaced: `systemctl status gerrit-jira-bridge` → "Unit could not be found." The OP-689 daemon's systemd unit (`deploy/systemd/gerrit-jira-bridge.service`) was checked into the repo but NEVER INSTALLED on the prod host. No tmux session running it either. So the intended `change-merged → JIRA Approved → 承認済み` transitions had never actually fired automatically — every JIRA ticket marked as published was transitioned manually (operator) without realising the automation was a no-op.
docs/sop/lessons/L-OP-689-gerrit-webhooks-plugin-v3-13-5-has-zero-auth-surface-op-689.md:20:- Operator action: install the systemd unit (`sudo cp deploy/systemd/gerrit-jira-bridge.service /etc/systemd/system/` + `systemctl enable --now`) — this also lights up OP-689's pre-existing path that was silently dead.
docs/sop/jira-ticket-conventions.md:1415:  ``OMNISIGHT_RUNNER_OPS_ONLY_DISABLED=1`` in the runner systemd unit.

exec
/bin/bash -lc "rg -n \"pipeline-coordinator.service|OMNISIGHT_COORDINATOR_DECISION_LOG_DIR|PYTHONUNBUFFERED|systemd\" backend/tests -g '*.py'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 106ms:
backend/tests/test_pipeline_coordinator_watchdog.py:105:    assert systemctl.calls == ["pipeline-coordinator.service"]
backend/tests/test_pipeline_coordinator_watchdog.py:146:    assert systemctl.calls == ["pipeline-coordinator.service"]
backend/tests/test_pipeline_coordinator_watchdog.py:171:    assert systemctl.calls == ["pipeline-coordinator.service"]
backend/tests/test_release_milestone_checker.py:18:SERVICE = REPO_ROOT / "deploy" / "systemd" / "release-milestone-checker.service"
backend/tests/test_release_milestone_checker.py:19:TIMER = REPO_ROOT / "deploy" / "systemd" / "release-milestone-checker.timer"
backend/tests/test_release_milestone_checker.py:189:def test_systemd_timer_runs_every_five_minutes() -> None:
backend/tests/test_g2_delivery_bundle.py:37:Docker, or systemd; we verify the checked-in text agrees with itself.
backend/tests/test_auto_tag_release.py:15:SERVICE = REPO_ROOT / "deploy" / "systemd" / "auto-tag-release.service"
backend/tests/test_auto_tag_release.py:265:def test_systemd_service_tails_staging_log_and_pushes_both_remotes() -> None:
backend/tests/test_memory_tool_monitor_op908.py:185:def test_systemd_timer_is_hourly_and_uses_monitor_script() -> None:
backend/tests/test_memory_tool_monitor_op908.py:187:    service = (REPO_ROOT / "deploy/systemd/memory-tool-monitor.service").read_text(encoding="utf-8")
backend/tests/test_memory_tool_monitor_op908.py:188:    timer = (REPO_ROOT / "deploy/systemd/memory-tool-monitor.timer").read_text(encoding="utf-8")
backend/tests/test_k8s_probes_g5_4.py:188:        # / systemd callers, but this manifest must use /livez.
backend/tests/test_staging_snapshot_restore.py:22:  * deploy/systemd/staging-pg-snapshot.{service,timer} — daily 02:00,
backend/tests/test_staging_snapshot_restore.py:27:dep) only. No docker / systemd / Postgres required.
backend/tests/test_staging_snapshot_restore.py:43:SYSTEMD = REPO_ROOT / "deploy" / "systemd"
backend/tests/test_staging_snapshot_restore.py:422:# 5. systemd units
backend/tests/test_pipeline_coordinator_chaos.py:158:        assert fake_systemctl.calls == ["pipeline-coordinator.service"]
backend/tests/test_force_promote_integration.py:147:    #    auto-promote consumer tails — with a systemd-journal-shaped prefix in
backend/tests/test_force_promote_integration.py:152:    event_log = tmp_path / "release-milestone-systemd.log"
backend/tests/test_force_promote_integration.py:239:    event_log = tmp_path / "release-milestone-systemd.log"

exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator.py | sed -n '1540,1588p;1620,1645p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
  1540	                resolution = "noop"
  1541	            out.append({"kind": kind, "target": target, "resolution": resolution})
  1542	        return out
  1543	
  1544	    def _read_recent_decision_log(self) -> list[dict[str, Any]]:
  1545	        """Return decision-log records from the last :data:`L6_REPLAY_WINDOW_HOURS`.
  1546	
  1547	        Reads the day-partitioned JSONL files spanning the window (today + the
  1548	        days it reaches back into), in chronological order, skipping malformed
  1549	        lines so a single torn record never aborts recovery.
  1550	        """
  1551	        now = self._clock()
  1552	        window_start = now - timedelta(hours=L6_REPLAY_WINDOW_HOURS)
  1553	        records: list[dict[str, Any]] = []
  1554	        day = window_start.date()
  1555	        seen_paths: list[Path] = []
  1556	        while day <= now.date():
  1557	            path = self._decision_log.directory / f"{day.isoformat()}.jsonl"
  1558	            if path.exists():
  1559	                seen_paths.append(path)
  1560	            day = day + timedelta(days=1)
  1561	        for path in seen_paths:
  1562	            try:
  1563	                lines = path.read_text(encoding="utf-8").splitlines()
  1564	            except OSError:
  1565	                continue
  1566	            for line in lines:
  1567	                if not line.strip():
  1568	                    continue
  1569	                try:
  1570	                    rec = json.loads(line)
  1571	                except json.JSONDecodeError:
  1572	                    continue
  1573	                if isinstance(rec, dict):
  1574	                    ts = record_ts(rec)
  1575	                    if ts is None or ts >= window_start:
  1576	                        records.append(rec)
  1577	        return records
  1578	
  1579	    def _startup_2_reconcile(self, report: ColdStartReport) -> None:
  1580	        """Startup-2: reconcile each interrupted (進行中) ticket (ADR §3.3).
  1581	
  1582	        Decision tree per ticket with no live runner:
  1583	          (b) Gerrit change exists + mergeable      → transition Under Review
  1584	          (a) feature branch has commits             → mark resumable, leave
  1585	          (c) no commits + stale (>2× CLI timeout)   → revert To Do (clear claim)
  1586	          (d) no commits + not yet stale (ambiguous) → @-operator, leave alone
  1587	        A ticket whose runner is still alive is left untouched (it's handling
  1588	        it). (b) is checked before (a): a mergeable change means the work is
  1620	    ) -> None:
  1621	        entry = {"event": STARTUP_PHASE_EVENT, "phase": 2, "ticket": key,
  1622	                 "classification": classification, "action": action}
  1623	        report.reconciled.append({"ticket": key, "classification": classification, "action": action})
  1624	        self._log_cold_start(entry)
  1625	
  1626	    def _startup_3_sweep(self, report: ColdStartReport) -> None:
  1627	        """Startup-3: drop orphaned claim:* / dead waiting-* + clear assignees."""
  1628	        gw = self._cold_start_gateway
  1629	        plan = gw.stale_sweep_plan()
  1630	        for key, labels in plan.stale_claims.items():
  1631	            for label in labels:
  1632	                gw.remove_label(key, label)
  1633	            gw.clear_assignee(key)
  1634	            self._record_sweep(report, key, "stale-claim", list(labels))
  1635	        for key in plan.orphan_assignees:
  1636	            gw.clear_assignee(key)
  1637	            self._record_sweep(report, key, "orphan-assignee", [])
  1638	        for key, labels in plan.resolved_waiting.items():
  1639	            for label in labels:
  1640	                gw.remove_label(key, label)
  1641	            self._record_sweep(report, key, "resolved-waiting", list(labels))
  1642	
  1643	    def _record_sweep(
  1644	        self, report: ColdStartReport, key: str, kind: str, labels: list[str]
  1645	    ) -> None:

exec
/bin/bash -lc "nl -ba backend/agents/pipeline_coordinator.py | sed -n '165,210p;600,625p'" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 0ms:
   165	        return cls(
   166	            config_dir=base,
   167	            heartbeat_path=base / "heartbeat",
   168	            decision_log_dir=decision_log_dir,
   169	            capacity_path=capacity_path_from_env(base, env),
   170	            bridge_events_path=Path(
   171	                env.get(BRIDGE_EVENTS_FILE_ENV, str(base / "bridge-events.jsonl"))
   172	            ).expanduser(),
   173	            jira_agent_class=env.get(JIRA_AGENT_CLASS_ENV, "subscription-claude"),
   174	        )
   175	
   176	
   177	# ── L1 heartbeat (ADR-0021 §9 L1) ─────────────────────────────────────
   178	
   179	
   180	class HeartbeatWriter:
   181	    """Writes the liveness file every tick (ADR-0021 §9 L1).
   182	
   183	    The watchdog (§9 L2, sibling deploy) polls this file's mtime/content;
   184	    here we only own the *write* side. The write is atomic (temp + rename)
   185	    so a crashed mid-write never leaves a watcher reading a torn file.
   186	    """
   187	
   188	    def __init__(self, path: Path, *, clock: Clock = _utc_now) -> None:
   189	        self._path = Path(path)
   190	        self._clock = clock
   191	
   192	    @property
   193	    def path(self) -> Path:
   194	        return self._path
   195	
   196	    def touch(self) -> datetime:
   197	        """Stamp the heartbeat with the current time + pid. Returns the ts."""
   198	        now = self._clock()
   199	        self._path.parent.mkdir(parents=True, exist_ok=True, mode=_DIR_MODE)
   200	        payload = {
   201	            "ts": now.isoformat(),
   202	            "pid": os.getpid(),
   203	            "event": "coordinator-alive",
   204	        }
   205	        tmp = self._path.with_name(self._path.name + f".{uuid.uuid4().hex}.tmp")
   206	        tmp.write_text(json.dumps(payload, sort_keys=True) + "\n", encoding="utf-8")
   207	        os.chmod(tmp, _FILE_MODE)
   208	        os.replace(tmp, self._path)
   209	        return now
   210	
   600	    def clear_assignee(self, key: str) -> None: ...
   601	
   602	    # ── shared (Startup-1 halt / ambiguous Startup-2 (d)) ──
   603	    def mention_operator(self, key: str, message: str, *, urgency: str = "high") -> None: ...
   604	
   605	
   606	class JiraDispatchColdStartGateway:
   607	    """Production :class:`ColdStartGateway` over ``jira_dispatch`` + git + shell.
   608	
   609	    Heavy collaborators (``jira_dispatch.make_client``, the Gerrit SSH path)
   610	    are imported + constructed lazily and wrapped so a failure degrades to a
   611	    safe default — the daemon must reach its loop even on a half-up host.
   612	    The agent class used for the JIRA/Gerrit identity defaults to the
   613	    coordinator's own bot account (``claude``); it only ever *reads* + applies
   614	    the §6.1-bounded actions (relabel / transition / comment), never +2.
   615	    """
   616	
   617	    def __init__(
   618	        self,
   619	        *,
   620	        repo_root: Path | None = None,
   621	        agent_class: str = "claude",
   622	        cli_timeout_seconds: float = 60.0,
   623	    ) -> None:
   624	        self._repo_root = Path(repo_root) if repo_root is not None else _repo_root()
   625	        self._agent_class = agent_class

codex
One concrete anchor problem surfaced: the draft references `backend/tests/test_pipeline_coordinator_systemd.py:68`, but that file is not present in the current tree. I’m checking whether that is just a stale filename or a real filing gap in T6’s test AC.
exec
/bin/bash -lc 'rg -n "def transition_|TRANSITION_IDS|to_in_progress|archiv|Archived" backend/agents/jira_dispatch.py backend/agents/pipeline_coordinator_rules.py docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md' in /home/user/work/sora/OmniSight-Productizer
 succeeded in 113ms:
backend/agents/jira_dispatch.py:10:- ``transition_to_in_progress(key, bot_account_id)`` — TODO → In Progress
backend/agents/jira_dispatch.py:465:TRANSITION_IDS = {
backend/agents/jira_dispatch.py:466:    "to_in_progress": "21",      # JP locale: "進行中"
backend/agents/jira_dispatch.py:471:    "to_archived": "8",          # "Archive" — Published → Archived
backend/agents/jira_dispatch.py:1583:ARCHIVED_STATUS_NAMES = {"Archived"}
backend/agents/jira_dispatch.py:1649:def transition_to_under_review_if_needed(
backend/agents/jira_dispatch.py:1671:        {"transition": {"id": TRANSITION_IDS["to_under_review"]}},
backend/agents/jira_dispatch.py:1700:            {"transition": {"id": TRANSITION_IDS["to_under_review"]}},
backend/agents/jira_dispatch.py:1709:            {"transition": {"id": TRANSITION_IDS["to_approved"]}},
backend/agents/jira_dispatch.py:1718:            {"transition": {"id": TRANSITION_IDS["to_published"]}},
backend/agents/jira_dispatch.py:1726:def transition_to_under_review(
backend/agents/jira_dispatch.py:1764:        raise RuntimeError(f"{key} is archived; refusing to force-publish")
backend/agents/jira_dispatch.py:1767:        steps = ("to_in_progress", "to_under_review", "to_approved", "to_published")
backend/agents/jira_dispatch.py:1783:            {"transition": {"id": TRANSITION_IDS[transition_name]}},
backend/agents/jira_dispatch.py:1870:        raise RuntimeError(f"{key} is archived; refusing to ops-forward")
backend/agents/jira_dispatch.py:1890:                {"transition": {"id": TRANSITION_IDS[transition_name]}},
backend/agents/jira_dispatch.py:1915:def transition_to_in_progress(client: DispatchClient, key: str, idem_key: str | None = None) -> None:
backend/agents/jira_dispatch.py:1925:        {"transition": {"id": TRANSITION_IDS["to_in_progress"]}},
backend/agents/jira_dispatch.py:1934:def transition_back_to_todo(
backend/agents/jira_dispatch.py:1994:        {"transition": {"id": TRANSITION_IDS["back_to_todo"]}},
backend/agents/jira_dispatch.py:2593:        f'AND status not in ("Published", "公開済み", "Archived") '
backend/agents/jira_dispatch.py:2730:# ``transition_to_in_progress`` + assign call are separated by several
backend/agents/jira_dispatch.py:2734:# ``transition_to_in_progress`` — JIRA accepts both writes and both CLIs
backend/agents/jira_dispatch.py:2813:      ``transition_to_in_progress``. Either we won this race (our fencing
backend/agents/jira_dispatch.py:2818:      ticket and MUST NOT call ``transition_to_in_progress``. ``lost_to``
backend/agents/jira_dispatch.py:3141:    """Atomically claim ``key`` before ``transition_to_in_progress``.
backend/agents/jira_dispatch.py:3630:# Anything else (TODO, Approved, Published, Archived, ...) is "released" —

exec
/bin/bash -lc 'rg -n "LiveActionExecutor|transition|Archived|mark_for_followup|file_ticket|mention_operator|escalate|ACTION_" docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md | head -120' in /home/user/work/sora/OmniSight-Productizer
 succeeded in 106ms:
25:| **B3 idem keys** | PARTIAL — `coord:{decision_id}:...` never dedupes across ticks (decision_id fresh per tick, rules.py:266; idempotency.py:90 caches exact-key only) | **key grain corrected** → `coord:{kind}:{target}:{content_hash}` (stable across ticks) with per-REST-call suffixes `:add:<label>` / `:remove:<label>` / `:clear-assignee` / `:transition` / `:comment` (jira_dispatch `_request_idempotent` caches by key, :331 — one key per PUT/POST). See §Idempotency. |
27:| **B6 cold-start cap** | PARTIAL — single global int ambiguous across systemctl/JIRA/sweep | **per-phase mutation accounting**: separate counters for Startup-1 `start_unit` (:653), Startup-2 transitions/labels (:735), Startup-3 sweep loop (:776), each with its own cap; "mutation" defined per phase. See §Cold-start budget. |
31:| **NEW (Sec2): destructive transition** | BLOCKER — `transition_back_to_todo` clears assignee (jira_dispatch:1991); rule context may be stale at execution time (:696) | **fresh recheck before destructive ops**: for `transition → To Do`, re-verify no-live-runner + status still 進行中 at execution time; abstain (`executed:False,reason:"context_stale"`) if changed. |
36:| **NEW (Sec3 NICE): file_ticket accounting** | clarification | `file_ticket` records BOTH `executed:False (file_ticket, requires_operator)` AND the `mention_operator` side effect as a distinct result line, so decision-log readers don't misread `executed:False` as "nothing happened". |
49:`LiveActionExecutor` is constructed only on the `acting=True` path
85:  `:add:<label>`, `:remove:<label>`, `:clear-assignee`, `:transition`,
88:  (`transition_to_under_review_if_needed` :1649, `transition_back_to_todo`
94:## §LiveActionExecutor dispatch (carried from v2, unchanged vocabulary)
101:| `transition` | `transition_back_to_todo` / `transition_to_under_review_if_needed` per `to_status` + **destructive-recheck for To Do** | `:transition` |
103:| `mention_operator` | `add_comment(@op)`; label iff `urgency=="high"` (:807) | `:comment`(+`:label`) |
104:| `escalate` | `add_label("needs-operator-action")` **+** `add_comment(reason)` — always both (B2) | `:label`+`:comment` |
105:| `mark_for_followup` | `add_label("coord-resume-after:{when}")` + comment | `:label`+`:comment` |
106:| `file_ticket` | NOT auto-executed → `mention_operator` + 2 result lines (accounting fix) | `:comment`+`:label` |
133:- Startup-2: JIRA transitions/labels/comments (:735) — cap N₂.
150:   idempotent, no double-apply. Recovery stays keyed on the ADR `ACTION_*`
192:- Destructive recheck: `transition→To Do` with a now-live runner → abstains.
214:1. Re-confirm B3/B4/B6/S10/N12/N13 + the 4 new BLOCKERs (destructive transition,

exec
/bin/bash -lc "sed -n '94,107p' docs/operations/2026-05-22-coordinator-acting-live-action-executor-design-v3.md" in /home/user/work/sora/OmniSight-Productizer
 succeeded in 114ms:
## §LiveActionExecutor dispatch (carried from v2, unchanged vocabulary)

Dispatch on `action.kind` (sprint_replan.py:141 pattern) → low-level
`jira_dispatch` via a client built from `config.jira_agent_class` (fixes S9):

| kind | live op | idem suffix |
|---|---|---|
| `transition` | `transition_back_to_todo` / `transition_to_under_review_if_needed` per `to_status` + **destructive-recheck for To Do** | `:transition` |
| `relabel` | `add_label`/`remove_label`; ride-along `clear_assignee` on claim-label removal | `:add:<l>`/`:remove:<l>`/`:clear-assignee` |
| `mention_operator` | `add_comment(@op)`; label iff `urgency=="high"` (:807) | `:comment`(+`:label`) |
| `escalate` | `add_label("needs-operator-action")` **+** `add_comment(reason)` — always both (B2) | `:label`+`:comment` |
| `mark_for_followup` | `add_label("coord-resume-after:{when}")` + comment | `:label`+`:comment` |
| `file_ticket` | NOT auto-executed → `mention_operator` + 2 result lines (accounting fix) | `:comment`+`:label` |
| `noop` / unknown | none → `executed:False` (`reason:"unsupported"` for unknown) | — |

codex
**Fix Checklist**

- **GAP — T1 / §LiveActionExecutor dispatch:** T1 does not fully spell out the carried dispatch table. Add explicit ACs for `mention_operator` = comment + high-urgency label behavior, `mark_for_followup` = `coord-resume-after:{when}` label + comment, `relabel` = add/remove plus clear-assignee on claim-label removal, and `noop`/unknown = no mutation with unsupported reason. The draft says “each Action.kind” but the concrete per-kind semantics are under-specified.

- **GAP — T5 / §Crash recovery + §Blast-radius dependency:** T5 should also be `blockedBy: T4`. `tick_intent.planned` must reflect the final executable action set after dry-run rewrite, cap/fairness/kill-switch decisions, and final stable idem-key resolution. Without T4 ordering, replay could be implemented against a pre-cap plan.

- **CONVENTION — T6 / §Tests:** `backend/tests/test_pipeline_coordinator_systemd.py:68` does not exist. Replace that cited anchor with an existing test file or state that T6 creates a new systemd/unit-contract test file. Current valid anchors include `backend/tests/test_pipeline_coordinator_coldstart.py:473` and `:954`.

- **OK — T3 / §dry_run Option B + condition #1:** Covered. T3 explicitly requires `dataclasses.replace` on non-noop actions, rebuilding `DecisionResult`, preserving `decision_id`, and verifying logged `dry_run:false`.

- **OK — T2 / §coord-skip/destructive recheck + condition #2:** Covered. T2 correctly requires `fetch_ticket_labels`, fail-closed label reads, `coord-skip`/`coord-quarantine` abstention, live status recheck, and strict fail-closed behavior around `_ticket_has_live_runner` uncertainty.

- **OK — T5 / §Crash recovery + condition #3:** Covered except the missing T4 dependency above. ACs include pre-execute `tick_intent`, final stable idem keys, same `decision_id`, outcome correlation, L6 replay, and 24h replay cutoff.

- **OK — T1 / §Idempotency + file_ticket accounting:** Covered. Stable semantic hash excludes volatile fields, includes operator-visible comment text, uses per-REST suffixes, and `file_ticket` gets two result lines.

- **OK — T4 / §Blast-radius:** Covered. Cap default 1, excess `tick_cap` results, per-action try/except, kill-switch, and decision-log-derived fairness cursor across 24h date files are all in ACs.

- **OK — T6 / §Cold-start budget + env-gate + capability scope:** Covered. `from_env`, `main`, `acting=True` wiring, `jira_agent_class`, cold-start caps `1/1/1`, and “not capability-matrix gated” are represented.

- **OK — T7 / §Activation:** Covered. Correctly tier `X`, `requires:operator-approval`, human-only, blocked by all implementation tickets plus `OP-1555`, systemd drop-in, budget coupling, phased flip, rollback, and runbook update.

- **OK — SCOPE-CREEP / T1-T7:** I do not see ticket ACs introducing material work outside v4. The apparent additions are either v4 round-4 answers, activation evidence, or filing-convention deployment/exercise wording.

- **OK — FILING CONVENTIONS / T1-T7:** 4-AC structure is present for all tickets. Area labels are broadly correct: T1-T5 `backend/tests`, T6 `backend/devops/tests`, T7 `devops/docs`. T7 is the only human-only `tier:X` ticket.

- **OK — CODE ANCHORS / cited source lines:** Most spot-checks are accurate: `run_once` `:1699-1708`, `_build_tick_record` `:1394`, L6 `:1493/:1514`, `from_env` `:149`, `main` `:1936`, `_default_cold_start_gateway` `:1149`, `fetch_ticket_labels` `:1826`, `get_issue_status` `:1586`, `_ticket_has_live_runner` `:986-990`, TTL `:40`, `_request_idempotent` `:331`, `sprint_replan.execute` `:141`, and systemd inline env lines `:29-32`.

FINAL VERDICT: needs-revision.
tokens used
83,969
**Fix Checklist**

- **GAP — T1 / §LiveActionExecutor dispatch:** T1 does not fully spell out the carried dispatch table. Add explicit ACs for `mention_operator` = comment + high-urgency label behavior, `mark_for_followup` = `coord-resume-after:{when}` label + comment, `relabel` = add/remove plus clear-assignee on claim-label removal, and `noop`/unknown = no mutation with unsupported reason. The draft says “each Action.kind” but the concrete per-kind semantics are under-specified.

- **GAP — T5 / §Crash recovery + §Blast-radius dependency:** T5 should also be `blockedBy: T4`. `tick_intent.planned` must reflect the final executable action set after dry-run rewrite, cap/fairness/kill-switch decisions, and final stable idem-key resolution. Without T4 ordering, replay could be implemented against a pre-cap plan.

- **CONVENTION — T6 / §Tests:** `backend/tests/test_pipeline_coordinator_systemd.py:68` does not exist. Replace that cited anchor with an existing test file or state that T6 creates a new systemd/unit-contract test file. Current valid anchors include `backend/tests/test_pipeline_coordinator_coldstart.py:473` and `:954`.

- **OK — T3 / §dry_run Option B + condition #1:** Covered. T3 explicitly requires `dataclasses.replace` on non-noop actions, rebuilding `DecisionResult`, preserving `decision_id`, and verifying logged `dry_run:false`.

- **OK — T2 / §coord-skip/destructive recheck + condition #2:** Covered. T2 correctly requires `fetch_ticket_labels`, fail-closed label reads, `coord-skip`/`coord-quarantine` abstention, live status recheck, and strict fail-closed behavior around `_ticket_has_live_runner` uncertainty.

- **OK — T5 / §Crash recovery + condition #3:** Covered except the missing T4 dependency above. ACs include pre-execute `tick_intent`, final stable idem keys, same `decision_id`, outcome correlation, L6 replay, and 24h replay cutoff.

- **OK — T1 / §Idempotency + file_ticket accounting:** Covered. Stable semantic hash excludes volatile fields, includes operator-visible comment text, uses per-REST suffixes, and `file_ticket` gets two result lines.

- **OK — T4 / §Blast-radius:** Covered. Cap default 1, excess `tick_cap` results, per-action try/except, kill-switch, and decision-log-derived fairness cursor across 24h date files are all in ACs.

- **OK — T6 / §Cold-start budget + env-gate + capability scope:** Covered. `from_env`, `main`, `acting=True` wiring, `jira_agent_class`, cold-start caps `1/1/1`, and “not capability-matrix gated” are represented.

- **OK — T7 / §Activation:** Covered. Correctly tier `X`, `requires:operator-approval`, human-only, blocked by all implementation tickets plus `OP-1555`, systemd drop-in, budget coupling, phased flip, rollback, and runbook update.

- **OK — SCOPE-CREEP / T1-T7:** I do not see ticket ACs introducing material work outside v4. The apparent additions are either v4 round-4 answers, activation evidence, or filing-convention deployment/exercise wording.

- **OK — FILING CONVENTIONS / T1-T7:** 4-AC structure is present for all tickets. Area labels are broadly correct: T1-T5 `backend/tests`, T6 `backend/devops/tests`, T7 `devops/docs`. T7 is the only human-only `tier:X` ticket.

- **OK — CODE ANCHORS / cited source lines:** Most spot-checks are accurate: `run_once` `:1699-1708`, `_build_tick_record` `:1394`, L6 `:1493/:1514`, `from_env` `:149`, `main` `:1936`, `_default_cold_start_gateway` `:1149`, `fetch_ticket_labels` `:1826`, `get_issue_status` `:1586`, `_ticket_has_live_runner` `:986-990`, TTL `:40`, `_request_idempotent` `:331`, `sprint_replan.execute` `:141`, and systemd inline env lines `:29-32`.

FINAL VERDICT: needs-revision.
