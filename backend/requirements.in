# backend/requirements.in — human-readable dependency ranges.
#
# N1 policy: this is the source of truth. `backend/requirements.txt` is
# regenerated from this file via:
#
#   pip-compile --generate-hashes --output-file=backend/requirements.txt \
#       backend/requirements.in
#
# Docker and CI install with `pip install --require-hashes` against the
# generated lockfile. Any drift between `.in` and `.txt` fails CI.

# --- Web framework -----------------------------------------------------
# FX.10.1 (2026-05-04): FastAPI 0.115.x caps Starlette at <0.47.0.
# 0.120.4 is the first checked FastAPI pin that allows starlette==0.49.1.
fastapi==0.120.4
starlette==0.49.1
uvicorn[standard]==0.34.2
python-multipart==0.0.26
sse-starlette==2.2.1

# --- Config / models ---------------------------------------------------
pydantic==2.11.3
pydantic-settings==2.9.1
python-dotenv==1.2.2
pyyaml==6.0.3

# --- Agent graph / LLM -------------------------------------------------
langgraph==1.1.6
langchain-core==1.2.31
langchain-anthropic==1.4.0
langchain-google-genai==4.2.1
langchain-openai==1.1.14
langchain-groq==1.1.2
langchain-together==0.4.0
langchain-ollama==1.1.0
# OP-840: repo-map preamble parses Python / TS / TSX imports for PageRank.
tree-sitter==0.21.3
tree-sitter-python==0.21.0
tree-sitter-typescript==0.21.2
# OP-863 (2026-05-11): minimum SDK floor for Managed Agents Memory Tool
# (``managed-agents-2026-04-01`` beta), Outcomes, and Dreaming audit.
# 0.97.0 added memory stores; 0.100.0 adds Outcomes and the complete
# Managed Agents beta surface we gate in ``test_sdk_version_pin.py``.
# Keep below 1.0 via langchain-anthropic's declared constraint until
# the next major is audited.
anthropic==0.100.0

# --- Reports (Jinja + PDF; weasyprint is non-Windows only) -------------
jinja2==3.1.6
markdown==3.10.2
# FX.4.9: enterprise_web_stack report exports use openpyxl for real XLSX bytes.
openpyxl==3.1.5
weasyprint==68.1; sys_platform != 'win32'

# --- Persistence / migrations -----------------------------------------
aiosqlite==0.21.0
alembic==1.14.0
# G4 Phase-3 wire-up (2026-04-20): PostgreSQL drivers promoted from
# CI-only to production. Earlier revisions kept these out of the
# runtime image to save ~11 MB on wheels the backend-tests /
# openapi-contract / renovate-config jobs didn't need; the cost of
# that optimisation was that G4 [x] never meant "production can
# actually talk to Postgres". ``asyncpg`` is the runtime driver
# used by backend.db_connection::_PostgresAsyncConnection.
# ``psycopg2-binary`` is the sync driver Alembic needs during
# ``alembic upgrade head`` (SQLAlchemy sync engine required, asyncpg
# not supported by Alembic's migration runner). Both stay optional
# from a CODE perspective — backend.db_connection lazy-imports them
# only when the target URL is a ``postgresql+*`` scheme; the plain
# SQLite path keeps working on deployments that don't set
# ``OMNISIGHT_DATABASE_URL``.
asyncpg==0.30.0
psycopg2-binary==2.9.10

# --- Observability ----------------------------------------------------
prometheus-client==0.21.1
structlog==24.4.0
# FX2.D5.1: backend distributed tracing (W3C trace context + Jaeger/Datadog export).
opentelemetry-sdk==1.41.1
opentelemetry-exporter-otlp-proto-http==1.41.1
# H1 host-metrics: psutil drives cpu/mem/disk sampling in
# backend/host_metrics.sample_host_once().
psutil==7.2.2
# H1 host-metrics: docker-py SDK is the primary path for
# sample_docker_once() — container count + mem reservation. The module
# soft-imports it and falls back to `docker stats --no-stream`, so this
# pin is strongly recommended (for fidelity) but not strictly required
# (Docker Desktop scenarios already fall through to the CLI).
docker==7.1.0

# --- HTTP client (production runtime; 47 backend modules import it) --
# FX.6.7 (2026-05-04): httpx STAYS here. The TODO entry that landed
# this Phase listed httpx alongside pytest/respx as a "test dep" but
# audit found 47 production modules import it directly at runtime —
# backend/security/oauth_client.py, backend/email_delivery/{postmark,
# ses,resend,http}.py, backend/storage_provisioning/{s3,supabase}.py,
# backend/cms/sanity.py, backend/llm_balance.py, etc. So the prod
# image MUST carry httpx. The dev lockfile (requirements-dev.in) does
# NOT re-pin httpx — pip-tools' ``-c requirements.txt`` constraint
# pins respx's transitive httpx requirement to this version.
httpx==0.28.1

# --- Testing / Lint / Audit (peeled out by FX.6.7 — 2026-05-04) -------
# pytest / pytest-asyncio / pytest-cov / respx / pip-audit / ruff /
# mypy moved to ``backend/requirements-dev.in`` so the prod lockfile
# (``requirements.txt``) stays runtime-only. See that file's header
# for the install pattern + rationale. The Dockerfile no longer needs
# to ``pip uninstall -y pytest …`` after install — those packages are
# never resolved into the prod lockfile in the first place.

# --- K5: MFA (TOTP + WebAuthn) ----------------------------------------
pyotp==2.9.0
qrcode[pil]==8.0
webauthn==2.7.1

# --- K7: Password policy + Argon2id -----------------------------------
argon2-cffi==25.1.0
zxcvbn-python==4.4.24

# --- SC.12: PII detection / masking -----------------------------------
# Microsoft Presidio powers backend.security.pii_presidio detection and
# anonymization helpers; engines are lazy-loaded by the adapter.
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362

# --- KS.1: Envelope encryption KMS adapters ---------------------------
# backend.security.kms_adapters lazy-loads these provider SDKs for
# AWS KMS, Google Cloud KMS, and HashiCorp Vault Transit wrap/unwrap.
boto3==1.43.2
google-cloud-kms==3.12.0
hvac==2.4.0
# backend.security.envelope uses libsodium sodium_memzero for KS.4.9
# secret buffer zeroization.
pynacl==1.6.2

# --- I9/I10: Redis-backed rate limiting + multi-worker shared state ---
# FX.6.2 (2026-05-04): removed duplicate ``redis==7.4.0`` line — kept only
# the extras-form ``redis[hiredis]`` because pip resolves both lines to the
# same redis package, so the bare line was a no-op that just made the file
# look ambiguous. The extras form pulls in the C-accelerated ``hiredis``
# parser, which is what backend code actually needs at runtime.
redis[hiredis]==7.4.0

# --- OP-852 Cognee KG (optional install — graph-layer; replaces B8 + B10) ----
# Pinning intent + install procedure (kept comment-only so the lockfile
# drift gate does not fail). Cognee + the Claude Agent SDK ship as an
# OPTIONAL bundle: the runtime adapter
# (``backend.agents.cognee_integration``) lazy-imports them and the
# helper functions silently fall back to the B8 PageRank repo-map and
# B10 BM25 lesson retrieval when the package is missing. This mirrors
# the paramiko pattern in the ``--- C1: SSH remote runner`` section
# below.
#
# To enable Cognee on a host (operator playbook — runbook at
# ``docs/operations/cognee-runbook.md``):
#
#   ./backend/.venv/bin/pip install \
#       cognee==0.1.46 \
#       cognee-integration-claude==0.1.5 \
#       neo4j==5.24.0 \
#       claude-agent-sdk==0.0.10
#
# (Versions are the latest stable line as of 2026-05-11; bump to the
# newest patch on the ``cognee`` 0.1.x track when regenerating
# requirements.txt for the prod image. The Neo4j 5.24 driver matches
# the Neo4j 5.24-community service in ``docker-compose.yml`` under the
# ``cognee`` compose profile.)
#
# AC #1 evidence: this comment block + ``cognee_full_rebuild.py`` install
# step + the runbook together pin the integration. When the integration
# graduates from "optional" to "default" (post-pilot), move the four
# ``==`` pins out of this comment and into real spec lines below, then
# regenerate requirements.txt via pip-compile.

# --- C1: SSH remote runner (optional) ----------------------------------
# Only needed for cross-arch targets requiring paramiko-based remote
# execution. The module (backend/ssh_runner.py) lazy-imports paramiko
# and raises a clear error at call time if missing — so the system
# starts and runs fine without it. Install when you need SSH:
#   pip install paramiko
#
# FX.6.4 (2026-05-04): paramiko is NOT pinned here. Earlier revisions
# kept ``paramiko==4.0.0`` as a hard pin under this header, which made
# it land in ``requirements.txt`` (a "ghost" — present in the lockfile
# but the surrounding comment said it should be optional). The pin
# also dragged ``bcrypt`` into the runtime image purely for paramiko's
# own host-key handling — neither is used by any non-SSH code path.
# To install on a host that actually needs SSH:
#   ./backend/.venv/bin/pip install paramiko==4.0.0
# (test_ssh_runner.py uses ``pytest.importorskip('paramiko')`` so the
#  unit suite stays green without it; ssh_runner.py itself raises
#  RuntimeError with an actionable install hint at call time.)
