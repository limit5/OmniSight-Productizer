; [OP-1025] systemd user unit -- legacy claude-bot runner.
;
; Install:
;   mkdir -p ~/.config/systemd/user
;   cp deploy/systemd/runner-claude-bot@default.service ~/.config/systemd/user/
;   systemctl --user daemon-reload
;   systemctl --user enable --now runner-claude-bot@default.service
;   loginctl enable-linger "$USER"

[Unit]
Description=OmniSight Claude Runner claude-bot (OP-1025)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=%h/work/sora/OmniSight-Productizer
Environment=PYTHONUNBUFFERED=1
Environment=OMNISIGHT_RUNNER_CLASS=subscription-claude
Environment=OMNISIGHT_RUNNER_INSTANCE_ID=default
Environment=OMNISIGHT_MEMORY_TOOL_ROOT=/var/omnisight/memory
Environment=OMNISIGHT_MEMORY_TOOL_CAP_MB=100
Environment=OMNISIGHT_IDEMPOTENCY_DB_PATH=%h/.config/omnisight/idem-keys-claude-bot-default.db
Environment=OMNISIGHT_RUNNER_QUOTA_STATE_DIR=%h/.cache/omnisight/runner-quota-state
ExecStart=%h/work/sora/OmniSight-Productizer/deploy/runner-wrappers/runner-claude.sh default
Restart=on-failure
RestartSec=90
KillSignal=SIGTERM
TimeoutStopSec=60
StandardOutput=append:%h/work/sora/logs/runner/claude-bot-default.log
StandardError=append:%h/work/sora/logs/runner/claude-bot-default.log

[Install]
WantedBy=default.target
